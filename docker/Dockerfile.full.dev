FROM python:3.10-slim-bullseye

WORKDIR /app
ADD . /app

# Install MLflow in editable mode with extras
RUN pip install --upgrade pip && pip install --no-cache-dir -e .[extras,db,databricks,gateway,genai]
