pytest==9.1.1
# Defer pandas 3 to keep the Python 3.11 upgrade self-contained. pandas 3 needs pyspark
# 4.2 (the first release that stops importing the private pandas.core.common._builtin_table),
# and no Delta Lake release is built against Spark 4.2 yet, so adopting it here would block
# this upgrade on upstream Delta. Drop this once Delta ships for Spark 4.2.
pandas<3
