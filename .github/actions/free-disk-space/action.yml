name: "free-disk-space"
description: "free disk space"
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        # Temporarily commented out slow disk cleanup operations
        # sudo rm -rf "$AGENT_TOOLSDIRECTORY"
        # sudo rm -rf /usr/share/dotnet
        # sudo rm -rf /usr/local/lib/android
        echo "Disk cleanup operations skipped for faster CI"
