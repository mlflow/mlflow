name: "show-versions"
description: "Show python package versions sorted by release date"
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        # Use uv run with aiohttp dependency
        uv run --with aiohttp python dev/show_package_release_dates.py
