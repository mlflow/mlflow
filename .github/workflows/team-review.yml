name: Team review

on:
  pull_request_target:
    types:
      - labeled

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

permissions: {}

jobs:
  review:
    runs-on: ubuntu-slim
    if: github.event.label.name == 'team-review'
    timeout-minutes: 5
    permissions:
      pull-requests: write
      issues: write
    steps:
      - name: Fetch script
        run: |
          curl -fsSL --retry 3 --connect-timeout 10 --max-time 30 \
            --write-out 'curl: %{time_total}s\n' \
            "https://raw.githubusercontent.com/$GITHUB_REPOSITORY/$GITHUB_SHA/.github/workflows/team-review.js" \
            -o team-review.js
      - name: Select reviewers
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          retries: 3
          script: |
            const script = require('./team-review.js');
            await script({ github, context });
