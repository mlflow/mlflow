version: 2.1

# TODO: Remove this job once MLflow 3.1.3 is released
jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - run: ls

workflows:
  build:
    jobs:
      - build
