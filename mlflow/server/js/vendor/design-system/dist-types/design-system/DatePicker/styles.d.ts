import type { Theme } from '@emotion/react';
export declare const getDayPickerStyles: (prefix: string, theme: Theme) => import("@emotion/utils").SerializedStyles;
/**
 * Additional styles applied to the DayPicker when used in week picker mode (granularity='week').
 * Highlights the entire week row on hover and for the selected week.
 */
export declare const getWeekPickerStyles: (prefix: string, theme: Theme) => import("@emotion/utils").SerializedStyles;
//# sourceMappingURL=styles.d.ts.map