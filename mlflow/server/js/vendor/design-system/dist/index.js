import { v as DesignSystemEventProvider, d as useDesignSystemContext, u as useDesignSystemTheme, w as DesignSystemAntDConfigProvider, x as RestoreAntDDefaultClsPrefix, y as safex, f as DesignSystemEventProviderAnalyticsEventTypes, g as useDesignSystemEventComponentCallbacks, D as DesignSystemEventProviderComponentTypes, z as useNotifyOnFirstView, A as addDebugOutlineStylesIfEnabled, E as ChevronDownIcon, G as getAnimationCss, H as InfoFillIcon, J as CheckCircleFillIcon, K as WarningFillIcon, N as DangerFillIcon, O as DesignSystemEventProviderComponentSubTypeMap, P as useStableUuidV4, B as Button, T as Typography, j as ChevronRightIcon, Q as CloseSmallIcon, a as addDebugOutlineIfEnabled, M as Modal, U as serverSideSafe, h as getDarkModePortalStyles, V as LegacyTooltip, X as useModalContext, I as InfoSmallIcon, Y as SparkleDoubleIcon, Z as LoadingState, _ as visuallyHidden, $ as DesignSystemEventSuppressInteractionProviderContext, a0 as DesignSystemEventSuppressInteractionTrueContextValue, a1 as tableStyles, a2 as repeatingElementsStyles, a3 as tableClassNames, a4 as hideIconButtonRowStyles, a5 as hideIconButtonActionCellClassName, p as CheckIcon, a6 as getNewChildren, a7 as dropdownItemStyles, a8 as dropdownContentStyles, a9 as handleKeyboardNavigation, aa as dropdownSeparatorStyles, ab as getValidationStateColor, c as useFormContext, ac as LockIcon, i as importantify, ad as XCircleFillIcon, k as ChevronLeftIcon, ae as ClockIcon, af as DialogComboboxContextProvider, C as ComponentFinderContext, ag as useDialogComboboxContext, ah as Spinner, ai as PlusIcon, aj as getComboboxOptionItemWrapperStyles, ak as getComboboxContentWrapperStyles, al as getFooterStyles, am as highlightFirstNonDisabledOption, an as DialogComboboxOptionListContextProvider, s as Tooltip, ao as useDialogComboboxOptionListContext, ap as generateUuidV4, aq as getContentOptions, ar as findHighlightedOption, as as highlightOption, at as SearchIcon, au as findClosestOptionSibling, av as DialogComboboxOptionListCheckboxItem, aw as DialogComboboxOptionListSelectItem, ax as useSelectContext, e as useComponentFinderContext, n as CloseIcon, ay as SMALL_BUTTON_HEIGHT$2, az as ApplyDesignSystemContextOverrides, r as getShadowScrollStyles, W as WarningIcon, aA as CheckCircleIcon, o as DangerIcon, aB as useUniqueId, aC as SelectContextProvider, aD as TypeaheadComboboxContextProvider, aE as useTypeaheadComboboxContext, aF as useComboboxState, aG as useMultipleSelectionState, aH as Checkbox, aI as LegacySelect, aJ as Title$1, aK as AccessibleContainer, aL as getCommonTabsTriggerStyles, aM as getCommonTabsListStyles, aN as DU_BOIS_ENABLE_ANIMATION_CLASSNAME, aO as CircleOffIcon, aP as CircleOutlineIcon, aQ as CircleIcon, aR as shimExports, aS as primitiveColors, aT as DropdownButton, aU as getDisabledPrimarySplitButtonStyles, aV as getDisabledSplitButtonStyles, aW as getPrimaryStyles, aX as getDefaultStyles, aY as SortAscendingIcon, aZ as SortDescendingIcon, a_ as SortUnsortedIcon, a$ as MinusSquareIcon, b0 as PlusSquareIcon, b1 as Icon } from './Tabs-Bz2WasfR.js';
export { b2 as AccessDeniedGraphic, b3 as AlignCenterIcon, b4 as AlignJustifyIcon, b5 as AlignLeftIcon, b6 as AlignRightIcon, b7 as AlignVerticalBottomIcon, b8 as AlignVerticalCenterIcon, b9 as AlignVerticalTopIcon, ba as AppIcon, bb as ApplyDesignSystemFlags, bc as ArrowDownDotIcon, bd as ArrowDownFillIcon, be as ArrowDownIcon, bf as ArrowInIcon, bg as ArrowLeftIcon, bh as ArrowOverIcon, bi as ArrowRightIcon, bj as ArrowUpDotIcon, bk as ArrowUpFillIcon, bl as ArrowUpIcon, bm as ArrowsConnectIcon, bn as ArrowsUpDownIcon, bo as AssistantIcon, bp as AtIcon, bq as Auth0Graphic, br as Auth0GraphicLarge, bs as AzHorizontalIcon, bt as AzVerticalIcon, bu as BANNER_MAX_HEIGHT, bv as BANNER_MIN_HEIGHT, bw as BackupIcon, bx as BadgeCodeIcon, by as BadgeCodeOffIcon, bz as Banner, bA as BarChartIcon, bB as BarGroupedIcon, bC as BarStackedIcon, bD as BarStackedPercentageIcon, bE as BarsAscendingHorizontalIcon, bF as BarsAscendingVerticalIcon, bG as BarsDescendingHorizontalIcon, bH as BarsDescendingVerticalIcon, bI as BeakerIcon, bJ as BinaryIcon, bK as BlockQuoteIcon, bL as BoldIcon, bM as BookIcon, bN as BookmarkFillIcon, bO as BookmarkIcon, bP as BooksIcon, bQ as BracketsCheckIcon, bR as BracketsCurlyIcon, bS as BracketsErrorIcon, bT as BracketsSquareIcon, bU as BracketsXIcon, bV as BranchCheckIcon, bW as BranchIcon, bX as BranchResetIcon, bY as BriefcaseFillIcon, bZ as BriefcaseIcon, b_ as BrushIcon, b$ as BugIcon, c0 as CalendarClockIcon, c1 as CalendarEventIcon, c2 as CalendarIcon, c3 as CalendarRangeIcon, c4 as CalendarSyncIcon, c5 as CameraIcon, c6 as CaretDownSquareIcon, c7 as CaretUpSquareIcon, c8 as CatalogCloudIcon, c9 as CatalogGearIcon, ca as CatalogHomeIcon, cb as CatalogIcon, cc as CatalogOffIcon, cd as CatalogSharedIcon, ce as CatalogUserHomeIcon, cf as CellsSquareIcon, cg as CertifiedFillIcon, ch as CertifiedFillSmallIcon, ci as CertifiedIcon, cj as ChainIcon, ck as ChartLineIcon, cl as CheckCircleBadgeIcon, cm as CheckCircleSmallIcon, cn as CheckLineIcon, co as CheckSmallIcon, cp as CheckboxIcon, cq as ChecklistIcon, cr as ChevronDoubleDownIcon, cs as ChevronDoubleLeftIcon, ct as ChevronDoubleLeftOffIcon, cu as ChevronDoubleRightIcon, cv as ChevronDoubleRightOffIcon, cw as ChevronDoubleUpIcon, cx as ChevronUpIcon, cy as ChipIcon, cz as CircleOffLargeIcon, cA as CircleOutlineLargeIcon, cB as ClipboardIcon, cC as ClockKeyIcon, cD as ClockOffIcon, cE as CloudDatabaseIcon, cF as CloudDownloadIcon, cG as CloudIcon, cH as CloudKeyIcon, cI as CloudModelIcon, cJ as CloudOffIcon, cK as CloudUploadIcon, cL as CodeIcon, cM as ColorFillIcon, cN as ColorVars, cO as ColumnIcon, cP as ColumnSplitIcon, cQ as ColumnsIcon, cR as CommandIcon, cS as CommandPaletteIcon, cT as CompassIcon, cU as ConnectIcon, cV as CopyIcon, cW as CreditCardIcon, cX as CursorClickIcon, cY as CursorIcon, cZ as CursorTypeIcon, c_ as CustomAppIcon, c$ as DagHorizontalIcon, d0 as DagIcon, d1 as DagVerticalIcon, d2 as DangerModal, d3 as DangerSmallIcon, d4 as DashIcon, d5 as DashboardCodeIcon, d6 as DashboardIcon, d7 as DataIcon, d8 as DataMaskiingGraphic, d9 as DatabaseClockIcon, da as DatabaseIcon, db as DatabaseImportIcon, dc as DecimalIcon, dd as DeprecatedIcon, de as DeprecatedSmallIcon, df as DesignSystemContext, dg as DesignSystemEventProviderComponentSubTypes, dh as DesignSystemProvider, di as DesignSystemThemeContext, dj as DesignSystemThemeProvider, dk as DollarIcon, dl as DomainCirclesThree, dm as DomainsIcon, dn as DotsCircleIcon, dp as DownloadIcon, dq as DragIcon, dr as DropdownMenu, ds as Empty, dt as EmptyDashboardGraphic, du as ErdIcon, dv as ExpandLessIcon, dw as ExpandMoreIcon, dx as FaceFrownIcon, dy as FaceNeutralIcon, dz as FaceSmileIcon, dA as FileCodeIcon, dB as FileCubeIcon, dC as FileDocumentIcon, dD as FileIcon, dE as FileImageIcon, dF as FileLockIcon, dG as FileModelIcon, dH as FileNewIcon, dI as FilePipelineIcon, dJ as FilterIcon, dK as FlagPointerIcon, dL as FloatIcon, dM as FlowIcon, dN as FolderBranchFillIcon, dO as FolderBranchIcon, dP as FolderCloudFilledIcon, dQ as FolderCloudIcon, dR as FolderCubeIcon, dS as FolderCubeOutlineIcon, dT as FolderFillIcon, dU as FolderHomeIcon, dV as FolderIcon, dW as FolderNewIcon, dX as FolderNodeIcon, dY as FolderOpenBranchIcon, dZ as FolderOpenCloudIcon, d_ as FolderOpenCubeIcon, d$ as FolderOpenIcon, e0 as FolderOpenPipelineIcon, e1 as FolderOutlinePipelineIcon, e2 as FolderSolidPipelineIcon, e3 as FontIcon, e4 as ForkHorizontalIcon, e5 as ForkIcon, e6 as FullscreenExitIcon, e7 as FullscreenIcon, e8 as FunctionIcon, e9 as GavelIcon, ea as GearFillIcon, eb as GearIcon, ec as GenieCodeIcon, ed as GenieDeepResearchIcon, ee as GiftIcon, ef as GitCommitIcon, eg as GlobeIcon, eh as Graphic, ei as GridDashIcon, ej as GridIcon, ek as GroupIcon, el as H1Icon, em as H2Icon, en as H3Icon, eo as H4Icon, ep as H5Icon, eq as H6Icon, er as HashIcon, es as HistoryIcon, et as HomeIcon, eu as ImageIcon, ev as IndentDecreaseIcon, ew as IndentIncreaseIcon, ex as InfinityIcon, ey as InfoBookIcon, ez as InfoIcon, eA as InfoTooltip, eB as IngestionIcon, eC as ItalicIcon, eD as JoinOperatorIcon, eE as KeyIcon, eF as KeyboardIcon, eG as LakeflowDesignerIcon, eH as LayerGraphIcon, eI as LayerIcon, eJ as Layout, eK as LeafIcon, eL as LegacyForm, eM as LegacyFormDubois, eN as LegacyOptGroup, eO as LegacyOption, eP as LegacySelectOptGroup, eQ as LegacySelectOption, eR as LetterFormatIcon, eS as LettersIcon, eT as LettersNumbersIcon, eU as LibrariesIcon, eV as LifesaverIcon, eW as LightbulbIcon, eX as LightningCircleFillIcon, eY as LightningIcon, eZ as LinearLineIcon, e_ as LinkIcon, e$ as LinkOffIcon, f0 as ListBorderIcon, f1 as ListClearIcon, t as ListIcon, f2 as ListNumberIcon, L as LoadingIcon, f3 as LoadingStateContext, f4 as LockFillIcon, f5 as LockShareIcon, f6 as LockUnlockedIcon, f7 as LoopIcon, f8 as MailIcon, f9 as MapIcon, fa as MarkdownIcon, fb as McpIcon, fc as MeasureIcon, fd as MegaphoneIcon, fe as MenuIcon, ff as MinusCircleFillIcon, fg as MinusCircleIcon, fh as MinusCircleSmallIcon, fi as MissingBranchGraphic, fj as MissingGraphic, fk as ModelsIcon, fl as MonotoneLineIcon, fm as MoonIcon, fn as NeonProjectIcon, fo as NewChatIcon, fp as NewWindowIcon, fq as NoIcon, fr as NotebookIcon, fs as NotebookPipelineIcon, ft as NotificationIcon, fu as NotificationOffIcon, fv as NumberFormatIcon, fw as NumbersIcon, fx as OfficeIcon, fy as OutageGraphic, fz as OverflowIcon, fA as PageBottomIcon, fB as PageFirstIcon, fC as PageLastIcon, fD as PageTopIcon, fE as PanelDockedIcon, fF as PanelFloatingIcon, fG as PaperclipIcon, fH as PauseIcon, fI as PencilFillIcon, fJ as PencilIcon, fK as PencilSparkleIcon, fL as PieChartIcon, fM as PinCancelIcon, fN as PinFillIcon, fO as PinIcon, fP as PipelineCodeIcon, fQ as PipelineCubeIcon, fR as PipelineIcon, fS as PivotOperatorIcon, fT as PlayCircleFillIcon, fU as PlayCircleIcon, fV as PlayDoubleIcon, fW as PlayIcon, fX as PlayMultipleIcon, fY as PlugIcon, fZ as PlusCircleFillIcon, f_ as PlusCircleIcon, f$ as PlusCircleSmallIcon, g0 as PlusMinusSquareIcon, g1 as PositionBottomIcon, g2 as PositionLeftIcon, g3 as PositionRightIcon, g4 as PositionTopIcon, g5 as PullRequestIcon, g6 as PuzzleIcon, g7 as QueryEditorIcon, g8 as QueryIcon, g9 as QuestionMarkFillIcon, ga as QuestionMarkIcon, gb as RadioIcon, gc as ReaderModeIcon, gd as RedoIcon, ge as RefreshIcon, gf as RefreshPlayIcon, gg as RefreshXIcon, gh as ReplyIcon, gi as ResizeIcon, gj as RichTextIcon, gk as RobotIcon, gl as RocketIcon, gm as RowsIcon, gn as RunIcon, go as RunningIcon, gp as SaveClockIcon, gq as SaveIcon, gr as SchemaIcon, gs as SchoolIcon, gt as SearchDataIcon, gu as SelectContext, gv as SendIcon, gw as ShareIcon, gx as ShieldCheckIcon, gy as ShieldIcon, gz as ShieldOffIcon, gA as ShortcutIcon, gB as SidebarAutoIcon, gC as SidebarClosedIcon, gD as SidebarCollapseIcon, gE as SidebarExpandIcon, gF as SidebarIcon, gG as SidebarOpenIcon, gH as SidebarSyncIcon, gI as SlashSquareIcon, gJ as SlidersIcon, gK as SortCustomHorizontalIcon, gL as SortCustomVerticalIcon, gM as SortHorizontalAscendingIcon, gN as SortHorizontalDescendingIcon, gO as SortLetterHorizontalAscendingIcon, gP as SortLetterHorizontalDescendingIcon, gQ as SortLetterUnsortedIcon, gR as SortLetterVerticalAscendingIcon, gS as SortLetterVerticalDescendingIcon, gT as SparkleDoubleFillIcon, gU as SparkleFillIcon, gV as SparkleIcon, gW as SparkleRectangleIcon, gX as SpeechBubbleIcon, gY as SpeechBubblePlusIcon, gZ as SpeechBubbleQuestionMarkFillIcon, g_ as SpeechBubbleQuestionMarkIcon, g$ as SpeechBubbleStarIcon, h0 as SpeedometerIcon, h1 as SqlIcon, h2 as StarFillIcon, h3 as StarIcon, h4 as StepAfterLineIcon, h5 as StepBeforeLineIcon, h6 as StopCircleFillIcon, h7 as StopCircleIcon, h8 as StopIcon, h9 as StoredProcedureIcon, ha as StorefrontIcon, hb as StreamIcon, hc as StrikeThroughIcon, hd as SunIcon, he as SyncIcon, hf as SyncSmallIcon, hg as SyncToFileIcon, hh as TableAsteriskIcon, hi as TableClockIcon, hj as TableCombineIcon, hk as TableGlassesIcon, hl as TableGlobeIcon, hm as TableIcon, hn as TableLightningIcon, ho as TableMeasureIcon, hp as TableModelIcon, hq as TableStreamIcon, hr as TableVectorIcon, hs as TableViewIcon, ht as Tabs, hu as TagIcon, hv as TargetIcon, hw as TerminalIcon, hx as TextBoxIcon, hy as TextIcon, hz as TextJustifyIcon, hA as TextUnderlineIcon, hB as ThreeDotsIcon, hC as ThumbsDownIcon, hD as ThumbsUpIcon, hE as TokenIcon, hF as TrashIcon, hG as TreeIcon, hH as TrendingFillIcon, hI as TrendingIcon, hJ as TriangleIcon, hK as TypeaheadComboboxCheckboxItem, hL as TypeaheadComboboxFooter, hM as TypeaheadComboboxMenuItem, hN as TypeaheadComboboxMultiSelectStateChangeTypes, hO as TypeaheadComboboxStateChangeTypes, hP as UnderlineIcon, hQ as UndoIcon, hR as UploadIcon, hS as UsageOverageGraphic, hT as UsageSpikeGraphic, hU as UsbIcon, hV as UserBadgeIcon, hW as UserCircleIcon, hX as UserGroupFillIcon, hY as UserGroupIcon, hZ as UserIcon, h_ as UserKeyIconIcon, h$ as UserShieldIcon, i0 as UserSparkleIcon, i1 as UserTeamIcon, i2 as VisibleFillIcon, i3 as VisibleIcon, i4 as VisibleOffIcon, i5 as WithDesignSystemThemeHoc, i6 as WorkflowCodeIcon, i7 as WorkflowCubeIcon, i8 as WorkflowsIcon, i9 as WorkspacesIcon, ia as WrenchIcon, ib as WrenchSparkleIcon, ic as XCircleIcon, id as ZaHorizontalIcon, ie as ZaVerticalIcon, ig as ZoomInIcon, ih as ZoomMarqueeSelection, ii as ZoomOutIcon, ij as ZoomToFitIcon, ik as __INTERNAL_DO_NOT_USE__FormItem, il as __INTERNAL_DO_NOT_USE__Group, im as __INTERNAL_DO_NOT_USE__wrapLegacyFormRules, io as augmentWithDataComponentProps, ip as dialogComboboxLookAheadKeyDown, iq as getBottomOnlyShadowScrollStyles, ir as getButtonEmotionStyles, is as getComboboxOptionLabelStyles, it as getDialogComboboxOptionLabelWidth, iu as getHorizontalTabShadowStyles, iv as getKeyboardNavigationFunctions, iw as getMemoizedButtonEmotionStyles, m as getPanelContainmentStyle, ix as getTypographyColor, iy as getVirtualizedComboboxMenuItemStyles, iz as getWrapperStyle, iA as isOptionDisabled, iB as resetTabIndexToFocusedElement, iC as setImplicitContextGetter, iD as skipHideIconButtonActionClassName, iE as useAntDConfigProviderContext, iF as useDesignSystemEventSuppressInteractionContext, iG as useRadixModalContext } from './Tabs-Bz2WasfR.js';
import { jsx, jsxs, Fragment } from '@emotion/react/jsx-runtime';
import { css, Global, keyframes, createElement, useTheme, ClassNames } from '@emotion/react';
import { Collapse, Alert as Alert$1, AutoComplete as AutoComplete$1, Breadcrumb as Breadcrumb$1, Input as Input$2, DatePicker as DatePicker$1, Dropdown as Dropdown$1, Radio as Radio$1, Switch as Switch$1, Col as Col$1, Row as Row$1, Space as Space$1, notification, Popover as Popover$1, Skeleton, Pagination as Pagination$1, Table as Table$1, Tabs, Menu as Menu$1, Steps as Steps$1, Tree as Tree$1 } from 'antd';
import React__default, { useState, useRef, useEffect, useCallback, useMemo, forwardRef, useImperativeHandle, createContext, useContext, Children, Fragment as Fragment$1, useLayoutEffect } from 'react';
import { InfoCircleOutlined } from '@ant-design/icons';
import classnames from 'classnames';
import * as DialogPrimitive from '@radix-ui/react-dialog';
export { DialogPrimitive as UNSAFE_DialogPrimitive };
import '@radix-ui/react-tooltip-patch';
import { R as Root$8, T as Trigger$2, C as Content$2, A as Arrow$1, S as Spacer } from './WizardStepContentWrapper-Dsny-2z5.js';
export { d as Content, D as DocumentationSidebar, F as FIXED_VERTICAL_STEPPER_WIDTH, M as MAX_VERTICAL_WIZARD_CONTENT_WIDTH, N as Nav, e as NavButton, P as Panel, f as PanelBody, g as PanelHeader, h as PanelHeaderButtons, i as PanelHeaderTitle, j as Popover, k as Sidebar, l as Stepper, W as Wizard, a as WizardControlled, b as WizardModal, c as WizardStepContentWrapper, u as useWizardCurrentStep } from './WizardStepContentWrapper-Dsny-2z5.js';
import { useMergeRefs, useFloating, autoUpdate, offset, flip, shift } from '@floating-ui/react';
import 'lodash/uniqueId';
import isUndefined from 'lodash/isUndefined';
import { ContextMenuArrow, ContextMenuSub, ContextMenuGroup, ContextMenuTrigger, ContextMenuItemIndicator, ContextMenuCheckboxItem, ContextMenuPortal, ContextMenuContent, ContextMenuSubContent, ContextMenuSubTrigger, ContextMenuSeparator, ContextMenuRadioItem, ContextMenuRadioGroup, ContextMenuItem, ContextMenuLabel, ContextMenu as ContextMenu$2 } from '@radix-ui/react-context-menu';
import { format, isBefore, startOfMonth, isAfter, parse, isValid, startOfToday, startOfYesterday, sub, startOfWeek, endOfToday, endOfYesterday, getISOWeekYear, getISOWeek, parseISO, startOfISOWeek } from 'date-fns';
import { DayPicker, useDayRender, Button as Button$1 } from 'react-day-picker';
import * as Popover from '@radix-ui/react-popover';
import 'lodash/isEqual';
import { tabbable } from 'tabbable';
import { useController } from 'react-hook-form';
import { computePosition, flip as flip$1, size } from '@floating-ui/dom';
import { createPortal } from 'react-dom';
import * as RadixHoverCard from '@radix-ui/react-hover-card';
import * as RadixNavigationMenu from '@radix-ui/react-navigation-menu';
import * as Toast from '@radix-ui/react-toast';
import { RadioGroupItem, RadioGroup as RadioGroup$1 } from '@radix-ui/react-radio-group';
import * as Progress$1 from '@radix-ui/react-progress';
import * as RadixSlider from '@radix-ui/react-slider';
import * as Toggle from '@radix-ui/react-toggle';
import chroma from 'chroma-js';
import 'lodash/random';
import 'lodash/times';
import 'downshift';
import '@radix-ui/react-dropdown-menu';
import 'lodash/isNil';
import '@radix-ui/react-scroll-area';
import '@radix-ui/react-tabs';
import 'lodash/debounce';
import 'lodash/memoize';
import '@emotion/unitless';
import 'lodash/endsWith';
import 'lodash/isBoolean';
import 'lodash/isNumber';
import 'lodash/isString';
import 'lodash/mapValues';
import 'react-resizable';
import 'lodash/noop';
import 'lodash/pick';
import 'lodash/compact';

/**
 * A minimal DOM read/write scheduler to prevent layout thrashing.
 * Loosely based on https://github.com/wilsonpage/fastdom and using the same API.
 */ const noop = ()=>{};
function blockActiveInteractions(holdName) {
    return globalThis.__perfHooks?.rit?.blockActiveInteractions?.(holdName) ?? noop;
}
let measureQueue = [];
let updateQueue = [];
let isScheduled = false;
let releaseInteractionHolds = noop;
function runTasks(tasks) {
    tasks.forEach((task)=>{
        try {
            task.fn();
            task.resolve();
        } catch (error) {
            task.reject(error);
        }
    });
}
function flush() {
    isScheduled = false;
    // Snapshot and clear queues to prevent tasks queued during flush from running in the same frame.
    const [tasksToMeasure, tasksToUpdate] = [
        measureQueue,
        updateQueue
    ];
    measureQueue = [];
    updateQueue = [];
    const releaseAfterFlush = releaseInteractionHolds;
    try {
        let start = performance.now();
        runTasks(tasksToMeasure);
        performance.measure('fastdom-measure', {
            start,
            end: performance.now(),
            detail: {
                tasks: tasksToMeasure.length
            }
        });
        start = performance.now();
        runTasks(tasksToUpdate);
        performance.measure('fastdom-update', {
            start,
            end: performance.now(),
            detail: {
                tasks: tasksToUpdate.length
            }
        });
    } finally{
        // Release the interaction holds after the flush has completed.
        releaseAfterFlush();
    }
}
function scheduleFlush() {
    if (!isScheduled) {
        isScheduled = true;
        releaseInteractionHolds = blockActiveInteractions('wait-for-fastdom');
        requestAnimationFrame(flush);
    }
}
function createCancellableTask(fn, queue) {
    let task;
    const promise = new Promise((resolve, reject)=>{
        task = {
            fn,
            resolve,
            reject
        };
        queue.push(task);
        scheduleFlush();
    });
    promise.cancel = ()=>{
        measureQueue = measureQueue.filter((t)=>t !== task);
        updateQueue = updateQueue.filter((t)=>t !== task);
    };
    return promise;
}
/**
 * Schedules a DOM read task (e.g., getBoundingClientRect, offsetWidth).
 * @returns Promise that resolves when task completes. Call .cancel() to abort.
 */ function measure(fn) {
    return createCancellableTask(fn, measureQueue);
}
/**
 * Schedules a DOM write task (e.g., style.width, classList.add).
 * @returns Promise that resolves when task completes. Call .cancel() to abort.
 */ function update(fn) {
    return createCancellableTask(fn, updateQueue);
}
/**
 * Flush all pending measure/update tasks immediately.
 * Intended for use in tests to avoid depending on requestAnimationFrame timing.
 */ function flushFastdomForTesting() {
    while(measureQueue.length > 0 || updateQueue.length > 0 || isScheduled){
        flush();
    }
}

/**
 * Schedules fastdom tasks with automatic cleanup on unmount.
 * Propagates the first error in a batch to the nearest Error Boundary.
 */ function useFastDom() {
    const [error, setError] = useState(null);
    const tasks = useRef([]);
    if (error) throw error;
    useEffect(()=>{
        return ()=>{
            tasks.current.forEach((task)=>task.cancel());
        };
    }, []);
    const scheduleTask = useCallback((type, fn)=>{
        const promise = (type === 'measure' ? measure : update)(fn);
        tasks.current.push(promise);
        promise.catch((err)=>setError((prev)=>prev || err)).finally(()=>{
            tasks.current = tasks.current.filter((t)=>t !== promise);
        });
        return promise;
    }, []);
    return {
        measure: useCallback((fn)=>scheduleTask('measure', fn), [
            scheduleTask
        ]),
        mutate: useCallback((fn)=>scheduleTask('update', fn), [
            scheduleTask
        ])
    };
}

/**
 * ES-1267895 When entering composed characters, we should not submit forms on Enter.
 * For instance Japanese characters are composed and we should not submit forms when
 * the user is still composing the characters.
 *
 * This hook provides a reusable way to invoke a callback on Enter,
 * but not when composing characters.
 * This can be used to invoke a form submission callback when Enter is pressed.
 * @param callback VoidFunction to call when the Enter is pressed
 * @param allowBasicEnter If true, the callback will be invoked when Enter is pressed without any modifiers
 * @param allowPlatformEnter If true, the callback will be invoked when Enter is pressed with the platform modifier (CMD on Mac, CTRL on Windows)
 * @returns Object with onKeyDown, onCompositionEnd, and onCompositionStart event handlers
 *
 * @example
 * ```tsx
 * const handleSubmit = (event: React.KeyboardEvent) => {
 *  event.preventDefault();
 * // Submit the form
 * };
 * const eventHandlers = useCallbackOnEnter({
 *   callback: handleSubmit,
 *   allowBasicEnter: true,
 *   allowPlatformEnter: true,
 * })
 * return <input {...eventHandlers} />;
 * ```
 */ const useCallbackOnEnter = ({ callback, allowBasicEnter, allowPlatformEnter })=>{
    const isMacOs = useMemo(()=>navigator.userAgent.includes('Mac'), []);
    // Keeping track of whether we are composing characters
    // This is stored in a ref so that it can be accessed in the onKeyDown event handler
    // without causing a re-renders
    const isComposing = useRef(false);
    // Handler for when the composition starts
    const onCompositionStart = useCallback(()=>{
        isComposing.current = true;
    }, []);
    // Handler for when the composition ends
    const onCompositionEnd = useCallback(()=>{
        isComposing.current = false;
    }, []);
    // Handler for when a key is pressed
    // Used to submit the form when Enter is pressed
    const onKeyDown = useCallback((event)=>{
        // Only invoke the callback on Enter
        if (event.key !== 'Enter') return;
        // Do not submit on Enter if user is composing characters
        if (isComposing.current) return;
        // Do not submit on Enter if both are false
        if (!allowBasicEnter && !allowPlatformEnter) return;
        // Check if the event is a valid Enter press
        const basicEnter = allowBasicEnter && !event.metaKey && !event.ctrlKey && !event.shiftKey && !event.altKey;
        const platformEnter = allowPlatformEnter && (isMacOs ? event.metaKey : event.ctrlKey);
        const isValidEnterPress = basicEnter || platformEnter;
        // Submit the form if the Enter press is valid
        if (isValidEnterPress) callback(event);
    }, [
        allowBasicEnter,
        allowPlatformEnter,
        callback,
        isMacOs
    ]);
    return {
        onKeyDown,
        onCompositionEnd,
        onCompositionStart
    };
};

const setupDesignSystemEventProviderForTesting = (eventCallback)=>{
    const eventCallbackWrapper = (arg)=>{
        if (arg?.componentViewId) {
            const { componentViewId, ...rest } = arg; // strip componentViewId out of jest call args
            eventCallback(rest);
            return;
        }
        eventCallback(arg);
    };
    return {
        DesignSystemEventProviderForTest: ({ children })=>/*#__PURE__*/ jsx(DesignSystemEventProvider, {
                callback: eventCallbackWrapper,
                children: children
            })
    };
};

function useDesignSystemFlags() {
    const context = useDesignSystemContext();
    return context.flags;
}

/**
 * A helper hook that allows quick creation of theme-dependent styles.
 * Results in more compact code than using useMemo and
 * useDesignSystemTheme separately.
 *
 * @example
 * const styles = useThemedStyles((theme) => ({
 *   overlay: {
 *     backgroundColor: theme.colors.backgroundPrimary,
 *     borderRadius: theme.borders.borderRadiusMd,
 *   },
 *   wrapper: {
 *     display: 'flex',
 *     gap: theme.spacing.md,
 *   },
 * }));

 * <div css={styles.overlay}>...</div>
 *
 * @param styleFactory Factory function that accepts theme object as a parameter and returns
 *     the style object. **Note**: factory function body is being memoized internally and is intended
 *     to be used only for simple style objects that depend solely on the theme. If you want to use
 *     styles that change depending on external values (state, props etc.) you should use
 *     `useDesignSystemTheme` directly with  your own reaction mechanism.
 * @returns The constructed style object
 */ const useThemedStyles = (styleFactory)=>{
    const { theme } = useDesignSystemTheme();
    // We can assume that the factory function won't change and we're
    // observing theme changes only.
    const styleFactoryRef = useRef(styleFactory);
    return useMemo(()=>styleFactoryRef.current(theme), [
        theme
    ]);
};

function getAccordionEmotionStyles({ clsPrefix, theme, alignContentToEdge, isLeftAligned, secondaryStyle }) {
    const classItem = `.${clsPrefix}-item`;
    const classItemActive = `${classItem}-active`;
    const classHeader = `.${clsPrefix}-header`;
    const classContent = `.${clsPrefix}-content`;
    const classContentBox = `.${clsPrefix}-content-box`;
    const classArrow = `.${clsPrefix}-arrow`;
    const styles = {
        border: '0 none',
        background: 'none',
        [classItem]: {
            border: '0 none',
            [`&:hover`]: {
                [classHeader]: {
                    color: theme.colors.actionPrimaryBackgroundHover
                },
                [classArrow]: {
                    color: theme.colors.actionPrimaryBackgroundHover
                }
            },
            [`&:active`]: {
                [classHeader]: {
                    color: theme.colors.actionPrimaryBackgroundPress
                },
                [classArrow]: {
                    color: theme.colors.actionPrimaryBackgroundPress
                }
            }
        },
        [classHeader]: {
            ...isLeftAligned && {
                display: 'inline-flex',
                alignItems: 'center'
            },
            '&:focus-visible': {
                outlineColor: `${theme.colors.actionDefaultBorderFocus} !important`,
                outlineStyle: 'auto !important'
            }
        },
        [`& > ${classItem} > ${classHeader} > ${classArrow}`]: {
            fontSize: theme.general.iconFontSize,
            right: alignContentToEdge || isLeftAligned ? 0 : 12,
            ...isLeftAligned && {
                verticalAlign: 'middle',
                marginTop: -2
            }
        },
        [classArrow]: {
            color: theme.colors.textSecondary
        },
        [`& > ${classItemActive} > ${classHeader} > ${classArrow}`]: {
            transform: isLeftAligned ? 'rotate(180deg)' : 'translateY(-50%) rotate(180deg)'
        },
        [classContent]: {
            border: '0 none',
            backgroundColor: theme.colors.backgroundPrimary
        },
        [classContentBox]: {
            padding: alignContentToEdge ? '8px 0px 16px' : '8px 16px 16px'
        },
        [`& > ${classItem} > ${classHeader}`]: {
            padding: '6px 44px 6px 0',
            lineHeight: theme.typography.lineHeightBase,
            color: secondaryStyle ? theme.colors.textSecondary : theme.colors.textPrimary,
            fontWeight: secondaryStyle ? 400 : 600
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    return /*#__PURE__*/ css(styles);
}
const AccordionPanel = ({ dangerouslySetAntdProps, dangerouslyAppendEmotionCSS, children, ...props })=>{
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Collapse.Panel, {
            ...props,
            ...dangerouslySetAntdProps,
            css: dangerouslyAppendEmotionCSS,
            children: /*#__PURE__*/ jsx(RestoreAntDDefaultClsPrefix, {
                children: children
            })
        })
    });
};
const Accordion = /* #__PURE__ */ (()=>{
    const Accordion = ({ dangerouslySetAntdProps, dangerouslyAppendEmotionCSS, displayMode = 'multiple', analyticsEvents, componentId, valueHasNoPii, onChange, alignContentToEdge = false, chevronAlignment = 'right', secondaryStyle = false, ...props })=>{
        const emitOnView = safex('databricks.fe.observability.defaultComponentView.accordion', false);
        const { theme, getPrefixedClassName } = useDesignSystemTheme();
        // While this component is called `Accordion` for correctness, in AntD it is called `Collapse`.
        const clsPrefix = getPrefixedClassName('collapse');
        const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
                DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
                DesignSystemEventProviderAnalyticsEventTypes.OnView
            ] : [
                DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
            ]), [
            analyticsEvents,
            emitOnView
        ]);
        const eventContext = useDesignSystemEventComponentCallbacks({
            componentType: DesignSystemEventProviderComponentTypes.Accordion,
            componentId,
            analyticsEvents: memoizedAnalyticsEvents,
            valueHasNoPii
        });
        const { elementRef: accordionRef } = useNotifyOnFirstView({
            onView: eventContext.onView
        });
        const onChangeWrapper = useCallback((newValue)=>{
            if (Array.isArray(newValue)) {
                eventContext.onValueChange(JSON.stringify(newValue));
            } else {
                eventContext.onValueChange(newValue);
            }
            onChange?.(newValue);
        }, [
            eventContext,
            onChange
        ]);
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsx(Collapse, {
                // eslint-disable-next-line @databricks/no-unstable-nested-components -- go/no-nested-components
                expandIcon: ()=>/*#__PURE__*/ jsx(ChevronDownIcon, {
                        ...eventContext.dataComponentProps,
                        ref: accordionRef
                    }),
                expandIconPosition: chevronAlignment,
                accordion: displayMode === 'single',
                ...props,
                ...dangerouslySetAntdProps,
                css: [
                    getAccordionEmotionStyles({
                        clsPrefix,
                        theme,
                        alignContentToEdge,
                        isLeftAligned: chevronAlignment === 'left',
                        secondaryStyle
                    }),
                    dangerouslyAppendEmotionCSS,
                    addDebugOutlineStylesIfEnabled(theme)
                ],
                onChange: onChangeWrapper
            })
        });
    };
    Accordion.Panel = AccordionPanel;
    return Accordion;
})();

// TODO: Replace with custom icons
// TODO: Reuse in Alert
const filledIconsMap = {
    error: DangerFillIcon,
    warning: WarningFillIcon,
    success: CheckCircleFillIcon,
    info: InfoFillIcon
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const SeverityIcon = /*#__PURE__*/ forwardRef(function(props, ref) {
    const FilledIcon = filledIconsMap[props.severity];
    return /*#__PURE__*/ jsx(FilledIcon, {
        ref: ref,
        ...props
    });
});

const Alert = ({ componentId, analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnView
], dangerouslySetAntdProps, closable = true, closeIconLabel = 'Close alert', onClose, actions, showMoreContent, showMoreText = 'Show details', showMoreModalTitle = 'Details', forceVerticalActionsPlacement = false, forceHorizontalActionsPlacement = false, collapsible = false, defaultExpanded = true, expandToggleAriaLabel = 'Expand alert details', collapseToggleAriaLabel = 'Collapse alert details', size = 'large', ...props })=>{
    const { theme, getPrefixedClassName } = useDesignSystemTheme();
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Alert,
        componentId,
        componentSubType: DesignSystemEventProviderComponentSubTypeMap[props.type],
        analyticsEvents: memoizedAnalyticsEvents
    });
    const closeButtonEventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Button,
        componentId: componentId ? `${componentId}.close` : 'codegen_design_system_src_design_system_alert_alert.tsx_50',
        analyticsEvents: [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick
        ]
    });
    const { elementRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    const clsPrefix = getPrefixedClassName('alert');
    const [isModalOpen, setIsModalOpen] = useState(false);
    const [isExpanded, setIsExpanded] = useState(defaultExpanded);
    const descriptionRegionId = useStableUuidV4();
    const mergedProps = {
        ...props,
        type: props.type || 'error',
        showIcon: true,
        closable
    };
    const closeIconRef = useRef(null);
    useEffect(()=>{
        if (closeIconRef.current) {
            closeIconRef.current.removeAttribute('aria-label');
            closeIconRef.current.closest('button')?.setAttribute('aria-label', closeIconLabel);
        }
    }, [
        mergedProps.closable,
        closeIconLabel,
        closeIconRef
    ]);
    const onCloseWrapper = (e)=>{
        closeButtonEventContext.onClick(e);
        onClose?.(e);
    };
    const memoizedActions = useMemo(()=>{
        if (!actions?.length) return null;
        return actions.map((action, index)=>/*#__PURE__*/ jsx(Button, {
                size: "small",
                ...action
            }, index));
    }, [
        actions
    ]);
    const actionCount = actions?.length ?? 0;
    const placeActionsHorizontally = actionCount > 0 && !forceVerticalActionsPlacement && (actionCount === 1 || forceHorizontalActionsPlacement);
    const horizontalActionsNode = memoizedActions && placeActionsHorizontally ? /*#__PURE__*/ jsx("div", {
        css: {
            display: 'flex',
            flexDirection: 'row',
            flexWrap: 'wrap',
            gap: theme.spacing.sm,
            alignItems: 'center',
            justifyContent: 'flex-end',
            flexShrink: 0,
            marginInlineStart: 'auto'
        },
        children: memoizedActions
    }) : null;
    const hasDescription = props.description != null || Boolean(showMoreContent);
    // Put horizontal `actions` inside `message` so the description body spans the full content width
    // and flows beneath the buttons (antd's `action` slot keeps description in a narrow column beside actions).
    // Reserve ~one short status title line (e.g. "Gateway starting") before sharing a row with actions;
    // when the row is too narrow, actions wrap below via flex-wrap instead of squeezing the title.
    const messageWithHorizontalActions = horizontalActionsNode && !mergedProps.action ? /*#__PURE__*/ jsxs("div", {
        css: {
            display: 'flex',
            flexDirection: 'row',
            flexWrap: actionCount > 1 ? 'wrap' : 'nowrap',
            alignItems: hasDescription ? 'flex-start' : 'center',
            gap: theme.spacing.md,
            justifyContent: 'flex-start',
            width: '100%'
        },
        children: [
            /*#__PURE__*/ jsx("div", {
                css: {
                    flex: '1 1 auto',
                    maxWidth: '100%'
                },
                children: mergedProps.message
            }),
            horizontalActionsNode
        ]
    }) : null;
    const hasVerticalActions = memoizedActions != null && !placeActionsHorizontally;
    const showCollapseToggle = collapsible && (hasDescription || hasVerticalActions);
    const hasDescriptionSlotContent = hasDescription || hasVerticalActions || showCollapseToggle;
    const descriptionTextNode = hasDescription ? /*#__PURE__*/ jsxs("div", {
        css: {
            overflowWrap: 'anywhere',
            wordBreak: 'break-word'
        },
        children: [
            props.description,
            ' ',
            showMoreContent && /*#__PURE__*/ jsx(Typography.Link, {
                href: "#",
                componentId: componentId ? `${componentId}.show_more` : 'alert.show_more',
                onClick: (e)=>{
                    e.preventDefault();
                    setIsModalOpen(true);
                },
                children: showMoreText
            })
        ]
    }) : null;
    const verticalActionsNode = hasVerticalActions ? /*#__PURE__*/ jsx("div", {
        css: {
            display: 'flex',
            gap: theme.spacing.sm,
            marginTop: theme.spacing.xs,
            marginBottom: theme.spacing.xs
        },
        children: memoizedActions
    }) : null;
    const descriptionBody = /*#__PURE__*/ jsxs(Fragment, {
        children: [
            descriptionTextNode,
            verticalActionsNode
        ]
    });
    // Always pass a description element so AntD consistently applies its `.ant-alert-with-description`
    // class. The style function overrides all dist side-effects of that class. Without this, title-only
    // alerts would get different base AntD styles (icon positioning, padding, font sizes) causing layout
    // shifts. The description container is hidden via CSS when there is no content to display.
    const finalDescription = !hasDescriptionSlotContent ? /*#__PURE__*/ jsx(Fragment, {}) : showCollapseToggle ? /*#__PURE__*/ jsx("div", {
        id: descriptionRegionId,
        css: !isExpanded ? {
            display: 'none'
        } : undefined,
        children: descriptionBody
    }) : descriptionBody;
    const alertIcon = /*#__PURE__*/ jsxs("div", {
        css: {
            display: 'inline-flex',
            flexDirection: 'row',
            alignItems: 'center',
            gap: theme.spacing.xs
        },
        children: [
            showCollapseToggle && /*#__PURE__*/ jsx(Button, {
                "aria-expanded": isExpanded,
                "aria-controls": descriptionRegionId,
                "aria-label": isExpanded ? collapseToggleAriaLabel : expandToggleAriaLabel,
                componentId: componentId ? `${componentId}.collapse_toggle` : 'alert.collapse_toggle',
                htmlType: "button",
                icon: isExpanded ? /*#__PURE__*/ jsx(ChevronDownIcon, {
                    css: {
                        color: theme.colors.textSecondary
                    }
                }) : /*#__PURE__*/ jsx(ChevronRightIcon, {
                    css: {
                        color: theme.colors.textSecondary
                    }
                }),
                onClick: ()=>setIsExpanded((v)=>!v),
                size: "small",
                type: "tertiary"
            }),
            /*#__PURE__*/ jsx(SeverityIcon, {
                severity: mergedProps.type,
                ref: elementRef
            })
        ]
    });
    return /*#__PURE__*/ jsxs(DesignSystemAntDConfigProvider, {
        children: [
            /*#__PURE__*/ jsx(Alert$1, {
                ...addDebugOutlineIfEnabled(),
                ...mergedProps,
                onClose: onCloseWrapper,
                className: classnames(mergedProps.className),
                css: getAlertEmotionStyles(clsPrefix, theme, mergedProps.type, size, mergedProps.banner, hasDescriptionSlotContent),
                icon: alertIcon,
                closeText: mergedProps.closable && /*#__PURE__*/ jsx(CloseSmallIcon, {
                    ref: closeIconRef,
                    "aria-label": closeIconLabel,
                    css: {
                        marginTop: placeActionsHorizontally ? theme.spacing.xs : 2
                    }
                }),
                description: finalDescription,
                message: messageWithHorizontalActions ?? mergedProps.message,
                action: mergedProps.action,
                ...dangerouslySetAntdProps,
                ...eventContext.dataComponentProps
            }),
            showMoreContent && /*#__PURE__*/ jsx(Modal, {
                title: showMoreModalTitle,
                visible: isModalOpen,
                onCancel: ()=>setIsModalOpen(false),
                componentId: componentId ? `${componentId}.show_more_modal` : 'alert.show_more_modal',
                footer: null,
                size: "wide",
                children: showMoreContent
            })
        ]
    });
};
/**
 * Single flat style function. Antd always receives a `description` prop so `-with-description` is
 * always applied. We override every dist side-effect (icon absolute positioning, enlarged font sizes,
 * altered padding) here so there is only one layout path: flex-wrap row with the description on row 2.
 */ const getAlertEmotionStyles = (clsPrefix, theme, alertType, size, isBanner, hasDescriptionContent)=>{
    const isSmall = size === 'small';
    const classCloseIcon = `.${clsPrefix}-close-icon`;
    const classCloseText = `.${clsPrefix}-close-text`;
    const classDescription = `.${clsPrefix}-description`;
    const classMessage = `.${clsPrefix}-message`;
    const classIcon = `.${clsPrefix}-icon`;
    const BORDER_SIZE = theme.general.borderWidth;
    const LARGE_PADDING = theme.spacing.xs * 3;
    const SMALL_PADDING = theme.spacing.sm;
    const ICON_GAP = theme.spacing.sm;
    const styles = {
        // --- Layout ---
        // AntD DOM: .alert-icon | .alert-content (message + description) | .alert-close-icon
        // When there is description content, flex-start keeps icon/close aligned with the first
        // line of the message while the description flows below. Without description content,
        // center aligns icon, message (which may include inline action buttons), and close button.
        display: 'flex !important',
        alignItems: `${hasDescriptionContent ? 'flex-start' : 'center'} !important`,
        // --- General tokens ---
        // !important defeats dist's `.alert-with-description { padding: 7px 15px 7px 24px }`
        // which applies asymmetric padding intended for the original absolute-positioned icon layout.
        padding: isSmall ? `${theme.spacing.xs + BORDER_SIZE}px ${SMALL_PADDING}px !important` : `${LARGE_PADDING - BORDER_SIZE}px ${LARGE_PADDING}px !important`,
        boxSizing: 'border-box',
        borderRadius: theme.borders.borderRadiusSm,
        borderColor: theme.colors.border,
        // --- Icon: reset dist absolute positioning, keep at top-start ---
        [classIcon]: {
            position: 'relative !important',
            top: 'auto !important',
            left: 'auto !important',
            fontSize: theme.general.iconFontSize,
            display: 'inline-flex',
            alignItems: 'center',
            height: theme.typography.lineHeightBase,
            marginTop: 0,
            marginRight: ICON_GAP
        },
        // --- Message: fill remaining space on row 1, override dist 18px ---
        [classMessage]: {
            flex: '1 1 auto',
            minWidth: 0,
            fontSize: theme.typography.fontSizeBase,
            fontWeight: theme.typography.typographyBoldFontWeight,
            lineHeight: theme.typography.lineHeightBase,
            marginBottom: 0
        },
        // --- Description: stacks below message inside .alert-content ---
        [classDescription]: {
            lineHeight: theme.typography.lineHeightBase,
            // Hide the description container for title-only alerts. We always pass a description
            // element to AntD to keep the `.ant-alert-with-description` class applied, but when
            // there is no actual content the container must not take up space.
            ...!hasDescriptionContent && {
                display: 'none'
            }
        },
        // --- Close button: height matches icon/text so the centered X aligns ---
        [classCloseIcon]: {
            lineHeight: theme.typography.lineHeightBase,
            height: theme.typography.lineHeightBase,
            width: theme.general.iconFontSize,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            position: 'relative !important',
            top: 'auto !important',
            right: 'auto !important',
            marginLeft: ICON_GAP,
            '&:focus-visible': {
                outlineStyle: 'auto',
                outlineColor: theme.colors.actionDefaultBorderFocus
            }
        },
        [`${classCloseText} > span`]: {
            lineHeight: theme.typography.lineHeightBase,
            height: theme.general.iconFontSize,
            width: theme.general.iconFontSize,
            fontSize: theme.general.iconFontSize,
            display: 'inline-flex',
            alignItems: 'center'
        },
        // --- Type colors ---
        ...alertType === 'warning' && {
            color: theme.colors.textValidationWarning,
            borderColor: theme.colors.yellow300
        },
        ...alertType === 'error' && {
            color: theme.colors.textValidationDanger,
            borderColor: theme.colors.red300
        },
        ...alertType === 'success' && {
            color: theme.colors.textValidationSuccess,
            borderColor: theme.colors.green300,
            backgroundColor: theme.colors.backgroundSuccess
        },
        // --- Banner ---
        ...isBanner && {
            borderStyle: 'solid',
            borderWidth: `${theme.general.borderWidth}px 0`
        },
        // --- After closed ---
        '&[data-show="false"]': {
            borderWidth: 0,
            padding: '0 !important',
            width: 0,
            height: 0
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    return /*#__PURE__*/ css(styles);
};

const getGlobalStyles = (theme)=>{
    const bodyStyles = {
        backgroundColor: theme.colors.backgroundPrimary,
        color: theme.colors.textPrimary,
        '--dubois-global-background-color': theme.colors.backgroundPrimary,
        '--dubois-global-color': theme.colors.textPrimary,
        ...globalResetStyles()
    };
    return /*#__PURE__*/ css({
        'body, .mfe-root': bodyStyles
    });
};
const globalResetStyles = ()=>{
    return serverSideSafe('databricks.fe.eda.enableBorderBoxCssReset', false) ? {
        boxSizing: 'border-box',
        '*, *::before, *::after': {
            boxSizing: 'border-box'
        }
    } : {
        '[class^=du-bois]': {
            boxSizing: 'border-box',
            '*, *::before, *::after': {
                boxSizing: 'border-box'
            }
        }
    };
};
// Only used in storybook
const getGlobalResetStyles = ()=>{
    return /*#__PURE__*/ css(globalResetStyles());
};
const ApplyGlobalStyles = ()=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(Global, {
        styles: [
            getGlobalStyles(theme)
        ]
    });
};

/**
 * @deprecated Use `TypeaheadCombobox` instead.
 */ const AutoComplete = /* #__PURE__ */ (()=>{
    const AutoComplete = ({ dangerouslySetAntdProps, ...props })=>{
        const { theme } = useDesignSystemTheme();
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsx(AutoComplete$1, {
                ...addDebugOutlineIfEnabled(),
                dropdownStyle: {
                    boxShadow: theme.general.shadowLow,
                    ...getDarkModePortalStyles(theme)
                },
                ...props,
                ...dangerouslySetAntdProps,
                css: css(getAnimationCss(theme.options.enableAnimation))
            })
        });
    };
    AutoComplete.Option = AutoComplete$1.Option;
    return AutoComplete;
})();

function SvgDatabricksIcon(props) {
    return /*#__PURE__*/ jsxs("svg", {
        viewBox: "0 0 24 24",
        width: "100%",
        height: "100%",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: [
            /*#__PURE__*/ jsx("rect", {
                width: "24",
                height: "24",
                fill: "#FF3621"
            }),
            /*#__PURE__*/ jsx("path", {
                d: "m18.8 10.515-6.8228 3.945-7.3059-4.215-0.35138 0.195v3.06l7.6573 4.41 6.8228-3.93v1.62l-6.8228 3.945-7.3059-4.215-0.35138 0.195v0.525l7.6573 4.41 7.6427-4.41v-3.06l-0.3514-0.195-7.2913 4.2-6.8374-3.93v-1.62l6.8374 3.93 7.6427-4.41v-3.015l-0.3807-0.225-7.262 4.185-6.486-3.72 6.486-3.735 5.3294 3.075 0.4685-0.27v-0.375l-5.7979-3.345-7.6573 4.41v0.48l7.6573 4.41 6.8228-3.945v1.62z",
                fill: "#fff"
            })
        ]
    });
}

function SvgUserIcon(props) {
    return /*#__PURE__*/ jsx("svg", {
        xmlns: "http://www.w3.org/2000/svg",
        width: "100%",
        height: "auto",
        fill: "none",
        viewBox: "0 0 400 400",
        ...props,
        children: /*#__PURE__*/ jsx("path", {
            d: "M200 69.333c-44.873 0-81.25 36.377-81.25 81.25s36.377 81.25 81.25 81.25 81.25-36.377 81.25-81.25-36.377-81.25-81.25-81.25Zm0 200c-69.162 0-130.835 32.119-170.89 82.181A18.748 18.748 0 0 0 25 363.228v37.355c0 10.356 8.395 18.75 18.75 18.75h312.5c10.355 0 18.75-8.394 18.75-18.75v-37.355c0-4.258-1.449-8.39-4.11-11.714-40.055-50.062-101.728-82.181-170.89-82.181Z",
            fill: "#5F7281"
        })
    });
}

/**
 * `LegacyInfoTooltip` is deprecated in favor of the new `InfoTooltip` component
 * @deprecated
 */ const LegacyInfoTooltip = ({ title, tooltipProps, iconTitle, isKeyboardFocusable = true, ...iconProps })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(LegacyTooltip, {
        useAsLabel: true,
        title: title,
        ...tooltipProps,
        children: /*#__PURE__*/ jsx("span", {
            ...addDebugOutlineIfEnabled(),
            style: {
                display: 'inline-flex'
            },
            children: /*#__PURE__*/ jsx(InfoCircleOutlined, {
                tabIndex: isKeyboardFocusable ? 0 : -1,
                "aria-hidden": "false",
                "aria-label": iconTitle,
                alt: iconTitle,
                css: {
                    fontSize: theme.typography.fontSizeSm,
                    color: theme.colors.textSecondary
                },
                ...iconProps
            })
        })
    });
};

const InfoPopover = ({ children, popoverProps, iconTitle, iconProps, isKeyboardFocusable = true, ariaLabel = 'More details' })=>{
    const { theme } = useDesignSystemTheme();
    const { isInsideModal } = useModalContext();
    const [open, setOpen] = useState(false);
    const handleKeyDown = (event)=>{
        if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            setOpen(!open);
        }
    };
    const { onKeyDown, ...restPopoverProps } = popoverProps || {};
    return /*#__PURE__*/ jsxs(Root$8, {
        componentId: "codegen_design-system_src_design-system_popover_infopopover.tsx_36",
        open: open,
        onOpenChange: setOpen,
        children: [
            /*#__PURE__*/ jsx(Trigger$2, {
                asChild: true,
                children: /*#__PURE__*/ jsx("span", {
                    style: {
                        display: 'inline-flex',
                        cursor: 'pointer'
                    },
                    tabIndex: isKeyboardFocusable ? 0 : -1,
                    onKeyDown: handleKeyDown,
                    "aria-label": iconTitle ? undefined : ariaLabel,
                    role: "button",
                    onClick: (e)=>{
                        e.preventDefault();
                        e.stopPropagation();
                        setOpen(!open);
                    },
                    children: /*#__PURE__*/ jsx(InfoSmallIcon, {
                        "aria-hidden": iconTitle ? false : true,
                        title: iconTitle,
                        "aria-label": iconTitle,
                        css: {
                            color: theme.colors.textSecondary
                        },
                        ...iconProps
                    })
                })
            }),
            /*#__PURE__*/ jsxs(Content$2, {
                align: "start",
                onKeyDown: (e)=>{
                    if (e.key === 'Escape') {
                        // If inside an AntD Modal, stop propagation of Escape key so that the modal doesn't close.
                        // This is specifically for that case, so we only do it if inside a modal to limit the blast radius.
                        if (isInsideModal) {
                            e.stopPropagation();
                            // If stopping propagation, we also need to manually close the popover since the radix
                            // library expects the event to bubble up to the parent components.
                            setOpen(false);
                        }
                    }
                    onKeyDown?.(e);
                },
                ...restPopoverProps,
                children: [
                    children,
                    /*#__PURE__*/ jsx(Arrow$1, {})
                ]
            })
        ]
    });
};

const OverflowPopover = ({ items, renderLabel, tooltipText, ariaLabel = 'More items', ...props })=>{
    const { theme } = useDesignSystemTheme();
    const [showTooltip, setShowTooltip] = useState(true);
    const label = `+${items.length}`;
    let trigger = /*#__PURE__*/ jsx("span", {
        css: {
            lineHeight: 0
        },
        ...addDebugOutlineIfEnabled(),
        children: /*#__PURE__*/ jsx(Trigger$2, {
            asChild: true,
            children: /*#__PURE__*/ jsx(Button, {
                componentId: "something",
                type: "link",
                children: renderLabel ? renderLabel(label) : label
            })
        })
    });
    if (showTooltip) {
        trigger = /*#__PURE__*/ jsx(LegacyTooltip, {
            title: tooltipText || 'See more items',
            children: trigger
        });
    }
    return /*#__PURE__*/ jsxs(Root$8, {
        componentId: "codegen_design-system_src_design-system_overflow_overflowpopover.tsx_37",
        onOpenChange: (open)=>setShowTooltip(!open),
        children: [
            trigger,
            /*#__PURE__*/ jsx(Content$2, {
                align: "start",
                "aria-label": ariaLabel,
                ...props,
                ...addDebugOutlineIfEnabled(),
                children: /*#__PURE__*/ jsx("div", {
                    css: {
                        display: 'flex',
                        flexDirection: 'column',
                        gap: theme.spacing.xs
                    },
                    children: items.map((item, index)=>/*#__PURE__*/ jsx("div", {
                            children: item
                        }, `overflow-${index}`))
                })
            })
        ]
    });
};

const SIZE = new Map([
    [
        'xl',
        {
            avatarSize: 48,
            fontSize: 18,
            groupShift: 12,
            iconSize: 24
        }
    ],
    [
        'lg',
        {
            avatarSize: 40,
            fontSize: 16,
            groupShift: 8,
            iconSize: 20
        }
    ],
    [
        'md',
        {
            avatarSize: 32,
            fontSize: 14,
            groupShift: 4,
            iconSize: 16
        }
    ],
    [
        'sm',
        {
            avatarSize: 24,
            fontSize: 12,
            groupShift: 4,
            iconSize: 14
        }
    ],
    [
        'xs',
        {
            avatarSize: 20,
            fontSize: 12,
            groupShift: 2,
            iconSize: 12
        }
    ],
    [
        'xxs',
        {
            avatarSize: 16,
            fontSize: 11,
            groupShift: 2,
            iconSize: 12
        }
    ]
]);
const DEFAULT_SIZE = 'sm';
function getAvatarEmotionStyles({ backgroundColor, size = DEFAULT_SIZE, theme }) {
    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion -- TODO(FEINF-3982)
    const { avatarSize, fontSize, iconSize } = SIZE.get(size);
    return {
        abbr: {
            color: theme.colors.tagText,
            textDecoration: 'none',
            textTransform: 'uppercase'
        },
        default: {
            height: avatarSize,
            width: avatarSize,
            fontSize,
            [`.${theme.general.iconfontCssPrefix}`]: {
                fontSize: iconSize
            }
        },
        icon: {
            alignItems: 'center',
            color: backgroundColor ? theme.colors.tagText : theme.colors.textSecondary,
            backgroundColor: backgroundColor ? theme.colors[backgroundColor] : theme.DU_BOIS_INTERNAL_ONLY.colors.tagBackgroundDefault,
            display: 'flex',
            justifyContent: 'center'
        },
        img: {
            objectFit: 'cover',
            objectPosition: 'center'
        },
        system: {
            borderRadius: theme.borders.borderRadiusSm,
            overflow: 'hidden'
        },
        user: {
            borderRadius: '100%',
            overflow: 'hidden'
        },
        userIcon: {
            alignItems: 'flex-end'
        }
    };
}
/** Generate random number from a string between 0 - (maxRange - 1) */ function getRandomNumberFromString({ value, maxRange }) {
    let hash = 0;
    let char = 0;
    if (value.length === 0) return hash;
    for(let i = 0; i < value.length; i++){
        char = value.charCodeAt(i);
        hash = (hash << 5) - hash + char;
        hash = hash & hash;
    }
    const idx = Math.abs(hash % maxRange);
    return idx;
}
function getAvatarBackgroundColor(label, theme) {
    const randomNumber = getRandomNumberFromString({
        value: label,
        maxRange: 5
    });
    switch(randomNumber){
        case 0:
            return theme.colors.indigo;
        case 1:
            return theme.colors.teal;
        case 2:
            return theme.colors.pink;
        case 3:
            return theme.colors.brown;
        case 4:
        default:
            return theme.colors.purple;
    }
}
function Avatar(props) {
    const { theme } = useDesignSystemTheme();
    const styles = getAvatarEmotionStyles({
        size: props.size,
        theme,
        backgroundColor: 'backgroundColor' in props ? props.backgroundColor : undefined
    });
    switch(props.type){
        case 'entity':
            if ('src' in props && props.src) {
                return /*#__PURE__*/ jsx("img", {
                    css: [
                        styles.default,
                        styles.img,
                        styles.system
                    ],
                    src: props.src,
                    alt: props.label
                });
            }
            if ('icon' in props && props.icon) {
                return /*#__PURE__*/ jsx("div", {
                    css: [
                        styles.default,
                        styles.system,
                        styles.icon
                    ],
                    role: "img",
                    "aria-label": props.label,
                    children: props.icon
                });
            }
            // display first initial of name when no image / icon is provided
            return /*#__PURE__*/ jsx("div", {
                css: [
                    styles.default,
                    styles.system,
                    styles.icon,
                    {
                        backgroundColor: getAvatarBackgroundColor(props.label, theme)
                    }
                ],
                children: /*#__PURE__*/ jsx("abbr", {
                    css: styles.abbr,
                    title: props.label,
                    children: props.label.substring(0, 1)
                })
            });
        case 'user':
            if ('label' in props && props.label.trim()) {
                if (props.src) {
                    return /*#__PURE__*/ jsx("img", {
                        css: [
                            styles.default,
                            styles.img,
                            styles.user
                        ],
                        src: props.src,
                        alt: props.label
                    });
                } else if (props.icon) {
                    return /*#__PURE__*/ jsx("div", {
                        css: [
                            styles.default,
                            styles.user,
                            styles.icon
                        ],
                        role: "img",
                        "aria-label": props.label,
                        children: props.icon
                    });
                }
                // display first initial of name when no image / icon is provided
                return /*#__PURE__*/ jsx("div", {
                    css: [
                        styles.default,
                        styles.user,
                        styles.icon,
                        {
                            backgroundColor: getAvatarBackgroundColor(props.label, theme)
                        }
                    ],
                    children: /*#__PURE__*/ jsx("abbr", {
                        css: styles.abbr,
                        title: props.label,
                        children: props.label.substring(0, 1)
                    })
                });
            }
            // default to user icon when no user info is provided
            return /*#__PURE__*/ jsx("div", {
                css: [
                    styles.default,
                    styles.user,
                    styles.icon,
                    styles.userIcon
                ],
                role: "img",
                "aria-label": "user",
                children: /*#__PURE__*/ jsx(SvgUserIcon, {})
            });
    }
}
function DBAssistantAvatar({ size, label }) {
    const defaultLabel = 'Genie Code';
    return /*#__PURE__*/ jsx(Avatar, {
        size: size,
        type: "entity",
        label: label ?? defaultLabel,
        icon: /*#__PURE__*/ jsx(SvgDatabricksIcon, {})
    });
}
function AssistantAvatar({ backgroundColor, size, label }) {
    const defaultLabel = 'Genie Code';
    return /*#__PURE__*/ jsx(Avatar, {
        backgroundColor: backgroundColor,
        size: size,
        type: "entity",
        label: label ?? defaultLabel,
        icon: /*#__PURE__*/ jsx(SparkleDoubleIcon, {})
    });
}
const MAX_AVATAR_GROUP_USERS = 3;
function getAvatarGroupEmotionStyles(theme) {
    return {
        container: {
            display: 'flex',
            alignItems: 'center',
            gap: theme.spacing.xs
        },
        avatarsContainer: {
            display: 'flex'
        },
        avatar: {
            display: 'flex',
            borderRadius: '100%',
            border: `1px solid ${theme.colors.backgroundPrimary}`,
            position: 'relative'
        }
    };
}
function AvatarGroup({ size = DEFAULT_SIZE, users }) {
    const { theme } = useDesignSystemTheme();
    const styles = getAvatarGroupEmotionStyles(theme);
    const displayedUsers = useMemo(()=>users.slice(0, MAX_AVATAR_GROUP_USERS), [
        users
    ]);
    const extraUsers = useMemo(()=>users.slice(MAX_AVATAR_GROUP_USERS), [
        users
    ]);
    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion -- TODO(FEINF-3982)
    const { avatarSize, groupShift: avatarShift, fontSize } = SIZE.get(size);
    return /*#__PURE__*/ jsxs("div", {
        css: styles.container,
        children: [
            /*#__PURE__*/ jsx("div", {
                css: {
                    ...styles.avatarsContainer,
                    width: (avatarSize + 2 - avatarShift) * displayedUsers.length + avatarShift
                },
                children: displayedUsers.map((user, idx)=>/*#__PURE__*/ jsx("div", {
                        css: {
                            ...styles.avatar,
                            left: -avatarShift * idx
                        },
                        children: /*#__PURE__*/ jsx(Avatar, {
                            size: size,
                            type: "user",
                            ...user
                        })
                    }, `${user.label}-idx`))
            }),
            extraUsers.length > 0 && /*#__PURE__*/ jsx(OverflowPopover, {
                items: extraUsers.map((user)=>user.label),
                tooltipText: "Show more users",
                renderLabel: (label)=>/*#__PURE__*/ jsx("span", {
                        css: {
                            fontSize: `${fontSize}px !important`
                        },
                        children: label
                    })
            })
        ]
    });
}

const Breadcrumb = /* #__PURE__ */ (()=>{
    const Breadcrumb = ({ dangerouslySetAntdProps, includeTrailingCaret = true, ...props })=>{
        const { theme, classNamePrefix } = useDesignSystemTheme();
        const separatorClass = `.${classNamePrefix}-breadcrumb-separator`;
        const styles = css({
            // `antd` forces the last anchor to be black, so that it doesn't look like an anchor
            // (even though it is one). This undoes that; if the user wants to make the last
            // text-colored, they can do that by not using an anchor.
            'span:last-child a': {
                color: theme.colors.primary,
                // TODO: Need to pull a global color for anchor hover/focus. Discuss with Ginny.
                ':hover, :focus': {
                    color: '#2272B4'
                }
            },
            // TODO: Consider making this global within dubois components
            a: {
                '&:focus-visible': {
                    outlineColor: `${theme.colors.actionDefaultBorderFocus} !important`,
                    outlineStyle: 'auto !important'
                }
            },
            [separatorClass]: {
                fontSize: theme.general.iconFontSize,
                '& .anticon': {
                    fontSize: 13
                }
            },
            '& > span': {
                display: 'inline-flex',
                alignItems: 'center'
            }
        });
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsxs(Breadcrumb$1, {
                ...addDebugOutlineIfEnabled(),
                separator: /*#__PURE__*/ jsx(ChevronRightIcon, {}),
                ...props,
                ...dangerouslySetAntdProps,
                css: css(getAnimationCss(theme.options.enableAnimation), styles),
                children: [
                    props.children,
                    includeTrailingCaret && props.children && /*#__PURE__*/ jsx(Breadcrumb.Item, {
                        children: " "
                    })
                ]
            })
        });
    };
    Breadcrumb.Item = Breadcrumb$1.Item;
    Breadcrumb.Separator = Breadcrumb$1.Separator;
    return Breadcrumb;
})();

// This is a very simple PRNG that is seeded (so that the output is deterministic).
// We need this in order to produce a random ragged edge for the table skeleton.
function pseudoRandomNumberGeneratorFromSeed(seed) {
    // This is a simple way to get a consistent number from a string;
    // `charCodeAt` returns a number between 0 and 65535, and we then just add them all up.
    const seedValue = seed.split('').map((char)=>char.charCodeAt(0)).reduce((prev, curr)=>prev + curr, 0);
    // This is a simple sine wave function that will always return a number between 0 and 1.
    // This produces a value akin to `Math.random()`, but has deterministic output.
    // Of course, sine curves are not a perfectly random distribution between 0 and 1, but
    // it's close enough for our purposes.
    return Math.sin(seedValue) / 2 + 0.5;
}
// This is a simple Fisher-Yates shuffler using the above PRNG.
function shuffleArray(arr, seed) {
    for(let i = arr.length - 1; i > 0; i--){
        const j = Math.floor(pseudoRandomNumberGeneratorFromSeed(seed + String(i)) * (i + 1));
        [arr[i], arr[j]] = [
            arr[j],
            arr[i]
        ];
    }
    return arr;
}
// Finally, we shuffle a list off offsets to apply to the widths of the cells.
// This ensures that the cells are not all the same width, but that they are
// random to produce a more realistic looking skeleton.
function getOffsets(seed) {
    return shuffleArray([
        48,
        24,
        0
    ], seed);
}
const skeletonLoading = /*#__PURE__*/ keyframes({
    '0%': {
        backgroundPosition: '100% 50%'
    },
    '100%': {
        backgroundPosition: '0 50%'
    }
});
const genSkeletonAnimatedColor = (theme, frameRate = 60)=>{
    // TODO: Pull this from the themes; it's not currently available.
    const color = theme.isDarkMode ? 'rgba(255, 255, 255, 0.1)' : 'rgba(31, 38, 45, 0.1)';
    // Light mode value copied from Ant's Skeleton animation
    const colorGradientEnd = theme.isDarkMode ? 'rgba(99, 99, 99, 0.24)' : 'rgba(129, 129, 129, 0.24)';
    return /*#__PURE__*/ css({
        animationDuration: '1.4s',
        background: `linear-gradient(90deg, ${color} 25%, ${colorGradientEnd} 37%, ${color} 63%)`,
        backgroundSize: '400% 100%',
        animationName: skeletonLoading,
        animationTimingFunction: `steps(${frameRate}, end)`,
        // Based on data from perf dashboard, p95 loading time goes up to about 20s, so about 14 iterations is needed.
        animationIterationCount: 14
    });
};

const GenericContainerStyles = /*#__PURE__*/ css({
    cursor: 'progress',
    borderRadius: 'var(--border-radius)'
});
const GenericSkeleton = ({ label, frameRate = 60, style, loading = true, loadingDescription = 'GenericSkeleton', ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsxs("div", {
        ...addDebugOutlineIfEnabled(),
        css: [
            GenericContainerStyles,
            genSkeletonAnimatedColor(theme, frameRate)
        ],
        style: {
            ...style,
            ['--border-radius']: `${theme.general.borderRadiusBase}px`
        },
        ...restProps,
        children: [
            loading && /*#__PURE__*/ jsx(LoadingState, {
                description: loadingDescription
            }),
            /*#__PURE__*/ jsx("span", {
                css: visuallyHidden,
                children: label
            })
        ]
    });
};

const paragraphContainerStyles = /*#__PURE__*/ css({
    cursor: 'progress',
    width: '100%',
    height: 20,
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center'
});
const paragraphFillStyles = /*#__PURE__*/ css({
    borderRadius: 'var(--border-radius)',
    height: 8
});
const ParagraphSkeleton = ({ label, seed = '', frameRate = 60, style, loading = true, loadingDescription = 'ParagraphSkeleton', ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    const offsetWidth = getOffsets(seed)[0];
    return /*#__PURE__*/ jsxs("div", {
        ...addDebugOutlineIfEnabled(),
        css: paragraphContainerStyles,
        style: {
            ...style,
            ['--border-radius']: `${theme.general.borderRadiusBase}px`
        },
        ...restProps,
        children: [
            loading && /*#__PURE__*/ jsx(LoadingState, {
                description: loadingDescription
            }),
            /*#__PURE__*/ jsx("span", {
                css: visuallyHidden,
                children: label
            }),
            /*#__PURE__*/ jsx("div", {
                "aria-hidden": true,
                css: [
                    paragraphFillStyles,
                    genSkeletonAnimatedColor(theme, frameRate),
                    {
                        width: `calc(100% - ${offsetWidth}px)`
                    }
                ]
            })
        ]
    });
};

const titleContainerStyles = /*#__PURE__*/ css({
    cursor: 'progress',
    width: '100%',
    height: 28,
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center'
});
const titleFillStyles = /*#__PURE__*/ css({
    borderRadius: 'var(--border-radius)',
    height: 12,
    width: '100%'
});
const TitleSkeleton = ({ label, seed = '', frameRate = 60, style, loading = true, loadingDescription = 'TitleSkeleton', ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    const offsetWidth = getOffsets(seed)[0];
    return /*#__PURE__*/ jsxs("div", {
        ...addDebugOutlineIfEnabled(),
        css: titleContainerStyles,
        style: {
            ...style,
            ['--border-radius']: `${theme.general.borderRadiusBase}px`
        },
        ...restProps,
        children: [
            loading && /*#__PURE__*/ jsx(LoadingState, {
                description: loadingDescription
            }),
            /*#__PURE__*/ jsx("span", {
                css: visuallyHidden,
                children: label
            }),
            /*#__PURE__*/ jsx("div", {
                "aria-hidden": true,
                css: [
                    titleFillStyles,
                    genSkeletonAnimatedColor(theme, frameRate),
                    {
                        width: `calc(100% - ${offsetWidth}px)`
                    }
                ]
            })
        ]
    });
};

const TableContext = /*#__PURE__*/ createContext({
    size: 'default',
    grid: false
});
const Table = /*#__PURE__*/ forwardRef(function Table({ children, size = 'default', someRowsSelected, style, pagination, empty, className, scrollable = false, grid = false, noMinHeight = false, onScroll, ...rest }, ref) {
    const { theme } = useDesignSystemTheme();
    const tableContentRef = useRef(null);
    useImperativeHandle(ref, ()=>tableContentRef.current);
    const minHeightCss = noMinHeight ? {} : {
        minHeight: !empty && pagination ? 150 : 100
    };
    return /*#__PURE__*/ jsx(DesignSystemEventSuppressInteractionProviderContext.Provider, {
        value: DesignSystemEventSuppressInteractionTrueContextValue,
        children: /*#__PURE__*/ jsx(TableContext.Provider, {
            value: useMemo(()=>{
                return {
                    size,
                    someRowsSelected,
                    grid
                };
            }, [
                size,
                someRowsSelected,
                grid
            ]),
            children: /*#__PURE__*/ jsxs("div", {
                ...addDebugOutlineIfEnabled(),
                ...rest,
                // This is a performance optimization; we want to statically create the styles for the table,
                // but for the dynamic theme values, we need to use CSS variables.
                // See: https://emotion.sh/docs/best-practices#advanced-css-variables-with-style
                style: {
                    ...style,
                    ['--table-header-active-color']: theme.colors.actionDefaultTextPress,
                    ['colorScheme']: theme.isDarkMode ? 'dark' : undefined,
                    ['--table-header-background-color']: theme.colors.backgroundPrimary,
                    ['--table-header-focus-color']: theme.colors.actionDefaultTextHover,
                    ['--table-header-sort-icon-color']: theme.colors.textSecondary,
                    ['--table-header-text-color']: theme.colors.actionDefaultTextDefault,
                    ['--table-row-hover']: theme.colors.tableRowHover,
                    ['--table-separator-color']: theme.colors.border,
                    ['--table-resize-handle-color']: theme.colors.borderDecorative,
                    ['--table-spacing-md']: `${theme.spacing.md}px`,
                    ['--table-spacing-sm']: `${theme.spacing.sm}px`,
                    ['--table-spacing-xs']: `${theme.spacing.xs}px`
                },
                css: [
                    tableStyles.tableWrapper,
                    minHeightCss
                ],
                className: classnames({
                    'table-isScrollable': scrollable,
                    'table-isGrid': grid
                }, className),
                children: [
                    /*#__PURE__*/ jsxs("div", {
                        role: "table",
                        ref: tableContentRef,
                        css: [
                            tableStyles.table,
                            scrollable && {
                                flex: 'initial !important'
                            }
                        ],
                        // Needed to make panel body content focusable when scrollable for keyboard-only users to be able to focus & scroll
                        // eslint-disable-next-line jsx-a11y/no-noninteractive-tabindex
                        tabIndex: scrollable ? 0 : -1,
                        onScroll: onScroll,
                        children: [
                            children,
                            empty && /*#__PURE__*/ jsx("div", {
                                css: {
                                    padding: theme.spacing.lg
                                },
                                children: empty
                            })
                        ]
                    }),
                    !empty && pagination && /*#__PURE__*/ jsx("div", {
                        css: tableStyles.paginationContainer,
                        children: pagination
                    })
                ]
            })
        })
    });
});

// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const TableCell = /*#__PURE__*/ forwardRef(function({ children, className, ellipsis = false, multiline = false, align = 'left', style, wrapContent = true, ...rest }, ref) {
    const { size, grid } = useContext(TableContext);
    const { classNamePrefix } = useDesignSystemTheme();
    const linkWithEllipsisCss = /*#__PURE__*/ css({
        [`& > .${classNamePrefix}-typography > a.${classNamePrefix}-typography-ellipsis`]: {
            display: 'block'
        }
    });
    let typographySize = 'md';
    if (size === 'small') {
        typographySize = 'sm';
    }
    const content = wrapContent === true ? /*#__PURE__*/ jsx(Typography.Text, {
        ellipsis: !multiline,
        size: typographySize,
        title: !multiline && typeof children === 'string' && children || undefined,
        // Needed for the button focus outline to be visible for the expand/collapse buttons
        css: {
            '&:has(> button)': {
                overflow: 'visible'
            }
        },
        children: children
    }) : children;
    return /*#__PURE__*/ jsx("div", {
        ...rest,
        role: "cell",
        style: {
            textAlign: align,
            ...style
        },
        ref: ref,
        // PE-259 Use more performance className for grid but keep css= for compatibility.
        css: [
            !grid ? repeatingElementsStyles.cell : undefined,
            linkWithEllipsisCss
        ],
        className: classnames(grid && tableClassNames.cell, className),
        children: content
    });
});

const TableRowContext = /*#__PURE__*/ createContext({
    isHeader: false
});
const TableRow = /*#__PURE__*/ forwardRef(function TableRow({ children, className, style, isHeader = false, skipIconHiding = false, verticalAlignment, ...rest }, ref) {
    const { size, grid } = useContext(TableContext);
    const { theme } = useDesignSystemTheme();
    // Vertical only be larger if the row is a header AND size is large.
    const shouldUseLargeVerticalPadding = isHeader && size === 'default';
    let rowPadding;
    if (shouldUseLargeVerticalPadding) {
        rowPadding = theme.spacing.sm;
    } else if (size === 'default') {
        rowPadding = 6;
    } else {
        rowPadding = theme.spacing.xs;
    }
    return /*#__PURE__*/ jsx(TableRowContext.Provider, {
        value: useMemo(()=>{
            return {
                isHeader
            };
        }, [
            isHeader
        ]),
        children: /*#__PURE__*/ jsx("div", {
            ...rest,
            ref: ref,
            role: "row",
            style: {
                ...style,
                ['--table-row-vertical-padding']: `${rowPadding}px`
            },
            // PE-259 Use more performance className for grid but keep css= for consistency.
            css: [
                !isHeader && !skipIconHiding && hideIconButtonRowStyles,
                !grid && repeatingElementsStyles.row
            ],
            className: classnames(className, grid && tableClassNames.row, {
                'table-isHeader': isHeader,
                'table-row-isGrid': grid
            }),
            children: children
        })
    });
});

const TableRowActionStyles = {
    container: /*#__PURE__*/ css({
        width: 32,
        paddingTop: 'var(--vertical-padding)',
        paddingBottom: 'var(--vertical-padding)',
        display: 'flex',
        alignItems: 'start',
        justifyContent: 'center'
    })
};
const TableRowAction = /*#__PURE__*/ forwardRef(function TableRowAction({ children, style, className, ...rest }, ref) {
    const { size } = useContext(TableContext);
    const { isHeader } = useContext(TableRowContext);
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        ...rest,
        ref: ref,
        role: isHeader ? 'columnheader' : 'cell',
        style: {
            ...style,
            ['--vertical-padding']: size === 'default' ? `${theme.spacing.xs}px` : 0
        },
        css: TableRowActionStyles.container,
        className: classnames(className, !isHeader && hideIconButtonActionCellClassName),
        children: children
    });
});
/** @deprecated Use `TableRowAction` instead */ const TableRowMenuContainer = TableRowAction;

const TableSkeletonStyles = {
    container: /*#__PURE__*/ css({
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'flex-start'
    }),
    cell: /*#__PURE__*/ css({
        width: '100%',
        height: 8,
        borderRadius: 4,
        background: 'var(--table-skeleton-color)',
        marginTop: 'var(--table-skeleton-row-vertical-margin)',
        marginBottom: 'var(--table-skeleton-row-vertical-margin)'
    })
};
const TableSkeleton = ({ lines = 1, seed = '', frameRate = 60, style, label, ...rest })=>{
    const { theme } = useDesignSystemTheme();
    const { size } = useContext(TableContext);
    const widths = getOffsets(seed);
    return /*#__PURE__*/ jsxs("div", {
        ...rest,
        ...addDebugOutlineIfEnabled(),
        "aria-busy": true,
        css: TableSkeletonStyles.container,
        role: "status",
        style: {
            ...style,
            // TODO: Pull this from the themes; it's not currently available.
            ['--table-skeleton-color']: theme.isDarkMode ? 'rgba(255, 255, 255, 0.1)' : 'rgba(31, 38, 45, 0.1)',
            ['--table-skeleton-row-vertical-margin']: size === 'small' ? '4px' : '6px'
        },
        children: [
            [
                ...Array(lines)
            ].map((_, idx)=>/*#__PURE__*/ jsx("div", {
                    css: [
                        TableSkeletonStyles.cell,
                        genSkeletonAnimatedColor(theme, frameRate),
                        {
                            width: `calc(100% - ${widths[idx % widths.length]}px)`
                        }
                    ]
                }, idx)),
            /*#__PURE__*/ jsx("span", {
                css: visuallyHidden,
                children: label
            })
        ]
    });
};
const TableSkeletonRows = ({ table, actionColumnIds = [], numRows = 3, loading = true, loadingDescription = 'Table skeleton rows', label })=>{
    const { theme } = useDesignSystemTheme();
    const { grid } = useContext(TableContext);
    return /*#__PURE__*/ jsxs(Fragment, {
        children: [
            loading && /*#__PURE__*/ jsx(LoadingState, {
                description: loadingDescription
            }),
            /*#__PURE__*/ jsx("span", {
                css: visuallyHidden,
                children: label
            }),
            [
                ...Array(numRows).keys()
            ].map((i)=>/*#__PURE__*/ jsx(TableRow, {
                    children: table.getFlatHeaders().map((header)=>{
                        const meta = header.column.columnDef.meta;
                        // When no explicit meta styles/width is set, fall back to header.getSize()-based
                        // sizing so skeleton cells match the proportions of real data rows.
                        // In flex mode, use flex; in grid mode, use maxWidth.
                        const fallbackStyle = grid ? {
                            maxWidth: header.getSize()
                        } : {
                            flex: header.getSize() / 100
                        };
                        return actionColumnIds.includes(header.id) ? /*#__PURE__*/ jsx(TableRowAction, {
                            children: /*#__PURE__*/ jsx(TableSkeleton, {
                                style: {
                                    width: theme.general.iconSize
                                }
                            })
                        }, `cell-${header.id}-${i}`) : /*#__PURE__*/ jsx(TableCell, {
                            style: meta?.styles ?? (meta?.width !== undefined ? {
                                maxWidth: meta.width
                            } : fallbackStyle),
                            children: /*#__PURE__*/ jsx(TableSkeleton, {
                                seed: `skeleton-${header.id}-${i}`,
                                lines: meta?.numSkeletonLines ?? undefined
                            })
                        }, `cell-${header.id}-${i}`);
                    })
                }, i))
        ]
    });
};

// Loading state requires a width since it'll have no content
const LOADING_STATE_DEFAULT_WIDTH = 300;
function getStyles$1(args) {
    const { theme, width, hasTopBar, hasBottomBar, isInteractive } = args;
    const hoverOrFocusStyle = isInteractive ? {
        border: `1px solid ${theme.colors.actionDefaultBorderHover}`,
        boxShadow: theme.shadows.md
    } : {};
    return /*#__PURE__*/ css({
        color: theme.colors.textPrimary,
        backgroundColor: theme.colors.backgroundPrimary,
        position: 'relative',
        display: 'flex',
        justifyContent: 'flex-start',
        flexDirection: 'column',
        paddingRight: hasTopBar || hasBottomBar ? 0 : theme.spacing.md,
        paddingLeft: hasTopBar || hasBottomBar ? 0 : theme.spacing.md,
        paddingTop: hasTopBar ? 0 : theme.spacing.md,
        paddingBottom: hasBottomBar ? 0 : theme.spacing.md,
        width: width ?? 'fit-content',
        borderRadius: theme.borders.borderRadiusMd,
        borderColor: theme.colors.border,
        borderWidth: '1px',
        borderStyle: 'solid',
        '&:hover': hoverOrFocusStyle,
        '&:focus': hoverOrFocusStyle,
        cursor: !isInteractive ? 'default' : 'pointer',
        boxShadow: theme.shadows.sm,
        transition: `box-shadow 0.2s ease-in-out`,
        textDecoration: 'none !important',
        ...getAnimationCss(theme.options.enableAnimation)
    });
}
function getBottomBarStyles(theme) {
    return /*#__PURE__*/ css({
        marginTop: theme.spacing.sm,
        borderBottomRightRadius: theme.borders.borderRadiusSm,
        borderBottomLeftRadius: theme.borders.borderRadiusSm,
        overflow: 'hidden'
    });
}
function getTopBarStyles(theme) {
    return /*#__PURE__*/ css({
        marginBottom: theme.spacing.sm,
        borderTopRightRadius: theme.borders.borderRadiusSm,
        borderTopLeftRadius: theme.borders.borderRadiusSm,
        overflow: 'hidden'
    });
}
const Card = ({ children, customLoadingContent, dangerouslyAppendEmotionCSS, loading, loadingDescription, width, bottomBarContent, topBarContent, disableHover, onClick, href, navigateFn, anchorProps, componentId, analyticsEvents, shouldStartInteraction, ...dataAndAttributes })=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.card', false);
    const { theme } = useDesignSystemTheme();
    const hasTopBar = !isUndefined(topBarContent);
    const hasBottomBar = !isUndefined(bottomBarContent);
    const hasHref = Boolean(href);
    const hasOnClick = Boolean(onClick);
    const hasNavigateFn = Boolean(navigateFn);
    const isInteractive = !disableHover && !loading && (hasHref || hasOnClick || hasNavigateFn);
    const cardStyle = /*#__PURE__*/ css(getStyles$1({
        theme,
        width,
        hasBottomBar,
        hasTopBar,
        isInteractive
    }));
    const ref = React__default.useRef(null);
    const bottomBar = bottomBarContent ? /*#__PURE__*/ jsx("div", {
        css: /*#__PURE__*/ css(getBottomBarStyles(theme)),
        children: bottomBarContent
    }) : null;
    const topBar = topBarContent ? /*#__PURE__*/ jsx("div", {
        css: /*#__PURE__*/ css(getTopBarStyles(theme)),
        children: topBarContent
    }) : null;
    const contentPadding = hasTopBar || hasBottomBar ? theme.spacing.lg : 0;
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Card,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        shouldStartInteraction
    });
    const { elementRef: cardRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    const mergedRef = useMergeRefs([
        ref,
        cardRef
    ]);
    const navigate = useCallback(async ()=>{
        if (navigateFn) {
            await navigateFn();
        }
    }, [
        navigateFn
    ]);
    const handleClick = useCallback(async (e)=>{
        eventContext.onClick(e);
        await navigate();
        onClick?.(e);
        ref.current?.blur();
    }, [
        navigate,
        eventContext,
        onClick
    ]);
    const handleSelection = useCallback(async (e)=>{
        eventContext.onClick(e);
        e.preventDefault();
        await navigate();
        onClick?.(e);
    }, [
        navigate,
        eventContext,
        onClick
    ]);
    const content = /*#__PURE__*/ jsx("div", {
        ref: mergedRef,
        tabIndex: !href ? 0 : undefined,
        ...addDebugOutlineIfEnabled(),
        css: href ? [] : [
            cardStyle,
            dangerouslyAppendEmotionCSS
        ],
        onClick: loading || href ? undefined : handleClick,
        ...dataAndAttributes,
        onKeyDown: async (e)=>{
            const isEnter = e.key === 'Enter';
            const isSpace = e.key === ' ';
            const isCardFocused = e.target === e.currentTarget; // ensures the element itself is focused
            if ((isEnter || isSpace) && isCardFocused) {
                await handleSelection(e);
            }
            dataAndAttributes.onKeyDown?.(e);
        },
        ...eventContext.dataComponentProps,
        children: loading ? /*#__PURE__*/ jsx(DefaultCardLoadingContent, {
            width: width,
            customLoadingContent: customLoadingContent,
            loadingDescription: loadingDescription
        }) : /*#__PURE__*/ jsxs(Fragment, {
            children: [
                topBar,
                /*#__PURE__*/ jsx("div", {
                    css: {
                        padding: `0px ${contentPadding}px`,
                        flexGrow: 1
                    },
                    children: children
                }),
                bottomBar
            ]
        })
    });
    return href ? /*#__PURE__*/ jsx("a", {
        css: [
            cardStyle,
            dangerouslyAppendEmotionCSS
        ],
        href: href,
        ...anchorProps,
        children: content
    }) : content;
};
function DefaultCardLoadingContent({ customLoadingContent, width, loadingDescription }) {
    if (customLoadingContent) {
        return /*#__PURE__*/ jsx(Fragment, {
            children: customLoadingContent
        });
    }
    return /*#__PURE__*/ jsxs("div", {
        css: {
            width: width ?? LOADING_STATE_DEFAULT_WIDTH
        },
        children: [
            /*#__PURE__*/ jsx(TitleSkeleton, {
                label: "Loading...",
                style: {
                    width: '50%'
                },
                loadingDescription: loadingDescription
            }),
            [
                ...Array(3).keys()
            ].map((i)=>/*#__PURE__*/ jsx(ParagraphSkeleton, {
                    label: "Loading..."
                }, i))
        ]
    });
}

const Trigger$1 = ContextMenuTrigger;
const ItemIndicator = ContextMenuItemIndicator;
const Group$2 = ContextMenuGroup;
const Arrow = ContextMenuArrow;
const Sub = ContextMenuSub;
const ContextMenuProps = /*#__PURE__*/ createContext({
    isOpen: false,
    setIsOpen: (isOpen)=>{}
});
const useContextMenuProps = ()=>React__default.useContext(ContextMenuProps);
const Root$7 = ({ children, onOpenChange, ...props })=>{
    const [isOpen, setIsOpen] = React__default.useState(false);
    const handleChange = (isOpen)=>{
        setIsOpen(isOpen);
        onOpenChange?.(isOpen);
    };
    return /*#__PURE__*/ jsx(ContextMenu$2, {
        onOpenChange: handleChange,
        ...props,
        children: /*#__PURE__*/ jsx(ContextMenuProps.Provider, {
            value: {
                isOpen,
                setIsOpen
            },
            children: children
        })
    });
};
const SubTrigger = ({ children, disabledReason, withChevron, ...props })=>{
    const { theme } = useDesignSystemTheme();
    const ref = useRef(null);
    return /*#__PURE__*/ jsxs(ContextMenuSubTrigger, {
        ...props,
        css: dropdownItemStyles(theme),
        ref: ref,
        onKeyDown: (e)=>{
            if (e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                e.preventDefault();
            }
            props.onKeyDown?.(e);
        },
        children: [
            getNewChildren(children, props, disabledReason, ref),
            withChevron && /*#__PURE__*/ jsx(ContextMenu.Hint, {
                children: /*#__PURE__*/ jsx(ChevronRightIcon, {})
            })
        ]
    });
};
const Content$1 = ({ children, minWidth, forceCloseOnEscape, onEscapeKeyDown, onKeyDown, ...childrenProps })=>{
    const { getPopupContainer } = useDesignSystemContext();
    const { theme } = useDesignSystemTheme();
    const { isInsideModal } = useModalContext();
    const { isOpen, setIsOpen } = useContextMenuProps();
    return /*#__PURE__*/ jsx(ContextMenuPortal, {
        container: getPopupContainer && getPopupContainer(),
        children: isOpen && /*#__PURE__*/ jsx(ContextMenuContent, {
            ...addDebugOutlineIfEnabled(),
            onWheel: (e)=>{
                e.stopPropagation();
            },
            onTouchMove: (e)=>{
                e.stopPropagation();
            },
            onKeyDown: (e)=>{
                // This is a workaround for Radix's ContextMenu.Content not receiving Escape key events
                // when nested inside a modal. We need to stop propagation of the event so that the modal
                // doesn't close when the DropdownMenu should.
                if (e.key === 'Escape') {
                    if (isInsideModal || forceCloseOnEscape) {
                        e.stopPropagation();
                        setIsOpen(false);
                    }
                    onEscapeKeyDown?.(e.nativeEvent);
                } else if (e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                    e.stopPropagation();
                    handleKeyboardNavigation(e);
                }
                onKeyDown?.(e);
            },
            ...childrenProps,
            css: [
                dropdownContentStyles(theme),
                {
                    minWidth
                }
            ],
            children: children
        })
    });
};
const SubContent = ({ children, minWidth, ...childrenProps })=>{
    const { getPopupContainer } = useDesignSystemContext();
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(ContextMenuPortal, {
        container: getPopupContainer && getPopupContainer(),
        children: /*#__PURE__*/ jsx(ContextMenuSubContent, {
            ...addDebugOutlineIfEnabled(),
            ...childrenProps,
            onWheel: (e)=>{
                e.stopPropagation();
            },
            onTouchMove: (e)=>{
                e.stopPropagation();
            },
            css: [
                dropdownContentStyles(theme),
                {
                    minWidth
                }
            ],
            onKeyDown: (e)=>{
                if (e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                    e.stopPropagation();
                    handleKeyboardNavigation(e);
                }
                childrenProps.onKeyDown?.(e);
            },
            children: children
        })
    });
};
const Item$2 = ({ children, disabledReason, onClick, componentId, analyticsEvents, asChild, ...props })=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.contextMenu', false);
    const { theme } = useDesignSystemTheme();
    const ref = useRef(null);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.ContextMenuItem,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents
    });
    const { elementRef: contextMenuItemRef } = useNotifyOnFirstView({
        onView: !asChild ? eventContext.onView : ()=>{}
    });
    const mergedRef = useMergeRefs([
        ref,
        contextMenuItemRef
    ]);
    const onClickWrapper = useCallback((e)=>{
        if (!asChild) {
            eventContext.onClick(e);
        }
        onClick?.(e);
    }, [
        asChild,
        eventContext,
        onClick
    ]);
    return /*#__PURE__*/ jsx(ContextMenuItem, {
        ...props,
        asChild: asChild,
        onClick: onClickWrapper,
        css: dropdownItemStyles(theme),
        ref: mergedRef,
        onKeyDown: (e)=>{
            if (e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                e.preventDefault();
            }
            props.onKeyDown?.(e);
        },
        ...eventContext.dataComponentProps,
        children: getNewChildren(children, props, disabledReason, ref)
    });
};
const CheckboxItem = ({ children, disabledReason, onCheckedChange, componentId, analyticsEvents, ...props })=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.contextMenu', false);
    const { theme } = useDesignSystemTheme();
    const ref = useRef(null);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.ContextMenuCheckboxItem,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: true
    });
    const { elementRef: contextMenuCheckboxItemRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: props.checked ?? props.defaultChecked
    });
    const mergedRef = useMergeRefs([
        ref,
        contextMenuCheckboxItemRef
    ]);
    const onCheckedChangeWrapper = useCallback((checked)=>{
        eventContext.onValueChange(checked);
        onCheckedChange?.(checked);
    }, [
        eventContext,
        onCheckedChange
    ]);
    return /*#__PURE__*/ jsxs(ContextMenuCheckboxItem, {
        ...props,
        onCheckedChange: onCheckedChangeWrapper,
        css: dropdownItemStyles(theme),
        ref: mergedRef,
        onKeyDown: (e)=>{
            if (e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                e.preventDefault();
            }
            props.onKeyDown?.(e);
        },
        ...eventContext.dataComponentProps,
        children: [
            /*#__PURE__*/ jsx(ContextMenuItemIndicator, {
                css: itemIndicatorStyles(theme),
                children: /*#__PURE__*/ jsx(CheckIcon, {})
            }),
            !props.checked && /*#__PURE__*/ jsx("div", {
                style: {
                    width: theme.general.iconFontSize + theme.spacing.xs
                }
            }),
            getNewChildren(children, props, disabledReason, ref)
        ]
    });
};
const RadioGroup = ({ onValueChange, componentId, analyticsEvents, valueHasNoPii, ...props })=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.contextMenu', false);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.ContextMenuRadioGroup,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const { elementRef: contextMenuRadioGroupRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: props.value ?? props.defaultValue
    });
    const onValueChangeWrapper = useCallback((value)=>{
        eventContext.onValueChange(value);
        onValueChange?.(value);
    }, [
        eventContext,
        onValueChange
    ]);
    return /*#__PURE__*/ jsx(ContextMenuRadioGroup, {
        ref: contextMenuRadioGroupRef,
        ...props,
        onValueChange: onValueChangeWrapper,
        ...eventContext.dataComponentProps
    });
};
const RadioItem = ({ children, disabledReason, ...props })=>{
    const { theme } = useDesignSystemTheme();
    const ref = useRef(null);
    return /*#__PURE__*/ jsxs(ContextMenuRadioItem, {
        ...props,
        css: [
            dropdownItemStyles(theme),
            {
                '&[data-state="unchecked"]': {
                    paddingLeft: theme.general.iconFontSize + theme.spacing.xs + theme.spacing.sm
                }
            }
        ],
        ref: ref,
        children: [
            /*#__PURE__*/ jsx(ContextMenuItemIndicator, {
                css: itemIndicatorStyles(theme),
                children: /*#__PURE__*/ jsx(CheckIcon, {})
            }),
            getNewChildren(children, props, disabledReason, ref)
        ]
    });
};
const Label$1 = ({ children, ...props })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(ContextMenuLabel, {
        ...props,
        css: {
            color: theme.colors.textSecondary,
            padding: `${theme.spacing.sm - 2}px ${theme.spacing.sm}px`
        },
        children: children
    });
};
const Hint$1 = ({ children })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("span", {
        css: {
            display: 'inline-flex',
            marginLeft: 'auto',
            paddingLeft: theme.spacing.sm
        },
        children: children
    });
};
const Separator$1 = ()=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(ContextMenuSeparator, {
        css: dropdownSeparatorStyles(theme)
    });
};
const itemIndicatorStyles = (theme)=>/*#__PURE__*/ css({
        display: 'inline-flex',
        paddingRight: theme.spacing.xs
    });
const ContextMenu = {
    Root: Root$7,
    Trigger: Trigger$1,
    Label: Label$1,
    Item: Item$2,
    Group: Group$2,
    RadioGroup,
    CheckboxItem,
    RadioItem,
    Arrow,
    Separator: Separator$1,
    Sub,
    SubTrigger,
    SubContent,
    Content: Content$1,
    Hint: Hint$1
};

var ContextMenu$1 = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Arrow: Arrow,
  CheckboxItem: CheckboxItem,
  Content: Content$1,
  ContextMenu: ContextMenu,
  Group: Group$2,
  Hint: Hint$1,
  Item: Item$2,
  ItemIndicator: ItemIndicator,
  Label: Label$1,
  RadioGroup: RadioGroup,
  RadioItem: RadioItem,
  Root: Root$7,
  Separator: Separator$1,
  Sub: Sub,
  SubContent: SubContent,
  SubTrigger: SubTrigger,
  Trigger: Trigger$1,
  itemIndicatorStyles: itemIndicatorStyles
});

/**
 * A React custom hook that allows a callback function to be executed exactly once until it is explicitly reset.
 *
 * Usage:
 *
 * const originalCallback = () => { console.log('originalCallback'); }
 * const { callbackOnceUntilReset, reset } = useCallbackOnceUntilReset(originalCallback);
 *
 * // To execute the callback
 * callbackOnceUntilReset(); // Prints 'originalCallback'
 * callbackOnceUntilReset(); // No effect for further calls
 * reset();
 * callbackOnceUntilReset(); // Prints 'originalCallback' again
 */ const useCallbackOnceUntilReset = (callback)=>{
    const canTriggerRef = useRef(true);
    const reset = useCallback(()=>{
        canTriggerRef.current = true;
    }, []);
    const callbackOnceUntilReset = useCallback(()=>{
        if (canTriggerRef.current) {
            callback();
            canTriggerRef.current = false;
        }
    }, [
        callback
    ]);
    return {
        callbackOnceUntilReset,
        reset
    };
};

/**
 * This hook provides a way to chain event handlers together.
 * Optionally, it can stop calling the next handler if the event has been defaultPrevented.
 * @param handlers Array of event handlers to chain together. Optional handlers are allowed for convenience.
 * @param stopOnDefaultPrevented If true, the next handler will not be called if the event has been defaultPrevented
 * @returns A function that will call each handler in the order they are provided
 * @example
 * ```tsx
 * const onClick = useChainEventHandlers({ handlers: [onClick1, onClick2] });
 * return <button onClick={onClick} />;
 */ const useChainEventHandlers = (props)=>{
    const { handlers, stopOnDefaultPrevented } = props;
    return useCallback((event)=>{
        // Loop over each handler in succession
        for (const handler of handlers){
            // Break if the event has been defaultPrevented and stopOnDefaultPrevented is true
            if (stopOnDefaultPrevented && event.defaultPrevented) return;
            // Call the handler if it exists
            handler?.(event);
        }
    }, [
        handlers,
        stopOnDefaultPrevented
    ]);
};

const SMALL_INPUT_HEIGHT = 24;
const getInputStyles$2 = (clsPrefix, theme, { validationState, type, hasValue, locked, size }, { useFocusWithin = false })=>{
    const inputClass = `.${clsPrefix}-input`;
    const affixClass = `.${clsPrefix}-input-affix-wrapper`;
    const affixClassDisabled = `.${clsPrefix}-input-affix-wrapper-disabled`;
    const affixClassFocused = `.${clsPrefix}-input-affix-wrapper-focused`;
    const clearIcon = `.${clsPrefix}-input-clear-icon`;
    const prefixIcon = `.${clsPrefix}-input-prefix`;
    const suffixIcon = `.${clsPrefix}-input-suffix`;
    const validationColor = getValidationStateColor(theme, validationState);
    const focusSpecifier = useFocusWithin ? 'focus-within' : 'focus';
    const styles = {
        '&&': {
            fontSize: theme.typography.fontSizeBase,
            lineHeight: theme.typography.lineHeightBase,
            ...size === 'small' ? {
                height: SMALL_INPUT_HEIGHT
            } : {
                minHeight: theme.general.heightSm
            },
            ...size === 'small' && {
                paddingLeft: theme.spacing.sm,
                paddingRight: theme.spacing.sm
            },
            borderColor: theme.colors.actionDefaultBorderDefault,
            ...validationState && {
                borderColor: validationColor
            },
            '&:hover': {
                borderColor: validationState ? validationColor : theme.colors.actionPrimaryBackgroundHover
            },
            [`&:${focusSpecifier}`]: {
                outlineColor: validationState ? validationColor : theme.colors.actionDefaultBorderFocus,
                outlineWidth: 2,
                outlineOffset: -2,
                outlineStyle: 'solid',
                borderColor: 'transparent'
            },
            '&:focus-visible': {
                outlineColor: validationState ? validationColor : theme.colors.actionDefaultBorderFocus,
                outlineWidth: 2,
                outlineOffset: -2,
                outlineStyle: 'solid',
                borderColor: 'transparent'
            },
            [`& + .${clsPrefix}-form-message`]: {
                marginTop: theme.spacing.sm
            }
        },
        [`&${inputClass}, ${inputClass}`]: {
            backgroundColor: 'transparent',
            borderRadius: theme.borders.borderRadiusSm,
            '&:disabled': {
                backgroundColor: theme.colors.actionDisabledBackground,
                color: theme.colors.actionDisabledText,
                borderColor: theme.colors.actionDisabledBorder
            },
            '&::placeholder': {
                color: theme.colors.textPlaceholder
            }
        },
        [`&${affixClass}`]: {
            backgroundColor: 'transparent',
            fontSize: theme.typography.fontSizeBase,
            lineHeight: theme.typography.lineHeightBase,
            paddingTop: 5,
            paddingBottom: 5,
            ...size === 'small' ? {
                height: SMALL_INPUT_HEIGHT
            } : {
                minHeight: theme.general.heightSm
            },
            '::before': {
                lineHeight: theme.typography.lineHeightBase
            },
            '&:hover': {
                borderColor: validationState ? validationColor : theme.colors.actionPrimaryBackgroundHover
            },
            [`input.${clsPrefix}-input`]: {
                fontSize: 'inherit',
                borderRadius: 0
            }
        },
        [`&${affixClassDisabled}`]: {
            backgroundColor: theme.colors.actionDisabledBackground
        },
        [`&${affixClassFocused}`]: {
            boxShadow: 'none',
            [`&&, &:${focusSpecifier}`]: {
                outlineColor: validationState ? validationColor : theme.colors.actionDefaultBorderFocus,
                outlineWidth: 2,
                outlineOffset: -2,
                outlineStyle: 'solid',
                boxShadow: 'none',
                borderColor: 'transparent'
            }
        },
        [clearIcon]: {
            fontSize: theme.typography.fontSizeSm
        },
        [prefixIcon]: {
            marginRight: theme.spacing.sm,
            color: theme.colors.textSecondary
        },
        [suffixIcon]: {
            marginLeft: theme.spacing.sm,
            color: theme.colors.textSecondary,
            ...!hasValue && type === 'number' && {
                display: 'none'
            }
        },
        ...locked ? {
            [`&${affixClass}`]: {
                backgroundColor: theme.colors.backgroundSecondary
            },
            [`input.${clsPrefix}-input`]: {
                backgroundColor: `${theme.colors.backgroundSecondary} !important`,
                color: `${theme.colors.textPrimary} !important`
            }
        } : {},
        ...getAnimationCss(theme.options.enableAnimation)
    };
    return styles;
};
const getInputEmotionStyles = (clsPrefix, theme, { validationState, type, hasValue, locked, size })=>{
    const styles = getInputStyles$2(clsPrefix, theme, {
        validationState,
        type,
        hasValue,
        locked,
        size
    }, {});
    return /*#__PURE__*/ css(importantify(styles));
};
const Input$1 = /*#__PURE__*/ forwardRef(function Input({ validationState, autoComplete = 'off', dangerouslySetAntdProps, dangerouslyAppendEmotionCSS, onChange, onClear, onFocus, onPressEnter, onWheel, onCompositionStart, onCompositionEnd, componentId, shouldPreventFormSubmission, analyticsEvents, readOnly, locked, size = 'middle', ...props }, ref) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.input', false);
    const disableNumberInputWheelValueChange = serverSideSafe('databricks.fe.designsystem.disableNumberInputWheelValueChange', false);
    const formContext = useFormContext();
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const [hasValue, setHasValue] = React__default.useState(props.value !== undefined && props.value !== null && props.value !== '');
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Input,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: false
    });
    const firstView = useRef(true);
    useEffect(()=>{
        if (firstView.current) {
            eventContext.onView();
            firstView.current = false;
        }
    }, [
        eventContext
    ]);
    // Prevents multiple onValueChange triggers until after a focus event resets it.
    const { callbackOnceUntilReset: sendAnalyticsEventOncePerFocus, reset: resetSendAnalyticsEventOnFocus } = useCallbackOnceUntilReset(eventContext.onValueChange);
    const handleChange = useCallback((e)=>{
        sendAnalyticsEventOncePerFocus();
        // If the input is cleared, call the onClear handler, but only
        // if the event is not an input event -- which is the case when you click the
        // ant-provided (X) button.
        if (!e.target.value && e.nativeEvent instanceof InputEvent === false && onClear) {
            onClear?.();
            setHasValue(false);
        } else {
            onChange?.(e);
            setHasValue(Boolean(e.target.value));
        }
    }, [
        onChange,
        onClear,
        sendAnalyticsEventOncePerFocus
    ]);
    const handleFocus = useCallback((e)=>{
        resetSendAnalyticsEventOnFocus();
        onFocus?.(e);
    }, [
        onFocus,
        resetSendAnalyticsEventOnFocus
    ]);
    const handleWheel = useCallback((e)=>{
        /*
        For additional context, read CJ-72625, PR 1693195 and https://github.com/facebook/react/issues/32156

        The code below fixes an issue when the following conditions are met:
          - An input with type = "number" is focused
          - The mouse is inside the input
          - The user scrolls the mouse wheel

        This can result in unexpected behavior where the input value changes, even though the user might intend
        to scroll on the page.

        React adds a wheel event listener to the input element. On Chrome, when an input with type = "number"
        has a wheel event listener attached, when receiving a WheelEvent, it will also update the input value.
        On Firefox, the input value is not updated.

        The fix here is to blur the input when all the above conditions are met, preventing the input value
        from being changed. See https://stackoverflow.com/questions/63224459/disable-scrolling-on-input-type-number-in-react.
      */ if (props.type === 'number' && !onWheel && disableNumberInputWheelValueChange) {
            e.currentTarget.blur();
            return;
        }
        onWheel?.(e);
    }, [
        disableNumberInputWheelValueChange,
        onWheel,
        props.type
    ]);
    const onFormSubmit = useCallback((e)=>{
        if (!formContext.formRef?.current) return;
        e.preventDefault();
        formContext.formRef.current.requestSubmit();
    }, [
        formContext.formRef
    ]);
    const submitHandlers = useCallbackOnEnter({
        callback: onFormSubmit,
        allowBasicEnter: !shouldPreventFormSubmission,
        allowPlatformEnter: !shouldPreventFormSubmission
    });
    const onPressEnterChain = useChainEventHandlers(useMemo(()=>({
            handlers: [
                submitHandlers.onKeyDown,
                onPressEnter
            ],
            stopOnDefaultPrevented: false
        }), [
        submitHandlers.onKeyDown,
        onPressEnter
    ]));
    const onCompositionStartChain = useChainEventHandlers(useMemo(()=>({
            handlers: [
                submitHandlers.onCompositionStart,
                onCompositionStart
            ]
        }), [
        submitHandlers.onCompositionStart,
        onCompositionStart
    ]));
    const onCompositionEndChain = useChainEventHandlers(useMemo(()=>({
            handlers: [
                submitHandlers.onCompositionEnd,
                onCompositionEnd
            ]
        }), [
        submitHandlers.onCompositionEnd,
        onCompositionEnd
    ]));
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Input$2, {
            ...addDebugOutlineIfEnabled(),
            autoComplete: autoComplete,
            "data-validation": validationState,
            ref: ref,
            css: [
                getInputEmotionStyles(classNamePrefix, theme, {
                    validationState,
                    type: props.type,
                    hasValue,
                    locked,
                    size
                }),
                dangerouslyAppendEmotionCSS
            ],
            onChange: handleChange,
            onFocus: handleFocus,
            onPressEnter: onPressEnterChain,
            onWheel: handleWheel,
            onCompositionStart: onCompositionStartChain,
            onCompositionEnd: onCompositionEndChain,
            ...props,
            readOnly: locked || readOnly,
            suffix: locked ? /*#__PURE__*/ jsx(LockIcon, {}) : props.suffix,
            ...dangerouslySetAntdProps,
            ...eventContext.dataComponentProps
        })
    });
});

// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const DatePickerInput = /*#__PURE__*/ forwardRef((props, ref)=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.input', false);
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const { componentId, analyticsEvents, showTimeZone, validationState, prefix, suffix, allowClear, onChange, onFocus, onKeyDown, onClear, ...restProps } = props;
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Input,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: false
    });
    const { callbackOnceUntilReset: sendAnalyticsEventOncePerFocus, reset: resetSendAnalyticsEventOnFocus } = useCallbackOnceUntilReset(eventContext.onValueChange);
    const handleChange = useCallback((e)=>{
        sendAnalyticsEventOncePerFocus();
        if (e.target.value) {
            onChange?.(e);
        }
    }, [
        onChange,
        sendAnalyticsEventOncePerFocus
    ]);
    const handleClear = useCallback(()=>{
        sendAnalyticsEventOncePerFocus();
        onClear?.();
    }, [
        onClear,
        sendAnalyticsEventOncePerFocus
    ]);
    const handleFocus = useCallback((e)=>{
        resetSendAnalyticsEventOnFocus();
        onFocus?.(e);
    }, [
        onFocus,
        resetSendAnalyticsEventOnFocus
    ]);
    return /*#__PURE__*/ jsxs("div", {
        className: classnames(`${classNamePrefix}-input-affix-wrapper`, restProps.className, restProps.disabled ? `${classNamePrefix}-input-affix-wrapper-disabled` : ''),
        css: [
            getInputStyles$2(classNamePrefix, theme, {
                validationState,
                type: 'date',
                hasValue: Boolean(restProps.value)
            }, {
                useFocusWithin: true
            }),
            {
                // Chrome, safari & webkit browsers specific fix to hide the calendar picker indicator
                '*::-webkit-calendar-picker-indicator': {
                    display: 'none'
                },
                [`.${classNamePrefix}-input`]: {
                    // vertical alignment fix for all browsers except chrome
                    display: 'inline-flex',
                    // Firefox specific fix to hide the calendar picker indicator
                    paddingRight: '32px !important',
                    marginRight: '-32px',
                    width: 'calc(100% + 32px)',
                    clipPath: 'inset(0 32px 0 0)'
                },
                [`.${classNamePrefix}-input-prefix`]: {
                    ...!restProps?.disabled && {
                        color: `${theme.colors.textPrimary} !important`
                    }
                },
                [`&.${classNamePrefix}-input-affix-wrapper > *`]: {
                    height: theme.typography.lineHeightBase
                },
                ...showTimeZone && {
                    [`.${classNamePrefix}-input-suffix`]: {
                        display: 'inline-flex',
                        flexDirection: 'row-reverse',
                        gap: theme.spacing.sm,
                        alignItems: 'center'
                    }
                }
            }
        ],
        children: [
            prefix && /*#__PURE__*/ jsx("span", {
                className: `${classNamePrefix}-input-prefix`,
                children: prefix
            }),
            /*#__PURE__*/ jsx("input", {
                placeholder: "Select Date",
                ...restProps,
                onChange: handleChange,
                onFocus: handleFocus,
                value: restProps.value ?? '',
                ref: ref,
                className: `${classNamePrefix}-input`,
                onKeyDown: (e)=>{
                    if (e.key === 'Backspace') {
                        handleClear();
                    }
                    onKeyDown?.(e);
                }
            }),
            suffix && /*#__PURE__*/ jsx("span", {
                className: `${classNamePrefix}-input-suffix`,
                children: suffix
            }),
            allowClear && restProps.value && /*#__PURE__*/ jsx("button", {
                "aria-label": "Clear date",
                type: "button",
                css: {
                    background: 'none',
                    border: 'none',
                    display: 'inline-flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                    marginLeft: `${theme.spacing.sm}px !important`,
                    padding: 0
                },
                onClick: (e)=>{
                    e.stopPropagation();
                    e.preventDefault();
                    handleClear();
                },
                children: /*#__PURE__*/ jsx(XCircleFillIcon, {
                    className: `${classNamePrefix}-input-clear-icon`
                })
            })
        ]
    });
});

const MONTHS_PER_YEAR = 12;
function getGridCellCss$1(theme, isRangeStart, isRangeEnd, isRangeMiddle, index) {
    return {
        padding: 0,
        backgroundColor: isRangeMiddle ? theme.colors.actionTertiaryBackgroundPress : 'transparent',
        ...isRangeStart && {
            borderTopLeftRadius: theme.general.borderRadiusBase,
            borderBottomLeftRadius: theme.general.borderRadiusBase,
            borderTopRightRadius: 0,
            borderBottomRightRadius: 0,
            backgroundColor: theme.colors.actionTertiaryBackgroundPress
        },
        ...isRangeEnd && {
            borderTopRightRadius: theme.general.borderRadiusBase,
            borderBottomRightRadius: theme.general.borderRadiusBase,
            borderTopLeftRadius: 0,
            borderBottomLeftRadius: 0,
            backgroundColor: theme.colors.actionTertiaryBackgroundPress
        },
        ...isRangeStart && isRangeEnd && {
            borderRadius: theme.general.borderRadiusBase
        },
        // Round left edge for first column cells in range
        ...isRangeMiddle && index % 4 === 0 && {
            borderTopLeftRadius: theme.general.borderRadiusBase,
            borderBottomLeftRadius: theme.general.borderRadiusBase
        },
        // Round right edge for last column cells in range
        ...isRangeMiddle && index % 4 === 3 && {
            borderTopRightRadius: theme.general.borderRadiusBase,
            borderBottomRightRadius: theme.general.borderRadiusBase
        }
    };
}
function getButtonCss$1(theme, selected, isRangeStart, isRangeEnd, isCurrentMonth) {
    return {
        width: '100%',
        // DS Button's importantify wraps borderColor with !important, so we must override here.
        // borderWidth is always 2px (matching DayPicker's --rdp-outline) so focus doesn't cause layout shift.
        borderColor: 'transparent !important',
        borderWidth: '2px !important',
        boxShadow: 'none !important',
        '&[disabled]': {
            borderColor: 'transparent !important'
        },
        fontWeight: isCurrentMonth ? 'bold' : 'normal',
        // Non-selected focused: DayPicker shows light blue bg + single inner border ring (no outer outline).
        // Selected+focused gets the 2-ring style (inner + outer with gap) via the selected override below.
        // borderWidth ensures the 2px ring matches DayPicker's --rdp-outline (2px solid accent).
        '&:focus-visible': {
            backgroundColor: `${theme.colors.actionTertiaryBackgroundPress} !important`,
            borderColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
            outlineColor: 'transparent !important'
        },
        // Use the exact same colors as DayPicker for selected/range start/end cells.
        // Manually applying actionPrimaryBackground* + #fff text ensures consistent
        // appearance across light and dark themes without relying on Button type='primary'.
        ...(selected || isRangeStart || isRangeEnd) && {
            backgroundColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
            color: '#fff !important',
            '&:hover': {
                backgroundColor: `${theme.colors.actionPrimaryBackgroundHover} !important`,
                color: '#fff !important',
                // Keep border transparent on hover — DS Button shows a border on hover by default
                borderColor: 'transparent !important'
            },
            '&:active': {
                backgroundColor: `${theme.colors.actionPrimaryBackgroundPress} !important`,
                color: '#fff !important'
            },
            // Keyboard focus on selected cell: DayPicker's .button:focus-visible:not([disabled]) has
            // higher specificity than .day_selected, so selected+focused shows the hover (light blue)
            // background with dark text, inner border ring, and outer outline ring.
            '&:focus-visible': {
                backgroundColor: `${theme.colors.actionTertiaryBackgroundPress} !important`,
                color: 'inherit !important',
                borderColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
                outlineColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
                outlineOffset: '2px !important'
            }
        },
        ...!(selected || isRangeStart || isRangeEnd) && {
            backgroundColor: 'transparent !important',
            '&:hover:not([disabled])': {
                backgroundColor: `${theme.colors.actionTertiaryBackgroundPress} !important`,
                color: 'inherit !important',
                // Keep border transparent on hover — DS Button shows a border on hover by default
                borderColor: 'transparent !important'
            }
        },
        // Match button border-radius to gridcell container for range start/end
        ...isRangeStart && !isRangeEnd && {
            borderTopRightRadius: 0,
            borderBottomRightRadius: 0
        },
        ...isRangeEnd && !isRangeStart && {
            borderTopLeftRadius: 0,
            borderBottomLeftRadius: 0
        }
    };
}
function MonthCell({ index, label, displayYear, selected, disabled, rangeFrom, rangeTo, focusedIndex, onSelect, onKeyDown, onFocus, renderCellContent }) {
    const { theme } = useDesignSystemTheme();
    const now = new Date();
    const isCurrentMonth = displayYear === now.getFullYear() && index === now.getMonth();
    const cellValue = displayYear * 12 + index;
    const rangeFromValue = rangeFrom !== undefined ? rangeFrom.getFullYear() * 12 + rangeFrom.getMonth() : undefined;
    const rangeToValue = rangeTo !== undefined ? rangeTo.getFullYear() * 12 + rangeTo.getMonth() : undefined;
    const inRange = rangeFromValue !== undefined && rangeToValue !== undefined && cellValue >= rangeFromValue && cellValue <= rangeToValue;
    const isRangeStart = rangeFromValue !== undefined && cellValue === rangeFromValue;
    const isRangeEnd = rangeToValue !== undefined && cellValue === rangeToValue;
    const isRangeMiddle = inRange && !isRangeStart && !isRangeEnd;
    const defaultButton = /*#__PURE__*/ jsx(Button, {
        componentId: "datepicker-month-grid-cell",
        "aria-label": `${label} ${displayYear}`,
        "data-selected": selected || undefined,
        "aria-current": isCurrentMonth ? 'date' : undefined,
        disabled: disabled,
        tabIndex: index === focusedIndex ? 0 : -1,
        onClick: ()=>!disabled && onSelect(index),
        onKeyDown: (e)=>onKeyDown(e, index),
        onFocus: ()=>onFocus(index),
        css: getButtonCss$1(theme, selected, isRangeStart, isRangeEnd, isCurrentMonth),
        children: label
    });
    return /*#__PURE__*/ jsx("div", {
        role: "gridcell",
        css: getGridCellCss$1(theme, isRangeStart, isRangeEnd, isRangeMiddle, index),
        children: renderCellContent ? renderCellContent(new Date(displayYear, index, 1), defaultButton) : defaultButton
    }, index);
}
/**
 * MonthPickerGrid renders a 4x3 grid of month names for a given year with year navigation.
 * It is used inside the DatePicker when `granularity='month'` is set.
 */ function MonthPickerGrid({ value, onSelect, min, max, rangeFrom, rangeTo, renderCellContent, isDateDisabled }) {
    const { theme } = useDesignSystemTheme();
    const selectedYear = value?.getFullYear();
    const selectedMonth = value?.getMonth();
    const [displayYear, setDisplayYear] = useState(selectedYear ?? new Date().getFullYear());
    const months = useMemo(()=>{
        const result = [];
        for(let i = 0; i < MONTHS_PER_YEAR; i++){
            result.push({
                index: i,
                label: format(new Date(displayYear, i, 1), 'MMM')
            });
        }
        return result;
    }, [
        displayYear
    ]);
    const isDisabled = useCallback((monthIndex)=>{
        const cellDate = new Date(displayYear, monthIndex, 1);
        if (min !== undefined && isBefore(cellDate, startOfMonth(min))) return true;
        if (max !== undefined && isAfter(cellDate, startOfMonth(max))) return true;
        if (isDateDisabled?.(cellDate)) return true;
        return false;
    }, [
        displayYear,
        min,
        max,
        isDateDisabled
    ]);
    // Roving tabindex: track which grid cell is the "active" tabbable one.
    // This ensures exactly one grid cell always has tabIndex=0 so Tab can reach the grid.
    const getInitialFocusIndex = useCallback(()=>{
        if (selectedYear !== undefined && selectedMonth !== undefined && displayYear === selectedYear) {
            if (!isDisabled(selectedMonth)) return selectedMonth;
        }
        // Fall back to first non-disabled cell so keyboard navigation always has a starting point
        const firstEnabled = Array.from({
            length: MONTHS_PER_YEAR
        }, (_, i)=>i).findIndex((m)=>!isDisabled(m));
        return firstEnabled >= 0 ? firstEnabled : 0;
    }, [
        selectedYear,
        selectedMonth,
        displayYear,
        isDisabled
    ]);
    const [focusedIndex, setFocusedIndex] = useState(getInitialFocusIndex);
    // Update focused index when display year changes (e.g. after prev/next navigation)
    useEffect(()=>{
        setFocusedIndex(getInitialFocusIndex());
    }, [
        displayYear,
        selectedYear,
        selectedMonth,
        getInitialFocusIndex
    ]);
    const handlePrevYear = useCallback(()=>{
        setDisplayYear((prev)=>prev - 1);
    }, []);
    const handleNextYear = useCallback(()=>{
        setDisplayYear((prev)=>prev + 1);
    }, []);
    const handleSelectMonth = useCallback((monthIndex)=>{
        onSelect(new Date(displayYear, monthIndex, 1));
    }, [
        displayYear,
        onSelect
    ]);
    const gridRef = useRef(null);
    const focusCellAtIndex = useCallback((nextIndex)=>{
        const grid = gridRef.current;
        if (grid) {
            const buttons = grid.querySelectorAll('[role="gridcell"] button');
            buttons[nextIndex]?.focus();
        }
    }, []);
    const handleKeyDown = useCallback((event, monthIndex)=>{
        let nextIndex = monthIndex;
        switch(event.key){
            case 'ArrowRight':
                event.preventDefault();
                if (monthIndex >= MONTHS_PER_YEAR - 1) {
                    // At the end of the grid — navigate to next year, focus first cell
                    setDisplayYear((prev)=>prev + 1);
                    nextIndex = 0;
                } else {
                    nextIndex = monthIndex + 1;
                }
                break;
            case 'ArrowLeft':
                event.preventDefault();
                if (monthIndex <= 0) {
                    // At the start of the grid — navigate to previous year, focus last cell
                    setDisplayYear((prev)=>prev - 1);
                    nextIndex = MONTHS_PER_YEAR - 1;
                } else {
                    nextIndex = monthIndex - 1;
                }
                break;
            case 'ArrowDown':
                event.preventDefault();
                if (monthIndex + 4 >= MONTHS_PER_YEAR) {
                    // At the bottom of the grid — navigate to next year, keep column position
                    setDisplayYear((prev)=>prev + 1);
                    nextIndex = monthIndex % 4; // keep same column, first row
                } else {
                    nextIndex = monthIndex + 4;
                }
                break;
            case 'ArrowUp':
                event.preventDefault();
                if (monthIndex - 4 < 0) {
                    // At the top of the grid — navigate to previous year, keep column position
                    setDisplayYear((prev)=>prev - 1);
                    nextIndex = MONTHS_PER_YEAR - 4 + monthIndex % 4; // keep same column, last row
                } else {
                    nextIndex = monthIndex - 4;
                }
                break;
            // Enter and Space are handled by the DS Button's onClick — no need to handle here
            default:
                return;
        }
        setFocusedIndex(nextIndex);
    }, []);
    // Navigation buttons are never disabled — users should always be able to browse pages.
    // Only individual grid cells are disabled by min/max constraints.
    const isPrevDisabled = false;
    const isNextDisabled = false;
    // Focus the active grid cell whenever focusedIndex changes (covers both initial mount
    // and post-navigation focus after arrow key presses or month page changes).
    useEffect(()=>{
        focusCellAtIndex(focusedIndex);
    }, [
        focusedIndex,
        focusCellAtIndex
    ]);
    return /*#__PURE__*/ jsxs("div", {
        style: {
            padding: theme.spacing.xs,
            minWidth: 225
        },
        children: [
            /*#__PURE__*/ jsxs("div", {
                style: {
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'space-between',
                    padding: `0 ${theme.spacing.xs}px`,
                    marginBottom: theme.spacing.xs
                },
                children: [
                    /*#__PURE__*/ jsx(Button, {
                        componentId: "datepicker-month-grid-prev",
                        "aria-label": "Previous year",
                        icon: /*#__PURE__*/ jsx(ChevronLeftIcon, {}),
                        onClick: handlePrevYear,
                        disabled: isPrevDisabled,
                        size: "small"
                    }),
                    /*#__PURE__*/ jsx(Typography.Text, {
                        bold: true,
                        "aria-live": "polite",
                        children: displayYear
                    }),
                    /*#__PURE__*/ jsx(Button, {
                        componentId: "datepicker-month-grid-next",
                        "aria-label": "Next year",
                        icon: /*#__PURE__*/ jsx(ChevronRightIcon, {}),
                        onClick: handleNextYear,
                        disabled: isNextDisabled,
                        size: "small"
                    })
                ]
            }),
            /*#__PURE__*/ jsx("div", {
                ref: gridRef,
                role: "grid",
                "aria-label": "Month picker",
                style: {
                    display: 'grid',
                    gridTemplateColumns: 'repeat(4, 1fr)'
                },
                children: months.map(({ index, label })=>/*#__PURE__*/ jsx(MonthCell, {
                        index: index,
                        label: label,
                        displayYear: displayYear,
                        selected: displayYear === selectedYear && index === selectedMonth,
                        disabled: isDisabled(index),
                        rangeFrom: rangeFrom,
                        rangeTo: rangeTo,
                        focusedIndex: focusedIndex,
                        onSelect: handleSelectMonth,
                        onKeyDown: handleKeyDown,
                        onFocus: setFocusedIndex,
                        renderCellContent: renderCellContent
                    }, index))
            })
        ]
    });
}

const YEARS_PER_PAGE = 12;
function getGridCellCss(theme, isRangeStart, isRangeEnd, isRangeMiddle, index) {
    return {
        padding: 0,
        // Use actionTertiaryBackgroundPress for range background to match DayPicker's --rdp-background-color
        backgroundColor: isRangeMiddle ? theme.colors.actionTertiaryBackgroundPress : 'transparent',
        // Range start: round left edge, square right edge (flush with middle range)
        ...isRangeStart && {
            borderTopLeftRadius: theme.general.borderRadiusBase,
            borderBottomLeftRadius: theme.general.borderRadiusBase,
            borderTopRightRadius: 0,
            borderBottomRightRadius: 0,
            backgroundColor: theme.colors.actionTertiaryBackgroundPress
        },
        // Range end: round right edge, square left edge (flush with middle range)
        ...isRangeEnd && {
            borderTopRightRadius: theme.general.borderRadiusBase,
            borderBottomRightRadius: theme.general.borderRadiusBase,
            borderTopLeftRadius: 0,
            borderBottomLeftRadius: 0,
            backgroundColor: theme.colors.actionTertiaryBackgroundPress
        },
        // When start === end, round both sides
        ...isRangeStart && isRangeEnd && {
            borderRadius: theme.general.borderRadiusBase
        },
        // Round left edge for first column cells in range
        ...isRangeMiddle && index % 4 === 0 && {
            borderTopLeftRadius: theme.general.borderRadiusBase,
            borderBottomLeftRadius: theme.general.borderRadiusBase
        },
        // Round right edge for last column cells in range
        ...isRangeMiddle && index % 4 === 3 && {
            borderTopRightRadius: theme.general.borderRadiusBase,
            borderBottomRightRadius: theme.general.borderRadiusBase
        }
    };
}
function getButtonCss(theme, selected, isRangeStart, isRangeEnd, isCurrentYear) {
    return {
        width: '100%',
        // DS Button's importantify wraps borderColor with !important, so we must override here.
        // borderWidth is always 2px (matching DayPicker's --rdp-outline) so focus doesn't cause layout shift.
        borderColor: 'transparent !important',
        borderWidth: '2px !important',
        boxShadow: 'none !important',
        '&[disabled]': {
            borderColor: 'transparent !important'
        },
        fontWeight: isCurrentYear ? 'bold' : 'normal',
        // Non-selected focused: DayPicker shows light blue bg + single inner border ring (no outer outline).
        // Selected+focused gets the 2-ring style (inner + outer with gap) via the selected override below.
        // borderWidth ensures the 2px ring matches DayPicker's --rdp-outline (2px solid accent).
        '&:focus-visible': {
            backgroundColor: `${theme.colors.actionTertiaryBackgroundPress} !important`,
            borderColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
            outlineColor: 'transparent !important'
        },
        // Use the exact same colors as DayPicker for selected/range start/end cells.
        // Manually applying actionPrimaryBackground* + #fff text ensures consistent
        // appearance across light and dark themes without relying on Button type='primary'.
        ...(selected || isRangeStart || isRangeEnd) && {
            backgroundColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
            color: '#fff !important',
            '&:hover': {
                backgroundColor: `${theme.colors.actionPrimaryBackgroundHover} !important`,
                color: '#fff !important',
                // Keep border transparent on hover — DS Button shows a border on hover by default
                borderColor: 'transparent !important'
            },
            '&:active': {
                backgroundColor: `${theme.colors.actionPrimaryBackgroundPress} !important`,
                color: '#fff !important'
            },
            // Keyboard focus on selected cell: DayPicker's .button:focus-visible:not([disabled]) has
            // higher specificity than .day_selected, so selected+focused shows the hover (light blue)
            // background with dark text. Match that behavior here.
            '&:focus-visible': {
                backgroundColor: `${theme.colors.actionTertiaryBackgroundPress} !important`,
                color: 'inherit !important',
                borderColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
                outlineColor: `${theme.colors.actionPrimaryBackgroundDefault} !important`,
                outlineOffset: '2px !important'
            }
        },
        ...!(selected || isRangeStart || isRangeEnd) && {
            backgroundColor: 'transparent !important',
            '&:hover:not([disabled])': {
                backgroundColor: `${theme.colors.actionTertiaryBackgroundPress} !important`,
                color: 'inherit !important',
                // Keep border transparent on hover — DS Button shows a border on hover by default
                borderColor: 'transparent !important'
            }
        },
        // Match button border-radius to gridcell container for range start/end
        ...isRangeStart && !isRangeEnd && {
            borderTopRightRadius: 0,
            borderBottomRightRadius: 0
        },
        ...isRangeEnd && !isRangeStart && {
            borderTopLeftRadius: 0,
            borderBottomLeftRadius: 0
        }
    };
}
function YearCell({ year, index, selected, disabled, isCurrentYear, rangeFrom, rangeTo, focusedIndex, onSelect, onKeyDown, onFocus, renderCellContent }) {
    const { theme } = useDesignSystemTheme();
    const rangeFromYear = rangeFrom?.getFullYear();
    const rangeToYear = rangeTo?.getFullYear();
    const inRange = rangeFromYear !== undefined && rangeToYear !== undefined && year >= rangeFromYear && year <= rangeToYear;
    const isRangeStart = rangeFromYear !== undefined && year === rangeFromYear;
    const isRangeEnd = rangeToYear !== undefined && year === rangeToYear;
    const isRangeMiddle = inRange && !isRangeStart && !isRangeEnd;
    const defaultButton = /*#__PURE__*/ jsx(Button, {
        componentId: "datepicker-year-grid-cell",
        "aria-label": String(year),
        "data-selected": selected || undefined,
        "aria-current": isCurrentYear ? 'date' : undefined,
        disabled: disabled,
        tabIndex: index === focusedIndex ? 0 : -1,
        onClick: ()=>!disabled && onSelect(year),
        onKeyDown: (e)=>onKeyDown(e, index),
        onFocus: ()=>onFocus(index),
        css: getButtonCss(theme, selected, isRangeStart, isRangeEnd, isCurrentYear),
        children: year
    });
    return /*#__PURE__*/ jsx("div", {
        role: "gridcell",
        css: getGridCellCss(theme, isRangeStart, isRangeEnd, isRangeMiddle, index),
        children: renderCellContent ? renderCellContent(new Date(year, 0, 1), defaultButton) : defaultButton
    }, year);
}
/**
 * YearPickerGrid renders a 4x3 grid of years with decade navigation.
 * It is used inside the DatePicker when `granularity='year'` is set.
 */ function YearPickerGrid({ value, onSelect, min, max, rangeFrom, rangeTo, renderCellContent, isDateDisabled }) {
    const { theme } = useDesignSystemTheme();
    const selectedYear = value?.getFullYear();
    // Compute the initial decade start from the selected year or the current year
    const initialDecadeStart = useMemo(()=>{
        const year = selectedYear ?? new Date().getFullYear();
        return year - year % YEARS_PER_PAGE;
    }, [
        selectedYear
    ]);
    const [decadeStart, setDecadeStart] = useState(initialDecadeStart);
    const minYear = min?.getFullYear();
    const maxYear = max?.getFullYear();
    const years = useMemo(()=>{
        const result = [];
        for(let i = 0; i < YEARS_PER_PAGE; i++){
            result.push(decadeStart + i);
        }
        return result;
    }, [
        decadeStart
    ]);
    const isDisabled = useCallback((year)=>{
        if (minYear !== undefined && year < minYear) return true;
        if (maxYear !== undefined && year > maxYear) return true;
        if (isDateDisabled?.(new Date(year, 0, 1))) return true;
        return false;
    }, [
        minYear,
        maxYear,
        isDateDisabled
    ]);
    // Roving tabindex: track which grid cell is the "active" tabbable one.
    // This ensures exactly one grid cell always has tabIndex=0 so Tab can reach the grid.
    const getInitialFocusIndex = useCallback((currentYears)=>{
        if (selectedYear !== undefined) {
            const idx = selectedYear - decadeStart;
            if (idx >= 0 && idx < YEARS_PER_PAGE && !isDisabled(currentYears[idx] ?? 0)) return idx;
        }
        // Fall back to first non-disabled cell so keyboard navigation always has a starting point
        const firstEnabled = currentYears.findIndex((y)=>!isDisabled(y));
        return firstEnabled >= 0 ? firstEnabled : 0;
    }, [
        selectedYear,
        decadeStart,
        isDisabled
    ]);
    const [focusedIndex, setFocusedIndex] = useState(()=>getInitialFocusIndex(Array.from({
            length: YEARS_PER_PAGE
        }, (_, i)=>initialDecadeStart + i)));
    // Update focused index when decade changes (e.g. after prev/next navigation)
    useEffect(()=>{
        setFocusedIndex(getInitialFocusIndex(years));
    }, [
        decadeStart,
        selectedYear,
        getInitialFocusIndex,
        years
    ]);
    const handlePrevDecade = useCallback(()=>{
        setDecadeStart((prev)=>prev - YEARS_PER_PAGE);
    }, []);
    const handleNextDecade = useCallback(()=>{
        setDecadeStart((prev)=>prev + YEARS_PER_PAGE);
    }, []);
    const handleSelectYear = useCallback((year)=>{
        onSelect(new Date(year, 0, 1));
    }, [
        onSelect
    ]);
    const gridRef = useRef(null);
    const focusCellAtIndex = useCallback((nextIndex)=>{
        const grid = gridRef.current;
        if (grid) {
            const buttons = grid.querySelectorAll('[role="gridcell"] button');
            buttons[nextIndex]?.focus();
        }
    }, []);
    const handleKeyDown = useCallback((event, index)=>{
        let nextIndex = index;
        switch(event.key){
            case 'ArrowRight':
                event.preventDefault();
                if (index >= YEARS_PER_PAGE - 1) {
                    // At the end of the grid — navigate to next page, focus first cell
                    setDecadeStart((prev)=>prev + YEARS_PER_PAGE);
                    nextIndex = 0;
                } else {
                    nextIndex = index + 1;
                }
                break;
            case 'ArrowLeft':
                event.preventDefault();
                if (index <= 0) {
                    // At the start of the grid — navigate to previous page, focus last cell
                    setDecadeStart((prev)=>prev - YEARS_PER_PAGE);
                    nextIndex = YEARS_PER_PAGE - 1;
                } else {
                    nextIndex = index - 1;
                }
                break;
            case 'ArrowDown':
                event.preventDefault();
                if (index + 4 >= YEARS_PER_PAGE) {
                    // At the bottom of the grid — navigate to next page, keep column position
                    setDecadeStart((prev)=>prev + YEARS_PER_PAGE);
                    nextIndex = index % 4; // keep same column, first row
                } else {
                    nextIndex = index + 4;
                }
                break;
            case 'ArrowUp':
                event.preventDefault();
                if (index - 4 < 0) {
                    // At the top of the grid — navigate to previous page, keep column position
                    setDecadeStart((prev)=>prev - YEARS_PER_PAGE);
                    nextIndex = YEARS_PER_PAGE - 4 + index % 4; // keep same column, last row
                } else {
                    nextIndex = index - 4;
                }
                break;
            // Enter and Space are handled by the DS Button's onClick — no need to handle here
            default:
                return;
        }
        setFocusedIndex(nextIndex);
    }, []);
    // Navigation buttons are never disabled — users should always be able to browse pages.
    // Only individual grid cells are disabled by min/max constraints.
    const isPrevDisabled = false;
    const isNextDisabled = false;
    // Focus the active grid cell whenever focusedIndex changes (covers both initial mount
    // and post-navigation focus after arrow key presses or decade page changes).
    useEffect(()=>{
        focusCellAtIndex(focusedIndex);
    }, [
        focusedIndex,
        focusCellAtIndex
    ]);
    return /*#__PURE__*/ jsxs("div", {
        style: {
            padding: theme.spacing.xs,
            minWidth: 225
        },
        children: [
            /*#__PURE__*/ jsxs("div", {
                style: {
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'space-between',
                    padding: `0 ${theme.spacing.xs}px`,
                    marginBottom: theme.spacing.xs
                },
                children: [
                    /*#__PURE__*/ jsx(Button, {
                        componentId: "datepicker-year-grid-prev",
                        "aria-label": "Previous decade",
                        icon: /*#__PURE__*/ jsx(ChevronLeftIcon, {}),
                        onClick: handlePrevDecade,
                        disabled: isPrevDisabled,
                        size: "small"
                    }),
                    /*#__PURE__*/ jsxs(Typography.Text, {
                        bold: true,
                        "aria-live": "polite",
                        children: [
                            decadeStart,
                            " – ",
                            decadeStart + YEARS_PER_PAGE - 1
                        ]
                    }),
                    /*#__PURE__*/ jsx(Button, {
                        componentId: "datepicker-year-grid-next",
                        "aria-label": "Next decade",
                        icon: /*#__PURE__*/ jsx(ChevronRightIcon, {}),
                        onClick: handleNextDecade,
                        disabled: isNextDisabled,
                        size: "small"
                    })
                ]
            }),
            /*#__PURE__*/ jsx("div", {
                ref: gridRef,
                role: "grid",
                "aria-label": "Year picker",
                style: {
                    display: 'grid',
                    gridTemplateColumns: 'repeat(4, 1fr)'
                },
                children: years.map((year, index)=>/*#__PURE__*/ jsx(YearCell, {
                        year: year,
                        index: index,
                        selected: year === selectedYear,
                        disabled: isDisabled(year),
                        isCurrentYear: year === new Date().getFullYear(),
                        rangeFrom: rangeFrom,
                        rangeTo: rangeTo,
                        focusedIndex: focusedIndex,
                        onSelect: handleSelectYear,
                        onKeyDown: handleKeyDown,
                        onFocus: setFocusedIndex,
                        renderCellContent: renderCellContent
                    }, year))
            })
        ]
    });
}

const getDayPickerStyles = (prefix, theme)=>/*#__PURE__*/ css(".", prefix, "{--rdp-cell-size:", theme.general.heightSm, "px;--rdp-caption-font-size:", theme.typography.fontSizeBase, "px;--rdp-accent-color:", theme.colors.actionPrimaryBackgroundDefault, ";--rdp-background-color:", theme.colors.actionTertiaryBackgroundPress, ";--rdp-accent-color-dark:", theme.colors.actionPrimaryBackgroundDefault, ";--rdp-background-color-dark:", theme.colors.actionTertiaryBackgroundPress, ";--rdp-outline:2px solid var(--rdp-accent-color);--rdp-outline-selected:3px solid var(--rdp-accent-color);--rdp-selected-color:#fff;padding:4px;}.", prefix, "-vhidden{box-sizing:border-box;padding:0;margin:0;background:transparent;border:0;-moz-appearance:none;-webkit-appearance:none;appearance:none;position:absolute !important;top:0;width:1px !important;height:1px !important;padding:0 !important;overflow:hidden !important;clip:rect(1px,1px,1px,1px) !important;border:0 !important;}.", prefix, "-button_reset{appearance:none;position:relative;margin:0;padding:0;cursor:default;color:inherit;background:none;font:inherit;-moz-appearance:none;-webkit-appearance:none;}.", prefix, "-button_reset:focus-visible{outline:none;}.", prefix, "-button{border:2px solid transparent;}.", prefix, "-button[disabled]:not(.", prefix, "-day_selected){opacity:0.25;}.", prefix, "-button:not([disabled]){cursor:pointer;}.", prefix, "-button:focus-visible:not([disabled]){color:inherit;background-color:var(--rdp-background-color);border:var(--rdp-outline);}.", prefix, "-button:hover:not([disabled]):not(.", prefix, "-day_selected){background-color:var(--rdp-background-color);}.", prefix, "-months{display:flex;justify-content:center;}.", prefix, "-month{margin:0 1em;}.", prefix, "-month:first-of-type{margin-left:0;}.", prefix, "-month:last-child{margin-right:0;}.", prefix, "-table{margin:0;max-width:calc(var(--rdp-cell-size) * 7);border-collapse:collapse;}.", prefix, "-with_weeknumber .", prefix, "-table{max-width:calc(var(--rdp-cell-size) * 8);border-collapse:collapse;}.", prefix, "-caption{display:flex;align-items:center;justify-content:space-between;padding:0;text-align:left;}.", prefix, "-multiple_months .", prefix, "-caption{position:relative;display:block;text-align:center;}.", prefix, "-caption_dropdowns{position:relative;display:inline-flex;}.", prefix, "-caption_label{position:relative;z-index:1;display:inline-flex;align-items:center;margin:0;padding:0 0.25em;white-space:nowrap;color:currentColor;border:0;border:2px solid transparent;font-family:inherit;font-size:var(--rdp-caption-font-size);font-weight:600;}.", prefix, "-nav{white-space:nowrap;}.", prefix, "-multiple_months .", prefix, "-caption_start .", prefix, "-nav{position:absolute;top:50%;left:0;transform:translateY(-50%);}.", prefix, "-multiple_months .", prefix, "-caption_end .", prefix, "-nav{position:absolute;top:50%;right:0;transform:translateY(-50%);}.", prefix, "-nav_button{display:inline-flex;align-items:center;justify-content:center;width:var(--rdp-cell-size);height:var(--rdp-cell-size);}.", prefix, "-dropdown_year,.", prefix, "-dropdown_month{position:relative;display:inline-flex;align-items:center;}.", prefix, "-dropdown{appearance:none;position:absolute;z-index:2;top:0;bottom:0;left:0;width:100%;margin:0;padding:0;cursor:inherit;opacity:0;border:none;background-color:transparent;font-family:inherit;font-size:inherit;line-height:inherit;}.", prefix, "-dropdown[disabled]{opacity:unset;color:unset;}.", prefix, "-dropdown:focus-visible:not([disabled]) + .", prefix, "-caption_label{background-color:var(--rdp-background-color);border:var(--rdp-outline);border-radius:6px;}.", prefix, "-dropdown_icon{margin:0 0 0 5px;}.", prefix, "-head{border:0;}.", prefix, "-head_row,.", prefix, "-row{height:100%;}.", prefix, "-head_cell{vertical-align:middle;font-size:inherit;font-weight:400;color:", theme.colors.textSecondary, ";text-align:center;height:100%;height:var(--rdp-cell-size);padding:0;text-transform:uppercase;}.", prefix, "-tbody{border:0;}.", prefix, "-tfoot{margin:0.5em;}.", prefix, "-cell{width:var(--rdp-cell-size);height:100%;height:var(--rdp-cell-size);padding:0;text-align:center;}.", prefix, "-weeknumber{font-size:0.75em;}.", prefix, "-weeknumber,.", prefix, "-day{display:flex;overflow:hidden;align-items:center;justify-content:center;box-sizing:border-box;width:var(--rdp-cell-size);max-width:var(--rdp-cell-size);height:var(--rdp-cell-size);margin:0;border:2px solid transparent;border-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-day_today:not(.", prefix, "-day_outside){font-weight:bold;}.", prefix, "-day_selected,.", prefix, "-day_selected:focus-visible,.", prefix, "-day_selected:hover{color:var(--rdp-selected-color);opacity:1;background-color:var(--rdp-accent-color);}.", prefix, "-day_outside{pointer-events:none;color:", theme.colors.textSecondary, ";}.", prefix, "-day_selected:focus-visible{outline:var(--rdp-outline);outline-offset:2px;z-index:1;}.", prefix, ":not([dir='rtl']) .", prefix, "-day_range_start:not(.", prefix, "-day_range_end){border-top-right-radius:0;border-bottom-right-radius:0;}.", prefix, ":not([dir='rtl']) .", prefix, "-day_range_end:not(.", prefix, "-day_range_start){border-top-left-radius:0;border-bottom-left-radius:0;}.", prefix, "[dir='rtl'] .", prefix, "-day_range_start:not(.", prefix, "-day_range_end){border-top-left-radius:0;border-bottom-left-radius:0;}.", prefix, "[dir='rtl'] .", prefix, "-day_range_end:not(.", prefix, "-day_range_start){border-top-right-radius:0;border-bottom-right-radius:0;}.", prefix, "-day_range_start,.", prefix, "-day_range_end{border:0;& > span{width:100%;height:100%;display:flex;align-items:center;justify-content:center;border-radius:", theme.general.borderRadiusBase, "px;background-color:var(--rdp-accent-color);color:", theme.colors.white, ";}}.", prefix, "-day_range_end.", prefix, "-day_range_start{border-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-day_range_middle{border-radius:0;background-color:var(--rdp-background-color);color:", theme.colors.actionDefaultTextDefault, ";&:hover{color:", theme.colors.actionTertiaryTextHover, ";}}.", prefix, "-row > td:last-of-type .", prefix, "-day_range_middle{border-top-right-radius:", theme.general.borderRadiusBase, "px;border-bottom-right-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-row > td:first-of-type .", prefix, "-day_range_middle{border-top-left-radius:", theme.general.borderRadiusBase, "px;border-bottom-left-radius:", theme.general.borderRadiusBase, "px;}");
/**
 * Additional styles applied to the DayPicker when used in week picker mode (granularity='week').
 * Highlights the entire week row on hover and for the selected week.
 */ const getWeekPickerStyles = (prefix, theme)=>/*#__PURE__*/ css(".", prefix, "-day{border-radius:0;}.", prefix, "-day_outside{pointer-events:auto !important;opacity:0.6 !important;}.", prefix, "-row:hover .", prefix, "-day{background-color:var(--rdp-background-color) !important;}.", prefix, "-row:hover td:nth-of-type(2) .", prefix, "-day{border-top-left-radius:", theme.general.borderRadiusBase, "px;border-bottom-left-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-row:hover td:last-of-type .", prefix, "-day{border-top-right-radius:", theme.general.borderRadiusBase, "px;border-bottom-right-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-row:has(.", prefix, "-day_selected) .", prefix, "-day{background-color:var(--rdp-accent-color) !important;color:var(--rdp-selected-color) !important;}.", prefix, "-row:has(.", prefix, "-day_selected) td:nth-of-type(2) .", prefix, "-day{border-top-left-radius:", theme.general.borderRadiusBase, "px;border-bottom-left-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-row:has(.", prefix, "-day_selected) td:last-of-type .", prefix, "-day{border-top-right-radius:", theme.general.borderRadiusBase, "px;border-bottom-right-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-day:focus-visible{border:2px solid transparent !important;outline:none !important;}.", prefix, "-row:focus-within .", prefix, "-day{background-color:var(--rdp-background-color) !important;}.", prefix, "-row:focus-within td:nth-of-type(2) .", prefix, "-day{border-top-left-radius:", theme.general.borderRadiusBase, "px;border-bottom-left-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-row:focus-within td:last-of-type .", prefix, "-day{border-top-right-radius:", theme.general.borderRadiusBase, "px;border-bottom-right-radius:", theme.general.borderRadiusBase, "px;}.", prefix, "-row:has(.", prefix, "-day_selected):focus-within .", prefix, "-day{background-color:var(--rdp-background-color) !important;color:inherit !important;}.", prefix, "-row:focus-within td:nth-of-type(2){position:relative;}.", prefix, "-row:focus-within:not(:has(.", prefix, "-day_selected)) td:nth-of-type(2)::after{content:'';position:absolute;top:0;left:0;height:100%;width:calc(7 * var(--rdp-cell-size));box-sizing:border-box;border:2px solid var(--rdp-accent-color);border-radius:", theme.general.borderRadiusBase, "px;pointer-events:none;z-index:2;}.", prefix, "-row:has(.", prefix, "-day_selected):focus-within td:nth-of-type(2)::after{content:'';position:absolute;top:0;left:0;height:100%;width:calc(7 * var(--rdp-cell-size));box-sizing:border-box;border:2px solid var(--rdp-accent-color);outline:2px solid var(--rdp-accent-color);outline-offset:2px;border-radius:", theme.general.borderRadiusBase, "px;pointer-events:none;z-index:2;}.", prefix, "-weeknumber{color:", theme.colors.textSecondary, ";}.", prefix, "-row .", prefix, "-day{cursor:pointer;}");

const generateDatePickerClassNames = (prefix)=>({
        root: `${prefix}`,
        multiple_months: `${prefix}-multiple_months`,
        with_weeknumber: `${prefix}-with_weeknumber`,
        vhidden: `${prefix}-vhidden`,
        button_reset: `${prefix}-button_reset`,
        button: `${prefix}-button`,
        caption: `${prefix}-caption`,
        caption_start: `${prefix}-caption_start`,
        caption_end: `${prefix}-caption_end`,
        caption_between: `${prefix}-caption_between`,
        caption_label: `${prefix}-caption_label`,
        caption_dropdowns: `${prefix}-caption_dropdowns`,
        dropdown: `${prefix}-dropdown`,
        dropdown_month: `${prefix}-dropdown_month`,
        dropdown_year: `${prefix}-dropdown_year`,
        dropdown_icon: `${prefix}-dropdown_icon`,
        months: `${prefix}-months`,
        month: `${prefix}-month`,
        table: `${prefix}-table`,
        tbody: `${prefix}-tbody`,
        tfoot: `${prefix}-tfoot`,
        head: `${prefix}-head`,
        head_row: `${prefix}-head_row`,
        head_cell: `${prefix}-head_cell`,
        nav: `${prefix}-nav`,
        nav_button: `${prefix}-nav_button`,
        nav_button_previous: `${prefix}-nav_button_previous`,
        nav_button_next: `${prefix}-nav_button_next`,
        nav_icon: `${prefix}-nav_icon`,
        row: `${prefix}-row`,
        weeknumber: `${prefix}-weeknumber`,
        cell: `${prefix}-cell`,
        day: `${prefix}-day`,
        day_today: `${prefix}-day_today`,
        day_outside: `${prefix}-day_outside`,
        day_selected: `${prefix}-day_selected`,
        day_disabled: `${prefix}-day_disabled`,
        day_hidden: `${prefix}-day_hidden`,
        day_range_start: `${prefix}-day_range_start`,
        day_range_end: `${prefix}-day_range_end`,
        day_range_middle: `${prefix}-day_range_middle`
    });
const isValidCustomInputDate = (date, format$1)=>{
    if (!date) {
        return false;
    }
    try {
        // Parse the date string using the provided format
        const parsedDate = parse(date, format$1, new Date());
        // Check if the parsed date is valid
        if (!isValid(parsedDate)) {
            return false;
        }
        // Additional check: ensure the formatted date matches the input
        // This prevents cases like parsing "2023-13-45" which might be invalid
        const formattedDate = format(parsedDate, format$1);
        return formattedDate === date;
    } catch (error) {
        return false;
    }
};

const DEFAULT_MIN_DATE = '1900-01-01 00:00:00';
const DEFAULT_MAX_DATE = '2100-12-31 23:59:59';
// Helps with adding the timezone offset to the date
// This is needed because the datepicker is in the local timezone and
// dates with negative timezone offsets that don't select time will be negatively offset
// leading to the previous day being selected
const correctDateTimezoneOffset = (date)=>{
    const dateWithCorrectedTimezone = new Date(date);
    if (dateWithCorrectedTimezone.getTimezoneOffset() > 0) {
        dateWithCorrectedTimezone.setMinutes(dateWithCorrectedTimezone.getMinutes() + dateWithCorrectedTimezone.getTimezoneOffset());
    }
    return dateWithCorrectedTimezone;
};
const formatDateLimitForInput = (date, includeTime, includeSeconds, defaultDate)=>{
    let limitDate = defaultDate ? new Date(defaultDate) : undefined;
    if (!limitDate) {
        return [
            undefined,
            undefined
        ];
    }
    if (date) {
        if (date instanceof Date && isValid(date)) {
            limitDate = date;
        } else if (isValid(new Date(date))) {
            limitDate = new Date(date);
        }
        if (!includeTime) {
            limitDate = correctDateTimezoneOffset(limitDate);
        }
    }
    if (includeTime) {
        if (includeSeconds) {
            return [
                limitDate,
                format(limitDate, 'yyyy-MM-dd HH:mm:ss')
            ];
        }
        return [
            limitDate,
            format(limitDate, 'yyyy-MM-dd HH:mm')
        ];
    }
    return [
        limitDate,
        format(limitDate, 'yyyy-MM-dd')
    ];
};
const handleInputKeyDown = (event, setIsVisible, backspaceFn, textMode = false)=>{
    if (event.key === ' ' || event.key === 'Enter' || event.key === 'Space') {
        event.preventDefault();
        event.stopPropagation();
        setIsVisible(true);
    } else if ((event.key === 'Backspace' || event.key === 'Delete') && backspaceFn && !textMode) {
        event.preventDefault();
        event.stopPropagation();
        backspaceFn?.();
    }
};
/**
 * Returns the ISO-compatible format string for the given granularity.
 * Native inputs (date, month, week) use these formats directly; the browser handles locale display.
 */ function getFormatForGranularity(granularity, includeTime, includeSeconds) {
    // Day format uses ISO for compatibility with native <input type="date"> which handles locale display
    const dayFormat = includeTime ? includeSeconds ? 'yyyy-MM-dd HH:mm:ss' : 'yyyy-MM-dd HH:mm' : 'yyyy-MM-dd';
    switch(granularity){
        case 'year':
            return 'yyyy';
        case 'month':
            // Use yyyy-MM format for compatibility with native <input type="month"> which handles locale display.
            // When allowCustomInput is used (text input), this still works as a parseable format.
            return 'yyyy-MM';
        case 'week':
            // Week format is handled specially by formatDateForGranularity
            return dayFormat;
        case 'day':
        default:
            return dayFormat;
    }
}
/**
 * Returns the Monday (start of ISO week) for the given year and ISO week number.
 * Jan 4 is always in ISO week 1; we add (week-1)*7 days to land in the target week,
 * then use startOfISOWeek to get Monday.
 */ function isoWeekToDate(year, week) {
    const jan4 = new Date(year, 0, 4);
    const dateInTargetWeek = new Date(jan4.getTime() + (week - 1) * 7 * 24 * 60 * 60 * 1000);
    return startOfISOWeek(dateInTargetWeek);
}
/**
 * Formats a Date for display in the input field based on granularity.
 * For week mode, produces "YYYY-Www" format (e.g., "2024-W03") compatible with native <input type="week">.
 */ function formatDateForGranularity(date, granularity, format$1) {
    if (granularity === 'week') {
        const weekYear = getISOWeekYear(date);
        const weekNum = getISOWeek(date);
        return `${weekYear}-W${String(weekNum).padStart(2, '0')}`;
    }
    return format(date, format$1);
}
const GRANULARITY_LABELS = {
    year: {
        prefix: 'Year:',
        placeholder: 'Select Year',
        ariaLabel: 'Select Year'
    },
    month: {
        prefix: 'Month:',
        placeholder: 'Select Month',
        ariaLabel: 'Select Month'
    },
    week: {
        prefix: 'Week:',
        placeholder: 'Select Week',
        ariaLabel: 'Select Week'
    }
};
function createDayComponent(renderCellContent) {
    return function Day(props) {
        const buttonRef = useRef(null);
        const dayRender = useDayRender(props.date, props.displayMonth, buttonRef);
        if (dayRender.isHidden) {
            return /*#__PURE__*/ jsx("div", {
                role: "cell"
            });
        }
        if (!dayRender.isButton) {
            return /*#__PURE__*/ jsx("div", {
                ...dayRender.divProps
            });
        }
        const ariaLabel = props.date.toLocaleDateString(undefined, {
            weekday: 'long',
            year: 'numeric',
            month: 'long',
            day: 'numeric'
        });
        const defaultButton = /*#__PURE__*/ jsx(Button$1, {
            name: "day",
            ref: buttonRef,
            ...dayRender.buttonProps,
            role: "gridcell",
            "aria-label": ariaLabel,
            children: dayRender.buttonProps.children
        });
        return renderCellContent ? renderCellContent(props.date, defaultButton) : defaultButton;
    };
}
const getDatePickerQuickActionBasic = ({ today, yesterday, sevenDaysAgo })=>[
        {
            label: 'Today',
            value: startOfToday(),
            ...today
        },
        {
            label: 'Yesterday',
            value: startOfYesterday(),
            ...yesterday
        },
        {
            label: '7 days ago',
            value: sub(startOfToday(), {
                days: 7
            }),
            ...sevenDaysAgo
        }
    ];
const DatePicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const useNewDatePickerInput = safex('databricks.fe.designsystem.useNewDatePickerInput', false);
    const { id, name, value, validationState, onChange, allowClear, onClear, includeTime, includeSeconds, defaultTime, onOpenChange, open, datePickerProps, timeInputProps, mode = 'single', selected, width, maxWidth, minWidth, dateTimeDisabledFn, quickActions, wrapperProps, onOkPress, okButtonLabel, showTimeZone, customTimeZoneLabel, min, max, allowCustomInput = false, granularity = 'day', renderCellContent, footer, ...restProps } = props;
    const format$1 = getFormatForGranularity(granularity, Boolean(includeTime), Boolean(includeSeconds));
    const [date, setDate] = useState(value instanceof Date ? value : undefined);
    const [inputText, setInputText] = useState(undefined);
    const [timezone, setTimezone] = useState(customTimeZoneLabel);
    const [isVisible, setIsVisible] = useState(Boolean(open));
    // Used to prevent the blur update when the datepicker is opened/closed
    const [preventBlurUpdate, setPreventBlurUpdate] = useState(false);
    const DayComponent = useMemo(()=>createDayComponent(renderCellContent), [
        renderCellContent
    ]);
    const inputRef = useRef(null);
    const visibleRef = useRef(isVisible);
    // Needed to avoid the clear icon click also reopening the datepicker
    const fromClearRef = useRef(null);
    const setDateInputValue = useCallback((date)=>{
        const formattedDate = date && isValid(date) ? formatDateForGranularity(date, granularity, format$1) : undefined;
        if (inputRef.current) {
            inputRef.current.value = formattedDate;
        }
    }, [
        format$1,
        granularity
    ]);
    useEffect(()=>{
        // Sync inputText with value prop for text-input modes (year, week, or allowCustomInput).
        // Native input types (date, month) handle their display via the value attribute directly.
        if (allowCustomInput || granularity !== 'day' && granularity !== 'month') {
            setInputText(value instanceof Date ? formatDateForGranularity(value, granularity, format$1) : value ?? '');
        }
    }, [
        value,
        format$1,
        allowCustomInput,
        granularity
    ]);
    const setDateAndDateInputValue = useCallback((date)=>{
        setDate(date);
        if (useNewDatePickerInput) {
            setDateInputValue(date);
        }
    }, [
        useNewDatePickerInput,
        setDateInputValue
    ]);
    useEffect(()=>{
        if (!isVisible && visibleRef.current) {
            inputRef.current?.focus();
        }
        visibleRef.current = isVisible;
        onOpenChange?.(isVisible);
    }, [
        isVisible,
        onOpenChange
    ]);
    useEffect(()=>{
        setIsVisible(Boolean(open));
    }, [
        open
    ]);
    useEffect(()=>{
        const now = new Date();
        if (showTimeZone) {
            if (customTimeZoneLabel) {
                setTimezone(customTimeZoneLabel);
                return;
            }
            setTimezone(Intl.DateTimeFormat('en-US', {
                timeZoneName: 'short'
            }).formatToParts(now).find((part)=>part.type === 'timeZoneName')?.value ?? format(now, 'z'));
        } else {
            setTimezone(undefined);
        }
    }, [
        showTimeZone,
        customTimeZoneLabel
    ]);
    useEffect(()=>{
        if (value) {
            if (value instanceof Date && isValid(value)) {
                // Normalize month granularity to the 1st so that a value like new Date() (e.g. Mar 14)
                // doesn't display as a different month after round-tripping through the formatted string.
                const normalizedValue = granularity === 'month' ? new Date(value.getFullYear(), value.getMonth(), 1) : value;
                setDateAndDateInputValue(normalizedValue);
            } else {
                // Prevent setting the date when custom input is in progress
                if (!allowCustomInput && isValid(new Date(value))) {
                    setDateAndDateInputValue(new Date(value));
                } else {
                    setDateAndDateInputValue(undefined);
                }
            }
        } else {
            setDateAndDateInputValue(undefined);
        }
    }, [
        value,
        setDateAndDateInputValue,
        allowCustomInput,
        granularity
    ]);
    const handleChange = useCallback((value, textValue, isCalendarUpdate)=>{
        if (onChange) {
            onChange({
                target: {
                    name,
                    value: value instanceof Date ? value : undefined,
                    textValue
                },
                type: 'change',
                updateLocation: isCalendarUpdate ? 'calendar' : 'input'
            });
        }
    }, [
        onChange,
        name
    ]);
    const handleDatePickerUpdate = (date)=>{
        // For week granularity, snap to the Monday (start of ISO week) of the selected date.
        // Always clone the date to avoid mutating the caller-provided Date instance.
        const effectiveDate = granularity === 'week' ? startOfISOWeek(date) : new Date(date);
        // Prevent the blur triggering an update event as well
        // custom input does not move focus, so no need to prevent blur update
        if (!allowCustomInput) {
            setPreventBlurUpdate(true);
        }
        setDate((prevDate)=>{
            setDateInputValue(effectiveDate);
            // Set default time if date is set the first time
            if (!prevDate && effectiveDate && includeTime && defaultTime) {
                const timeSplit = defaultTime.split(':');
                effectiveDate?.setHours(+timeSplit[0]);
                effectiveDate?.setMinutes(+timeSplit[1]);
                effectiveDate.setSeconds(timeSplit.length > 2 ? +timeSplit[2] : 0);
            } else if (prevDate && effectiveDate && includeTime) {
                effectiveDate.setHours(prevDate.getHours());
                effectiveDate.setMinutes(prevDate.getMinutes());
                if (includeSeconds) {
                    effectiveDate.setSeconds(prevDate.getSeconds());
                }
            }
            const dateString = allowCustomInput ? formatDateForGranularity(effectiveDate, granularity, format$1) : undefined;
            setInputText(dateString);
            handleChange(effectiveDate, dateString, true);
            return effectiveDate;
        });
        if (!includeTime) {
            setIsVisible(false);
        }
    };
    const handleInputUpdate = (dateString)=>{
        // For text-input granularities (year), update the display text on each keystroke.
        // Parsing happens on blur to avoid erratic behavior with partial input like "202".
        // Week, month, and day with native inputs go through processInputDate directly.
        if (inputType === 'text' && granularity !== 'day') {
            setInputText(dateString);
            if (!dateString) {
                setDateAndDateInputValue(undefined);
                handleChange(undefined, undefined, false);
            }
            return;
        }
        // Update inputValue state when allowCustomInput is enabled
        if (allowCustomInput) {
            setInputText(dateString);
        }
        // For native week inputs, defer processing to onBlur. The browser's spin-box accumulates
        // digits as the user types (e.g. "2", "20", "202", "2026"), and processing on each onChange
        // causes a React-controlled value update that resets Chrome's active segment cursor to
        // the first segment (week number), breaking year entry. We track the typed string in
        // inputText so the controlled value prop stays in sync with what Chrome shows, preventing
        // React from reverting the input on re-renders triggered by unrelated state changes.
        if (granularity === 'week' && !allowCustomInput) {
            setInputText(dateString ?? '');
            return;
        }
        processInputDate(dateString);
    };
    const handleBlur = (e)=>{
        if (isVisible) {
            // For native week inputs we defer onChange processing to blur, so we need to commit the
            // typed value on blur even while the popover is open (e.g. user tabs away after typing
            // a year). If the user clicked a calendar cell instead, handleDatePickerUpdate will fire
            // after this blur and override the value with the calendar selection.
            if (granularity === 'week' && !allowCustomInput) {
                if (!preventBlurUpdate) {
                    processInputDate(e.target.value || undefined, true);
                    setInputText(undefined);
                } else {
                    setPreventBlurUpdate(false);
                }
            }
            return;
        }
        if (preventBlurUpdate) {
            // If the datepicker is opened/closed, prevent the blur update
            setPreventBlurUpdate(false);
            return;
        }
        // For text-input granularities (year, and week with allowCustomInput), parse typed values on blur.
        // Week with native input (type="week"), month, and day go through the standard date parsing below.
        if (inputType === 'text' && granularity !== 'day') {
            const inputValue = e.target.value?.trim();
            if (inputValue) {
                // Try parsing with multiple strategies
                let parsed;
                // For week granularity (allowCustomInput text mode), try parsing "YYYY-Www" format first
                if (granularity === 'week') {
                    const weekMatch = inputValue.match(/^(\d{4})-W(\d{1,2})$/);
                    if (weekMatch) {
                        const year = parseInt(weekMatch[1] ?? '0', 10);
                        const week = parseInt(weekMatch[2] ?? '0', 10);
                        if (week >= 1 && week <= 53) {
                            const targetMonday = isoWeekToDate(year, week);
                            if (isValid(targetMonday)) {
                                parsed = targetMonday;
                            }
                        }
                    }
                }
                // Try parsing with date-fns parse using the current format
                if (!parsed) {
                    const dateFnsParsed = parse(inputValue, format$1, new Date());
                    if (isValid(dateFnsParsed)) {
                        parsed = dateFnsParsed;
                    }
                }
                // Fallback: try ISO format (e.g., "2024-03" or "2024")
                if (!parsed) {
                    const isoParsed = parseISO(inputValue);
                    if (isValid(isoParsed)) {
                        parsed = isoParsed;
                    }
                }
                if (parsed) {
                    // For week granularity, snap to Monday
                    const effectiveDate = granularity === 'week' ? startOfISOWeek(parsed) : new Date(parsed);
                    setDateAndDateInputValue(effectiveDate);
                    const formattedText = formatDateForGranularity(effectiveDate, granularity, format$1);
                    setInputText(formattedText);
                    handleChange(effectiveDate, formattedText, false);
                } else if (date && isValid(date)) {
                    setDateAndDateInputValue(date);
                    setInputText(formatDateForGranularity(date, granularity, format$1));
                } else {
                    // No previous value and invalid input — clear everything
                    setInputText(undefined);
                }
            } else {
                // Empty input — clear the value
                setDateAndDateInputValue(undefined);
                handleChange(undefined, undefined, false);
            }
            return;
        }
        // For native week inputs, new Date("2024-W03") is invalid in JS, so pass the raw value directly.
        if (granularity === 'week') {
            processInputDate(e.target.value || undefined, true);
            return;
        }
        const newValue = new Date(e.target.value);
        if (allowCustomInput || newValue && isValid(newValue)) {
            processInputDate(e.target.value, true);
        } else {
            processInputDate(undefined, true);
        }
        if (granularity === 'day') {
            restProps?.onBlur?.(e);
        }
    };
    const [minimumAllowedDate, minimumAllowedDateString] = useMemo(()=>{
        return formatDateLimitForInput(min, Boolean(includeTime), Boolean(includeSeconds), DEFAULT_MIN_DATE);
    }, [
        min,
        includeTime,
        includeSeconds
    ]);
    const [maximumAllowedDate, maximumAllowedDateString] = useMemo(()=>{
        return formatDateLimitForInput(max, Boolean(includeTime), Boolean(includeSeconds), DEFAULT_MAX_DATE);
    }, [
        max,
        includeTime,
        includeSeconds
    ]);
    // shouldEmitOnChange should be true for instances when the updated date should propagate to the parent
    // when just typing, shouldEmitOnChange should be false
    // when blurring the input, shouldEmitOnChange should be true
    const processInputDate = (updatedDateString, shouldEmitOnChange = false)=>{
        // Parse "YYYY-Www" format from native <input type="week"> (e.g., "2024-W03").
        // new Date("2024-W03") is invalid in JS, so we parse it manually.
        let updatedDate;
        if (granularity === 'week' && updatedDateString) {
            const weekMatch = updatedDateString.match(/^(\d{4})-W(\d{2})$/);
            if (weekMatch) {
                const year = parseInt(weekMatch[1] ?? '0', 10);
                const week = parseInt(weekMatch[2] ?? '0', 10);
                if (week >= 1 && week <= 53) {
                    const targetMonday = isoWeekToDate(year, week);
                    if (isValid(targetMonday)) {
                        updatedDate = targetMonday;
                    }
                }
            }
        } else {
            updatedDate = updatedDateString ? parseISO(updatedDateString) : undefined;
        }
        // If the input is empty, set the date to undefined and emit the change event if shouldEmitOnChange is true
        if (!updatedDate) {
            setDateAndDateInputValue(undefined);
            if (shouldEmitOnChange) {
                handleChange(undefined, inputText, false);
            }
            return;
        }
        if (!isValidCustomInputDate(updatedDateString, format$1) && allowCustomInput) {
            if (shouldEmitOnChange) {
                handleChange(undefined, inputText, false);
            }
            return;
        }
        // If the date is invalid, don't update the date, the user is still typing
        if (!isValid(updatedDate)) {
            return;
        }
        let validInputDate = updatedDate;
        if (!includeTime) {
            validInputDate = correctDateTimezoneOffset(updatedDate);
        }
        // Don't emit onChange events for dates that have less than 4 digits in the year and before 1900
        // Only allow the input to be updated so the user can keep typing the date
        if (isBefore(validInputDate, new Date(DEFAULT_MIN_DATE)) || isAfter(validInputDate, new Date(DEFAULT_MAX_DATE))) {
            if (shouldEmitOnChange) {
                setDateAndDateInputValue(value instanceof Date ? value : undefined);
            } else {
                setDateAndDateInputValue(validInputDate);
            }
            return;
        }
        // If the value should be propagated, it means the input is blurred and the limit checks have already been done
        // so we can update the date and emit the change event
        if (shouldEmitOnChange) {
            if (validInputDate !== value) {
                setDateAndDateInputValue(validInputDate);
                handleChange(validInputDate, inputText, false);
            }
            return;
        }
        // Verify limits of the date and don't update the date if it's out of range
        if (minimumAllowedDate && isBefore(validInputDate, minimumAllowedDate)) {
            validInputDate = minimumAllowedDate;
        }
        if (maximumAllowedDate && isAfter(validInputDate, maximumAllowedDate)) {
            validInputDate = maximumAllowedDate;
        }
        setDateAndDateInputValue(validInputDate);
        if (!includeTime && isVisible) {
            setIsVisible(false);
        }
    };
    const handleClear = useCallback(()=>{
        setDateAndDateInputValue(undefined);
        setInputText(undefined);
        handleChange(undefined, undefined, false);
        onClear?.();
    }, [
        onClear,
        handleChange,
        setDateAndDateInputValue
    ]);
    const handleTimeUpdate = (e)=>{
        const newTime = e.nativeEvent?.target?.value;
        const time = date && isValid(date) ? format(date, includeSeconds ? 'HH:mm:ss' : 'HH:mm') : undefined;
        if (newTime && newTime !== time) {
            if (date) {
                const updatedDate = new Date(date);
                const timeSplit = newTime.split(':');
                updatedDate.setHours(+timeSplit[0]);
                updatedDate.setMinutes(+timeSplit[1]);
                if (includeSeconds) {
                    updatedDate.setSeconds(+timeSplit[2]);
                }
                processInputDate(format(updatedDate, format$1));
            }
        }
    };
    const handleTimeInputCommit = ()=>{
        if (date) {
            handleDatePickerUpdate(date);
        }
    };
    const handleTimeInputKeyDown = (e)=>{
        if (e.key === 'Enter') {
            handleTimeInputCommit();
        }
    };
    // Manually add the clear icon click event listener to avoid reopening the datepicker when clearing the input
    useEffect(()=>{
        if (allowClear && inputRef.current && !useNewDatePickerInput) {
            const clearIcon = inputRef.current.input?.closest('[type="button"]')?.querySelector(`.${classNamePrefix}-input-clear-icon`);
            if (clearIcon !== fromClearRef.current) {
                fromClearRef.current = clearIcon;
                const clientEventListener = (e)=>{
                    e.stopPropagation();
                    e.preventDefault();
                    handleClear();
                };
                clearIcon.addEventListener('click', clientEventListener);
            }
        }
    }, [
        classNamePrefix,
        defaultTime,
        handleClear,
        allowClear,
        useNewDatePickerInput
    ]);
    const { classNames, datePickerStyles, weekPickerStyles } = useMemo(()=>({
            classNames: generateDatePickerClassNames(`${classNamePrefix}-datepicker`),
            datePickerStyles: getDayPickerStyles(`${classNamePrefix}-datepicker`, theme),
            weekPickerStyles: getWeekPickerStyles(`${classNamePrefix}-datepicker`, theme)
        }), [
        classNamePrefix,
        theme
    ]);
    // Handler for year/month granularity grid selection
    const handleGranularitySelect = useCallback((selectedDate)=>{
        // Prevent the blur triggering an update event as well
        if (!allowCustomInput) {
            setPreventBlurUpdate(true);
        }
        setDate(()=>{
            setDateInputValue(selectedDate);
            // Always set inputText for non-day granularities so the text input displays the formatted value
            const dateString = formatDateForGranularity(selectedDate, granularity, format$1);
            setInputText(dateString);
            handleChange(selectedDate, dateString, true);
            return selectedDate;
        });
        setIsVisible(false);
    }, [
        format$1,
        granularity,
        handleChange,
        setDateInputValue,
        allowCustomInput
    ]);
    const chevronLeftIconComp = (props)=>/*#__PURE__*/ jsx(ChevronLeftIcon, {
            ...props
        });
    const chevronRightIconComp = (props)=>/*#__PURE__*/ jsx(ChevronRightIcon, {
            ...props
        });
    // Determine the input prefix label and placeholder based on granularity
    const granularityLabels = GRANULARITY_LABELS[granularity];
    // Determine input type:
    // - 'week' granularity uses native <input type="week"> for browser-native week selection
    // - 'month' granularity uses native <input type="month"> for locale-aware display (like day uses type="date")
    // - 'year' granularity uses type="text" since there's no well-supported native input type
    // - When allowCustomInput is enabled with non-day granularities, always use type="text" for free-form typing
    // - For day granularity with allowCustomInput, use text only when no date is set (existing behavior)
    const useTextInput = granularity === 'year' || allowCustomInput && granularity !== 'day' || allowCustomInput && !(date && isValid(date));
    const inputType = useTextInput ? 'text' : granularity === 'week' ? 'week' : granularity === 'month' ? 'month' : includeTime ? 'datetime-local' : 'date';
    // Arrow-key increment/decrement for year text input (analogous to native number/date inputs)
    const handleYearInputArrowKey = useCallback((event)=>{
        if (granularity !== 'year' || inputType !== 'text') return false;
        if (event.key !== 'ArrowUp' && event.key !== 'ArrowDown') return false;
        event.preventDefault();
        const currentYear = date?.getFullYear() ?? (inputText ? parseInt(inputText, 10) : new Date().getFullYear());
        if (isNaN(currentYear)) return true;
        const newYear = event.key === 'ArrowUp' ? currentYear + 1 : currentYear - 1;
        const newDate = new Date(newYear, 0, 1);
        setDateAndDateInputValue(newDate);
        setInputText(String(newYear));
        handleChange(newDate, String(newYear), false);
        return true;
    }, [
        granularity,
        inputType,
        date,
        inputText,
        setDateAndDateInputValue,
        setInputText,
        handleChange
    ]);
    const handleDatePickerInputKeyDown = (event)=>{
        if (handleYearInputArrowKey(event)) return;
        handleInputKeyDown(event, setIsVisible, ()=>handleInputUpdate(undefined), allowCustomInput && inputType === 'text');
    };
    const datePickerValue = inputType === 'text' ? // so users can type and see their input.
    granularity !== 'day' ? inputText ?? (date && isValid(date) ? formatDateForGranularity(date, granularity, format$1) : undefined) : allowCustomInput && !date ? inputText : date && isValid(date) ? formatDateForGranularity(date, granularity, format$1) : undefined : // controlled value stays in sync with Chrome's intermediate spin-box values. This
    // prevents React from reconciling the value prop back to the last committed date
    // and resetting Chrome's active segment cursor mid-typing.
    inputType === 'week' ? inputText !== undefined ? inputText : date && isValid(date) ? formatDateForGranularity(date, granularity, format$1) : undefined : date && isValid(date) ? formatDateForGranularity(date, granularity, format$1) : undefined;
    return /*#__PURE__*/ jsx("div", {
        className: `${classNamePrefix}-datepicker`,
        css: {
            width,
            minWidth,
            maxWidth,
            pointerEvents: restProps?.disabled ? 'none' : 'auto'
        },
        ...wrapperProps,
        children: /*#__PURE__*/ jsxs(Root$8, {
            componentId: "codegen_design-system_src_development_datepicker_datepicker.tsx_330",
            open: isVisible,
            onOpenChange: setIsVisible,
            children: [
                /*#__PURE__*/ jsx(Trigger$2, {
                    asChild: true,
                    disabled: restProps?.disabled,
                    role: "combobox",
                    children: /*#__PURE__*/ jsx("div", {
                        children: useNewDatePickerInput ? /*#__PURE__*/ jsxs(Fragment, {
                            children: [
                                /*#__PURE__*/ jsx(DatePickerInput, {
                                    id: id,
                                    // Once we get rid of the old datepicker input, we can remove the as any
                                    ref: inputRef,
                                    name: name,
                                    validationState: validationState,
                                    allowClear: allowClear,
                                    onClear: allowClear ? handleClear : undefined,
                                    placeholder: granularityLabels?.placeholder ?? 'Select Date',
                                    "aria-label": granularityLabels?.ariaLabel ?? (includeTime ? 'Select Date and Time' : 'Select Date'),
                                    prefix: granularityLabels?.prefix ?? 'Date:',
                                    role: "textbox",
                                    suffix: showTimeZone ? /*#__PURE__*/ jsx("span", {
                                        children: timezone
                                    }) : undefined,
                                    min: minimumAllowedDateString,
                                    max: maximumAllowedDateString,
                                    ...restProps,
                                    type: inputType,
                                    step: includeTime && includeSeconds ? 1 : undefined,
                                    onKeyDown: handleDatePickerInputKeyDown,
                                    onChange: (e)=>handleInputUpdate(e.target.value),
                                    value: datePickerValue,
                                    onBlur: handleBlur,
                                    onClick: (e)=>{
                                        e.preventDefault();
                                        e.stopPropagation();
                                        setIsVisible(!isVisible);
                                        restProps?.onClick?.(e);
                                    }
                                }),
                                /*#__PURE__*/ jsx("input", {
                                    type: "hidden",
                                    ref: ref,
                                    value: datePickerValue ?? ''
                                })
                            ]
                        }) : /*#__PURE__*/ jsxs(Fragment, {
                            children: [
                                /*#__PURE__*/ jsx(Input$1, {
                                    id: id,
                                    ref: inputRef,
                                    name: name,
                                    validationState: validationState,
                                    allowClear: allowClear,
                                    placeholder: granularityLabels?.placeholder ?? 'Select Date',
                                    "aria-label": granularityLabels?.ariaLabel ?? (includeTime ? 'Select Date and Time' : 'Select Date'),
                                    prefix: granularityLabels?.prefix ?? 'Date:',
                                    role: "textbox",
                                    suffix: showTimeZone ? /*#__PURE__*/ jsx("span", {
                                        children: timezone
                                    }) : undefined,
                                    min: minimumAllowedDateString,
                                    max: maximumAllowedDateString,
                                    ...restProps,
                                    css: {
                                        // Chrome, safari & webkit browsers specific fix to hide the calendar picker indicator
                                        '*::-webkit-calendar-picker-indicator': {
                                            display: 'none'
                                        },
                                        [`.${classNamePrefix}-input`]: {
                                            // vertical alignment fix for all browsers except chrome
                                            display: 'inline-flex',
                                            // Firefox specific fix to hide the calendar picker indicator
                                            marginRight: '-32px',
                                            paddingRight: '32px !important',
                                            width: 'calc(100% + 32px)',
                                            clipPath: 'inset(0 32px 0 0)'
                                        },
                                        [`.${classNamePrefix}-input-prefix`]: {
                                            ...!restProps?.disabled && {
                                                color: `${theme.colors.textPrimary} !important`
                                            }
                                        },
                                        [`&.${classNamePrefix}-input-affix-wrapper > *`]: {
                                            height: theme.typography.lineHeightBase
                                        },
                                        ...showTimeZone && {
                                            [`.${classNamePrefix}-input-suffix`]: {
                                                display: 'inline-flex',
                                                flexDirection: 'row-reverse',
                                                gap: theme.spacing.sm,
                                                alignItems: 'center'
                                            }
                                        }
                                    },
                                    type: inputType,
                                    step: includeTime && includeSeconds ? 1 : undefined,
                                    onKeyDown: handleDatePickerInputKeyDown,
                                    onChange: (e)=>handleInputUpdate(e.target.value),
                                    value: datePickerValue,
                                    onBlur: handleBlur,
                                    onClick: (e)=>{
                                        e.preventDefault();
                                        e.stopPropagation();
                                        setIsVisible(!isVisible);
                                        restProps?.onClick?.(e);
                                    }
                                }),
                                /*#__PURE__*/ jsx("input", {
                                    type: "hidden",
                                    ref: ref,
                                    value: datePickerValue ?? ''
                                })
                            ]
                        })
                    })
                }),
                /*#__PURE__*/ jsxs(Content$2, {
                    align: "start",
                    css: [
                        datePickerStyles,
                        granularity === 'week' && weekPickerStyles
                    ],
                    onOpenAutoFocus: (e)=>{
                        // When allowCustomInput is enabled, prevent the popover from auto-focusing
                        // This keeps focus on the input field so users can type immediately
                        if (allowCustomInput) {
                            e.preventDefault();
                        }
                    },
                    children: [
                        granularity === 'year' ? /*#__PURE__*/ jsx(YearPickerGrid, {
                            value: date,
                            onSelect: handleGranularitySelect,
                            min: minimumAllowedDate,
                            max: maximumAllowedDate,
                            rangeFrom: mode === 'range' && selected?.from instanceof Date ? selected.from : undefined,
                            rangeTo: mode === 'range' && selected?.to instanceof Date ? selected.to : undefined,
                            renderCellContent: renderCellContent,
                            isDateDisabled: dateTimeDisabledFn
                        }) : granularity === 'month' ? /*#__PURE__*/ jsx(MonthPickerGrid, {
                            value: date,
                            onSelect: handleGranularitySelect,
                            min: minimumAllowedDate,
                            max: maximumAllowedDate,
                            rangeFrom: mode === 'range' && selected?.from instanceof Date ? selected.from : undefined,
                            rangeTo: mode === 'range' && selected?.to instanceof Date ? selected.to : undefined,
                            renderCellContent: renderCellContent,
                            isDateDisabled: dateTimeDisabledFn
                        }) : // role="none" makes this wrapper transparent to the accessibility tree.
                        // onKeyDownCapture intercepts ArrowLeft/ArrowRight before react-day-picker's internal
                        // navigation handler runs, restricting week-mode keyboard navigation to up/down only.
                        /*#__PURE__*/ jsx("div", {
                            role: "none",
                            onKeyDownCapture: (e)=>{
                                if (granularity === 'week' && (e.key === 'ArrowLeft' || e.key === 'ArrowRight')) {
                                    e.preventDefault();
                                    e.stopPropagation();
                                }
                            },
                            children: /*#__PURE__*/ jsx(DayPicker, {
                                initialFocus: !allowCustomInput,
                                ...datePickerProps,
                                mode: mode,
                                selected: mode === 'range' ? selected : date,
                                onDayClick: handleDatePickerUpdate,
                                showOutsideDays: mode === 'range' ? false : true,
                                showWeekNumber: granularity === 'week',
                                ISOWeek: granularity === 'week',
                                formatters: {
                                    formatWeekdayName: (date)=>format(date, 'iiiii', {
                                            locale: datePickerProps?.locale
                                        })
                                },
                                components: {
                                    Day: DayComponent,
                                    IconLeft: chevronLeftIconComp,
                                    IconRight: chevronRightIconComp
                                },
                                defaultMonth: date,
                                classNames: classNames
                            })
                        }),
                        quickActions?.length && /*#__PURE__*/ jsx("div", {
                            style: {
                                display: 'flex',
                                gap: theme.spacing.sm,
                                marginBottom: theme.spacing.md,
                                padding: `${theme.spacing.xs}px ${theme.spacing.xs}px 0`,
                                maxWidth: 225,
                                flexWrap: 'wrap'
                            },
                            children: quickActions?.map((action, i)=>/*#__PURE__*/ jsx(Button, {
                                    size: "small",
                                    componentId: "codegen_design-system_src_design-system_datepicker_datepicker.tsx_281",
                                    onClick: ()=>action.onClick ? action.onClick(action.value) : !Array.isArray(action.value) && handleDatePickerUpdate(action.value),
                                    children: action.label
                                }, i))
                        }),
                        includeTime && /*#__PURE__*/ jsx(Input$1, {
                            componentId: "codegen_design-system_src_development_datepicker_datepicker.tsx_306",
                            type: "time",
                            step: includeSeconds ? 1 : undefined,
                            "aria-label": "Time",
                            role: "textbox",
                            min: minimumAllowedDateString,
                            max: maximumAllowedDateString,
                            ...timeInputProps,
                            value: date && isValid(date) ? format(date, includeSeconds ? 'HH:mm:ss' : 'HH:mm') : undefined,
                            onChange: handleTimeUpdate,
                            onBlur: handleTimeInputCommit,
                            onKeyDown: handleTimeInputKeyDown,
                            css: {
                                '*::-webkit-calendar-picker-indicator': {
                                    position: 'absolute',
                                    right: -8,
                                    width: theme.general.iconSize,
                                    height: theme.general.iconSize,
                                    zIndex: theme.options.zIndexBase + 1,
                                    color: 'transparent',
                                    background: 'transparent'
                                },
                                [`.${classNamePrefix}-input-suffix`]: {
                                    position: 'absolute',
                                    right: 12,
                                    top: 8
                                }
                            },
                            suffix: /*#__PURE__*/ jsx(ClockIcon, {}),
                            disabled: timeInputProps?.disabled
                        }),
                        mode === 'range' && includeTime && onOkPress && /*#__PURE__*/ jsx("div", {
                            css: {
                                paddingTop: theme.spacing.md,
                                display: 'flex',
                                justifyContent: 'flex-end'
                            },
                            children: /*#__PURE__*/ jsx(Button, {
                                "aria-label": "Open end date picker",
                                type: "primary",
                                componentId: "datepicker-dubois-ok-button",
                                onClick: ()=>{
                                    // Commit current input value immediately to parent before closing
                                    handleChange(date, inputText, false);
                                    onOkPress?.();
                                },
                                children: okButtonLabel ?? 'Ok'
                            })
                        }),
                        footer
                    ]
                })
            ]
        })
    });
});
const getRangeQuickActionsBasic = ({ today, yesterday, lastWeek })=>{
    const todayStart = startOfToday();
    const weekStart = startOfWeek(todayStart);
    return [
        {
            label: 'Today',
            value: [
                todayStart,
                endOfToday()
            ],
            ...today
        },
        {
            label: 'Yesterday',
            value: [
                startOfYesterday(),
                endOfYesterday()
            ],
            ...yesterday
        },
        {
            label: 'Last week',
            value: [
                sub(weekStart, {
                    days: 7
                }),
                sub(weekStart, {
                    days: 1
                })
            ],
            ...lastWeek
        }
    ];
};
const RangePicker$1 = (props)=>{
    const { id, onChange, startDatePickerProps, endDatePickerProps, includeTime, includeSeconds, allowClear, minWidth, maxWidth, width, disabled, quickActions, wrapperProps, noRangeValidation, allowCustomInput = false, granularity } = props;
    const [range, setRange] = useState({
        from: startDatePickerProps?.value,
        to: endDatePickerProps?.value
    });
    const { classNamePrefix } = useDesignSystemTheme();
    // Focus is lost when the popover is closed, we need to set the focus back to the input that opened the popover manually.
    const [isFromVisible, setIsFromVisible] = useState(false);
    const [isToVisible, setIsToVisible] = useState(false);
    const [isRangeInputFocused, setIsRangeInputFocused] = useState(false);
    const fromInputRef = useRef(null);
    const toInputRef = useRef(null);
    useImperativeHandle(startDatePickerProps?.ref, ()=>fromInputRef.current);
    useImperativeHandle(endDatePickerProps?.ref, ()=>toInputRef.current);
    const fromInputRefVisible = useRef(isFromVisible);
    const toInputRefVisible = useRef(isToVisible);
    useEffect(()=>{
        if (!isFromVisible && fromInputRefVisible.current) {
            fromInputRef.current?.focus();
        }
        fromInputRefVisible.current = isFromVisible;
    }, [
        isFromVisible
    ]);
    useEffect(()=>{
        if (!isToVisible && toInputRefVisible.current) {
            toInputRef.current?.focus();
        }
        toInputRefVisible.current = isToVisible;
    }, [
        isToVisible
    ]);
    const checkIfDateTimeIsDisabled = useCallback((date, isStart = false)=>{
        const dateToCompareTo = isStart ? range?.to : range?.from;
        if (date && dateToCompareTo) {
            return isStart ? isAfter(date, dateToCompareTo) : isBefore(date, dateToCompareTo);
        }
        return false;
    }, [
        range
    ]);
    useEffect(()=>{
        setRange((prevValue)=>({
                from: startDatePickerProps?.value,
                to: prevValue?.to
            }));
    }, [
        startDatePickerProps?.value
    ]);
    useEffect(()=>{
        setRange((prevValue)=>({
                from: prevValue?.from,
                to: endDatePickerProps?.value
            }));
    }, [
        endDatePickerProps?.value
    ]);
    const quickActionsWithHandler = useMemo(()=>{
        if (quickActions) {
            return quickActions.map((action)=>{
                if (Array.isArray(action.value)) {
                    return {
                        ...action,
                        onClick: (value)=>{
                            setRange({
                                from: value[0],
                                to: value[1]
                            });
                            onChange?.({
                                target: {
                                    name: props.name,
                                    value: {
                                        from: value[0],
                                        to: value[1]
                                    }
                                },
                                type: 'change',
                                updateLocation: 'preset'
                            });
                            action.onClick?.(value);
                            setIsFromVisible(false);
                            setIsToVisible(false);
                        }
                    };
                }
                return action;
            });
        }
        return quickActions;
    }, [
        quickActions,
        onChange,
        props.name
    ]);
    const handleUpdateDate = useCallback((e, isStart)=>{
        const date = e.target.value ?? (allowCustomInput ? e.target.textValue : undefined);
        const newRange = isStart ? {
            from: date,
            to: range?.to
        } : {
            from: range?.from,
            to: date
        };
        if (!includeTime) {
            if (isStart) {
                setIsFromVisible(false);
                if (e.updateLocation === 'calendar') {
                    setIsToVisible(true);
                }
            } else {
                setIsToVisible(false);
            }
        }
        if (isStart) {
            startDatePickerProps?.onChange?.(e);
        } else {
            endDatePickerProps?.onChange?.(e);
        }
        const rangeInString = isStart ? {
            from: e.target.textValue,
            to: toInputRef.current?.value
        } : {
            from: fromInputRef.current?.value,
            to: e.target.textValue
        };
        setRange(newRange);
        onChange?.({
            target: {
                name: props.name,
                value: {
                    from: newRange.from instanceof Date ? newRange.from : undefined,
                    to: newRange.to instanceof Date ? newRange.to : undefined
                },
                rangeInString
            },
            type: 'change',
            updateLocation: e.updateLocation
        });
    }, [
        allowCustomInput,
        range?.to,
        range?.from,
        includeTime,
        onChange,
        props.name,
        startDatePickerProps,
        endDatePickerProps
    ]);
    // Use useMemo to calculate disabled dates
    const disabledDates = useMemo(()=>{
        let startDisabledFromProps, endDisabledFromProps;
        if (startDatePickerProps?.datePickerProps?.disabled) {
            startDisabledFromProps = Array.isArray(startDatePickerProps?.datePickerProps?.disabled) ? startDatePickerProps?.datePickerProps?.disabled : [
                startDatePickerProps?.datePickerProps?.disabled
            ];
        }
        const startDisabled = [
            {
                after: range?.to
            },
            ...startDisabledFromProps ? startDisabledFromProps : []
        ].filter(Boolean);
        if (endDatePickerProps?.datePickerProps?.disabled) {
            endDisabledFromProps = Array.isArray(endDatePickerProps?.datePickerProps?.disabled) ? endDatePickerProps?.datePickerProps?.disabled : [
                endDatePickerProps?.datePickerProps?.disabled
            ];
        }
        const endDisabled = [
            {
                before: range?.from
            },
            ...endDisabledFromProps ? endDisabledFromProps : []
        ].filter(Boolean);
        return {
            startDisabled,
            endDisabled
        };
    }, [
        range?.from,
        range?.to,
        startDatePickerProps?.datePickerProps?.disabled,
        endDatePickerProps?.datePickerProps?.disabled
    ]);
    const [startDateMaxInputDate] = useMemo(()=>formatDateLimitForInput(noRangeValidation ? undefined : range?.to, Boolean(includeTime), Boolean(includeSeconds), DEFAULT_MAX_DATE), [
        includeTime,
        includeSeconds,
        noRangeValidation,
        range?.to
    ]);
    const [endDateMinInputDate] = useMemo(()=>formatDateLimitForInput(noRangeValidation ? undefined : range?.from, Boolean(includeTime), Boolean(includeSeconds), DEFAULT_MIN_DATE), [
        includeTime,
        includeSeconds,
        noRangeValidation,
        range?.from
    ]);
    const openEndDatePicker = ()=>{
        setIsFromVisible(false);
        setIsToVisible(true);
    };
    const closeEndDatePicker = ()=>{
        setIsToVisible(false);
    };
    const handleTimePickerKeyPress = (e)=>{
        if (e.key === 'Enter') {
            openEndDatePicker();
        }
        props.startDatePickerProps?.timeInputProps?.onKeyDown?.(e);
    };
    return /*#__PURE__*/ jsxs("div", {
        className: `${classNamePrefix}-rangepicker`,
        ...wrapperProps,
        "data-focused": isRangeInputFocused,
        css: {
            display: 'flex',
            alignItems: 'center',
            minWidth,
            maxWidth,
            width
        },
        children: [
            /*#__PURE__*/ jsx(DatePicker, {
                quickActions: quickActionsWithHandler,
                prefix: "Start:",
                open: isFromVisible,
                onOpenChange: setIsFromVisible,
                okButtonLabel: "Next",
                allowCustomInput: allowCustomInput,
                granularity: granularity,
                ...startDatePickerProps,
                id: id,
                ref: fromInputRef,
                disabled: disabled || startDatePickerProps?.disabled,
                onChange: (e)=>handleUpdateDate(e, true),
                includeTime: includeTime,
                includeSeconds: includeSeconds,
                allowClear: allowClear,
                max: startDateMaxInputDate,
                datePickerProps: {
                    ...startDatePickerProps?.datePickerProps,
                    disabled: disabledDates.startDisabled
                },
                timeInputProps: {
                    onKeyDown: handleTimePickerKeyPress
                },
                // @ts-expect-error - DatePickerProps does not have a mode property in the public API but is needed for this use case
                mode: "range",
                selected: range,
                value: range?.from,
                dateTimeDisabledFn: (date)=>checkIfDateTimeIsDisabled(date, true),
                onFocus: (e)=>{
                    setIsRangeInputFocused(true);
                    startDatePickerProps?.onFocus?.(e);
                },
                onBlur: (e)=>{
                    setIsRangeInputFocused(false);
                    startDatePickerProps?.onBlur?.(e);
                },
                css: {
                    '*::-webkit-calendar-picker-indicator': {
                        display: 'none'
                    },
                    borderTopRightRadius: 0,
                    borderBottomRightRadius: 0
                },
                wrapperProps: {
                    style: {
                        width: '50%'
                    }
                },
                onOkPress: openEndDatePicker
            }),
            /*#__PURE__*/ jsx(DatePicker, {
                quickActions: quickActionsWithHandler,
                prefix: "End:",
                min: endDateMinInputDate,
                okButtonLabel: "Close",
                allowCustomInput: allowCustomInput,
                granularity: granularity,
                ...endDatePickerProps,
                ref: toInputRef,
                disabled: disabled || endDatePickerProps?.disabled,
                onChange: (e)=>handleUpdateDate(e, false),
                includeTime: includeTime,
                includeSeconds: includeSeconds,
                open: isToVisible,
                onOpenChange: setIsToVisible,
                allowClear: allowClear,
                datePickerProps: {
                    ...endDatePickerProps?.datePickerProps,
                    disabled: disabledDates.endDisabled
                },
                // @ts-expect-error - DatePickerProps does not have a mode property in the public API but is needed for this use case
                mode: "range",
                selected: range,
                value: range?.to,
                dateTimeDisabledFn: (date)=>checkIfDateTimeIsDisabled(date, false),
                onFocus: (e)=>{
                    setIsRangeInputFocused(true);
                    startDatePickerProps?.onFocus?.(e);
                },
                onBlur: (e)=>{
                    setIsRangeInputFocused(false);
                    startDatePickerProps?.onBlur?.(e);
                },
                css: {
                    borderTopLeftRadius: 0,
                    borderBottomLeftRadius: 0,
                    left: -1
                },
                wrapperProps: {
                    style: {
                        width: '50%'
                    }
                },
                onOkPress: closeEndDatePicker
            })
        ]
    });
};

function getEmotionStyles(clsPrefix, theme) {
    const classFocused = `.${clsPrefix}-focused`;
    const classActiveBar = `.${clsPrefix}-active-bar`;
    const classSeparator = `.${clsPrefix}-separator`;
    const classSuffix = `.${clsPrefix}-suffix`;
    const styles = {
        height: 32,
        borderRadius: theme.borders.borderRadiusSm,
        borderColor: theme.colors.border,
        color: theme.colors.textPrimary,
        transition: 'border 0s, box-shadow 0s',
        [`&${classFocused},:hover`]: {
            borderColor: theme.colors.actionDefaultBorderHover
        },
        '&:active': {
            borderColor: theme.colors.actionDefaultBorderPress
        },
        [`&${classFocused}`]: {
            boxShadow: `none !important`,
            outline: `${theme.colors.actionDefaultBorderFocus} solid 2px !important`,
            outlineOffset: '-2px !important',
            borderColor: 'transparent !important'
        },
        [`& ${classActiveBar}`]: {
            background: `${theme.colors.actionDefaultBorderPress} !important`
        },
        [`& input::placeholder, & ${classSeparator}, & ${classSuffix}`]: {
            color: theme.colors.textPrimary
        }
    };
    return /*#__PURE__*/ css(styles);
}
const getDropdownStyles = (theme)=>{
    return {
        zIndex: theme.options.zIndexBase + 50
    };
};
function useDatePickerStyles() {
    const { theme, getPrefixedClassName } = useDesignSystemTheme();
    const clsPrefix = getPrefixedClassName('picker');
    return getEmotionStyles(clsPrefix, theme);
}
const AccessibilityWrapper = ({ children, ariaLive = 'assertive', ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    const ref = useRef(null);
    useEffect(()=>{
        if (ref.current) {
            const inputs = theme.isDarkMode ? ref.current.querySelectorAll('.du-bois-dark-picker-input > input') : ref.current.querySelectorAll('.du-bois-light-picker-input > input');
            inputs.forEach((input)=>input.setAttribute('aria-live', ariaLive));
        }
    }, [
        ref,
        ariaLive,
        theme.isDarkMode
    ]);
    return /*#__PURE__*/ jsx("div", {
        ...restProps,
        ref: ref,
        children: children
    });
};
const DuboisDatePicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...addDebugOutlineIfEnabled(),
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1, {
                css: styles,
                ref: ref,
                ...restProps,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
const RangePicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1.RangePicker, {
                ...addDebugOutlineIfEnabled(),
                css: styles,
                ...restProps,
                ref: ref,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
const TimePicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...addDebugOutlineIfEnabled(),
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1.TimePicker, {
                css: styles,
                ...restProps,
                ref: ref,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
const QuarterPicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...addDebugOutlineIfEnabled(),
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1.QuarterPicker, {
                css: styles,
                ...restProps,
                ref: ref,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
const WeekPicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...addDebugOutlineIfEnabled(),
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1.WeekPicker, {
                css: styles,
                ...restProps,
                ref: ref,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
const MonthPicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...addDebugOutlineIfEnabled(),
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1.MonthPicker, {
                css: styles,
                ...restProps,
                ref: ref,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
const YearPicker = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
(props, ref)=>{
    const styles = useDatePickerStyles();
    const { theme } = useDesignSystemTheme();
    const { ariaLive, wrapperDivProps, ...restProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(AccessibilityWrapper, {
            ...addDebugOutlineIfEnabled(),
            ...wrapperDivProps,
            ariaLive: ariaLive,
            children: /*#__PURE__*/ jsx(DatePicker$1.YearPicker, {
                css: styles,
                ...restProps,
                ref: ref,
                popupStyle: {
                    ...getDropdownStyles(theme),
                    ...props.popupStyle || {}
                }
            })
        })
    });
});
/**
 * `LegacyDatePicker` was added as a temporary solution pending an
 * official Du Bois replacement. Use with caution.
 * @deprecated
 */ const LegacyDatePicker = /* #__PURE__ */ Object.assign(DuboisDatePicker, {
    /**
   * See deprecation notice for `LegacyDatePicker`.
   * @deprecated
   */ RangePicker,
    /**
   * See deprecation notice for `LegacyDatePicker`.
   * @deprecated
   */ TimePicker,
    /**
   * See deprecation notice for `LegacyDatePicker`.
   * @deprecated
   */ QuarterPicker,
    /**
   * See deprecation notice for `LegacyDatePicker`.
   * @deprecated
   */ WeekPicker,
    /**
   * See deprecation notice for `LegacyDatePicker`.
   * @deprecated
   */ MonthPicker,
    /**
   * See deprecation notice for `LegacyDatePicker`.
   * @deprecated
   */ YearPicker
});

const DialogCombobox = ({ children, label, id, value = [], open, emptyText, scrollToSelectedElement = true, rememberLastScrollPosition = false, componentId, analyticsEvents, valueHasNoPii, onOpenChange, ...props })=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.dialogCombobox', false);
    // Used to avoid infinite loop when value is controlled from within the component (DialogComboboxOptionControlledList)
    // We can't remove setValue altogether because uncontrolled component users need to be able to set the value from root for trigger to update
    const [isControlled, setIsControlled] = useState(false);
    const [selectedValue, setSelectedValue] = useState(value);
    const [isOpen, setIsOpenState] = useState(Boolean(open));
    const setIsOpen = useCallback((isOpen)=>{
        setIsOpenState(isOpen);
        onOpenChange?.(isOpen);
    }, [
        setIsOpenState,
        onOpenChange
    ]);
    const [contentWidth, setContentWidth] = useState();
    const [textOverflowMode, setTextOverflowMode] = useState('multiline');
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const [disableMouseOver, setDisableMouseOver] = useState(false);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.DialogCombobox,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const setSelectedValueWrapper = useCallback((newValue)=>{
        eventContext.onValueChange(JSON.stringify(newValue));
        setSelectedValue(newValue);
    }, [
        eventContext
    ]);
    useEffect(()=>{
        if ((!Array.isArray(selectedValue) || !Array.isArray(value)) && selectedValue !== value || selectedValue && value && selectedValue.length === value.length && selectedValue.every((v, i)=>v === value[i])) {
            return;
        }
        if (!isControlled) {
            setSelectedValueWrapper(value);
        }
    }, [
        value,
        isControlled,
        selectedValue,
        setSelectedValueWrapper
    ]);
    return /*#__PURE__*/ jsx(DialogComboboxContextProvider, {
        value: {
            id,
            label,
            value: selectedValue,
            setValue: setSelectedValueWrapper,
            setIsControlled,
            contentWidth,
            setContentWidth,
            textOverflowMode,
            setTextOverflowMode,
            isInsideDialogCombobox: true,
            multiSelect: props.multiSelect,
            stayOpenOnSelection: props.stayOpenOnSelection,
            isOpen,
            setIsOpen,
            emptyText,
            scrollToSelectedElement,
            rememberLastScrollPosition,
            componentId,
            analyticsEvents,
            valueHasNoPii,
            disableMouseOver,
            setDisableMouseOver,
            onView: eventContext.onView
        },
        children: /*#__PURE__*/ jsx(Root$6, {
            open: open !== undefined ? open : isOpen,
            ...props,
            children: /*#__PURE__*/ jsx(ComponentFinderContext.Provider, {
                value: {
                    dataComponentProps: eventContext.dataComponentProps
                },
                children: children
            })
        })
    });
};
const Root$6 = (props)=>{
    const { children, stayOpenOnSelection, multiSelect, ...restProps } = props;
    const { value, setIsOpen, onView } = useDialogComboboxContext();
    const firstView = useRef(true);
    useEffect(()=>{
        if (firstView.current) {
            onView(value);
            firstView.current = false;
        }
    }, [
        onView,
        value
    ]);
    const handleOpenChange = (open)=>{
        setIsOpen(open);
    };
    useEffect(()=>{
        if (!stayOpenOnSelection && (typeof stayOpenOnSelection === 'boolean' || !multiSelect)) {
            setIsOpen(false);
        }
    // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [
        value,
        stayOpenOnSelection,
        multiSelect
    ]); // Don't trigger when setIsOpen changes.
    return /*#__PURE__*/ jsx(Popover.Root, {
        onOpenChange: handleOpenChange,
        ...restProps,
        children: children
    });
};

const getButtonStyles = (theme)=>{
    return /*#__PURE__*/ css({
        color: theme.colors.textPlaceholder,
        fontSize: theme.typography.fontSizeSm,
        marginLeft: theme.spacing.sm,
        ':hover': {
            color: theme.colors.actionTertiaryTextHover
        }
    });
};
const ClearSelectionButton = ({ ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(XCircleFillIcon, {
        "aria-hidden": "false",
        css: getButtonStyles(theme),
        role: "button",
        "aria-label": "Clear selection",
        ...restProps
    });
};

const EmptyResults = ({ emptyText, id })=>{
    const { theme } = useDesignSystemTheme();
    const { emptyText: emptyTextFromContext } = useDialogComboboxContext();
    return /*#__PURE__*/ jsx("div", {
        "aria-live": "assertive",
        id: id,
        css: {
            color: theme.colors.textSecondary,
            textAlign: 'center',
            padding: '6px 12px',
            width: '100%',
            boxSizing: 'border-box'
        },
        children: emptyTextFromContext ?? emptyText ?? 'No results found'
    });
};

const LoadingSpinner = (props)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(Spinner, {
        css: {
            display: 'flex',
            alignSelf: 'center',
            justifyContent: 'center',
            alignItems: 'center',
            height: theme.general.heightSm,
            width: theme.general.heightSm,
            '> span': {
                fontSize: 20
            }
        },
        ...props
    });
};

const SectionHeader = ({ children, ...props })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        ...props,
        css: {
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'flex-start',
            padding: `${theme.spacing.xs}px ${theme.spacing.lg / 2}px`,
            alignSelf: 'stretch',
            fontWeight: 400,
            color: theme.colors.textSecondary
        },
        children: children
    });
};

const Separator = (props)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        ...props,
        css: {
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            margin: `${theme.spacing.xs}px ${theme.spacing.lg / 2}px`,
            border: `1px solid ${theme.colors.borderDecorative}`,
            borderBottom: 0,
            alignSelf: 'stretch'
        }
    });
};

const DialogComboboxAddButton = ({ children, ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    const { isInsideDialogCombobox, componentId } = useDialogComboboxContext();
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxAddButton` must be used within `DialogCombobox`');
    }
    return /*#__PURE__*/ jsx(Button, {
        // eslint-disable-next-line @databricks/no-dynamic-property-value -- Contextual component ID validated through DialogCombobox component exempt:fc0846cb-b73b-401b-834e-75bb8b6d4c20
        componentId: `${componentId ? componentId : 'design_system.dialogcombobox'}.add_option`,
        ...restProps,
        type: "tertiary",
        className: "combobox-footer-add-button",
        css: {
            ...getComboboxOptionItemWrapperStyles(theme),
            .../*#__PURE__*/ css(importantify({
                width: '100%',
                padding: 0,
                display: 'flex',
                alignItems: 'center',
                borderRadius: 0,
                '&:focus': {
                    background: theme.colors.actionTertiaryBackgroundHover,
                    outline: 'none'
                }
            }))
        },
        icon: /*#__PURE__*/ jsx(PlusIcon, {}),
        children: children
    });
};

const defaultMaxHeight = 'var(--radix-popover-content-available-height)';
const DialogComboboxContent = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ children, loading, loadingDescription = 'DialogComboboxContent', matchTriggerWidth, textOverflowMode, maxHeight, maxWidth, minHeight, minWidth = 240, width, align = 'start', side = 'bottom', sideOffset = 4, onEscapeKeyDown, onKeyDown, forceCloseOnEscape, ...restProps }, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const { label, isInsideDialogCombobox, contentWidth, setContentWidth, textOverflowMode: contextTextOverflowMode, setTextOverflowMode, multiSelect, isOpen, rememberLastScrollPosition, setIsOpen } = useDialogComboboxContext();
    const { isInsideModal } = useModalContext();
    const { getPopupContainer } = useDesignSystemContext();
    const [lastScrollPosition, setLastScrollPosition] = useState(0);
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxContent` must be used within `DialogCombobox`');
    }
    const contentRef = useRef(null);
    useImperativeHandle(forwardedRef, ()=>contentRef.current);
    const realContentWidth = matchTriggerWidth ? 'var(--radix-popover-trigger-width)' : width;
    useEffect(()=>{
        if (rememberLastScrollPosition) {
            if (!isOpen && contentRef.current) {
                setLastScrollPosition(contentRef.current.scrollTop);
            } else {
                // Wait for the popover to render and scroll to the last scrolled position
                const interval = setInterval(()=>{
                    if (contentRef.current) {
                        // Verify if the popover's content can be scrolled to the last scrolled position
                        if (lastScrollPosition && contentRef.current.scrollHeight >= lastScrollPosition) {
                            contentRef.current.scrollTo({
                                top: lastScrollPosition,
                                behavior: 'smooth'
                            });
                        }
                        clearInterval(interval);
                    }
                }, 50);
                return ()=>clearInterval(interval);
            }
        }
        return;
    }, [
        isOpen,
        rememberLastScrollPosition,
        lastScrollPosition
    ]);
    useEffect(()=>{
        if (contentWidth !== realContentWidth) {
            setContentWidth(realContentWidth);
        }
    }, [
        realContentWidth,
        contentWidth,
        setContentWidth
    ]);
    useEffect(()=>{
        if (textOverflowMode !== contextTextOverflowMode) {
            setTextOverflowMode(textOverflowMode ? textOverflowMode : 'multiline');
        }
    }, [
        textOverflowMode,
        contextTextOverflowMode,
        setTextOverflowMode
    ]);
    return /*#__PURE__*/ jsx(Popover.Portal, {
        container: getPopupContainer && getPopupContainer(),
        children: /*#__PURE__*/ jsx(Popover.Content, {
            ...addDebugOutlineIfEnabled(),
            "aria-label": `${label} options`,
            "aria-busy": loading,
            role: "listbox",
            "aria-multiselectable": multiSelect,
            css: getComboboxContentWrapperStyles(theme, {
                maxHeight: maxHeight ? `min(${maxHeight}px, ${defaultMaxHeight})` : defaultMaxHeight,
                maxWidth,
                minHeight,
                minWidth,
                width: realContentWidth
            }),
            align: align,
            side: side,
            sideOffset: sideOffset,
            onKeyDown: (e)=>{
                // This is a workaround for Radix's DialogCombobox.Content not receiving Escape key events
                // when nested inside a modal. We need to stop propagation of the event so that the modal
                // doesn't close when the DropdownMenu should.
                if (e.key === 'Escape') {
                    if (isInsideModal || forceCloseOnEscape) {
                        e.stopPropagation();
                        setIsOpen(false);
                    }
                    onEscapeKeyDown?.(e.nativeEvent);
                }
                onKeyDown?.(e);
            },
            ...restProps,
            ref: contentRef,
            children: /*#__PURE__*/ jsx("div", {
                css: {
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'flex-start',
                    justifyContent: 'center'
                },
                children: loading ? /*#__PURE__*/ jsx(LoadingSpinner, {
                    label: "Loading",
                    alt: "Loading spinner",
                    loadingDescription: loadingDescription
                }) : children ? children : /*#__PURE__*/ jsx(EmptyResults, {})
            })
        })
    });
});

const getCountBadgeStyles = (theme)=>/*#__PURE__*/ css(importantify({
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'center',
        boxSizing: 'border-box',
        padding: `${theme.spacing.xs / 2}px ${theme.spacing.xs}px`,
        background: theme.colors.tagDefault,
        borderRadius: theme.general.borderRadiusBase,
        fontSize: theme.typography.fontSizeBase,
        height: 20
    }));
const DialogComboboxCountBadge = (props)=>{
    const { countStartAt, ...restOfProps } = props;
    const { theme } = useDesignSystemTheme();
    const { value } = useDialogComboboxContext();
    return /*#__PURE__*/ jsx("div", {
        ...restOfProps,
        css: getCountBadgeStyles(theme),
        children: Array.isArray(value) ? countStartAt ? `+${value.length - countStartAt}` : value.length : value ? 1 : 0
    });
};

const DialogComboboxFooter = ({ children, ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    const { isInsideDialogCombobox } = useDialogComboboxContext();
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxFooter` must be used within `DialogCombobox`');
    }
    return /*#__PURE__*/ jsx("div", {
        ...restProps,
        css: getFooterStyles(theme),
        children: children
    });
};

const DialogComboboxHintRow = ({ children })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        css: {
            minWidth: '100%',
            color: theme.colors.textSecondary,
            fontSize: theme.typography.fontSizeSm,
            '[data-disabled] &': {
                color: theme.colors.actionDisabledText
            }
        },
        children: children
    });
};

const DialogComboboxOptionList = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ children, loading, loadingDescription = 'DialogComboboxOptionList', withProgressiveLoading, ...restProps }, forwardedRef)=>{
    const { isInsideDialogCombobox } = useDialogComboboxContext();
    const ref = useRef(null);
    useImperativeHandle(forwardedRef, ()=>ref.current);
    const [lookAhead, setLookAhead] = useState('');
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxOptionList` must be used within `DialogCombobox`');
    }
    const lookAheadTimeout = useRef(null);
    useEffect(()=>{
        if (lookAheadTimeout.current) {
            clearTimeout(lookAheadTimeout.current);
        }
        lookAheadTimeout.current = setTimeout(()=>{
            setLookAhead('');
        }, 1500);
        return ()=>{
            if (lookAheadTimeout.current) {
                clearTimeout(lookAheadTimeout.current);
            }
        };
    }, [
        lookAhead
    ]);
    useEffect(()=>{
        if (loading && !withProgressiveLoading) {
            return;
        }
        const optionItems = ref.current?.querySelectorAll('[role="option"]');
        const hasTabIndexedOption = Array.from(optionItems ?? []).some((optionItem)=>{
            return optionItem.getAttribute('tabindex') === '0';
        });
        if (!hasTabIndexedOption) {
            const firstOptionItem = optionItems?.[0];
            if (firstOptionItem) {
                highlightFirstNonDisabledOption(firstOptionItem, 'start');
            }
        }
    }, [
        loading,
        withProgressiveLoading
    ]);
    const handleOnMouseEnter = (event)=>{
        const target = event.target;
        if (target) {
            const options = target.hasAttribute('data-combobox-option-list') ? target.querySelectorAll('[role="option"]') : target?.closest('[data-combobox-option-list="true"]')?.querySelectorAll('[role="option"]');
            if (options) {
                options.forEach((option)=>option.removeAttribute('data-highlighted'));
            }
        }
    };
    return /*#__PURE__*/ jsx("div", {
        ref: ref,
        "aria-busy": loading,
        "data-combobox-option-list": "true",
        css: {
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'flex-start',
            width: '100%'
        },
        onMouseEnter: handleOnMouseEnter,
        ...restProps,
        children: /*#__PURE__*/ jsx(DialogComboboxOptionListContextProvider, {
            value: {
                isInsideDialogComboboxOptionList: true,
                lookAhead,
                setLookAhead
            },
            children: loading ? withProgressiveLoading ? /*#__PURE__*/ jsxs(Fragment, {
                children: [
                    children,
                    /*#__PURE__*/ jsx(LoadingSpinner, {
                        "aria-label": "Loading",
                        alt: "Loading spinner",
                        loadingDescription: loadingDescription
                    })
                ]
            }) : /*#__PURE__*/ jsx(LoadingSpinner, {
                "aria-label": "Loading",
                alt: "Loading spinner",
                loadingDescription: loadingDescription
            }) : children && Children.toArray(children).some((child)=>/*#__PURE__*/ React__default.isValidElement(child)) ? children : /*#__PURE__*/ jsx(EmptyResults, {})
        })
    });
});

/**
 * A variant of Tooltip that only shows when the child's text content is truncated.
 * The tooltip content is automatically derived from the child element's textContent,
 * so consumers don't need to pass the text.
 *
 * Truncation is checked lazily only on mouse enter, not during render.
 * This ensures negligible performance impact even with many TruncationTooltips on the page.
 *
 * Detects both:
 * - Horizontal ellipsis (text-overflow: ellipsis or overflow: hidden)
 * - Vertical line-clamp (line-clamp or -webkit-line-clamp)
 *
 * See go/dubois.
 */ const TruncationTooltip = ({ children, ...tooltipProps })=>{
    const [open, setOpen] = useState(false);
    const [tooltipContent, setTooltipContent] = useState('');
    const childRef = useRef(null);
    const handleOpenChange = useCallback((shouldOpen)=>{
        if (shouldOpen && childRef.current) {
            const isHorizontallyTruncated = childRef.current.scrollWidth > childRef.current.clientWidth;
            const isVerticallyTruncated = childRef.current.scrollHeight > childRef.current.clientHeight;
            if (isHorizontallyTruncated || isVerticallyTruncated) {
                setTooltipContent(childRef.current.textContent ?? '');
                setOpen(true);
            }
        } else {
            setOpen(false);
        }
    }, []);
    const wrappedChildren = useMemo(()=>{
        if (/*#__PURE__*/ React__default.isValidElement(children)) {
            const childElement = children;
            // TypeScript doesn't expose ref directly on ReactElement, so we need to cast
            const originalRef = childElement.ref;
            return /*#__PURE__*/ React__default.cloneElement(childElement, {
                ref: (node)=>{
                    childRef.current = node;
                    if (typeof originalRef === 'function') {
                        originalRef(node);
                    } else if (originalRef) {
                        originalRef.current = node;
                    }
                },
                // Radix Tooltip sets aria-describedby on the trigger pointing to the tooltip content.
                // For truncation tooltips this causes screen readers to double-read: the full text is
                // already in the DOM (CSS truncation is visual-only), so the aria-describedby is
                // redundant. Setting undefined here overrides Radix's value via Slot prop merging.
                'aria-describedby': undefined
            });
        }
        return /*#__PURE__*/ jsx("span", {
            ref: childRef,
            "aria-describedby": undefined,
            children: children
        });
    }, [
        children
    ]);
    return /*#__PURE__*/ jsx(Tooltip, {
        ...tooltipProps,
        content: tooltipContent,
        open: open,
        onOpenChange: handleOpenChange,
        children: wrappedChildren
    });
};

const getInputGroupStyling = (clsPrefix, theme, buttonSide)=>{
    const inputClass = `.${clsPrefix}-input`;
    const buttonClass = `.${clsPrefix}-btn`;
    const buttonOnRight = buttonSide === 'right';
    return /*#__PURE__*/ css({
        display: 'inline-flex !important',
        width: 'auto',
        // This is a hack to achieve (0, 3, 0) specificity, so that
        // these styles ALWAYS override the (0,2,0) specificity
        // styles with !important present in Input.
        [`&& > ${inputClass}`]: {
            flexGrow: 1,
            // Square off the inner edge (the edge adjacent to the button).
            ...buttonOnRight ? {
                borderTopRightRadius: '0px !important',
                borderBottomRightRadius: '0px !important'
            } : {
                borderTopLeftRadius: '0px !important',
                borderBottomLeftRadius: '0px !important'
            },
            '&:disabled': {
                border: 'none',
                background: theme.colors.actionDisabledBackground,
                '&:hover': {
                    borderRight: `1px solid ${theme.colors.actionDisabledBorder} !important`
                }
            },
            '&[data-validation]': {
                marginRight: 0
            }
        },
        [`& > ${buttonClass}`]: {
            boxShadow: 'none !important',
            // Square off the inner edge (the edge adjacent to the input).
            ...buttonOnRight ? {
                borderTopLeftRadius: '0px !important',
                borderBottomLeftRadius: '0px !important'
            } : {
                borderTopRightRadius: '0px !important',
                borderBottomRightRadius: '0px !important',
                // In button-left mode the input (later in DOM) renders on top of the button's
                // right border by default. Elevate the button on hover so its right border
                // hover state is not hidden behind the input.
                '&:hover': {
                    position: 'relative',
                    zIndex: 2
                }
            }
        },
        [`& > ${buttonClass} > span`]: {
            verticalAlign: 'middle'
        },
        [`& > ${buttonClass}:disabled, & > ${buttonClass}:disabled:hover`]: {
            borderLeft: `1px solid ${theme.colors.actionDisabledBorder} !important`,
            backgroundColor: `${theme.colors.actionDisabledBackground} !important`,
            color: `${theme.colors.actionDisabledText} !important`
        }
    });
};
const Group$1 = ({ dangerouslySetAntdProps, dangerouslyAppendEmotionCSS, compact = true, buttonSide = 'right', ...props })=>{
    const { classNamePrefix, theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Input$2.Group, {
            ...addDebugOutlineIfEnabled(),
            css: [
                getInputGroupStyling(classNamePrefix, theme, buttonSide),
                dangerouslyAppendEmotionCSS
            ],
            compact: compact,
            ...props,
            ...dangerouslySetAntdProps
        })
    });
};

const Password = /*#__PURE__*/ forwardRef(function Password({ validationState, autoComplete = 'off', dangerouslySetAntdProps, dangerouslyAppendEmotionCSS, ...props }, ref) {
    const { classNamePrefix, theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Input$2.Password, {
            ...addDebugOutlineIfEnabled(),
            visibilityToggle: false,
            ref: ref,
            autoComplete: autoComplete,
            css: [
                getInputEmotionStyles(classNamePrefix, theme, {
                    validationState
                }),
                dangerouslyAppendEmotionCSS
            ],
            ...props,
            ...dangerouslySetAntdProps
        })
    });
});

const TextArea = /*#__PURE__*/ forwardRef(function TextArea({ validationState, autoComplete = 'off', dangerouslySetAntdProps, dangerouslyAppendEmotionCSS, componentId, analyticsEvents, onChange, onFocus, onKeyDown, onCompositionStart, onCompositionEnd, allowFormSubmitOnEnter = false, ...props }, ref) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.textArea', false);
    const formContext = useFormContext();
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.TextArea,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: false
    });
    const firstView = useRef(true);
    useEffect(()=>{
        if (firstView.current) {
            eventContext.onView();
            firstView.current = false;
        }
    }, [
        eventContext
    ]);
    // Prevents multiple onValueChange triggers until after a focus event resets it.
    const { callbackOnceUntilReset: sendAnalyticsEventOncePerFocus, reset: resetSendAnalyticsEventOnFocus } = useCallbackOnceUntilReset(eventContext.onValueChange);
    const handleChange = useCallback((e)=>{
        sendAnalyticsEventOncePerFocus();
        onChange?.(e);
    }, [
        onChange,
        sendAnalyticsEventOncePerFocus
    ]);
    const handleFocus = useCallback((e)=>{
        resetSendAnalyticsEventOnFocus();
        onFocus?.(e);
    }, [
        onFocus,
        resetSendAnalyticsEventOnFocus
    ]);
    // Callback used to submit the parent form
    // This is used when allowFormSubmitOnEnter is true
    const handleSubmitForm = useCallback((event)=>{
        if (!formContext.formRef?.current) return;
        event.preventDefault();
        formContext.formRef.current.requestSubmit();
    }, [
        formContext.formRef
    ]);
    // Event handlers for submitting the form on Enter
    // This hook takes meta keys into account for platform-specific behavior
    const submitHandlers = useCallbackOnEnter({
        callback: handleSubmitForm,
        allowBasicEnter: allowFormSubmitOnEnter,
        allowPlatformEnter: true
    });
    // Chains the event handlers from useCallbackOnEnter together with potential event handlers from props
    const onKeyDownChain = useChainEventHandlers(useMemo(()=>({
            handlers: [
                submitHandlers.onKeyDown,
                onKeyDown
            ],
            stopOnDefaultPrevented: true
        }), [
        onKeyDown,
        submitHandlers.onKeyDown
    ]));
    const onCompositionStartChain = useChainEventHandlers(useMemo(()=>({
            handlers: [
                submitHandlers.onCompositionStart,
                onCompositionStart
            ]
        }), [
        submitHandlers.onCompositionStart,
        onCompositionStart
    ]));
    const onCompositionEndChain = useChainEventHandlers(useMemo(()=>({
            handlers: [
                submitHandlers.onCompositionEnd,
                onCompositionEnd
            ]
        }), [
        submitHandlers.onCompositionEnd,
        onCompositionEnd
    ]));
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Input$2.TextArea, {
            ...addDebugOutlineIfEnabled(),
            ref: ref,
            autoComplete: autoComplete,
            css: [
                getInputEmotionStyles(classNamePrefix, theme, {
                    validationState
                }),
                dangerouslyAppendEmotionCSS
            ],
            onChange: handleChange,
            onFocus: handleFocus,
            onKeyDown: onKeyDownChain,
            onCompositionStart: onCompositionStartChain,
            onCompositionEnd: onCompositionEndChain,
            "data-component-type": DesignSystemEventProviderComponentTypes.TextArea,
            "data-component-id": componentId,
            ...props,
            ...dangerouslySetAntdProps
        })
    });
});

// Properly creates the namespace and dot-notation components with correct types.
const InputNamespace = /* #__PURE__ */ Object.assign(Input$1, {
    TextArea,
    Password,
    Group: Group$1
});
const Input = InputNamespace;

const extractTextContent = (node)=>{
    if (typeof node === 'string' || typeof node === 'number') {
        return node.toString();
    }
    if (/*#__PURE__*/ React__default.isValidElement(node) && node.props.children) {
        return React__default.Children.toArray(node.props.children).map(extractTextContent).join(' ');
    }
    return '';
};
const filterChildren = (children, searchValue)=>{
    const lowerCaseSearchValue = searchValue.toLowerCase();
    return React__default.Children.map(children, (child)=>{
        if (/*#__PURE__*/ React__default.isValidElement(child)) {
            const childType = child.props['__EMOTION_TYPE_PLEASE_DO_NOT_USE__']?.defaultProps._TYPE ?? child.props._TYPE;
            if (childType === 'DialogComboboxOptionListSelectItem' || childType === 'DialogComboboxOptionListCheckboxItem') {
                const childTextContent = extractTextContent(child).toLowerCase();
                const childValue = child.props.value?.toLowerCase() ?? '';
                return childTextContent.includes(lowerCaseSearchValue) || childValue.includes(lowerCaseSearchValue) ? child : null;
            }
        }
        return child;
    })?.filter((child)=>child);
};
const DialogComboboxOptionListSearch = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ onChange, onSearch, virtualized, children, hasWrapper, controlledValue, setControlledValue, rightSearchControls, ...restProps }, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const { componentId } = useDialogComboboxContext();
    const { isInsideDialogComboboxOptionList } = useDialogComboboxOptionListContext();
    const noResultId = `no-result-${generateUuidV4()}`;
    const inputRef = useRef(null);
    useImperativeHandle(forwardedRef, ()=>inputRef.current);
    const [searchValue, setSearchValue] = React__default.useState();
    if (!isInsideDialogComboboxOptionList) {
        throw new Error('`DialogComboboxOptionListSearch` must be used within `DialogComboboxOptionList`');
    }
    const handleOnChange = (event)=>{
        if (!virtualized) {
            setSearchValue(event.target.value);
        }
        setControlledValue?.(event.target.value);
        onSearch?.(event.target.value);
    };
    let filteredChildren = children;
    if (searchValue && !virtualized && controlledValue === undefined) {
        filteredChildren = filterChildren(hasWrapper ? children.props.children : children, searchValue);
        if (hasWrapper) {
            filteredChildren = /*#__PURE__*/ React__default.cloneElement(children, {}, filteredChildren);
        }
    }
    const inputWrapperRef = useRef(null);
    // When the search value changes, highlight the first option
    useEffect(()=>{
        if (!inputWrapperRef.current) {
            return;
        }
        const optionItems = getContentOptions(inputWrapperRef.current);
        if (optionItems) {
            // Reset previous highlights
            const highlightedOption = findHighlightedOption(optionItems);
            const firstOptionItem = optionItems?.[0];
            if (firstOptionItem) {
                highlightOption(firstOptionItem, highlightedOption, false);
            }
        }
    }, [
        searchValue
    ]);
    const handleOnKeyDown = (event)=>{
        if (event.key === 'ArrowDown' || event.key === 'ArrowUp' || event.key === 'Enter') {
            event.preventDefault();
        } else {
            return;
        }
        // Find closest parent of type DialogComboboxOptionList and get all options within it
        const options = getContentOptions(event.target);
        if (!options) {
            return;
        }
        const highlightedOption = findHighlightedOption(options);
        // If the user is navigating the option is highlighted without focusing in order to avoid losing focus on the input
        if (event.key === 'ArrowDown' || event.key === 'ArrowUp') {
            if (highlightedOption) {
                const nextOption = findClosestOptionSibling(highlightedOption, event.key === 'ArrowDown' ? 'next' : 'previous');
                if (nextOption) {
                    highlightOption(nextOption, highlightedOption, false);
                } else if (event.key === 'ArrowDown') {
                    // If there is no next option, highlight the first option
                    const firstOption = options[0];
                    highlightOption(firstOption, highlightedOption, false);
                } else if (event.key === 'ArrowUp') {
                    // If there is no previous option, highlight the last option
                    const lastOption = options[options.length - 1];
                    highlightOption(lastOption, highlightedOption, false);
                }
            } else {
                // In case there is no highlighted option, highlight the first / last option depending on key
                const nextOption = event.key === 'ArrowDown' ? options[0] : options[options.length - 1];
                if (nextOption) {
                    highlightOption(nextOption, undefined, false);
                }
            }
        // On Enter trigger a click event on the highlighted option
        } else if (event.key === 'Enter' && highlightedOption) {
            highlightedOption.click();
        }
    };
    const childrenIsNotEmpty = Children.toArray(hasWrapper ? children.props.children : children).some((child)=>/*#__PURE__*/ React__default.isValidElement(child));
    const hasFilteredResults = hasWrapper && filteredChildren?.props.children?.length || !hasWrapper && filteredChildren?.length;
    useEffect(()=>{
        if (!hasFilteredResults) {
            inputRef.current?.input?.setAttribute('aria-activedescendant', noResultId);
        }
    }, [
        hasFilteredResults,
        noResultId,
        inputRef.current?.input?.value
    ]);
    return /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx("div", {
                ref: inputWrapperRef,
                css: {
                    padding: `${theme.spacing.sm}px ${theme.spacing.lg / 2}px ${theme.spacing.sm}px`,
                    width: '100%',
                    boxSizing: 'border-box',
                    position: 'sticky',
                    top: 0,
                    background: theme.colors.backgroundPrimary,
                    zIndex: theme.options.zIndexBase + 1
                },
                children: /*#__PURE__*/ jsxs("div", {
                    css: {
                        display: 'flex',
                        flexDirection: 'row',
                        gap: theme.spacing.sm
                    },
                    children: [
                        /*#__PURE__*/ jsx(Input, {
                            // eslint-disable-next-line @databricks/no-dynamic-property-value -- Contextual component ID validated through DialogCombobox component exempt:a992c761-9f18-43b0-af9d-dc9ee629260a
                            componentId: componentId ? `${componentId}.search` : 'codegen_design_system_src_design_system_dialogcombobox_dialogcomboboxoptionlistsearch.tsx_173',
                            type: "search",
                            name: "search",
                            ref: inputRef,
                            prefix: /*#__PURE__*/ jsx(SearchIcon, {}),
                            placeholder: "Search",
                            onChange: handleOnChange,
                            onKeyDown: (event)=>{
                                handleOnKeyDown(event);
                                restProps.onKeyDown?.(event);
                            },
                            value: controlledValue ?? searchValue,
                            shouldPreventFormSubmission: true,
                            ...restProps
                        }),
                        rightSearchControls
                    ]
                })
            }),
            virtualized ? children : hasFilteredResults && childrenIsNotEmpty ? /*#__PURE__*/ jsx("div", {
                "aria-live": "polite",
                css: {
                    width: '100%'
                },
                children: filteredChildren
            }) : /*#__PURE__*/ jsx(EmptyResults, {
                id: noResultId
            })
        ]
    });
});

const DialogComboboxOptionControlledList = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ options, onChange, loading, loadingDescription = 'DialogComboboxOptionControlledList', withProgressiveLoading, withSearch, showAllOption, allOptionLabel = 'All', ...restProps }, forwardedRef)=>{
    const { isInsideDialogCombobox, multiSelect, value, setValue, setIsControlled } = useDialogComboboxContext();
    const [lookAhead, setLookAhead] = useState('');
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxOptionControlledList` must be used within `DialogCombobox`');
    }
    const lookAheadTimeout = useRef(null);
    const ref = useRef(null);
    useImperativeHandle(forwardedRef, ()=>ref.current);
    useEffect(()=>{
        if (lookAheadTimeout.current) {
            clearTimeout(lookAheadTimeout.current);
        }
        lookAheadTimeout.current = setTimeout(()=>{
            setLookAhead('');
        }, 1500);
        return ()=>{
            if (lookAheadTimeout.current) {
                clearTimeout(lookAheadTimeout.current);
            }
        };
    }, [
        lookAhead
    ]);
    useEffect(()=>{
        if (loading && !withProgressiveLoading) {
            return;
        }
        const optionItems = ref.current?.querySelectorAll('[role="option"]');
        const hasTabIndexedOption = Array.from(optionItems ?? []).some((optionItem)=>{
            return optionItem.getAttribute('tabindex') === '0';
        });
        if (!hasTabIndexedOption) {
            const firstOptionItem = optionItems?.[0];
            if (firstOptionItem) {
                highlightOption(firstOptionItem, undefined, false);
            }
        }
    }, [
        loading,
        withProgressiveLoading
    ]);
    const isOptionChecked = options.reduce((acc, option)=>{
        acc[option] = value?.includes(option);
        return acc;
    }, {});
    const handleUpdate = (updatedValue)=>{
        setIsControlled(true);
        let newValue = [];
        if (multiSelect) {
            if (value.find((item)=>item === updatedValue)) {
                newValue = value.filter((item)=>item !== updatedValue);
            } else {
                newValue = [
                    ...value,
                    updatedValue
                ];
            }
        } else {
            newValue = [
                updatedValue
            ];
        }
        setValue(newValue);
        isOptionChecked[updatedValue] = !isOptionChecked[updatedValue];
        if (onChange) {
            onChange(newValue);
        }
    };
    const handleSelectAll = ()=>{
        setIsControlled(true);
        if (value.length === options.length) {
            setValue([]);
            options.forEach((option)=>{
                isOptionChecked[option] = false;
            });
            if (onChange) {
                onChange([]);
            }
        } else {
            setValue(options);
            options.forEach((option)=>{
                isOptionChecked[option] = true;
            });
            if (onChange) {
                onChange(options);
            }
        }
    };
    const renderedOptions = /*#__PURE__*/ jsxs(Fragment, {
        children: [
            showAllOption && multiSelect && /*#__PURE__*/ jsx(DialogComboboxOptionListCheckboxItem, {
                value: "all",
                onChange: handleSelectAll,
                checked: value.length === options.length,
                indeterminate: Boolean(value.length) && value.length !== options.length,
                children: allOptionLabel
            }),
            options && options.length > 0 ? options.map((option, key)=>multiSelect ? /*#__PURE__*/ jsx(DialogComboboxOptionListCheckboxItem, {
                    value: option,
                    checked: isOptionChecked[option],
                    onChange: handleUpdate,
                    children: option
                }, key) : /*#__PURE__*/ jsx(DialogComboboxOptionListSelectItem, {
                    value: option,
                    checked: isOptionChecked[option],
                    onChange: handleUpdate,
                    children: option
                }, key)) : /*#__PURE__*/ jsx(EmptyResults, {})
        ]
    });
    const optionList = /*#__PURE__*/ jsx(DialogComboboxOptionList, {
        children: withSearch ? /*#__PURE__*/ jsx(DialogComboboxOptionListSearch, {
            hasWrapper: true,
            children: renderedOptions
        }) : renderedOptions
    });
    return /*#__PURE__*/ jsx("div", {
        ref: ref,
        "aria-busy": loading,
        css: {
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'flex-start',
            width: '100%'
        },
        ...restProps,
        children: /*#__PURE__*/ jsx(DialogComboboxOptionListContextProvider, {
            value: {
                isInsideDialogComboboxOptionList: true,
                lookAhead,
                setLookAhead
            },
            children: /*#__PURE__*/ jsx(Fragment, {
                children: loading ? withProgressiveLoading ? /*#__PURE__*/ jsxs(Fragment, {
                    children: [
                        optionList,
                        /*#__PURE__*/ jsx(LoadingSpinner, {
                            "aria-label": "Loading",
                            alt: "Loading spinner",
                            loadingDescription: loadingDescription
                        })
                    ]
                }) : /*#__PURE__*/ jsx(LoadingSpinner, {
                    "aria-label": "Loading",
                    alt: "Loading spinner",
                    loadingDescription: loadingDescription
                }) : optionList
            })
        })
    });
});

const DialogComboboxSectionHeader = ({ children, ...props })=>{
    const { isInsideDialogCombobox } = useDialogComboboxContext();
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxSectionHeader` must be used within `DialogCombobox`');
    }
    return /*#__PURE__*/ jsx(SectionHeader, {
        ...props,
        children: children
    });
};

const DialogComboboxSeparator = (props)=>{
    const { isInsideDialogCombobox } = useDialogComboboxContext();
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxSeparator` must be used within `DialogCombobox`');
    }
    return /*#__PURE__*/ jsx(Separator, {
        ...props
    });
};

const getTriggerWrapperStyles = (theme, clsPrefix, removable, disabled, width)=>/*#__PURE__*/ css(importantify({
        display: 'inline-flex',
        alignItems: 'center',
        [`& + .${clsPrefix}-form-message`]: {
            marginTop: theme.spacing.sm
        },
        ...width && {
            width: width
        },
        ...removable && {
            '& > button:last-of-type': importantify({
                borderBottomLeftRadius: 0,
                borderTopLeftRadius: 0,
                marginLeft: -1
            })
        },
        ...disabled && {
            cursor: 'not-allowed'
        }
    }));
const getTriggerStyles = (theme, disabled = false, maxWidth, minWidth, removable, width, validationState, isBare, isSelect, triggerSize)=>{
    const removeButtonInteractionStyles = {
        ...removable && {
            zIndex: theme.options.zIndexBase + 2,
            '&& + button': {
                marginLeft: -1,
                zIndex: theme.options.zIndexBase + 1
            }
        }
    };
    const validationColor = getValidationStateColor(theme, validationState);
    return /*#__PURE__*/ css(importantify({
        position: 'relative',
        display: 'inline-flex',
        alignItems: 'center',
        maxWidth,
        minWidth,
        justifyContent: 'flex-start',
        background: 'transparent',
        padding: isBare ? 0 : triggerSize === 'small' ? '0 8px' : '6px 8px 6px 12px',
        boxSizing: 'border-box',
        height: isBare ? theme.typography.lineHeightBase : triggerSize === 'small' ? SMALL_BUTTON_HEIGHT$2 : theme.general.heightSm,
        border: isBare ? 'none' : `1px solid ${theme.colors.actionDefaultBorderDefault}`,
        ...!isBare && {
            boxShadow: theme.shadows.xs
        },
        borderRadius: theme.borders.borderRadiusSm,
        color: theme.colors.textPrimary,
        lineHeight: theme.typography.lineHeightBase,
        fontSize: theme.typography.fontSizeBase,
        cursor: 'pointer',
        ...width && {
            width: width,
            // Only set flex: 1 to items with width, otherwise in flex containers the trigger will take up all the space and break current usages that depend on content for width
            flex: 1
        },
        ...removable && {
            borderBottomRightRadius: 0,
            borderTopRightRadius: 0,
            borderRightColor: 'transparent'
        },
        '&:hover': {
            background: isBare ? 'transparent' : theme.colors.actionDefaultBackgroundHover,
            borderColor: theme.colors.actionDefaultBorderHover,
            ...removeButtonInteractionStyles
        },
        '&:focus': {
            borderColor: theme.colors.actionDefaultBorderFocus,
            outline: 'none',
            ...removeButtonInteractionStyles
        },
        '&:focus, &[data-state="open"]': {
            outlineColor: theme.colors.actionDefaultBorderFocus,
            outlineWidth: 2,
            outlineOffset: -2,
            outlineStyle: 'solid'
        },
        ...validationState && {
            borderColor: validationColor,
            '&:hover': {
                borderColor: validationColor
            },
            '&:focus': {
                outlineColor: validationColor,
                outlineOffset: -2
            }
        },
        ...isSelect && !disabled && {
            '&&, &&:hover, &&:focus': {
                background: 'transparent'
            },
            '&&:hover': {
                borderColor: theme.colors.actionDefaultBorderHover
            },
            '&&:focus, &[data-state="open"]': {
                borderColor: 'transparent'
            }
        },
        [`&[disabled]`]: {
            background: theme.colors.actionDisabledBackground,
            color: theme.colors.actionDisabledText,
            pointerEvents: 'none',
            userSelect: 'none',
            borderColor: theme.colors.actionDisabledBorder
        }
    }));
};
const DialogComboboxTrigger = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ removable = false, onRemove, children, minWidth = 0, maxWidth = 9999, showTagAfterValueCount = 3, allowClear = true, controlled, onClear, wrapperProps, width, withChevronIcon = true, validationState, withInlineLabel = true, placeholder, id: legacyId, isBare = false, triggerSize = 'middle', renderDisplayedValue: formatDisplayedValue = (value)=>value, ...restProps }, forwardedRef)=>{
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const { label, id: topLevelId, value, isInsideDialogCombobox, multiSelect, setValue } = useDialogComboboxContext();
    const { isSelect, placeholder: selectPlaceholder } = useSelectContext();
    const id = topLevelId ?? legacyId;
    if (!isInsideDialogCombobox) {
        throw new Error('`DialogComboboxTrigger` must be used within `DialogCombobox`');
    }
    const handleRemove = ()=>{
        if (!onRemove) {
            // eslint-disable-next-line no-console -- TODO(FEINF-3587)
            console.warn('DialogCombobox.Trigger: Attempted remove without providing onRemove handler');
        } else {
            onRemove();
        }
    };
    const handleClear = (e)=>{
        e.stopPropagation();
        if (controlled) {
            setValue([]);
            onClear?.();
        } else if (!onClear) {
            // eslint-disable-next-line no-console -- TODO(FEINF-3587)
            console.warn('DialogCombobox.Trigger: Attempted clear without providing onClear handler');
        } else {
            onClear();
        }
    };
    const [showTooltip, setShowTooltip] = React__default.useState();
    const triggerContentRef = React__default.useRef(null);
    useEffect(()=>{
        if (value?.length > showTagAfterValueCount) {
            setShowTooltip(true);
        } else if (triggerContentRef.current) {
            const { clientWidth, scrollWidth } = triggerContentRef.current;
            setShowTooltip(clientWidth < scrollWidth);
        }
    }, [
        showTagAfterValueCount,
        value
    ]);
    const renderFormattedValue = (v, index)=>{
        const formattedValue = formatDisplayedValue(v);
        return /*#__PURE__*/ jsxs(React__default.Fragment, {
            children: [
                index > 0 && ', ',
                typeof formattedValue === 'string' ? formattedValue : /*#__PURE__*/ jsx("span", {
                    children: formattedValue
                })
            ]
        }, index);
    };
    const getStringValue = (v)=>{
        const formattedValue = formatDisplayedValue(v);
        return typeof formattedValue === 'string' ? formattedValue : v;
    };
    const numValues = Array.isArray(value) ? value.length : 1;
    const concatenatedValues = Array.isArray(value) ? /*#__PURE__*/ jsxs(Fragment, {
        children: [
            value.slice(0, numValues > 10 ? 10 : undefined).map(renderFormattedValue),
            numValues > 10 && ` + ${numValues - 10}`
        ]
    }) : renderFormattedValue(value, 0);
    const displayedValues = /*#__PURE__*/ jsx("span", {
        children: concatenatedValues
    });
    const valuesBeforeBadge = Array.isArray(value) ? /*#__PURE__*/ jsx(Fragment, {
        children: value.slice(0, showTagAfterValueCount).map(renderFormattedValue)
    }) : renderFormattedValue(value, 0);
    let ariaLabel = '';
    if (!isSelect && !id && label) {
        ariaLabel = /*#__PURE__*/ React__default.isValidElement(label) ? 'Dialog Combobox' : `${label}`;
        if (value?.length) {
            const stringValues = Array.isArray(value) ? value.map(getStringValue).join(', ') : getStringValue(value);
            ariaLabel += multiSelect ? `, multiselectable, ${value.length} options selected: ${stringValues}` : `, selected option: ${stringValues}`;
        } else {
            ariaLabel += multiSelect ? ', multiselectable, 0 options selected' : ', no option selected';
        }
    } else if (isSelect) {
        ariaLabel = ((typeof label === 'string' ? label : '') || restProps['aria-label']) ?? '';
    }
    const customSelectContent = isSelect && children ? children : null;
    const dialogComboboxClassname = !isSelect ? `${classNamePrefix}-dialogcombobox` : '';
    const selectV2Classname = isSelect ? `${classNamePrefix}-selectv2` : '';
    const triggerContent = isSelect ? /*#__PURE__*/ jsxs(Popover.Trigger, {
        ...ariaLabel && {
            'aria-label': ariaLabel
        },
        ref: forwardedRef,
        role: "combobox",
        "aria-haspopup": "listbox",
        "aria-invalid": validationState === 'error',
        id: id,
        ...restProps,
        css: getTriggerStyles(theme, restProps.disabled, maxWidth, minWidth, removable, width, validationState, isBare, isSelect, triggerSize),
        children: [
            /*#__PURE__*/ jsx("span", {
                css: {
                    whiteSpace: 'nowrap',
                    overflow: 'hidden',
                    textOverflow: 'ellipsis',
                    height: theme.typography.lineHeightBase,
                    marginRight: 'auto'
                },
                ref: triggerContentRef,
                children: value?.length ? customSelectContent ?? displayedValues : /*#__PURE__*/ jsx("span", {
                    css: {
                        color: theme.colors.textPlaceholder
                    },
                    children: selectPlaceholder
                })
            }),
            allowClear && value?.length ? /*#__PURE__*/ jsx(ClearSelectionButton, {
                onClick: handleClear
            }) : null,
            /*#__PURE__*/ jsx(ChevronDownIcon, {
                css: {
                    marginLeft: theme.spacing.xs,
                    color: restProps.disabled ? theme.colors.actionDisabledText : theme.colors.textSecondary
                }
            })
        ]
    }) : /*#__PURE__*/ jsxs(Popover.Trigger, {
        id: id,
        ...ariaLabel && {
            'aria-label': ariaLabel
        },
        ref: forwardedRef,
        role: "combobox",
        "aria-haspopup": "listbox",
        "aria-invalid": validationState === 'error',
        ...restProps,
        css: getTriggerStyles(theme, restProps.disabled, maxWidth, minWidth, removable, width, validationState, isBare, isSelect, triggerSize),
        children: [
            /*#__PURE__*/ jsxs("span", {
                css: {
                    display: 'flex',
                    alignItems: 'center',
                    height: theme.typography.lineHeightBase,
                    marginRight: 'auto',
                    '&, & > *': {
                        whiteSpace: 'nowrap',
                        overflow: 'hidden',
                        textOverflow: 'ellipsis'
                    }
                },
                ref: triggerContentRef,
                children: [
                    withInlineLabel ? /*#__PURE__*/ jsxs("span", {
                        css: {
                            height: theme.typography.lineHeightBase,
                            marginRight: theme.spacing.xs,
                            whiteSpace: 'unset',
                            overflow: 'unset',
                            textOverflow: 'unset'
                        },
                        children: [
                            label,
                            value?.length ? ':' : null
                        ]
                    }) : !value?.length && /*#__PURE__*/ jsx("span", {
                        css: {
                            color: theme.colors.textPlaceholder
                        },
                        children: placeholder
                    }),
                    value?.length > showTagAfterValueCount ? /*#__PURE__*/ jsxs(Fragment, {
                        children: [
                            /*#__PURE__*/ jsx("span", {
                                style: {
                                    marginRight: theme.spacing.xs
                                },
                                children: valuesBeforeBadge
                            }),
                            /*#__PURE__*/ jsx(DialogComboboxCountBadge, {
                                countStartAt: showTagAfterValueCount,
                                role: "status",
                                "aria-label": "Selected options count"
                            })
                        ]
                    }) : displayedValues
                ]
            }),
            allowClear && value?.length ? /*#__PURE__*/ jsx(ClearSelectionButton, {
                onClick: handleClear
            }) : null,
            withChevronIcon ? /*#__PURE__*/ jsx(ChevronDownIcon, {
                css: {
                    color: restProps.disabled ? theme.colors.actionDisabledText : theme.colors.textSecondary,
                    justifySelf: 'flex-end',
                    marginLeft: theme.spacing.xs
                }
            }) : null
        ]
    });
    const dataComponentProps = useComponentFinderContext(DesignSystemEventProviderComponentTypes.DialogCombobox);
    return /*#__PURE__*/ jsxs("div", {
        ...wrapperProps,
        className: `${restProps?.className ?? ''} ${dialogComboboxClassname} ${selectV2Classname}`.trim(),
        css: [
            getTriggerWrapperStyles(theme, classNamePrefix, removable, restProps.disabled, width),
            wrapperProps?.css
        ],
        ...addDebugOutlineIfEnabled(),
        ...dataComponentProps,
        children: [
            showTooltip && value?.length ? /*#__PURE__*/ jsx(Tooltip, {
                content: customSelectContent ?? displayedValues,
                componentId: "dialogcombobox-trigger-tooltip",
                children: triggerContent
            }) : triggerContent,
            removable && /*#__PURE__*/ jsx(Button, {
                componentId: "codegen_design-system_src_design-system_dialogcombobox_dialogcomboboxtrigger.tsx_355",
                "aria-label": `Remove ${label}`,
                onClick: handleRemove,
                dangerouslySetForceIconStyles: true,
                children: /*#__PURE__*/ jsx(CloseIcon, {
                    "aria-label": `Remove ${label}`,
                    "aria-hidden": "false"
                })
            })
        ]
    });
});
/**
 * A custom button trigger that can be wrapped around any button.
 */ const DialogComboboxCustomButtonTriggerWrapper = ({ children })=>{
    return /*#__PURE__*/ jsx(Popover.Trigger, {
        asChild: true,
        children: children
    });
};

const DEFAULT_WIDTH = 320;
const MIN_WIDTH = 320;
const MAX_WIDTH = '90vw';
const DEFAULT_POSITION = 'right';
const ZINDEX_OVERLAY = 1;
const ZINDEX_CONTENT = ZINDEX_OVERLAY + 1;
/** Context to track if drawer is nested within a parent drawer */ const DrawerContext = /*#__PURE__*/ React__default.createContext({
    isParentDrawerOpen: false
});
const Content = ({ children, footer, title, width, position: positionOverride, useCustomScrollBehavior, expandContentToFullHeight, disableOpenAutoFocus, onInteractOutside, seeThrough, hideClose, closeOnClickOutside, onCloseClick, componentId = 'design_system.drawer.content', analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnView
], size = 'default', ...props })=>{
    const { getPopupContainer } = useDesignSystemContext();
    const { theme } = useDesignSystemTheme();
    const horizontalContentPadding = size === 'small' ? theme.spacing.md : theme.spacing.lg;
    const [shouldContentBeFocusable, setShouldContentBeFocusable] = useState(false);
    const contentContainerRef = useRef(null);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Drawer,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents
    });
    const { isParentDrawerOpen } = React__default.useContext(DrawerContext);
    // Fix for github.com/radix-ui/primitives/issues/3185
    // Problem: When a Tooltip trigger is inside a Dialog/Drawer, pressing Tab breaks keyboard navigation.
    // The Dialog's focus trap intercepts the Tab key and moves focus to an incorrect element.
    // Solution: Handle Tab navigation manually within the drawer to bypass the focus trap interference.
    // Note: Tab events are intercepted early (capture phase) and don't propagate to child components.
    // Components that need to respond to focus changes should use onFocus/onBlur instead of onKeyDown.
    const handleKeyDownCapture = useCallback((event)=>{
        if (event.key !== 'Tab') return;
        // Always stop Tab events from propagating to child components' onKeyDown handlers
        // This prevents children from receiving Tab events, forcing them to use onFocus/onBlur instead
        event.stopPropagation();
        const dialog = event.currentTarget;
        const activeElement = document.activeElement;
        // Find all tabbable elements in the dialog using the tabbable library
        // This provides comprehensive coverage for all focusable elements including edge cases
        const focusable = tabbable(dialog).filter((el)=>{
            // Exclude tooltip portal content (floating UI elements)
            if (el.closest('[role="tooltip"]')) return false;
            // Exclude the scrollable content container div (has tabindex but isn't interactive)
            if (el.tagName === 'DIV' && el.getAttribute('tabindex') === '0' && el.contains(activeElement)) return false;
            return true;
        });
        const currentIndex = focusable.indexOf(activeElement);
        if (currentIndex === -1) return; // Let browser handle if active element not in our list
        // We handle focus manually, so prevent default Tab behavior
        event.preventDefault();
        // Calculate next element with wrap-around for focus trap behavior
        let nextIndex = currentIndex + (event.shiftKey ? -1 : 1);
        if (nextIndex < 0) nextIndex = focusable.length - 1;
        else if (nextIndex >= focusable.length) nextIndex = 0;
        const nextElement = focusable[nextIndex];
        if (nextElement) {
            nextElement.focus();
        }
    }, []);
    const { elementRef: onViewRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    const contentRef = useCallback((node)=>{
        if (!node || !node.clientHeight) return;
        setShouldContentBeFocusable(node.scrollHeight > node.clientHeight);
    }, []);
    const mergedContentRef = useMergeRefs([
        contentRef,
        onViewRef
    ]);
    const position = positionOverride ?? DEFAULT_POSITION;
    const overlayShow = position === 'right' ? /*#__PURE__*/ keyframes({
        '0%': {
            transform: 'translate(100%, 0)'
        },
        '100%': {
            transform: 'translate(0, 0)'
        }
    }) : /*#__PURE__*/ keyframes({
        '0%': {
            transform: 'translate(-100%, 0)'
        },
        '100%': {
            transform: 'translate(0, 0)'
        }
    });
    const dialogPrimitiveContentStyle = /*#__PURE__*/ css({
        color: theme.colors.textPrimary,
        backgroundColor: theme.colors.backgroundPrimary,
        boxShadow: theme.shadows.xl,
        position: 'fixed',
        top: 0,
        left: position === 'left' ? 0 : undefined,
        right: position === 'right' ? 0 : undefined,
        boxSizing: 'border-box',
        width: width ?? DEFAULT_WIDTH,
        minWidth: MIN_WIDTH,
        maxWidth: MAX_WIDTH,
        zIndex: theme.options.zIndexBase + ZINDEX_CONTENT,
        height: '100vh',
        paddingTop: size === 'small' ? theme.spacing.sm : theme.spacing.md,
        paddingLeft: 0,
        paddingBottom: 0,
        paddingRight: 0,
        overflow: 'hidden',
        '&:focus': {
            outline: 'none'
        },
        ...isParentDrawerOpen ? {} : {
            '@media (prefers-reduced-motion: no-preference)': {
                animation: `${overlayShow} 350ms cubic-bezier(0.16, 1, 0.3, 1)`
            }
        }
    });
    return /*#__PURE__*/ jsxs(DialogPrimitive.Portal, {
        container: getPopupContainer && getPopupContainer(),
        children: [
            /*#__PURE__*/ jsx(DialogPrimitive.Overlay, {
                "data-testid": "drawer-overlay",
                css: {
                    backgroundColor: theme.colors.overlayOverlay,
                    position: 'fixed',
                    inset: 0,
                    // needed so that it covers the PersonaNavSidebar
                    zIndex: theme.options.zIndexBase + ZINDEX_OVERLAY,
                    opacity: seeThrough || isParentDrawerOpen ? 0 : 1
                },
                onClick: closeOnClickOutside ? onCloseClick : undefined
            }),
            /*#__PURE__*/ jsx(DialogPrimitive.DialogContent, {
                ...addDebugOutlineIfEnabled(),
                css: dialogPrimitiveContentStyle,
                style: {
                    display: 'flex',
                    flexDirection: 'column',
                    justifyContent: 'flex-start',
                    opacity: seeThrough ? 0 : 1,
                    ...theme.isDarkMode && {
                        borderLeft: `1px solid ${theme.colors.borderDecorative}`
                    }
                },
                onKeyDownCapture: handleKeyDownCapture,
                onWheel: (e)=>{
                    e.stopPropagation();
                },
                onTouchMove: (e)=>{
                    e.stopPropagation();
                },
                "aria-hidden": seeThrough,
                ref: contentContainerRef,
                onOpenAutoFocus: (event)=>{
                    if (disableOpenAutoFocus) {
                        event.preventDefault();
                    }
                },
                onInteractOutside: onInteractOutside,
                ...props,
                ...eventContext.dataComponentProps,
                children: /*#__PURE__*/ jsxs(ApplyDesignSystemContextOverrides, {
                    getPopupContainer: ()=>contentContainerRef.current ?? document.body,
                    children: [
                        (title || !hideClose) && /*#__PURE__*/ jsxs("div", {
                            css: {
                                flexGrow: 0,
                                flexShrink: 1,
                                display: 'flex',
                                flexDirection: 'row',
                                justifyContent: 'space-between',
                                alignItems: 'center',
                                paddingRight: horizontalContentPadding,
                                paddingLeft: horizontalContentPadding,
                                marginBottom: theme.spacing.sm
                            },
                            children: [
                                /*#__PURE__*/ jsx(DialogPrimitive.Title, {
                                    title: typeof title === 'string' ? title : undefined,
                                    asChild: typeof title === 'string',
                                    css: {
                                        flexGrow: 1,
                                        marginBottom: 0,
                                        marginTop: 0,
                                        whiteSpace: 'nowrap',
                                        overflow: 'hidden'
                                    },
                                    children: typeof title === 'string' ? /*#__PURE__*/ jsx(Typography.Title, {
                                        elementLevel: 2,
                                        level: size === 'small' ? 3 : 2,
                                        withoutMargins: true,
                                        ellipsis: true,
                                        children: title
                                    }) : title
                                }),
                                !hideClose && /*#__PURE__*/ jsx(DialogPrimitive.Close, {
                                    asChild: true,
                                    css: {
                                        flexShrink: 1,
                                        marginLeft: theme.spacing.xs
                                    },
                                    onClick: onCloseClick,
                                    children: /*#__PURE__*/ jsx(Button, {
                                        componentId: `${componentId}.close`,
                                        "aria-label": "Close",
                                        icon: /*#__PURE__*/ jsx(CloseIcon, {}),
                                        size: size === 'small' ? 'small' : undefined
                                    })
                                })
                            ]
                        }),
                        /*#__PURE__*/ jsxs("div", {
                            ref: mergedContentRef,
                            // Needed to make drawer content focusable when scrollable for keyboard-only users to be able to focus & scroll
                            // eslint-disable-next-line jsx-a11y/no-noninteractive-tabindex
                            tabIndex: shouldContentBeFocusable ? 0 : -1,
                            css: {
                                // in order to have specific content in the drawer scroll with fixed title
                                // hide overflow here and remove padding on the right side; content will be responsible for setting right padding
                                // so that the scrollbar will appear in the padding right gutter
                                paddingRight: useCustomScrollBehavior ? 0 : horizontalContentPadding,
                                paddingLeft: horizontalContentPadding,
                                overflowY: useCustomScrollBehavior ? 'hidden' : 'auto',
                                height: expandContentToFullHeight ? '100%' : undefined,
                                ...!useCustomScrollBehavior ? getShadowScrollStyles(theme) : {}
                            },
                            children: [
                                /*#__PURE__*/ jsx(DrawerContext.Provider, {
                                    value: {
                                        isParentDrawerOpen: true
                                    },
                                    children: children
                                }),
                                !footer && /*#__PURE__*/ jsx(Spacer, {
                                    size: size === 'small' ? 'md' : 'lg'
                                })
                            ]
                        }),
                        footer && /*#__PURE__*/ jsx("div", {
                            style: {
                                paddingTop: theme.spacing.md,
                                paddingRight: horizontalContentPadding,
                                paddingLeft: horizontalContentPadding,
                                paddingBottom: size === 'small' ? theme.spacing.md : theme.spacing.lg,
                                flexGrow: 0,
                                flexShrink: 1
                            },
                            children: footer
                        })
                    ]
                })
            })
        ]
    });
};
function Root$5(props) {
    return /*#__PURE__*/ jsx(DialogPrimitive.Root, {
        ...props
    });
}
function Trigger(props) {
    return /*#__PURE__*/ jsx(DialogPrimitive.Trigger, {
        asChild: true,
        ...props
    });
}

var Drawer = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Content: Content,
  Root: Root$5,
  Trigger: Trigger
});

/**
 * @deprecated Use `DropdownMenu` instead.
 */ const Dropdown = ({ dangerouslySetAntdProps, ...props })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Dropdown$1, {
            ...addDebugOutlineIfEnabled(),
            mouseLeaveDelay: 0.25,
            ...props,
            overlayStyle: {
                zIndex: theme.options.zIndexBase + 50,
                ...props.overlayStyle
            },
            ...dangerouslySetAntdProps
        })
    });
};

const getMessageStyles = (clsPrefix, theme)=>{
    const errorClass = `.${clsPrefix}-form-error-message`;
    const infoClass = `.${clsPrefix}-form-info-message`;
    const successClass = `.${clsPrefix}-form-success-message`;
    const warningClass = `.${clsPrefix}-form-warning-message`;
    const styles = {
        '&&': {
            lineHeight: theme.typography.lineHeightSm,
            fontSize: theme.typography.fontSizeSm,
            marginTop: theme.spacing.sm,
            display: 'flex',
            alignItems: 'start'
        },
        [`&${errorClass}`]: {
            color: theme.colors.actionDangerPrimaryBackgroundDefault
        },
        [`&${infoClass}`]: {
            color: theme.colors.textPrimary
        },
        [`&${successClass}`]: {
            color: theme.colors.textValidationSuccess
        },
        [`&${warningClass}`]: {
            color: theme.colors.textValidationWarning
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    return /*#__PURE__*/ css(styles);
};
const VALIDATION_STATE_ICONS = {
    error: DangerIcon,
    success: CheckCircleIcon,
    warning: WarningIcon,
    info: InfoSmallIcon
};
function FormMessage({ id, message, type = 'error', className = '', css }) {
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const stateClass = `${classNamePrefix}-form-${type}-message`;
    const StateIcon = VALIDATION_STATE_ICONS[type];
    const wrapperClass = `${classNamePrefix}-form-message ${className} ${stateClass}`.trim();
    return /*#__PURE__*/ jsxs("div", {
        ...id && {
            id
        },
        className: wrapperClass,
        ...addDebugOutlineIfEnabled(),
        css: [
            getMessageStyles(classNamePrefix, theme),
            css
        ],
        role: "alert",
        children: [
            /*#__PURE__*/ jsx(StateIcon, {}),
            /*#__PURE__*/ jsx("div", {
                style: {
                    paddingLeft: theme.spacing.xs
                },
                children: message
            })
        ]
    });
}

const getHintStyles = (classNamePrefix, theme)=>{
    const styles = {
        display: 'block',
        color: theme.colors.textSecondary,
        lineHeight: theme.typography.lineHeightSm,
        fontSize: theme.typography.fontSizeSm,
        [`&& + .${classNamePrefix}-input, && + .${classNamePrefix}-input-affix-wrapper, && + .${classNamePrefix}-select, && + .${classNamePrefix}-selectv2, && + .${classNamePrefix}-dialogcombobox, && + .${classNamePrefix}-checkbox-group, && + .${classNamePrefix}-radio-group, && + .${classNamePrefix}-typeahead-combobox, && + .${classNamePrefix}-datepicker, && + .${classNamePrefix}-rangepicker`]: {
            marginTop: theme.spacing.sm
        }
    };
    return /*#__PURE__*/ css(styles);
};
const Hint = (props)=>{
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const { className, ...restProps } = props;
    return /*#__PURE__*/ jsx("span", {
        ...addDebugOutlineIfEnabled(),
        className: classnames(`${classNamePrefix}-hint`, className),
        css: getHintStyles(classNamePrefix, theme),
        ...restProps
    });
};

const getLabelStyles = (theme, { inline })=>{
    const styles = {
        '&&': {
            color: theme.colors.textPrimary,
            fontWeight: theme.typography.typographyBoldFontWeight,
            display: inline ? 'inline' : 'block',
            lineHeight: theme.typography.lineHeightBase
        }
    };
    return /*#__PURE__*/ css(styles);
};
const getLabelWrapperStyles = (classNamePrefix, theme)=>{
    const styles = {
        display: 'flex',
        gap: theme.spacing.xs,
        alignItems: 'center',
        [`&& + .${classNamePrefix}-input, && + .${classNamePrefix}-input-affix-wrapper, && + .${classNamePrefix}-select, && + .${classNamePrefix}-selectv2, && + .${classNamePrefix}-dialogcombobox, && + .${classNamePrefix}-checkbox-group, && + .${classNamePrefix}-radio-group, && + .${classNamePrefix}-typeahead-combobox, && + .${classNamePrefix}-datepicker, && + .${classNamePrefix}-rangepicker`]: {
            marginTop: theme.spacing.sm
        }
    };
    return /*#__PURE__*/ css(styles);
};
const Label = (props)=>{
    const { children, className, inline, required, infoPopoverContents, infoPopoverProps = {}, labelTopRightElement, ...restProps } = props; // Destructure the new prop
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const label = /*#__PURE__*/ jsx("label", {
        ...addDebugOutlineIfEnabled(),
        css: [
            getLabelStyles(theme, {
                inline
            }),
            ...!infoPopoverContents ? [
                getLabelWrapperStyles(classNamePrefix, theme)
            ] : []
        ],
        className: classnames(`${classNamePrefix}-label`, className),
        ...restProps,
        children: /*#__PURE__*/ jsxs("span", {
            css: {
                display: 'flex',
                alignItems: 'center'
            },
            children: [
                children,
                required && /*#__PURE__*/ jsx("span", {
                    "aria-hidden": "true",
                    children: "*"
                }),
                labelTopRightElement && /*#__PURE__*/ jsx("span", {
                    css: {
                        marginLeft: 'auto'
                    },
                    children: labelTopRightElement
                })
            ]
        })
    });
    return infoPopoverContents ? /*#__PURE__*/ jsxs("div", {
        css: getLabelWrapperStyles(classNamePrefix, theme),
        children: [
            label,
            /*#__PURE__*/ jsx(InfoPopover, {
                ...infoPopoverProps,
                children: infoPopoverContents
            })
        ]
    }) : label;
};

const RadioGroupContext$1 = /*#__PURE__*/ React__default.createContext(undefined);
const useRadioGroupContext = ()=>{
    const context = React__default.useContext(RadioGroupContext$1);
    if (!context) {
        throw new Error('Radio components are only allowed within a Radio.Group');
    }
    return context;
};
const getRadioInputStyles = ({ clsPrefix, theme })=>({
        [`.${clsPrefix}`]: {
            alignSelf: 'start',
            // Unchecked Styles
            [`> .${clsPrefix}-input + .${clsPrefix}-inner`]: {
                width: theme.spacing.md,
                height: theme.spacing.md,
                background: theme.colors.actionDefaultBackgroundDefault,
                borderStyle: 'solid',
                borderColor: theme.colors.actionDefaultBorderDefault,
                boxShadow: 'unset',
                transform: 'unset',
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
                borderRadius: theme.borders.borderRadiusFull,
                '&:after': {
                    all: 'unset'
                }
            },
            // Hover
            [`&:not(.${clsPrefix}-disabled) > .${clsPrefix}-input:hover + .${clsPrefix}-inner`]: {
                borderColor: theme.colors.actionPrimaryBackgroundHover,
                background: theme.colors.actionDefaultBackgroundHover
            },
            // Focus
            [`&:not(.${clsPrefix}-disabled)> .${clsPrefix}-input:focus + .${clsPrefix}-inner`]: {
                borderColor: theme.colors.actionPrimaryBackgroundDefault
            },
            // Active
            [`&:not(.${clsPrefix}-disabled)> .${clsPrefix}-input:active + .${clsPrefix}-inner`]: {
                borderColor: theme.colors.actionPrimaryBackgroundPress,
                background: theme.colors.actionDefaultBackgroundPress
            },
            // Disabled
            [`&.${clsPrefix}-disabled > .${clsPrefix}-input + .${clsPrefix}-inner`]: {
                borderColor: `${theme.colors.actionDisabledBorder} !important`,
                background: theme.colors.actionDisabledBackground,
                '@media (forced-colors: active)': {
                    borderColor: 'GrayText !important'
                }
            },
            // Checked Styles
            [`&.${clsPrefix}-checked`]: {
                '&:after': {
                    border: 'unset'
                },
                [`> .${clsPrefix}-input + .${clsPrefix}-inner`]: {
                    background: theme.colors.actionPrimaryBackgroundDefault,
                    borderColor: theme.colors.primary,
                    boxShadow: theme.shadows.xs,
                    '&:after': {
                        content: `""`,
                        borderRadius: theme.spacing.xs,
                        backgroundColor: theme.colors.white,
                        width: theme.spacing.xs,
                        height: theme.spacing.xs
                    },
                    '@media (forced-colors: active)': {
                        borderColor: 'Highlight !important',
                        backgroundColor: 'Highlight !important'
                    }
                },
                // Hover
                [`&:not(.${clsPrefix}-disabled) > .${clsPrefix}-input:hover + .${clsPrefix}-inner`]: {
                    background: theme.colors.actionPrimaryBackgroundHover,
                    borderColor: theme.colors.actionPrimaryBackgroundPress
                },
                // Focus
                [`&:not(.${clsPrefix}-disabled) > .${clsPrefix}-input:focus-visible + .${clsPrefix}-inner`]: {
                    background: theme.colors.actionPrimaryBackgroundDefault,
                    borderColor: theme.colors.actionDefaultBorderFocus,
                    outline: `1px solid ${theme.colors.actionPrimaryBackgroundDefault}`,
                    outlineOffset: 1
                },
                // Active
                [`&:not(.${clsPrefix}-disabled) > .${clsPrefix}-input:active + .${clsPrefix}-inner`]: {
                    background: theme.colors.actionDefaultBackgroundPress,
                    borderColor: theme.colors.actionDefaultBorderPress
                },
                // Disabled
                [`&.${clsPrefix}-disabled > .${clsPrefix}-input + .${clsPrefix}-inner`]: {
                    background: theme.colors.actionDisabledBackground,
                    border: `1px solid ${theme.colors.actionDisabledBorder} !important`,
                    '&:after': {
                        backgroundColor: theme.colors.actionDisabledText
                    },
                    '@media (forced-colors: active)': {
                        borderColor: 'GrayText !important',
                        backgroundColor: 'GrayText !important'
                    }
                }
            }
        }
    });
const getCommonRadioGroupStyles = ({ theme, clsPrefix, classNamePrefix })=>/*#__PURE__*/ css({
        '& > label': {
            [`&.${classNamePrefix}-radio-wrapper-disabled > span`]: {
                color: theme.colors.actionDisabledText
            }
        },
        [`& > label + .${classNamePrefix}-hint`]: {
            paddingLeft: theme.spacing.lg
        },
        ...getRadioInputStyles({
            theme,
            clsPrefix
        }),
        ...getAnimationCss(theme.options.enableAnimation)
    });
const getHorizontalRadioGroupStyles = ({ theme, classNamePrefix, useEqualColumnWidths })=>/*#__PURE__*/ css({
        '&&': {
            display: 'grid',
            gridTemplateRows: '[label] auto [hint] auto',
            gridAutoColumns: useEqualColumnWidths ? 'minmax(0, 1fr)' : 'max-content',
            gridColumnGap: theme.spacing.md,
            [`& + .${classNamePrefix}-form-message`]: {
                marginTop: theme.spacing.sm
            }
        },
        [`:has(> .${classNamePrefix}-hint)`]: {
            marginTop: theme.spacing.sm
        },
        [`& > label, & > .${classNamePrefix}-radio-tile`]: {
            gridRow: 'label / label',
            marginRight: 0
        },
        [`& > label + .${classNamePrefix}-hint`]: {
            display: 'inline-block',
            gridRow: 'hint / hint'
        }
    });
const getVerticalRadioGroupStyles = ({ theme, classNamePrefix })=>/*#__PURE__*/ css({
        display: 'flex',
        flexDirection: 'column',
        flexWrap: 'wrap',
        [`& + .${classNamePrefix}-form-message`]: {
            marginTop: theme.spacing.sm
        },
        [`~ .${classNamePrefix}-label)`]: {
            marginTop: theme.spacing.sm,
            background: 'red'
        },
        [`.${classNamePrefix}-radio-tile + .${classNamePrefix}-radio-tile`]: {
            marginTop: theme.spacing.md
        },
        '& > label': {
            fontWeight: 'normal',
            paddingBottom: theme.spacing.sm
        },
        [`& > label:last-of-type`]: {
            paddingBottom: 0
        },
        [`& > label + .${classNamePrefix}-hint`]: {
            marginBottom: theme.spacing.sm,
            paddingLeft: theme.spacing.lg,
            marginTop: `-${theme.spacing.sm}px`
        },
        [`& > label:last-of-type + .${classNamePrefix}-hint`]: {
            marginTop: 0
        }
    });
const getRadioStyles = ({ theme, clsPrefix })=>{
    // Default as bold for standalone radios
    const fontWeight = 'normal';
    const styles = {
        fontWeight
    };
    return /*#__PURE__*/ css({
        ...getRadioInputStyles({
            theme,
            clsPrefix
        }),
        ...styles
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const DuboisRadio = /*#__PURE__*/ forwardRef(function Radio({ children, dangerouslySetAntdProps, __INTERNAL_DISABLE_RADIO_ROLE, componentId, analyticsEvents, valueHasNoPii, onChange, ...props }, ref) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.radio', false);
    const { theme, getPrefixedClassName } = useDesignSystemTheme();
    const { componentId: contextualComponentId } = React__default.useContext(RadioGroupContext$1) ?? {};
    const clsPrefix = getPrefixedClassName('radio');
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Radio,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const firstView = useRef(true);
    useEffect(()=>{
        // Only call the onView callback if the Radio is standalone and not part of a RadioGroup
        if (componentId && contextualComponentId === undefined && firstView.current) {
            eventContext.onView(props.value);
            firstView.current = false;
        }
    }, [
        eventContext,
        componentId,
        contextualComponentId,
        props.value
    ]);
    const onChangeWrapper = useCallback((e)=>{
        // Only call the onValueChange callback if the Radio is standalone and not part of a RadioGroup
        if (contextualComponentId === undefined) {
            eventContext.onValueChange?.(e.target.value);
        }
        onChange?.(e);
    }, [
        contextualComponentId,
        eventContext,
        onChange
    ]);
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Radio$1, {
            ...addDebugOutlineIfEnabled(),
            css: getRadioStyles({
                theme,
                clsPrefix
            }),
            ...props,
            ...dangerouslySetAntdProps,
            ...__INTERNAL_DISABLE_RADIO_ROLE ? {
                role: 'none'
            } : {},
            onChange: onChangeWrapper,
            ref: ref,
            "data-component-type": contextualComponentId ? DesignSystemEventProviderComponentTypes.RadioGroup : DesignSystemEventProviderComponentTypes.Radio,
            // eslint-disable-next-line @databricks/no-dynamic-property-value -- Context component ID is validated through the RadioGroup exempt:8b3795a8-d0ff-4b21-888c-a6fbd0f3d7f2
            "data-component-id": contextualComponentId ?? componentId,
            children: /*#__PURE__*/ jsx(RestoreAntDDefaultClsPrefix, {
                children: children
            })
        })
    });
});
const StyledRadioGroup = /*#__PURE__*/ forwardRef(function StyledRadioGroup({ children, dangerouslySetAntdProps, componentId, analyticsEvents, valueHasNoPii, onChange, ...props }, ref) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.radio', false);
    const { theme, getPrefixedClassName, classNamePrefix } = useDesignSystemTheme();
    const uniqueId = useUniqueId();
    const clsPrefix = getPrefixedClassName('radio');
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const [value, setValue] = React__default.useState(props.defaultValue ?? '');
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.RadioGroup,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const internalRef = useRef();
    useImperativeHandle(ref, ()=>internalRef.current);
    const { elementRef: radioGroupRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: props.value ?? props.defaultValue
    });
    const mergedRef = useMergeRefs([
        internalRef,
        radioGroupRef
    ]);
    const onChangeWrapper = useCallback((e)=>{
        eventContext.onValueChange?.(e.target.value);
        setValue(e.target.value);
        onChange?.(e);
    }, [
        eventContext,
        onChange
    ]);
    useEffect(()=>{
        if (props.value !== undefined) {
            setValue(props.value);
            // Antd's Radio (rc-checkbox) is not updating checked state correctly even though state is managed appropriately on our end
            // Manually add and remove checked attribute to the radio input to ensure it is checked and A11y tools and tests can rely on this for validation
            if (internalRef?.current) {
                // Remove checked attribute from old radio input
                const checkedInput = internalRef.current.querySelector('input[checked]');
                if (checkedInput) {
                    checkedInput.removeAttribute('checked');
                }
                // Add checked attribute to new radio input
                const toBeCheckedInput = internalRef.current.querySelector(`input[value="${props.value}"]`);
                if (toBeCheckedInput) {
                    toBeCheckedInput.setAttribute('checked', 'checked');
                }
            }
        }
    }, [
        props.value
    ]);
    const ariaLabelledby = props['aria-labelledby'];
    // A11y helper (FIT-1649): Effect helping to set the aria-labelledby attribute on the radio group if it is not provided
    useEffect(()=>{
        if (ariaLabelledby) {
            return;
        }
        if (props.id) {
            // look for the label of the group pointing to the id
            const label = document.querySelector(`label[for="${props.id}"]`);
            if (label) {
                // If the label already has an id, map the radio group to it
                if (label.hasAttribute('id')) {
                    internalRef.current?.setAttribute('aria-labelledby', label.getAttribute('id') ?? '');
                } else {
                    label.setAttribute('id', `${props.id}${uniqueId}-label`);
                    internalRef.current?.setAttribute('aria-labelledby', `${props.id}${uniqueId}-label`);
                }
            }
        }
    }, [
        ariaLabelledby,
        props.id,
        uniqueId
    ]);
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(RadioGroupContext$1.Provider, {
            value: {
                componentId,
                value,
                onChange: onChangeWrapper
            },
            children: /*#__PURE__*/ jsx(Radio$1.Group, {
                /* @ts-expect-error - role is not a valid prop for RadioGroup in Antd but it applies it correctly to the group */ role: "radiogroup",
                ...addDebugOutlineIfEnabled(),
                ...props,
                css: getCommonRadioGroupStyles({
                    theme,
                    clsPrefix,
                    classNamePrefix
                }),
                value: value,
                onChange: onChangeWrapper,
                ...dangerouslySetAntdProps,
                ref: mergedRef,
                children: /*#__PURE__*/ jsx(RestoreAntDDefaultClsPrefix, {
                    children: children
                })
            })
        })
    });
});
const HorizontalGroup = /*#__PURE__*/ forwardRef(function HorizontalGroup({ dangerouslySetAntdProps, useEqualColumnWidths, ...props }, ref) {
    const { theme, classNamePrefix } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(StyledRadioGroup, {
        css: getHorizontalRadioGroupStyles({
            theme,
            classNamePrefix,
            useEqualColumnWidths
        }),
        ...props,
        ref: ref,
        ...dangerouslySetAntdProps
    });
});
const Group = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
function HorizontalGroup({ dangerouslySetAntdProps, layout = 'vertical', useEqualColumnWidths, ...props }, ref) {
    const { theme, classNamePrefix } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(StyledRadioGroup, {
        css: layout === 'horizontal' ? getHorizontalRadioGroupStyles({
            theme,
            classNamePrefix,
            useEqualColumnWidths
        }) : getVerticalRadioGroupStyles({
            theme,
            classNamePrefix
        }),
        ...props,
        ref: ref,
        ...dangerouslySetAntdProps
    });
});
// Note: We are overriding ant's default "Group" with our own.
const RadioNamespace = /* #__PURE__ */ Object.assign(DuboisRadio, {
    Group,
    HorizontalGroup
});
const Radio = RadioNamespace;
// TODO: I'm doing this to support storybook's docgen;
// We should remove this once we have a better storybook integration,
// since these will be exposed in the library's exports.
// We should ideally be using __Group instead of __VerticalGroup, but that exists under Checkbox too and conflicts, therefore
// we show a wrong component name in "Show code" in docs, fix included in story to replace this with correct name
const __INTERNAL_DO_NOT_USE__VerticalGroup = Group;
const __INTERNAL_DO_NOT_USE__HorizontalGroup = HorizontalGroup;

/**
 * Please use `SimpleSelect` unless you have a specific use-case for this primitive.
 * Ask in #dubois if you have questions!
 */ const Select = (props)=>{
    const { children, placeholder, value, label, ...restProps } = props;
    return /*#__PURE__*/ jsx(SelectContextProvider, {
        value: {
            isSelect: true,
            placeholder
        },
        children: /*#__PURE__*/ jsx(DialogCombobox, {
            label: label,
            value: value ? [
                value
            ] : [],
            ...restProps,
            children: children
        })
    });
};

/**
 * Please use `SimpleSelect` unless you have a specific use-case for this primitive.
 * Ask in #dubois if you have questions!
 */ const SelectContent = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ children, minWidth = 150, ...restProps }, ref)=>{
    return /*#__PURE__*/ jsx(DialogComboboxContent, {
        minWidth: minWidth,
        ...restProps,
        ref: ref,
        children: /*#__PURE__*/ jsx(DialogComboboxOptionList, {
            children: children
        })
    });
});

/**
 * Please use `SimpleSelect` unless you have a specific use-case for this primitive.
 * Ask in #dubois if you have questions!
 */ // eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const SelectOption = /*#__PURE__*/ forwardRef((props, ref)=>{
    const { value } = useDialogComboboxContext();
    return /*#__PURE__*/ jsx(DialogComboboxOptionListSelectItem, {
        checked: value && value[0] === props.value,
        ...props,
        ref: ref
    });
});

/**
 * Please use `SimpleSelect` unless you have a specific use-case for this primitive.
 * Ask in #dubois if you have questions!
 */ const SelectOptionGroup = (props)=>{
    const { name, children, ...restProps } = props;
    return /*#__PURE__*/ jsxs(Fragment, {
        children: [
            /*#__PURE__*/ jsx(DialogComboboxSectionHeader, {
                ...restProps,
                children: name
            }),
            children
        ]
    });
};

/**
 * Please use `SimpleSelect` unless you have a specific use-case for this primitive.
 * Ask in #dubois if you have questions!
 */ // eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const SelectTrigger = /*#__PURE__*/ forwardRef((props, ref)=>{
    const { children, ...restProps } = props;
    return /*#__PURE__*/ jsx(DialogComboboxTrigger, {
        allowClear: false,
        ...restProps,
        ref: ref,
        children: children
    });
});

const SimpleSelectContext = /*#__PURE__*/ createContext(undefined);
const getSelectedOption = (children, value)=>{
    const childArray = React__default.Children.toArray(children);
    for (const child of childArray){
        if (/*#__PURE__*/ React__default.isValidElement(child)) {
            if (child.type === SimpleSelectOption && child.props.value === value) {
                return child;
            }
            if (child.props.children) {
                const nestedOption = getSelectedOption(child.props.children, value);
                if (nestedOption) {
                    return nestedOption;
                }
            }
        }
    }
    return undefined;
};
const getSelectedOptionLabel = (children, value)=>{
    const selectedOption = getSelectedOption(children, value);
    if (/*#__PURE__*/ React__default.isValidElement(selectedOption)) {
        return selectedOption.props.children;
    }
    return '';
};
/**
 * This is the future `Select` component which simplifies the API of the current Select primitives.
 * It is temporarily named `SimpleSelect` pending cleanup.
 */ const SimpleSelect = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ defaultValue, name, placeholder, children, contentProps, onChange, onOpenChange, id, label, value, validationState, forceCloseOnEscape, componentId, analyticsEvents, valueHasNoPii, ...rest }, ref)=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.simpleSelect', false);
    const [defaultLabel] = useState(()=>{
        if (value) {
            return getSelectedOptionLabel(children, value);
        }
        return '';
    });
    const innerRef = useRef(null);
    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion -- TODO(FEINF-3982)
    useImperativeHandle(ref, ()=>innerRef.current, []);
    const previousExternalValue = useRef(value);
    const [internalValue, setInternalValue] = useState(value);
    const [selectedLabel, setSelectedLabel] = useState(defaultLabel);
    const isControlled = value !== undefined;
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.SimpleSelect,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const { elementRef: simpleSelectRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: value ?? defaultValue
    });
    // Controlled state setup.
    useEffect(()=>{
        if (value !== undefined && value !== previousExternalValue.current) {
            setInternalValue(value);
            previousExternalValue.current = value;
        }
    }, [
        value
    ]);
    // Uncontrolled state setup.
    useEffect(()=>{
        if (isControlled) {
            return;
        }
        // Set initial state.
        const element = innerRef.current;
        const initialValue = defaultValue || element?.value || '';
        setInternalValue(initialValue);
        previousExternalValue.current = initialValue;
    }, [
        isControlled,
        defaultValue,
        value
    ]);
    // Separately update the label when the value changes; this responds
    // to either the controlled or uncontrolled useEffects above.
    useEffect(()=>{
        setSelectedLabel(getSelectedOptionLabel(children, internalValue || ''));
    }, [
        internalValue,
        children
    ]);
    // Handles controlled state, and propagates changes to the input element.
    const handleChange = (newValue)=>{
        eventContext.onValueChange(newValue);
        innerRef.current?.setAttribute('value', newValue || '');
        setInternalValue(newValue);
        setSelectedLabel(getSelectedOptionLabel(children, newValue));
        if (onChange) {
            onChange({
                target: {
                    name,
                    type: 'select',
                    value: newValue
                },
                type: 'change'
            });
        }
    };
    return /*#__PURE__*/ jsx(SimpleSelectContext.Provider, {
        value: {
            value: internalValue,
            onChange: handleChange
        },
        children: /*#__PURE__*/ jsx(Select, {
            // SimpleSelect emits its own value change events rather than emitting them from the underlying
            // DialogCombobox due to how SimpleSelect sets its initial state. The Select componentId is explicitly
            // set to undefined to prevent it from emitting events if the componentId prop is required in the future.
            // eslint-disable-next-line @databricks/no-dynamic-property-value -- Valid case exempt:e5d37a4d-10cd-4a8f-a964-2223405cc738
            componentId: undefined,
            value: internalValue,
            placeholder: placeholder,
            label: label ?? rest['aria-label'],
            id: id,
            children: /*#__PURE__*/ jsxs(SimpleSelectContentWrapper, {
                onOpenChange: onOpenChange,
                children: [
                    /*#__PURE__*/ jsx(SelectTrigger, {
                        ref: simpleSelectRef,
                        ...rest,
                        validationState: validationState,
                        onClear: ()=>{
                            handleChange('');
                        },
                        id: id,
                        value: internalValue,
                        ...eventContext.dataComponentProps,
                        children: selectedLabel || placeholder
                    }),
                    /*#__PURE__*/ jsx("input", {
                        type: "hidden",
                        ref: innerRef
                    }),
                    /*#__PURE__*/ jsx(SelectContent, {
                        forceCloseOnEscape: forceCloseOnEscape,
                        ...contentProps,
                        children: children
                    })
                ]
            })
        })
    });
});
// This component is used to propagate the open state of the DialogCombobox to the SimpleSelect.
// We don't directly pass through `onOpenChange` since it's tied into the actual state; `SimpleSelect` merely
// needs to communicate via the optional prop if the dropdown is open or not and doesn't need to control it.
const SimpleSelectContentWrapper = ({ children, onOpenChange })=>{
    const { isOpen } = useDialogComboboxContext();
    useEffect(()=>{
        if (onOpenChange) {
            onOpenChange(Boolean(isOpen));
        }
    }, [
        isOpen,
        onOpenChange
    ]);
    return /*#__PURE__*/ jsx(Fragment, {
        children: children
    });
};
const SimpleSelectOption = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ value, children, ...rest }, ref)=>{
    const context = useContext(SimpleSelectContext);
    if (!context) {
        throw new Error('SimpleSelectOption must be used within a SimpleSelect');
    }
    const { onChange } = context;
    return /*#__PURE__*/ jsx(SelectOption, {
        ...rest,
        ref: ref,
        value: value,
        onChange: ({ value })=>{
            onChange(value);
        },
        children: children
    });
});
const SimpleSelectOptionGroup = ({ children, label, ...props })=>{
    const context = useContext(SimpleSelectContext);
    if (!context) {
        throw new Error('SimpleSelectOptionGroup must be used within a SimpleSelect');
    }
    return /*#__PURE__*/ jsx(SelectOptionGroup, {
        ...props,
        name: label,
        children: children
    });
};

const getSwitchWithLabelStyles = ({ clsPrefix, theme, disabled })=>{
    // Default value
    const SWITCH_WIDTH = 28;
    const styles = {
        display: 'flex',
        alignItems: 'center',
        ...disabled && {
            '&&, label': {
                color: theme.colors.actionDisabledText
            }
        },
        [`&.${clsPrefix}-switch, &.${clsPrefix}-switch-checked`]: {
            [`.${clsPrefix}-switch-handle`]: {
                top: -1
            },
            [`.${clsPrefix}-switch-handle, .${clsPrefix}-switch-handle:before`]: {
                width: 16,
                height: 16,
                borderRadius: theme.borders.borderRadiusFull
            }
        },
        // Switch is Off
        [`&.${clsPrefix}-switch`]: {
            backgroundColor: theme.colors.backgroundPrimary,
            border: `1px solid ${theme.colors.actionDefaultBorderDefault}`,
            borderRadius: theme.borders.borderRadiusFull,
            [`.${clsPrefix}-switch-handle:before`]: {
                border: `1px solid ${theme.colors.actionDefaultBorderDefault}`,
                boxShadow: theme.shadows.xs,
                left: -1,
                transition: 'none',
                borderRadius: theme.borders.borderRadiusFull
            },
            [`&:hover:not(.${clsPrefix}-switch-disabled)`]: {
                backgroundColor: theme.colors.actionDefaultBackgroundHover,
                border: `1px solid ${theme.colors.actionPrimaryBackgroundHover}`,
                [`.${clsPrefix}-switch-handle:before`]: {
                    boxShadow: theme.shadows.xs,
                    border: `1px solid ${theme.colors.actionPrimaryBackgroundHover}`
                }
            },
            [`&:active:not(.${clsPrefix}-switch-disabled)`]: {
                backgroundColor: theme.colors.actionDefaultBackgroundPress,
                border: `1px solid ${theme.colors.actionPrimaryBackgroundPress}`,
                [`.${clsPrefix}-switch-handle:before`]: {
                    boxShadow: 'none',
                    border: `1px solid ${theme.colors.actionPrimaryBackgroundPress}`
                }
            },
            [`&.${clsPrefix}-switch-disabled`]: {
                backgroundColor: theme.colors.actionDisabledBackground,
                border: `1px solid ${theme.colors.actionDisabledBorder}`,
                [`.${clsPrefix}-switch-handle:before`]: {
                    boxShadow: 'none',
                    border: `1px solid ${theme.colors.actionDisabledBorder}`
                }
            },
            [`&:focus-visible`]: {
                border: `1px solid ${theme.colors.actionPrimaryBackgroundDefault}`,
                boxShadow: 'none',
                outlineStyle: 'solid',
                outlineWidth: '1px',
                outlineColor: theme.colors.actionDefaultBorderFocus,
                [`.${clsPrefix}-switch-handle:before`]: {
                    boxShadow: theme.shadows.xs,
                    border: `1px solid ${theme.colors.actionPrimaryBackgroundDefault}`
                }
            },
            [`&:focus`]: {
                boxShadow: 'none'
            }
        },
        // Switch is On
        [`&.${clsPrefix}-switch-checked`]: {
            backgroundColor: theme.colors.actionPrimaryBackgroundDefault,
            border: `1px solid ${theme.colors.actionPrimaryBackgroundDefault}`,
            [`&:hover:not(.${clsPrefix}-switch-disabled)`]: {
                backgroundColor: theme.colors.actionPrimaryBackgroundHover,
                border: `1px solid ${theme.colors.actionPrimaryBackgroundHover}`,
                [`.${clsPrefix}-switch-handle:before`]: {
                    border: `1px solid ${theme.colors.actionPrimaryBackgroundHover}`
                }
            },
            [`&:active:not(.${clsPrefix}-switch-disabled)`]: {
                backgroundColor: theme.colors.actionPrimaryBackgroundPress,
                border: `1px solid ${theme.colors.actionPrimaryBackgroundPress}`,
                [`.${clsPrefix}-switch-handle:before`]: {
                    border: `1px solid ${theme.colors.actionPrimaryBackgroundPress}`
                }
            },
            [`.${clsPrefix}-switch-handle:before`]: {
                boxShadow: theme.shadows.xs,
                border: `1px solid ${theme.colors.actionPrimaryBackgroundDefault}`,
                right: -1
            },
            [`&.${clsPrefix}-switch-disabled`]: {
                backgroundColor: theme.colors.actionDisabledText,
                border: `1px solid ${theme.colors.actionDisabledText}`,
                [`.${clsPrefix}-switch-handle:before`]: {
                    boxShadow: 'none',
                    border: `1px solid ${theme.colors.actionDisabledText}`
                }
            },
            [`&:focus-visible`]: {
                outlineOffset: '1px'
            }
        },
        [`.${clsPrefix}-switch-handle:before`]: {
            backgroundColor: theme.colors.backgroundPrimary
        },
        [`&& + .${clsPrefix}-hint, && + .${clsPrefix}-form-message`]: {
            paddingLeft: theme.spacing.sm + SWITCH_WIDTH
        },
        [`&& + .${clsPrefix}-form-message`]: {
            marginTop: 0
        },
        [`.${clsPrefix}-click-animating-node`]: {
            animation: 'none'
        },
        opacity: 1
    };
    const importantStyles = importantify(styles);
    return /*#__PURE__*/ css(importantStyles);
};
const Switch = ({ dangerouslySetAntdProps, label, labelProps, activeLabel, inactiveLabel, disabledLabel, componentId, analyticsEvents, onChange, ...props })=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.switch', false);
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const duboisId = useUniqueId('dubois-switch');
    const uniqueId = props.id ?? duboisId;
    const [isChecked, setIsChecked] = useState(props.checked || props.defaultChecked);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Switch,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: true
    });
    const { elementRef: viewRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: isChecked
    });
    const handleToggle = (newState, event)=>{
        eventContext.onValueChange(newState);
        if (onChange) {
            onChange(newState, event);
        } else {
            setIsChecked(newState);
        }
    };
    const onChangeHandler = (newState, event)=>{
        eventContext.onValueChange(newState);
        onChange?.(newState, event);
    };
    useEffect(()=>{
        if (props.checked === undefined) {
            return;
        }
        setIsChecked(props.checked);
    }, [
        props.checked
    ]);
    const hasNewLabels = activeLabel && inactiveLabel && disabledLabel;
    const stateMessage = isChecked ? activeLabel : inactiveLabel;
    // AntDSwitch's interface does not include `id` even though it passes it through and works as expected
    // We are using this to bypass that check
    const idPropObj = {
        id: uniqueId
    };
    const switchComponent = /*#__PURE__*/ jsx(Switch$1, {
        ...addDebugOutlineIfEnabled(),
        ...props,
        ...dangerouslySetAntdProps,
        onChange: handleToggle,
        ...idPropObj,
        css: {
            .../*#__PURE__*/ css(getAnimationCss(theme.options.enableAnimation)),
            ...getSwitchWithLabelStyles({
                clsPrefix: classNamePrefix,
                theme,
                disabled: props.disabled
            })
        },
        ref: viewRef
    });
    const labelComponent = /*#__PURE__*/ jsx(Label, {
        inline: true,
        ...labelProps,
        htmlFor: uniqueId,
        style: {
            ...hasNewLabels && {
                marginRight: theme.spacing.sm
            }
        },
        children: label
    });
    return label ? /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx("div", {
            ...addDebugOutlineIfEnabled(),
            css: getSwitchWithLabelStyles({
                clsPrefix: classNamePrefix,
                theme,
                disabled: props.disabled
            }),
            ...eventContext.dataComponentProps,
            children: hasNewLabels ? /*#__PURE__*/ jsxs(Fragment, {
                children: [
                    labelComponent,
                    /*#__PURE__*/ jsx("span", {
                        style: {
                            marginLeft: 'auto',
                            marginRight: theme.spacing.sm,
                            color: theme.colors.textPrimary
                        },
                        children: `${stateMessage}${props.disabled ? ` (${disabledLabel})` : ''}`
                    }),
                    switchComponent
                ]
            }) : /*#__PURE__*/ jsxs(Fragment, {
                children: [
                    switchComponent,
                    labelComponent
                ]
            })
        })
    }) : /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Switch$1, {
            onChange: onChangeHandler,
            ...addDebugOutlineIfEnabled(),
            ...props,
            ...dangerouslySetAntdProps,
            ...idPropObj,
            css: {
                .../*#__PURE__*/ css(getAnimationCss(theme.options.enableAnimation)),
                ...getSwitchWithLabelStyles({
                    clsPrefix: classNamePrefix,
                    theme,
                    disabled: props.disabled
                })
            },
            ...eventContext.dataComponentProps,
            ref: viewRef
        })
    });
};

const TypeaheadComboboxRoot = /*#__PURE__*/ forwardRef(({ comboboxState, multiSelect = false, children, ...props }, ref)=>{
    const { classNamePrefix } = useDesignSystemTheme();
    const { refs, floatingStyles } = useFloating({
        whileElementsMounted: autoUpdate,
        middleware: [
            offset(4),
            flip(),
            shift()
        ],
        placement: 'bottom-start'
    });
    const { elementRef: typeaheadComboboxRootRef } = useNotifyOnFirstView({
        onView: comboboxState.onView,
        value: comboboxState.firstOnViewValue
    });
    const mergedRef = useMergeRefs([
        ref,
        typeaheadComboboxRootRef
    ]);
    return /*#__PURE__*/ jsx(TypeaheadComboboxContextProvider, {
        value: {
            componentId: comboboxState.componentId,
            multiSelect,
            isInsideTypeaheadCombobox: true,
            floatingUiRefs: refs,
            floatingStyles: floatingStyles
        },
        children: /*#__PURE__*/ jsx("div", {
            ...comboboxState.getComboboxProps({}, {
                suppressRefError: true
            }),
            className: `${classNamePrefix}-typeahead-combobox`,
            css: {
                display: 'inline-block',
                width: '100%'
            },
            ...props,
            ref: mergedRef,
            "data-component-type": DesignSystemEventProviderComponentTypes.TypeaheadCombobox,
            // eslint-disable-next-line @databricks/no-dynamic-property-value -- Valid case, as combostate componentId is checked in the hook exempt:a7de1e72-e1ae-4747-9806-90853bef6a06
            "data-component-id": comboboxState.componentId,
            children: children
        })
    });
});

const getTypeaheadComboboxMenuStyles = ()=>{
    return /*#__PURE__*/ css({
        padding: 0,
        margin: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'flex-start',
        position: 'absolute'
    });
};
const TypeaheadComboboxMenu = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ comboboxState, loading, emptyText, width, minWidth = 240, maxWidth, minHeight, maxHeight, listWrapperHeight, virtualizerRef, children, matchTriggerWidth, ...restProps }, ref)=>{
    const { getMenuProps, isOpen } = comboboxState;
    const { ref: downshiftRef, ...downshiftProps } = getMenuProps({}, {
        suppressRefError: true
    });
    const [viewPortMaxHeight, setViewPortMaxHeight] = useState(undefined);
    const { floatingUiRefs, floatingStyles, isInsideTypeaheadCombobox, inputWidth } = useTypeaheadComboboxContext();
    if (!isInsideTypeaheadCombobox) {
        throw new Error('`TypeaheadComboboxMenu` must be used within `TypeaheadCombobox`');
    }
    const mergedRef = useMergeRefs([
        ref,
        downshiftRef,
        floatingUiRefs?.setFloating,
        virtualizerRef
    ]);
    const { theme } = useDesignSystemTheme();
    const { getPopupContainer } = useDesignSystemContext();
    const recalculateMaxHeight = useCallback(()=>{
        if (isOpen && floatingUiRefs?.floating && floatingUiRefs.reference.current && floatingUiRefs?.reference && floatingUiRefs.floating.current) {
            computePosition(floatingUiRefs.reference.current, floatingUiRefs.floating.current, {
                middleware: [
                    flip$1(),
                    size({
                        padding: theme.spacing.sm,
                        apply ({ availableHeight }) {
                            setViewPortMaxHeight(availableHeight);
                        }
                    })
                ]
            });
        }
    }, [
        isOpen,
        floatingUiRefs,
        theme.spacing.sm
    ]);
    useEffect(()=>{
        if (!isOpen || maxHeight) {
            return;
        }
        recalculateMaxHeight();
        window.addEventListener('scroll', recalculateMaxHeight);
        return ()=>{
            window.removeEventListener('scroll', recalculateMaxHeight);
        };
    }, [
        isOpen,
        maxHeight,
        recalculateMaxHeight
    ]);
    if (!isOpen) return null;
    const hasFragmentWrapper = children && !Array.isArray(children) && children.type === Fragment$1;
    const filterableChildren = hasFragmentWrapper ? children.props.children : children;
    const hasResults = filterableChildren && Children.toArray(filterableChildren).some((child)=>{
        if (/*#__PURE__*/ React__default.isValidElement(child)) {
            const childType = child.props['__EMOTION_TYPE_PLEASE_DO_NOT_USE__']?.defaultProps._type ?? child.props._type;
            return [
                'TypeaheadComboboxMenuItem',
                'TypeaheadComboboxCheckboxItem'
            ].includes(childType);
        }
        return false;
    });
    const [menuItemChildren, footer] = Children.toArray(filterableChildren).reduce((acc, child)=>{
        const isFooter = /*#__PURE__*/ React__default.isValidElement(child) && child.props._type === 'TypeaheadComboboxFooter';
        if (isFooter) {
            acc[1].push(child);
        } else {
            acc[0].push(child);
        }
        return acc;
    }, [
        [],
        []
    ]);
    return /*#__PURE__*/ createPortal(/*#__PURE__*/ jsx("ul", {
        ...addDebugOutlineIfEnabled(),
        "aria-busy": loading,
        ...downshiftProps,
        ref: mergedRef,
        css: [
            getComboboxContentWrapperStyles(theme, {
                maxHeight: maxHeight ?? viewPortMaxHeight,
                maxWidth,
                minHeight,
                minWidth,
                width
            }),
            getTypeaheadComboboxMenuStyles(),
            matchTriggerWidth && inputWidth && {
                width: inputWidth
            }
        ],
        style: {
            ...floatingStyles
        },
        ...restProps,
        children: loading ? /*#__PURE__*/ jsx(LoadingSpinner, {
            "aria-label": "Loading",
            alt: "Loading spinner"
        }) : hasResults ? /*#__PURE__*/ jsxs(Fragment, {
            children: [
                /*#__PURE__*/ jsx("div", {
                    style: {
                        position: 'relative',
                        width: '100%',
                        ...listWrapperHeight && {
                            height: listWrapperHeight,
                            flexShrink: 0
                        }
                    },
                    children: menuItemChildren
                }),
                footer
            ]
        }) : /*#__PURE__*/ jsxs(Fragment, {
            children: [
                /*#__PURE__*/ jsx(EmptyResults, {
                    emptyText: emptyText
                }),
                footer
            ]
        })
    }), getPopupContainer ? getPopupContainer() : document.body);
});

const getToggleButtonStyles = (theme, disabled)=>{
    return /*#__PURE__*/ css({
        cursor: 'pointer',
        userSelect: 'none',
        color: theme.colors.textSecondary,
        backgroundColor: 'transparent',
        border: 'none',
        padding: 0,
        marginLeft: theme.spacing.xs,
        height: 16,
        width: 16,
        ...disabled && {
            pointerEvents: 'none',
            color: theme.colors.actionDisabledText
        }
    });
};
const TypeaheadComboboxToggleButton = /*#__PURE__*/ React__default.forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ disabled, ...restProps }, ref)=>{
    const { theme } = useDesignSystemTheme();
    const { onClick } = restProps;
    function handleClick(e) {
        e.stopPropagation();
        onClick(e);
    }
    return /*#__PURE__*/ jsx("button", {
        type: "button",
        "aria-label": "toggle menu",
        ref: ref,
        css: getToggleButtonStyles(theme, disabled),
        ...restProps,
        onClick: handleClick,
        children: /*#__PURE__*/ jsx(ChevronDownIcon, {})
    });
});

const TypeaheadComboboxControls = ({ getDownshiftToggleButtonProps, showClearSelectionButton, showComboboxToggleButton = true, handleClear, disabled })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsxs("div", {
        css: {
            position: 'absolute',
            top: theme.spacing.sm,
            right: 7,
            height: 16,
            zIndex: 1,
            cursor: disabled ? 'not-allowed' : undefined
        },
        children: [
            showClearSelectionButton && /*#__PURE__*/ jsx(ClearSelectionButton, {
                onClick: handleClear,
                css: {
                    pointerEvents: 'all',
                    verticalAlign: 'text-top'
                }
            }),
            showComboboxToggleButton && /*#__PURE__*/ jsx(TypeaheadComboboxToggleButton, {
                ...getDownshiftToggleButtonProps(),
                disabled: disabled
            })
        ]
    });
};

const getContainerStyles$1 = ()=>{
    return /*#__PURE__*/ css({
        display: 'flex',
        position: 'relative'
    });
};
const getInputStyles$1 = (theme, showComboboxToggleButton)=>/*#__PURE__*/ css({
        paddingRight: showComboboxToggleButton ? 52 : 26,
        width: '100%',
        minWidth: 72,
        borderRadius: theme.borders.borderRadiusSm,
        borderColor: theme.colors.actionDefaultBorderDefault,
        '&:disabled': {
            borderColor: theme.colors.actionDisabledBorder,
            backgroundColor: theme.colors.actionDisabledBackground,
            color: theme.colors.actionDisabledText
        },
        '&:not(:disabled)': {
            backgroundColor: 'transparent'
        }
    });
const TypeaheadComboboxInput = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ comboboxState, allowClear = true, showComboboxToggleButton = true, formOnChange, onClick, clearInputValueOnFocus = false, ...restProps }, ref)=>{
    const { isInsideTypeaheadCombobox, floatingUiRefs, setInputWidth, inputWidth } = useTypeaheadComboboxContext();
    if (!isInsideTypeaheadCombobox) {
        throw new Error('`TypeaheadComboboxInput` must be used within `TypeaheadCombobox`');
    }
    const { getInputProps, getToggleButtonProps, toggleMenu, inputValue, setInputValue, reset, isOpen, selectedItem, componentId } = comboboxState;
    const { ref: downshiftRef, ...downshiftProps } = getInputProps({}, {
        suppressRefError: true
    });
    const mergedRef = useMergeRefs([
        ref,
        downshiftRef
    ]);
    const { theme } = useDesignSystemTheme();
    const handleClick = (e)=>{
        onClick?.(e);
        toggleMenu();
    };
    const previousInputValue = useRef(null);
    useEffect(()=>{
        if (!clearInputValueOnFocus) {
            return;
        }
        // If the input is open and has value, clear the input value
        if (isOpen && !previousInputValue.current) {
            previousInputValue.current = {
                selectedItem: selectedItem,
                inputValue: inputValue
            };
            setInputValue('');
        }
        // If the input is closed and the input value was cleared, restore the input value
        if (!isOpen && previousInputValue.current) {
            // Only restore the input value if the selected item is the same as the previous selected item
            if (previousInputValue.current.selectedItem === selectedItem) {
                setInputValue(previousInputValue.current.inputValue);
            }
            previousInputValue.current = null;
        }
    }, [
        isOpen,
        inputValue,
        setInputValue,
        previousInputValue,
        clearInputValueOnFocus,
        selectedItem
    ]);
    const handleClear = ()=>{
        setInputValue('');
        reset();
        formOnChange?.(null);
    };
    // Gets the width of the input and sets it inside the context for rendering the dropdown when `matchTriggerWidth` is true on the menu
    useEffect(()=>{
        // Use the DOM reference of the TypeaheadComboboxInput container div to get the width of the input
        if (floatingUiRefs?.domReference) {
            const width = floatingUiRefs.domReference.current?.getBoundingClientRect().width ?? 0;
            // Only update context width when the input width updated
            if (width !== inputWidth) {
                setInputWidth?.(width);
            }
        }
    }, [
        floatingUiRefs?.domReference,
        setInputWidth,
        inputWidth
    ]);
    return /*#__PURE__*/ jsxs("div", {
        ref: floatingUiRefs?.setReference,
        css: getContainerStyles$1(),
        className: restProps.className,
        ...addDebugOutlineIfEnabled(),
        children: [
            /*#__PURE__*/ jsx(Input, {
                // eslint-disable-next-line @databricks/no-dynamic-property-value -- Valid case, as combostate componentId is checked in the hook exempt:ef606c74-b984-4035-bbdb-9a6ae8e4e88e
                componentId: componentId ? `${componentId}.input` : 'design_system.typeahead_combobox.input',
                ref: mergedRef,
                ...downshiftProps,
                "aria-controls": comboboxState.isOpen ? downshiftProps['aria-controls'] : undefined,
                onClick: handleClick,
                css: getInputStyles$1(theme, showComboboxToggleButton),
                ...restProps
            }),
            /*#__PURE__*/ jsx(TypeaheadComboboxControls, {
                getDownshiftToggleButtonProps: getToggleButtonProps,
                showClearSelectionButton: allowClear && Boolean(inputValue) && !restProps.disabled,
                showComboboxToggleButton: showComboboxToggleButton,
                handleClear: handleClear,
                disabled: restProps.disabled
            })
        ]
    });
});

const getSelectedItemStyles = (theme, disabled)=>{
    return /*#__PURE__*/ css({
        backgroundColor: theme.colors.tagDefault,
        borderRadius: theme.general.borderRadiusBase,
        color: theme.colors.textPrimary,
        lineHeight: theme.typography.lineHeightBase,
        fontSize: theme.typography.fontSizeBase,
        marginTop: 2,
        marginBottom: 2,
        marginInlineEnd: theme.spacing.xs,
        paddingRight: 0,
        paddingTop: 0,
        paddingBottom: 0,
        paddingInlineStart: theme.spacing.xs,
        position: 'relative',
        flex: 'none',
        maxWidth: '100%',
        wordBreak: 'break-word',
        ...disabled && {
            pointerEvents: 'none'
        }
    });
};
const getIconContainerStyles = (theme, disabled)=>{
    return /*#__PURE__*/ css({
        width: 16,
        height: 16,
        ':hover': {
            color: theme.colors.actionTertiaryTextHover,
            backgroundColor: theme.colors.tagHover
        },
        ...disabled && {
            pointerEvents: 'none',
            color: theme.colors.actionDisabledText
        }
    });
};
const getXIconStyles = (theme)=>{
    return /*#__PURE__*/ css({
        fontSize: theme.typography.fontSizeSm,
        verticalAlign: '-1px',
        paddingLeft: theme.spacing.xs / 2,
        paddingRight: theme.spacing.xs / 2
    });
};
const TypeaheadComboboxSelectedItem = /*#__PURE__*/ forwardRef(({ label, item, getSelectedItemProps, removeSelectedItem, disabled, ...restProps }, ref)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsxs("span", {
        ...getSelectedItemProps({
            selectedItem: item
        }),
        css: getSelectedItemStyles(theme, disabled),
        ref: ref,
        ...restProps,
        children: [
            /*#__PURE__*/ jsx("span", {
                css: {
                    marginRight: 2,
                    ...disabled && {
                        color: theme.colors.actionDisabledText
                    }
                },
                children: label
            }),
            /*#__PURE__*/ jsx("span", {
                css: getIconContainerStyles(theme, disabled),
                children: /*#__PURE__*/ jsx(CloseIcon, {
                    "aria-hidden": "false",
                    onClick: (e)=>{
                        if (!disabled) {
                            e.stopPropagation();
                            removeSelectedItem(item);
                        }
                    },
                    css: getXIconStyles(theme),
                    role: "button",
                    "aria-label": "Remove selected item"
                })
            })
        ]
    });
});

const CountBadge = ({ countStartAt, totalCount, disabled })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        css: [
            getSelectedItemStyles(theme),
            {
                paddingInlineEnd: theme.spacing.xs,
                ...disabled && {
                    color: theme.colors.actionDisabledText
                }
            }
        ],
        children: countStartAt ? `+${totalCount - countStartAt}` : totalCount
    });
};

const getContainerStyles = (theme, validationState, width, maxHeight, disabled)=>{
    const validationColor = getValidationStateColor(theme, validationState);
    return /*#__PURE__*/ css({
        cursor: 'text',
        display: 'inline-block',
        verticalAlign: 'top',
        border: `1px solid ${theme.colors.actionDefaultBorderDefault}`,
        borderRadius: theme.general.borderRadiusBase,
        minHeight: 32,
        height: 'auto',
        minWidth: 0,
        ...width ? {
            width
        } : {},
        ...maxHeight ? {
            maxHeight
        } : {},
        padding: '5px 52px 5px 12px',
        position: 'relative',
        overflow: 'auto',
        textOverflow: 'ellipsis',
        '&:hover': {
            border: `1px solid ${theme.colors.actionPrimaryBackgroundHover}`
        },
        '&:focus-within': {
            outlineColor: theme.colors.actionDefaultBorderFocus,
            outlineWidth: 2,
            outlineOffset: -2,
            outlineStyle: 'solid',
            boxShadow: 'none',
            borderColor: 'transparent'
        },
        '&&': {
            ...validationState && {
                borderColor: validationColor
            },
            '&:hover': {
                borderColor: validationState ? validationColor : theme.colors.actionPrimaryBackgroundHover
            },
            '&:focus': {
                outlineColor: validationState ? validationColor : theme.colors.actionDefaultBorderFocus,
                outlineWidth: 2,
                outlineOffset: -2,
                outlineStyle: 'solid',
                boxShadow: 'none',
                borderColor: 'transparent'
            },
            ...disabled && {
                borderColor: theme.colors.actionDisabledBorder,
                backgroundColor: theme.colors.actionDisabledBackground,
                cursor: 'not-allowed',
                outline: 'none',
                '&:hover': {
                    border: `1px solid ${theme.colors.actionDisabledBorder}`
                },
                '&:focus-within': {
                    outline: 'none',
                    borderColor: theme.colors.actionDisabledBorder
                }
            }
        }
    });
};
const getContentWrapperStyles = ()=>{
    return /*#__PURE__*/ css({
        display: 'flex',
        flex: 'auto',
        flexWrap: 'wrap',
        maxWidth: '100%',
        position: 'relative'
    });
};
const getInputWrapperStyles = ()=>{
    return /*#__PURE__*/ css({
        display: 'inline-flex',
        position: 'relative',
        maxWidth: '100%',
        alignSelf: 'auto',
        flex: 'none'
    });
};
const getInputStyles = (theme)=>{
    return /*#__PURE__*/ css({
        lineHeight: 20,
        height: 24,
        margin: 0,
        padding: 0,
        appearance: 'none',
        cursor: 'auto',
        width: '100%',
        backgroundColor: 'transparent',
        color: theme.colors.textPrimary,
        '&, &:hover, &:focus-visible': {
            border: 'none',
            outline: 'none'
        },
        '&::placeholder': {
            color: theme.colors.textPlaceholder
        }
    });
};
const TypeaheadComboboxMultiSelectInput = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ comboboxState, multipleSelectionState, selectedItems, setSelectedItems, getSelectedItemLabel, allowClear = true, showTagAfterValueCount = 20, width, maxHeight, placeholder, validationState, showComboboxToggleButton, disableTooltip = false, ...restProps }, ref)=>{
    const { isInsideTypeaheadCombobox } = useTypeaheadComboboxContext();
    if (!isInsideTypeaheadCombobox) {
        throw new Error('`TypeaheadComboboxMultiSelectInput` must be used within `TypeaheadCombobox`');
    }
    const { getInputProps, getToggleButtonProps, toggleMenu, inputValue, setInputValue } = comboboxState;
    const { getSelectedItemProps, getDropdownProps, reset, removeSelectedItem } = multipleSelectionState;
    const { ref: downshiftRef, ...downshiftProps } = getInputProps(getDropdownProps({}, {
        suppressRefError: true
    }));
    const { floatingUiRefs, setInputWidth: setContextInputWidth, inputWidth: contextInputWidth } = useTypeaheadComboboxContext();
    const containerRef = useRef(null);
    const mergedContainerRef = useMergeRefs([
        containerRef,
        floatingUiRefs?.setReference
    ]);
    const itemsRef = useRef(null);
    const measureRef = useRef(null);
    const innerRef = useRef(null);
    const mergedInputRef = useMergeRefs([
        ref,
        innerRef,
        downshiftRef
    ]);
    const { theme } = useDesignSystemTheme();
    const [inputWidth, setInputWidth] = useState(0);
    const shouldShowCountBadge = selectedItems.length > showTagAfterValueCount;
    const [showTooltip, setShowTooltip] = useState(shouldShowCountBadge);
    const selectedItemsToRender = selectedItems.slice(0, showTagAfterValueCount);
    const handleClick = ()=>{
        if (!restProps.disabled) {
            innerRef.current?.focus();
            toggleMenu();
        }
    };
    const handleClear = ()=>{
        setInputValue('');
        reset();
        setSelectedItems([]);
    };
    // We measure width and set to the input immediately
    useLayoutEffect(()=>{
        if (measureRef?.current) {
            const measuredWidth = measureRef.current.scrollWidth;
            setInputWidth(measuredWidth);
        }
    }, [
        measureRef?.current?.scrollWidth,
        selectedItems?.length
    ]);
    // Gets the width of the input and sets it inside the context for rendering the dropdown when `matchTriggerWidth` is true on the menu
    useEffect(()=>{
        // Use the DOM reference of the TypeaheadComboboxInput container div to get the width of the input
        if (floatingUiRefs?.domReference) {
            const width = floatingUiRefs.domReference.current?.getBoundingClientRect().width ?? 0;
            // Only update context width when the input width updated
            if (width !== contextInputWidth) {
                setContextInputWidth?.(width);
            }
        }
    }, [
        floatingUiRefs?.domReference,
        setContextInputWidth,
        contextInputWidth
    ]);
    // Determine whether to show tooltip
    useEffect(()=>{
        let isPartiallyHidden = false;
        if (itemsRef.current && containerRef.current) {
            const { clientHeight: innerHeight } = itemsRef.current;
            const { clientHeight: outerHeight } = containerRef.current;
            isPartiallyHidden = innerHeight > outerHeight;
        }
        setShowTooltip(!disableTooltip && (shouldShowCountBadge || isPartiallyHidden));
    }, [
        shouldShowCountBadge,
        itemsRef.current?.clientHeight,
        containerRef.current?.clientHeight,
        disableTooltip
    ]);
    const content = /*#__PURE__*/ jsxs("div", {
        ...addDebugOutlineIfEnabled(),
        onClick: handleClick,
        ref: mergedContainerRef,
        css: getContainerStyles(theme, validationState, width, maxHeight, restProps.disabled),
        tabIndex: restProps.disabled ? -1 : 0,
        children: [
            /*#__PURE__*/ jsxs("div", {
                ref: itemsRef,
                css: getContentWrapperStyles(),
                children: [
                    selectedItemsToRender?.map((selectedItemForRender, index)=>/*#__PURE__*/ jsx(TypeaheadComboboxSelectedItem, {
                            label: getSelectedItemLabel(selectedItemForRender),
                            item: selectedItemForRender,
                            getSelectedItemProps: getSelectedItemProps,
                            removeSelectedItem: removeSelectedItem,
                            disabled: restProps.disabled
                        }, `selected-item-${index}`)),
                    shouldShowCountBadge && /*#__PURE__*/ jsx(CountBadge, {
                        countStartAt: showTagAfterValueCount,
                        totalCount: selectedItems.length,
                        role: "status",
                        "aria-label": "Selected options count",
                        disabled: restProps.disabled
                    }),
                    /*#__PURE__*/ jsxs("div", {
                        css: getInputWrapperStyles(),
                        children: [
                            /*#__PURE__*/ jsx("input", {
                                ...downshiftProps,
                                ref: mergedInputRef,
                                css: [
                                    getInputStyles(theme),
                                    {
                                        width: inputWidth
                                    }
                                ],
                                placeholder: selectedItems?.length ? undefined : placeholder,
                                "aria-controls": comboboxState.isOpen ? downshiftProps['aria-controls'] : undefined,
                                ...restProps
                            }),
                            /*#__PURE__*/ jsxs("span", {
                                ref: measureRef,
                                "aria-hidden": true,
                                css: {
                                    visibility: 'hidden',
                                    whiteSpace: 'pre',
                                    position: 'absolute'
                                },
                                children: [
                                    innerRef.current?.value ? innerRef.current.value : placeholder,
                                    "\xa0"
                                ]
                            })
                        ]
                    })
                ]
            }),
            /*#__PURE__*/ jsx(TypeaheadComboboxControls, {
                getDownshiftToggleButtonProps: getToggleButtonProps,
                showComboboxToggleButton: showComboboxToggleButton,
                showClearSelectionButton: allowClear && (Boolean(inputValue) || selectedItems && selectedItems.length > 0) && !restProps.disabled,
                handleClear: handleClear,
                disabled: restProps.disabled
            })
        ]
    });
    if (showTooltip && selectedItems.length > 0) {
        return /*#__PURE__*/ jsx(Tooltip, {
            componentId: "typeahead-combobox-multi-select-input-tooltip",
            side: "right",
            content: selectedItems.map((item)=>getSelectedItemLabel(item)).join(', '),
            children: content
        });
    }
    return content;
});

const TypeaheadComboboxSectionHeader = ({ children, ...props })=>{
    const { isInsideTypeaheadCombobox } = useTypeaheadComboboxContext();
    if (!isInsideTypeaheadCombobox) {
        throw new Error('`TypeaheadComboboxSectionHeader` must be used within `TypeaheadComboboxMenu`');
    }
    return /*#__PURE__*/ jsx(SectionHeader, {
        ...props,
        children: children
    });
};

const TypeaheadComboboxSeparator = (props)=>{
    const { isInsideTypeaheadCombobox } = useTypeaheadComboboxContext();
    if (!isInsideTypeaheadCombobox) {
        throw new Error('`TypeaheadComboboxSeparator` must be used within `TypeaheadComboboxMenu`');
    }
    return /*#__PURE__*/ jsx(Separator, {
        ...props
    });
};

const TypeaheadComboboxAddButton = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ children, ...restProps }, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const { isInsideTypeaheadCombobox, componentId } = useTypeaheadComboboxContext();
    if (!isInsideTypeaheadCombobox) {
        throw new Error('`TypeaheadComboboxAddButton` must be used within `TypeaheadCombobox`');
    }
    return /*#__PURE__*/ jsx(Button, {
        ...restProps,
        // eslint-disable-next-line @databricks/no-dynamic-property-value -- Validated in the Root for the context exempt:844c1e83-84b8-41ba-95af-bae4799834aa
        componentId: `${componentId}.add_option`,
        type: "tertiary",
        onClick: (event)=>{
            event.stopPropagation();
            restProps.onClick?.(event);
        },
        onMouseUp: (event)=>{
            event.stopPropagation();
            restProps.onMouseUp?.(event);
        },
        className: "combobox-footer-add-button",
        css: {
            ...getComboboxOptionItemWrapperStyles(theme),
            .../*#__PURE__*/ css(importantify({
                width: '100%',
                padding: 0,
                display: 'flex',
                alignItems: 'center',
                borderRadius: 0,
                '&:focus': {
                    background: theme.colors.actionTertiaryBackgroundHover,
                    outline: 'none'
                }
            }))
        },
        icon: /*#__PURE__*/ jsx(PlusIcon, {}),
        ref: forwardedRef,
        children: children
    });
});

function RHFControlledInput({ name, control, rules, inputRef, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    const mergedRef = useMergeRefs([
        field.ref,
        inputRef
    ]);
    return /*#__PURE__*/ jsx(Input, {
        ...restProps,
        ...field,
        ref: mergedRef,
        value: field.value,
        defaultValue: restProps.defaultValue
    });
}
function RHFControlledPasswordInput({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    return /*#__PURE__*/ jsx(Input.Password, {
        ...restProps,
        ...field,
        value: field.value,
        defaultValue: restProps.defaultValue
    });
}
function RHFControlledTextArea({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    return /*#__PURE__*/ jsx(Input.TextArea, {
        ...restProps,
        ...field,
        value: field.value,
        defaultValue: restProps.defaultValue
    });
}
/**
 * @deprecated Use `RHFControlledSelect` instead.
 */ function RHFControlledLegacySelect({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    return /*#__PURE__*/ jsx(LegacySelect, {
        ...restProps,
        ...field,
        value: field.value,
        defaultValue: field.value
    });
}
/**
 * @deprecated This component is no longer necessary as `SimpleSelect` can be used uncontrolled by RHF.
 * Please consult the Forms Guide on go/dubois.
 */ function RHFControlledSelect({ name, control, rules, options, validationState, children, width, triggerProps, contentProps, optionProps, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    const [selectedValueLabel, setSelectedValueLabel] = useState(field.value ? field.value.label ? field.value.label : field.value : '');
    const handleOnChange = (option)=>{
        field.onChange(typeof option === 'object' ? option.value : option);
    };
    useEffect(()=>{
        if (!field.value) {
            return;
        }
        // Find the appropriate label for the selected value
        if (!options?.length && children) {
            const renderedChildren = children({
                onChange: handleOnChange
            });
            const child = (Array.isArray(renderedChildren) ? renderedChildren : Children.toArray(renderedChildren.props.children)).find((child)=>/*#__PURE__*/ React__default.isValidElement(child) && child.props.value === field.value);
            if (child) {
                if (child.props?.children !== field.value) {
                    setSelectedValueLabel(child.props.children);
                } else {
                    setSelectedValueLabel(field.value);
                }
            }
        } else if (options?.length) {
            const option = options.find((option)=>option.value === field.value);
            setSelectedValueLabel(option?.label);
        }
    // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [
        field.value
    ]);
    return /*#__PURE__*/ jsxs(Select, {
        ...restProps,
        value: field.value,
        children: [
            /*#__PURE__*/ jsx(SelectTrigger, {
                ...triggerProps,
                width: width,
                onBlur: field.onBlur,
                validationState: validationState,
                ref: field.ref,
                children: selectedValueLabel
            }),
            /*#__PURE__*/ jsx(SelectContent, {
                ...contentProps,
                side: "bottom",
                children: options && options.length > 0 ? options.map((option)=>/*#__PURE__*/ createElement(SelectOption, {
                        ...optionProps,
                        key: option.value,
                        value: option.value,
                        onChange: handleOnChange
                    }, option.label)) : // We expose onChange through a children renderer function to let users pass this down to SelectOption
                children?.({
                    onChange: handleOnChange
                })
            })
        ]
    });
}
function RHFControlledDialogCombobox({ name, control, rules, children, allowClear, validationState, placeholder, width, triggerProps, contentProps, optionListProps, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    const [valueMap, setValueMap] = useState({});
    const updateValueMap = useCallback((updatedValue)=>{
        if (updatedValue) {
            if (Array.isArray(updatedValue)) {
                setValueMap(updatedValue.reduce((acc, value)=>{
                    acc[value] = true;
                    return acc;
                }, {}));
            } else {
                setValueMap({
                    [updatedValue]: true
                });
            }
        } else {
            setValueMap({});
        }
    }, []);
    useEffect(()=>{
        updateValueMap(field.value);
    }, [
        field.value,
        updateValueMap
    ]);
    const handleOnChangeSingleSelect = (option)=>{
        let updatedValue = field.value;
        if (field.value === option) {
            updatedValue = undefined;
        } else {
            updatedValue = option;
        }
        field.onChange(updatedValue);
        updateValueMap(updatedValue);
    };
    const handleOnChangeMultiSelect = (option)=>{
        let updatedValue;
        if (field.value?.includes(option)) {
            updatedValue = field.value.filter((value)=>value !== option);
        } else if (!field.value) {
            updatedValue = [
                option
            ];
        } else {
            updatedValue = [
                ...field.value,
                option
            ];
        }
        field.onChange(updatedValue);
        updateValueMap(updatedValue);
    };
    const handleOnChange = (option)=>{
        if (restProps.multiSelect) {
            handleOnChangeMultiSelect(option);
        } else {
            handleOnChangeSingleSelect(option);
        }
    };
    const isChecked = (option)=>{
        return valueMap[option];
    };
    const handleOnClear = ()=>{
        field.onChange(Array.isArray(field.value) ? [] : '');
        setValueMap({});
    };
    return /*#__PURE__*/ jsxs(DialogCombobox, {
        ...restProps,
        value: field.value ? Array.isArray(field.value) ? field.value : [
            field.value
        ] : undefined,
        children: [
            /*#__PURE__*/ jsx(DialogComboboxTrigger, {
                ...triggerProps,
                onBlur: field.onBlur,
                allowClear: allowClear,
                validationState: validationState,
                onClear: handleOnClear,
                withInlineLabel: false,
                placeholder: placeholder,
                width: width,
                ref: field.ref
            }),
            /*#__PURE__*/ jsx(DialogComboboxContent, {
                ...contentProps,
                side: "bottom",
                width: width,
                children: /*#__PURE__*/ jsx(DialogComboboxOptionList, {
                    ...optionListProps,
                    children: children?.({
                        onChange: handleOnChange,
                        value: field.value,
                        isChecked
                    })
                })
            })
        ]
    });
}
function RHFControlledTypeaheadCombobox({ name, control, rules, allItems, itemToString, matcher, allowNewValue, children, validationState, inputProps, menuProps, onInputChange, componentId, analyticsEvents, valueHasNoPii, preventUnsetOnBlur = false, ...props }) {
    const { field } = useController({
        name,
        control,
        rules
    });
    const [items, setItems] = useState(allItems);
    const comboboxState = useComboboxState({
        allItems,
        items,
        setItems,
        itemToString,
        matcher,
        allowNewValue,
        formValue: field.value,
        formOnChange: field.onChange,
        formOnBlur: field.onBlur,
        componentId,
        analyticsEvents,
        valueHasNoPii,
        preventUnsetOnBlur
    });
    const lastEmmitedInputValue = useRef(inputProps?.value);
    useEffect(()=>{
        setItems(allItems);
    }, [
        allItems
    ]);
    useEffect(()=>{
        if (onInputChange && lastEmmitedInputValue.current !== comboboxState.inputValue) {
            onInputChange(comboboxState.inputValue);
            lastEmmitedInputValue.current = comboboxState.inputValue;
        }
    }, [
        comboboxState.inputValue,
        onInputChange
    ]);
    return /*#__PURE__*/ jsxs(TypeaheadComboboxRoot, {
        ...props,
        comboboxState: comboboxState,
        children: [
            /*#__PURE__*/ jsx(TypeaheadComboboxInput, {
                ...inputProps,
                validationState: validationState,
                formOnChange: field.onChange,
                comboboxState: comboboxState,
                ref: field.ref
            }),
            /*#__PURE__*/ jsx(TypeaheadComboboxMenu, {
                ...menuProps,
                comboboxState: comboboxState,
                children: children({
                    comboboxState,
                    items
                })
            })
        ]
    });
}
function RHFControlledMultiSelectTypeaheadCombobox({ name, control, rules, allItems, itemToString, matcher, children, validationState, inputProps, menuProps, onInputChange, componentId, analyticsEvents, valueHasNoPii, ...props }) {
    const { field } = useController({
        name,
        control,
        rules
    });
    const [inputValue, setInputValue] = useState('');
    const [selectedItems, setSelectedItems] = useState(field.value || []);
    useEffect(()=>{
        setSelectedItems(field.value || []);
    }, [
        field.value
    ]);
    const items = React__default.useMemo(()=>allItems.filter((item)=>matcher(item, inputValue)), [
        inputValue,
        matcher,
        allItems
    ]);
    const handleItemUpdate = (item)=>{
        field.onChange(item);
        setSelectedItems(item);
    };
    const comboboxState = useComboboxState({
        allItems,
        items,
        setInputValue,
        matcher,
        itemToString,
        multiSelect: true,
        selectedItems,
        setSelectedItems: handleItemUpdate,
        formValue: field.value,
        formOnChange: field.onChange,
        formOnBlur: field.onBlur,
        componentId,
        analyticsEvents,
        valueHasNoPii
    });
    const multipleSelectionState = useMultipleSelectionState(selectedItems, handleItemUpdate, comboboxState);
    const lastEmmitedInputValue = useRef(inputProps?.value);
    useEffect(()=>{
        if (onInputChange && lastEmmitedInputValue.current !== comboboxState.inputValue) {
            onInputChange(comboboxState.inputValue);
            lastEmmitedInputValue.current = comboboxState.inputValue;
        }
    }, [
        comboboxState.inputValue,
        onInputChange
    ]);
    return /*#__PURE__*/ jsxs(TypeaheadComboboxRoot, {
        ...props,
        comboboxState: comboboxState,
        children: [
            /*#__PURE__*/ jsx(TypeaheadComboboxMultiSelectInput, {
                ...inputProps,
                multipleSelectionState: multipleSelectionState,
                selectedItems: selectedItems,
                setSelectedItems: handleItemUpdate,
                getSelectedItemLabel: itemToString,
                comboboxState: comboboxState,
                validationState: validationState,
                ref: field.ref
            }),
            /*#__PURE__*/ jsx(TypeaheadComboboxMenu, {
                ...menuProps,
                comboboxState: comboboxState,
                children: children({
                    comboboxState,
                    items,
                    selectedItems
                })
            })
        ]
    });
}
function RHFControlledCheckboxGroup({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    return /*#__PURE__*/ jsx(Checkbox.Group, {
        ...restProps,
        ...field,
        value: field.value
    });
}
function RHFControlledCheckbox({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        css: {
            marginTop: theme.spacing.sm
        },
        children: /*#__PURE__*/ jsx(Checkbox, {
            ...restProps,
            ...field,
            isChecked: field.value
        })
    });
}
function RHFControlledRadioGroup({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    return /*#__PURE__*/ jsx(Radio.Group, {
        ...restProps,
        ...field
    });
}
function RHFControlledSwitch({ name, control, rules, ...restProps }) {
    const { field } = useController({
        name: name,
        control: control,
        rules: rules
    });
    return /*#__PURE__*/ jsx(Switch, {
        ...restProps,
        ...field,
        checked: field.value
    });
}
const RHFControlledComponents = {
    Input: RHFControlledInput,
    Password: RHFControlledPasswordInput,
    TextArea: RHFControlledTextArea,
    LegacySelect: RHFControlledLegacySelect,
    Select: RHFControlledSelect,
    DialogCombobox: RHFControlledDialogCombobox,
    Checkbox: RHFControlledCheckbox,
    CheckboxGroup: RHFControlledCheckboxGroup,
    RadioGroup: RHFControlledRadioGroup,
    TypeaheadCombobox: RHFControlledTypeaheadCombobox,
    MultiSelectTypeaheadCombobox: RHFControlledMultiSelectTypeaheadCombobox,
    Switch: RHFControlledSwitch
};

const getHorizontalInputStyles = (theme, labelColWidth, inputColWidth)=>{
    return /*#__PURE__*/ css({
        display: 'flex',
        gap: theme.spacing.sm,
        '& > input, & > textarea, & > select': {
            marginTop: '0 !important'
        },
        '& > div:nth-of-type(1)': {
            width: labelColWidth
        },
        '& > div:nth-of-type(2)': {
            width: inputColWidth
        }
    });
};
const HorizontalFormRow = ({ children, labelColWidth = '33%', inputColWidth = '66%', ...restProps })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        css: getHorizontalInputStyles(theme, labelColWidth, inputColWidth),
        ...restProps,
        children: children
    });
};
const FormUI = {
    Message: FormMessage,
    Label: Label,
    Hint: Hint,
    HorizontalFormRow
};

const Col = ({ dangerouslySetAntdProps, ...props })=>/*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Col$1, {
            ...props,
            ...dangerouslySetAntdProps
        })
    });

const ROW_GUTTER_SIZE = 8;
const Row = ({ gutter = ROW_GUTTER_SIZE, ...props })=>{
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Row$1, {
            gutter: gutter,
            ...props
        })
    });
};

const Space = ({ dangerouslySetAntdProps, ...props })=>{
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Space$1, {
            ...props,
            ...dangerouslySetAntdProps
        })
    });
};

const getHeaderStyles = (clsPrefix, theme)=>{
    const breadcrumbClass = `.${clsPrefix}-breadcrumb`;
    const styles = {
        [breadcrumbClass]: {
            lineHeight: theme.typography.lineHeightBase
        }
    };
    return /*#__PURE__*/ css(importantify(styles));
};
const Header = ({ breadcrumbs, title, titleAddOns, dangerouslyAppendEmotionCSS, buttons, children, titleElementLevel, allowTitleWrap = true, ...rest })=>{
    const { classNamePrefix: clsPrefix, theme } = useDesignSystemTheme();
    const buttonsArray = Array.isArray(buttons) ? buttons : buttons ? [
        buttons
    ] : [];
    // TODO: Move to getHeaderStyles for consistency, followup ticket: https://databricks.atlassian.net/browse/FEINF-1222
    const styles = {
        titleWrapper: /*#__PURE__*/ css({
            display: 'flex',
            alignItems: 'flex-start',
            justifyContent: 'space-between',
            flexWrap: allowTitleWrap ? 'wrap' : 'nowrap',
            rowGap: theme.spacing.sm,
            // Buttons have 32px height while Title level 2 elements used by this component have a height of 28px
            // These paddings enforce height to be the same without buttons too
            ...buttonsArray.length === 0 && {
                paddingTop: breadcrumbs ? 0 : theme.spacing.xs / 2,
                paddingBottom: theme.spacing.xs / 2
            }
        }),
        breadcrumbWrapper: /*#__PURE__*/ css({
            lineHeight: theme.typography.lineHeightBase,
            marginBottom: theme.spacing.xs
        }),
        title: /*#__PURE__*/ css({
            marginTop: 0,
            marginBottom: '0 !important',
            alignSelf: 'stretch',
            ...!allowTitleWrap && {
                // Allow the title to shrink if wrapping is disabled
                flex: 1,
                minWidth: 0
            }
        }),
        // TODO: Look into a more emotion-idomatic way of doing this.
        titleIfOtherElementsPresent: /*#__PURE__*/ css({
            marginTop: 2
        }),
        buttonContainer: /*#__PURE__*/ css({
            marginLeft: 8
        }),
        titleAddOnsWrapper: /*#__PURE__*/ css({
            display: 'inline-flex',
            verticalAlign: 'middle',
            alignItems: 'center',
            flexWrap: 'wrap',
            marginLeft: theme.spacing.sm,
            gap: theme.spacing.xs
        })
    };
    return /*#__PURE__*/ jsxs("div", {
        ...addDebugOutlineIfEnabled(),
        css: [
            getHeaderStyles(clsPrefix, theme),
            dangerouslyAppendEmotionCSS
        ],
        ...rest,
        children: [
            breadcrumbs && /*#__PURE__*/ jsx("div", {
                css: styles.breadcrumbWrapper,
                children: breadcrumbs
            }),
            /*#__PURE__*/ jsxs("div", {
                css: styles.titleWrapper,
                children: [
                    /*#__PURE__*/ jsxs(Title$1, {
                        level: 2,
                        elementLevel: titleElementLevel,
                        css: [
                            styles.title,
                            (buttons || breadcrumbs) && styles.titleIfOtherElementsPresent
                        ],
                        children: [
                            title,
                            titleAddOns && /*#__PURE__*/ jsx("span", {
                                css: styles.titleAddOnsWrapper,
                                children: titleAddOns
                            })
                        ]
                    }),
                    buttons && /*#__PURE__*/ jsx("div", {
                        css: styles.buttonContainer,
                        children: /*#__PURE__*/ jsx(Space, {
                            dangerouslySetAntdProps: {
                                wrap: true
                            },
                            size: 8,
                            children: buttonsArray.filter(Boolean).map((button, i)=>{
                                const defaultKey = `dubois-header-button-${i}`;
                                return /*#__PURE__*/ React__default.isValidElement(button) ? /*#__PURE__*/ React__default.cloneElement(button, {
                                    key: button.key || defaultKey
                                }) : /*#__PURE__*/ jsx(React__default.Fragment, {
                                    children: button
                                }, defaultKey);
                            })
                        })
                    })
                ]
            })
        ]
    });
};

/**
 * The HoverCard component combines Radix's HoverCard primitives into a single, easy-to-use component.
 * It handles the setup of the trigger, content, and arrow elements, as well as applying custom styles
 * using Emotion CSS
 */ const HoverCard = ({ trigger, content, side = 'top', sideOffset = 4, align = 'center', minWidth = 220, maxWidth, backgroundColor, withArrow = true, ...props })=>{
    const { getPopupContainer } = useDesignSystemContext();
    const hoverCardStyles = useHoverCardStyles({
        minWidth,
        maxWidth,
        backgroundColor
    });
    return /*#__PURE__*/ jsxs(RadixHoverCard.Root, {
        ...props,
        children: [
            /*#__PURE__*/ jsx(RadixHoverCard.Trigger, {
                asChild: true,
                children: trigger
            }),
            /*#__PURE__*/ jsx(RadixHoverCard.Portal, {
                container: getPopupContainer && getPopupContainer(),
                children: /*#__PURE__*/ jsxs(RadixHoverCard.Content, {
                    side: side,
                    sideOffset: sideOffset,
                    align: align,
                    css: hoverCardStyles['content'],
                    children: [
                        content,
                        withArrow && /*#__PURE__*/ jsx(RadixHoverCard.Arrow, {
                            css: hoverCardStyles['arrow']
                        })
                    ]
                })
            })
        ]
    });
};
// CONSTANTS used for defining the Arrow's appearance and behavior
const CONSTANTS = {
    arrowWidth: 12,
    arrowHeight: 6,
    arrowBottomLength () {
        // The built in arrow is a polygon: 0,0 30,0 15,10
        return 30;
    },
    arrowSide () {
        return 2 * (this.arrowHeight ** 2 * 2) ** 0.5;
    },
    arrowStrokeWidth () {
        // This is eyeballed b/c relative to the svg viewbox coordinate system
        return 2;
    }
};
/**
 * A custom hook to generate CSS styles for the HoverCard's content and arrow.
 * These styles are dynamically generated based on the theme and optional min/max width props.
 * The hook also applies necessary dark mode adjustments
 */ const useHoverCardStyles = ({ minWidth, maxWidth, backgroundColor })=>{
    const { theme } = useDesignSystemTheme();
    return {
        content: {
            backgroundColor: backgroundColor ?? theme.colors.backgroundPrimary,
            color: theme.colors.textPrimary,
            lineHeight: theme.typography.lineHeightBase,
            border: `1px solid ${theme.colors.border}`,
            borderRadius: theme.borders.borderRadiusSm,
            padding: `${theme.spacing.sm}px`,
            boxShadow: theme.shadows.lg,
            userSelect: 'none',
            zIndex: theme.options.zIndexBase + 30,
            minWidth,
            maxWidth,
            ...getDarkModePortalStyles(theme),
            a: importantify({
                color: theme.colors.actionTertiaryTextDefault,
                cursor: 'default',
                '&:hover, &:focus': {
                    color: theme.colors.actionTertiaryTextHover
                }
            }),
            '&:focus-visible': {
                outlineStyle: 'solid',
                outlineWidth: '2px',
                outlineOffset: '1px',
                outlineColor: theme.colors.actionDefaultBorderFocus
            }
        },
        arrow: {
            fill: backgroundColor ?? theme.colors.backgroundPrimary,
            height: CONSTANTS.arrowHeight,
            stroke: theme.colors.borderDecorative,
            strokeDashoffset: -30,
            strokeDasharray: CONSTANTS.arrowBottomLength() + 2 * CONSTANTS.arrowSide(),
            strokeWidth: CONSTANTS.arrowStrokeWidth(),
            width: CONSTANTS.arrowWidth,
            position: 'relative',
            top: -1,
            zIndex: theme.options.zIndexBase + 30
        }
    };
};

// Note: AntD only exposes context to notifications via the `useNotification` hook, and we need context to apply themes
// to AntD. As such you can currently only use notifications from within functional components.
/**
 * `useLegacyNotification` is deprecated in favor of the new `Notification` component.
 * @deprecated
 */ function useLegacyNotification() {
    const [notificationInstance, contextHolder] = notification.useNotification();
    const { getPrefixedClassName, theme } = useDesignSystemTheme();
    const { getPopupContainer: getContainer } = useDesignSystemContext();
    const clsPrefix = getPrefixedClassName('notification');
    const open = useCallback((args)=>{
        const mergedArgs = {
            getContainer,
            ...defaultProps,
            ...args,
            style: {
                zIndex: theme.options.zIndexBase + 30,
                boxShadow: theme.general.shadowLow
            }
        };
        const iconClassName = `${clsPrefix}-notice-icon-${mergedArgs.type}`;
        mergedArgs.icon = /*#__PURE__*/ jsx(SeverityIcon, {
            severity: mergedArgs.type,
            className: iconClassName
        });
        mergedArgs.closeIcon = /*#__PURE__*/ jsx(CloseIcon, {
            "aria-hidden": "false",
            css: {
                cursor: 'pointer',
                fontSize: theme.general.iconSize
            },
            "aria-label": mergedArgs.closeLabel || 'Close notification'
        });
        notificationInstance.open(mergedArgs);
    }, [
        notificationInstance,
        getContainer,
        theme,
        clsPrefix
    ]);
    const wrappedNotificationAPI = useMemo(()=>{
        const error = (args)=>open({
                ...args,
                type: 'error'
            });
        const warning = (args)=>open({
                ...args,
                type: 'warning'
            });
        const info = (args)=>open({
                ...args,
                type: 'info'
            });
        const success = (args)=>open({
                ...args,
                type: 'success'
            });
        const close = (key)=>notification.close(key);
        return {
            open,
            close,
            error,
            warning,
            info,
            success
        };
    }, [
        open
    ]);
    // eslint-disable-next-line react/jsx-key -- TODO(FEINF-1756)
    return [
        wrappedNotificationAPI,
        /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: contextHolder
        })
    ];
}
const defaultProps = {
    type: 'info',
    duration: 3
};
/**
 * A higher-order component factory function, enables using notifications in
 * class components in a similar way to useNotification() hook. Wrapped component will have
 * additional "notificationAPI" and "notificationContextHolder" props injected containing
 * the notification API object and context holder react node respectively.
 *
 * The wrapped component can implement WithNotificationsHOCProps<OwnProps> type which
 * enriches the component's interface with the mentioned props.
 *
 * @deprecated Please migrate components to functional components and use useNotification() hook instead.
 */ const withNotifications = (Component)=>// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
    /*#__PURE__*/ forwardRef((props, ref)=>{
        const [notificationAPI, notificationContextHolder] = useLegacyNotification();
        return /*#__PURE__*/ jsx(Component, {
            ref: ref,
            notificationAPI: notificationAPI,
            notificationContextHolder: notificationContextHolder,
            ...props
        });
    });

/**
 * `LegacyPopover` is deprecated in favor of the new `Popover` component.
 * @deprecated
 */ const LegacyPopover = ({ content, dangerouslySetAntdProps, ...props })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Popover$1, {
            zIndex: theme.options.zIndexBase + 30,
            ...props,
            content: /*#__PURE__*/ jsx(RestoreAntDDefaultClsPrefix, {
                children: content
            })
        })
    });
};

/** @deprecated This component is deprecated. Use ParagraphSkeleton, TitleSkeleton, or GenericSkeleton instead. */ const LegacySkeleton = /* #__PURE__ */ (()=>{
    const LegacySkeleton = ({ dangerouslySetAntdProps, label, loadingDescription = 'LegacySkeleton', ...props })=>{
        // There is a conflict in how the 'loading' prop is handled here, so we can't default it to true in
        // props destructuring above like we do for 'loadingDescription'. The 'loading' param is used both
        // for <LoadingState> and in <AntDSkeleton>. The intent is for 'loading' to default to true in
        // <LoadingState>, but if we do that, <AntDSkeleton> will not render the children. The workaround
        // here is to default 'loading' to true only when considering whether to render a <LoadingState>.
        // Also, AntDSkeleton looks at the presence of 'loading' in props, so We cannot explicitly destructure
        // 'loading' in the constructor since we would no longer be able to differentiate between it not being
        // passed in at all and being passed undefined.
        const loadingStateLoading = props.loading ?? true;
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsxs(AccessibleContainer, {
                label: label,
                children: [
                    loadingStateLoading && /*#__PURE__*/ jsx(LoadingState, {
                        description: loadingDescription
                    }),
                    /*#__PURE__*/ jsx(Skeleton, {
                        ...props,
                        ...dangerouslySetAntdProps
                    })
                ]
            })
        });
    };
    LegacySkeleton.Button = Skeleton.Button;
    LegacySkeleton.Image = Skeleton.Image;
    LegacySkeleton.Input = Skeleton.Input;
    return LegacySkeleton;
})();

function getPaginationEmotionStyles(clsPrefix, theme) {
    const classRoot = `.${clsPrefix}-pagination`;
    const classItem = `.${clsPrefix}-pagination-item`;
    const classLink = `.${clsPrefix}-pagination-item-link`;
    const classActive = `.${clsPrefix}-pagination-item-active`;
    const classEllipsis = `.${clsPrefix}-pagination-item-ellipsis`;
    const classNext = `.${clsPrefix}-pagination-next`;
    const classPrev = `.${clsPrefix}-pagination-prev`;
    const classJumpNext = `.${clsPrefix}-pagination-jump-next`;
    const classJumpPrev = `.${clsPrefix}-pagination-jump-prev`;
    const classQuickJumper = `.${clsPrefix}-pagination-options-quick-jumper`;
    const classSizeChanger = `.${clsPrefix}-pagination-options-size-changer`;
    const classOptions = `.${clsPrefix}-pagination-options`;
    const classDisabled = `.${clsPrefix}-pagination-disabled`;
    const classSelector = `.${clsPrefix}-select-selector`;
    const classDropdown = `.${clsPrefix}-select-dropdown`;
    const styles = {
        'span[role=img]': {
            color: theme.colors.textSecondary,
            '> *': {
                color: 'inherit'
            }
        },
        [classItem]: {
            backgroundColor: 'none',
            border: 'none',
            color: theme.colors.textSecondary,
            '&:focus-visible': {
                outline: 'auto'
            },
            '> a': {
                color: theme.colors.textSecondary,
                textDecoration: 'none',
                '&:hover': {
                    color: theme.colors.actionDefaultTextHover
                },
                '&:active': {
                    color: theme.colors.actionDefaultTextPress
                }
            },
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress
            }
        },
        [classActive]: {
            backgroundColor: theme.colors.actionDefaultBackgroundPress,
            color: theme.colors.actionDefaultTextPress,
            border: 'none',
            '> a': {
                color: theme.colors.actionDefaultTextPress
            },
            '&:focus-visible': {
                outline: 'auto'
            },
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress,
                color: theme.colors.actionDefaultTextPress
            }
        },
        [classLink]: {
            border: 'none',
            color: theme.colors.textSecondary,
            '&[disabled]': {
                display: 'none'
            },
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress
            },
            '&:focus-visible': {
                outline: 'auto'
            }
        },
        [classEllipsis]: {
            color: 'inherit'
        },
        [`${classNext}, ${classPrev}, ${classJumpNext}, ${classJumpPrev}`]: {
            color: theme.colors.textSecondary,
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress
            },
            '&:focus-visible': {
                outline: 'auto'
            },
            [`&${classDisabled}`]: {
                pointerEvents: 'none'
            }
        },
        [`&${classRoot}.mini, ${classRoot}.mini`]: {
            [`${classItem}, ${classNext}, ${classPrev}, ${classJumpNext}, ${classJumpPrev}`]: {
                height: '32px',
                minWidth: '32px',
                width: 'auto',
                lineHeight: '32px'
            },
            [classSizeChanger]: {
                marginLeft: 4
            },
            [`input,  ${classOptions}`]: {
                height: '32px'
            },
            [`${classSelector}`]: {
                boxShadow: theme.shadows.xs,
                borderColor: theme.colors.actionDefaultBorderDefault
            }
        },
        [`${classQuickJumper} > input`]: {
            borderColor: theme.colors.actionDefaultBorderDefault
        },
        [`${classDropdown}`]: {
            borderColor: theme.colors.actionDefaultBorderDefault
        }
    };
    const importantStyles = importantify(styles);
    return /*#__PURE__*/ css(importantStyles);
}
const Pagination = function Pagination({ currentPageIndex, pageSize = 10, numTotal, onChange, style, hideOnSinglePage, dangerouslySetAntdProps, componentId, analyticsEvents }) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.pagination', false);
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const { pageSizeSelectAriaLabel, pageQuickJumperAriaLabel, ...restDangerouslySetAntdProps } = dangerouslySetAntdProps ?? {};
    const ref = useRef(null);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Pagination,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: true
    });
    const onChangeWrapper = useCallback((pageIndex, pageSize)=>{
        eventContext.onValueChange(pageIndex);
        onChange(pageIndex, pageSize);
    }, [
        eventContext,
        onChange
    ]);
    const { elementRef: paginationRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    const mergedRef = useMergeRefs([
        ref,
        paginationRef
    ]);
    useEffect(()=>{
        if (ref && ref.current) {
            const selectDropdown = ref.current.querySelector(`.${classNamePrefix}-select-selection-search-input`);
            if (selectDropdown) {
                selectDropdown.setAttribute('aria-label', pageSizeSelectAriaLabel ?? 'Select page size');
            }
            const pageQuickJumper = ref.current.querySelector(`.${classNamePrefix}-pagination-options-quick-jumper > input`);
            if (pageQuickJumper) {
                pageQuickJumper.setAttribute('aria-label', pageQuickJumperAriaLabel ?? 'Go to page');
            }
        }
    }, [
        pageQuickJumperAriaLabel,
        pageSizeSelectAriaLabel,
        classNamePrefix
    ]);
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx("div", {
            ref: mergedRef,
            children: /*#__PURE__*/ jsx(Pagination$1, {
                ...addDebugOutlineIfEnabled(),
                css: getPaginationEmotionStyles(classNamePrefix, theme),
                current: currentPageIndex,
                pageSize: pageSize,
                responsive: false,
                total: numTotal,
                onChange: onChangeWrapper,
                showSizeChanger: false,
                showQuickJumper: false,
                size: "small",
                style: style,
                hideOnSinglePage: hideOnSinglePage,
                ...restDangerouslySetAntdProps,
                ...eventContext.dataComponentProps
            })
        })
    });
};
const CursorPagination = function CursorPagination({ onNextPage, onPreviousPage, hasNextPage, hasPreviousPage, nextPageText = 'Next', previousPageText = 'Previous', pageSizeSelect: { options: pageSizeOptions, default: defaultPageSize, getOptionText: getPageSizeOptionText, onChange: onPageSizeChange, ariaLabel = 'Select page size' } = {}, componentId = 'design_system.cursor_pagination', analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
], valueHasNoPii }) {
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const [pageSizeValue, setPageSizeValue] = useState(defaultPageSize);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const pageSizeEventComponentId = `${componentId}.page_size`;
    const pageSizeEventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.LegacySelect,
        componentId: pageSizeEventComponentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const getPageSizeOptionTextDefault = (pageSize)=>`${pageSize} / page`;
    return /*#__PURE__*/ jsxs("div", {
        css: {
            display: 'flex',
            flexDirection: 'row',
            gap: theme.spacing.sm,
            [`.${classNamePrefix}-select-selector::after`]: {
                content: 'none'
            }
        },
        ...pageSizeEventContext.dataComponentProps,
        children: [
            /*#__PURE__*/ jsx(Button, {
                componentId: `${componentId}.previous_page`,
                icon: /*#__PURE__*/ jsx(ChevronLeftIcon, {}),
                disabled: !hasPreviousPage,
                onClick: onPreviousPage,
                type: "tertiary",
                children: previousPageText
            }),
            /*#__PURE__*/ jsx(Button, {
                componentId: `${componentId}.next_page`,
                endIcon: /*#__PURE__*/ jsx(ChevronRightIcon, {}),
                disabled: !hasNextPage,
                onClick: onNextPage,
                type: "tertiary",
                children: nextPageText
            }),
            pageSizeOptions && /*#__PURE__*/ jsx(LegacySelect, {
                "aria-label": ariaLabel,
                value: String(pageSizeValue),
                css: {
                    width: 120
                },
                onChange: (pageSize)=>{
                    const updatedPageSize = Number(pageSize);
                    onPageSizeChange?.(updatedPageSize);
                    setPageSizeValue(updatedPageSize);
                    // When this usage of LegacySelect is migrated to Select, this call
                    // can be removed in favor of passing a componentId to Select
                    pageSizeEventContext.onValueChange(pageSize);
                },
                children: pageSizeOptions.map((pageSize)=>/*#__PURE__*/ jsx(LegacySelect.Option, {
                        value: String(pageSize),
                        children: (getPageSizeOptionText || getPageSizeOptionTextDefault)(pageSize)
                    }, pageSize))
            })
        ]
    });
};

const getTableEmotionStyles = (classNamePrefix, theme, scrollableInFlexibleContainer)=>{
    const styles = [
        /*#__PURE__*/ css({
            [`.${classNamePrefix}-table-pagination`]: {
                ...getPaginationEmotionStyles(classNamePrefix, theme)
            }
        })
    ];
    if (scrollableInFlexibleContainer) {
        styles.push(getScrollableInFlexibleContainerStyles(classNamePrefix));
    }
    return styles;
};
const getScrollableInFlexibleContainerStyles = (clsPrefix)=>{
    const styles = {
        minHeight: 0,
        [`.${clsPrefix}-spin-nested-loading`]: {
            height: '100%'
        },
        [`.${clsPrefix}-spin-container`]: {
            height: '100%',
            display: 'flex',
            flexDirection: 'column'
        },
        [`.${clsPrefix}-table-container`]: {
            height: '100%',
            display: 'flex',
            flexDirection: 'column'
        },
        [`.${clsPrefix}-table`]: {
            minHeight: 0
        },
        [`.${clsPrefix}-table-header`]: {
            flexShrink: 0
        },
        [`.${clsPrefix}-table-body`]: {
            minHeight: 0
        }
    };
    return /*#__PURE__*/ css(styles);
};
const DEFAULT_LOADING_SPIN_PROPS = {
    indicator: /*#__PURE__*/ jsx(Spinner, {})
};
/**
 * `LegacyTable` is deprecated in favor of the new `Table` component
 * For more information please join #dubois-table-early-adopters in Slack.
 * @deprecated
 */ // eslint-disable-next-line @typescript-eslint/no-restricted-types
const LegacyTable = (props)=>{
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const { loading, scrollableInFlexibleContainer, children, ...tableProps } = props;
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Table$1, {
            ...addDebugOutlineIfEnabled(),
            // NOTE(FEINF-1273): The default loading indicator from AntD does not animate
            // and the design system spinner is recommended over the AntD one. Therefore,
            // if `loading` is `true`, render the design system <Spinner/> component.
            loading: loading === true ? DEFAULT_LOADING_SPIN_PROPS : loading,
            scroll: scrollableInFlexibleContainer ? {
                y: 'auto'
            } : undefined,
            ...tableProps,
            css: getTableEmotionStyles(classNamePrefix, theme, Boolean(scrollableInFlexibleContainer)),
            // ES-902549 this allows column names of "children", using a name that is less likely to be hit
            expandable: {
                childrenColumnName: '__antdChildren'
            },
            children: /*#__PURE__*/ jsx(RestoreAntDDefaultClsPrefix, {
                children: children
            })
        })
    });
};

const getLegacyTabEmotionStyles = (clsPrefix, theme)=>{
    const classTab = `.${clsPrefix}-tabs-tab`;
    const classButton = `.${clsPrefix}-tabs-tab-btn`;
    const classActive = `.${clsPrefix}-tabs-tab-active`;
    const classDisabled = `.${clsPrefix}-tabs-tab-disabled`;
    const classUnderline = `.${clsPrefix}-tabs-ink-bar`;
    const classClosable = `.${clsPrefix}-tabs-tab-with-remove`;
    const classNav = `.${clsPrefix}-tabs-nav`;
    const classCloseButton = `.${clsPrefix}-tabs-tab-remove`;
    const classAddButton = `.${clsPrefix}-tabs-nav-add`;
    const styles = {
        '&&': {
            overflow: 'unset'
        },
        [classTab]: {
            borderBottom: 'none',
            backgroundColor: 'transparent',
            border: 'none',
            paddingLeft: 0,
            paddingRight: 0,
            paddingTop: 6,
            paddingBottom: 6,
            marginRight: 24
        },
        [classButton]: {
            color: theme.colors.textSecondary,
            fontWeight: theme.typography.typographyBoldFontWeight,
            textShadow: 'none',
            fontSize: theme.typography.fontSizeMd,
            lineHeight: theme.typography.lineHeightBase,
            '&:hover': {
                color: theme.colors.actionDefaultTextHover
            },
            '&:active': {
                color: theme.colors.actionDefaultTextPress
            },
            outlineWidth: 2,
            outlineStyle: 'none',
            outlineColor: theme.colors.actionDefaultBorderFocus,
            outlineOffset: 2,
            '&:focus-visible': {
                outlineStyle: 'auto'
            }
        },
        [classActive]: {
            [classButton]: {
                color: theme.colors.textPrimary
            },
            // Use box-shadow instead of border to prevent it from affecting the size of the element, which results in visual
            // jumping when switching tabs.
            boxShadow: `inset 0 -3px 0 ${theme.colors.actionPrimaryBackgroundDefault}`
        },
        [classDisabled]: {
            [classButton]: {
                color: theme.colors.actionDisabledText,
                '&:hover': {
                    color: theme.colors.actionDisabledText
                },
                '&:active': {
                    color: theme.colors.actionDisabledText
                }
            }
        },
        [classUnderline]: {
            display: 'none'
        },
        [classClosable]: {
            borderTop: 'none',
            borderLeft: 'none',
            borderRight: 'none',
            background: 'none',
            paddingTop: 0,
            paddingBottom: 0,
            height: theme.general.heightSm
        },
        [classNav]: {
            height: theme.general.heightSm,
            '&::before': {
                borderColor: theme.colors.actionDefaultBorderDefault
            }
        },
        [classCloseButton]: {
            height: 24,
            width: 24,
            padding: 6,
            borderRadius: theme.legacyBorders.borderRadiusMd,
            marginTop: 0,
            marginRight: 0,
            marginBottom: 0,
            marginLeft: 4,
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover,
                color: theme.colors.actionDefaultTextHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress,
                color: theme.colors.actionDefaultTextPress
            },
            '&:focus-visible': {
                outlineWidth: 2,
                outlineStyle: 'solid',
                outlineColor: theme.colors.actionDefaultBorderFocus
            }
        },
        [classAddButton]: {
            backgroundColor: 'transparent',
            color: theme.colors.textValidationInfo,
            border: 'none',
            borderRadius: theme.legacyBorders.borderRadiusMd,
            margin: 4,
            height: 24,
            width: 24,
            padding: 0,
            minWidth: 'auto',
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover,
                color: theme.colors.actionDefaultTextHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress,
                color: theme.colors.actionDefaultTextPress
            },
            '&:focus-visible': {
                outlineWidth: 2,
                outlineStyle: 'solid',
                outlineColor: theme.colors.actionDefaultBorderFocus
            },
            '& > .anticon': {
                fontSize: 16
            }
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    const importantStyles = importantify(styles);
    return importantStyles;
};
/**
 * `LegacyTabs` is deprecated in favor of the new `Tabs` component
 * @deprecated
 */ const LegacyTabPane = ({ children, ...props })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Tabs.TabPane, {
            closeIcon: /*#__PURE__*/ jsx(CloseIcon, {
                css: {
                    fontSize: theme.general.iconSize
                }
            }),
            ...props,
            ...props.dangerouslySetAntdProps,
            children: /*#__PURE__*/ jsx(RestoreAntDDefaultClsPrefix, {
                children: children
            })
        })
    });
};
/**
 * `LegacyTabs` is deprecated in favor of the new `Tabs` component
 * @deprecated
 */ const LegacyTabs = /* #__PURE__ */ (()=>{
    const LegacyTabs = ({ editable = false, activeKey, defaultActiveKey, onChange, onEdit, children, destroyInactiveTabPane = false, dangerouslySetAntdProps = {}, dangerouslyAppendEmotionCSS = {}, ...props })=>{
        const { theme, classNamePrefix } = useDesignSystemTheme();
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsx(Tabs, {
                ...addDebugOutlineIfEnabled(),
                activeKey: activeKey,
                defaultActiveKey: defaultActiveKey,
                onChange: onChange,
                onEdit: onEdit,
                destroyInactiveTabPane: destroyInactiveTabPane,
                type: editable ? 'editable-card' : 'card',
                addIcon: /*#__PURE__*/ jsx(PlusIcon, {
                    css: {
                        fontSize: theme.general.iconSize
                    }
                }),
                css: [
                    getLegacyTabEmotionStyles(classNamePrefix, theme),
                    importantify(dangerouslyAppendEmotionCSS)
                ],
                ...dangerouslySetAntdProps,
                ...props,
                children: children
            })
        });
    };
    LegacyTabs.TabPane = LegacyTabPane;
    return LegacyTabs;
})();

const ListboxContext = /*#__PURE__*/ createContext(null);
const useListboxContext = ()=>{
    const context = useContext(ListboxContext);
    if (!context) {
        throw new Error('useListboxContext must be used within a ListboxProvider');
    }
    return context;
};
const ListboxRoot = ({ children, className, onSelect, initialSelectedValue, listBoxDivRef })=>{
    const [selectedValue, setSelectedValue] = useState(initialSelectedValue);
    const [highlightedValue, setHighlightedValue] = useState();
    // Generate stable unique ID on first render (better for SSR than Math.random())
    const idRef = useRef();
    if (!idRef.current) {
        idRef.current = `listbox-${generateUuidV4()}`;
    }
    const listboxId = idRef.current;
    const getContentOptions = (element)=>{
        const options = element?.querySelectorAll('[role="option"], [role="link"]');
        return options ? Array.from(options) : undefined;
    };
    const handleKeyNavigation = useCallback((event, options)=>{
        const currentIndex = options.findIndex((option)=>option.value === highlightedValue);
        let nextIndex = currentIndex;
        switch(event.key){
            case 'ArrowDown':
                event.preventDefault();
                nextIndex = currentIndex < options.length - 1 ? currentIndex + 1 : 0;
                break;
            case 'ArrowUp':
                event.preventDefault();
                nextIndex = currentIndex > 0 ? currentIndex - 1 : options.length - 1;
                break;
            case 'Home':
                event.preventDefault();
                nextIndex = 0;
                break;
            case 'End':
                event.preventDefault();
                nextIndex = options.length - 1;
                break;
            case 'Enter':
            case ' ':
                event.preventDefault();
                if (highlightedValue !== undefined) {
                    onSelect?.(highlightedValue);
                    if (options[currentIndex].href) {
                        window.open(options[currentIndex].href, '_blank');
                    } else {
                        setSelectedValue(highlightedValue);
                    }
                }
                break;
            default:
                return;
        }
        if (nextIndex !== currentIndex && listBoxDivRef?.current) {
            setHighlightedValue(options[nextIndex].value);
            const optionsList = getContentOptions(listBoxDivRef?.current);
            if (optionsList && optionsList[nextIndex]) {
                const nextOption = optionsList[nextIndex];
                nextOption.scrollIntoView?.({
                    block: 'center'
                });
                // Update aria-activedescendant immediately for screen reader announcement.
                // This is imperative DOM manipulation to ensure the screen reader announces
                // the highlighted option instantly during keyboard navigation, before React re-renders.
                // Matches DialogCombobox's highlightOption function pattern.
                const input = listBoxDivRef.current.querySelector('input[type="text"], input[type="search"]');
                const listbox = listBoxDivRef.current.querySelector('[role="listbox"]');
                const activeElement = input || listbox;
                if (activeElement && nextOption.id) {
                    activeElement.setAttribute('aria-activedescendant', nextOption.id);
                }
            }
        }
    }, [
        highlightedValue,
        onSelect,
        listBoxDivRef
    ]);
    const contextValue = useMemo(()=>({
            selectedValue,
            setSelectedValue,
            highlightedValue,
            setHighlightedValue,
            listboxId,
            handleKeyNavigation
        }), [
        selectedValue,
        highlightedValue,
        listboxId,
        handleKeyNavigation
    ]);
    return /*#__PURE__*/ jsx(ListboxContext.Provider, {
        value: contextValue,
        children: /*#__PURE__*/ jsx("div", {
            className: className,
            children: children
        })
    });
};

const ListboxInput = ({ value, onChange, placeholder, 'aria-controls': ariaControls, 'aria-activedescendant': ariaActiveDescendant, className, options })=>{
    const { handleKeyNavigation } = useListboxContext();
    const designSystemTheme = useDesignSystemTheme();
    const handleChange = useCallback((event)=>{
        onChange(event.target.value);
    }, [
        onChange
    ]);
    const handleKeyDown = useCallback((event)=>{
        // Only handle navigation keys if there are options
        if (options.length > 0 && [
            'ArrowDown',
            'ArrowUp',
            'Home',
            'End',
            'Enter'
        ].includes(event.key)) {
            handleKeyNavigation(event, options);
        }
    }, [
        handleKeyNavigation,
        options
    ]);
    return /*#__PURE__*/ jsx("div", {
        css: {
            position: 'sticky',
            top: 0,
            background: designSystemTheme.theme.colors.backgroundPrimary,
            zIndex: designSystemTheme.theme.options.zIndexBase + 1
        },
        children: /*#__PURE__*/ jsx(Input, {
            componentId: "listbox-filter-input",
            role: "combobox",
            "aria-controls": ariaControls,
            "aria-activedescendant": ariaActiveDescendant,
            "aria-expanded": "true",
            "aria-autocomplete": "list",
            value: value,
            onChange: handleChange,
            onKeyDown: handleKeyDown,
            placeholder: placeholder,
            prefix: /*#__PURE__*/ jsx(SearchIcon, {}),
            className: className,
            allowClear: true
        })
    });
};

const ListboxOptions = ({ options, onSelect, onHighlight, className })=>{
    const theme = useTheme();
    const { listboxId, selectedValue, setSelectedValue, highlightedValue, handleKeyNavigation } = useListboxContext();
    const listboxRef = useRef(null);
    const handleKeyDown = useCallback((event)=>{
        handleKeyNavigation(event, options);
    }, [
        handleKeyNavigation,
        options
    ]);
    const handleClick = useCallback((event, option)=>{
        onSelect?.(option.value);
        if (option.href) {
            event.preventDefault();
            window.open(option.href, '_blank');
        } else {
            setSelectedValue(option.value);
        }
    }, [
        setSelectedValue,
        onSelect
    ]);
    useEffect(()=>{
        // If no option is highlighted, highlight the first one
        if (!highlightedValue && options.length > 0) {
            onHighlight(options[0].value);
        }
    }, [
        highlightedValue,
        onHighlight,
        options
    ]);
    return /*#__PURE__*/ jsx("div", {
        ref: listboxRef,
        role: "listbox",
        id: listboxId,
        className: className,
        tabIndex: 0,
        onKeyDown: handleKeyDown,
        "aria-activedescendant": highlightedValue ? `${listboxId}-${highlightedValue}` : undefined,
        css: /*#__PURE__*/ css({
            outline: 'none',
            '&:focus-visible': {
                boxShadow: `0 0 0 2px ${theme.colors.actionDefaultBorderFocus}`,
                borderRadius: theme.borders.borderRadiusSm
            }
        }),
        children: options.map((option)=>(option.renderOption || ((additionalProps)=>/*#__PURE__*/ jsx("div", {
                    ...additionalProps,
                    children: option.label
                })))({
                key: option.value,
                role: option.href ? 'link' : 'option',
                id: `${listboxId}-${option.value}`,
                'aria-selected': option.value === selectedValue,
                onClick: (event)=>handleClick(event, option),
                onMouseEnter: ()=>onHighlight(option.value),
                'data-highlighted': option.value === highlightedValue,
                css: [
                    getComboboxOptionItemWrapperStyles(theme),
                    {
                        '&:focus': {
                            outline: 'none'
                        }
                    }
                ],
                href: option.href,
                tabIndex: -1
            }))
    });
};

const ListboxContent = ({ options, filterValue, setFilterValue, filterInputPlaceholder, onSelect, ariaLabel, includeFilterInput, filterInputEmptyMessage, listBoxDivRef })=>{
    const [highlightedValue, setHighlightedValue] = useState();
    const { listboxId } = useListboxContext();
    const designSystemTheme = useDesignSystemTheme();
    const noResultsId = useMemo(()=>`${listboxId}-no-results`, [
        listboxId
    ]);
    const filteredOptions = useMemo(()=>{
        if (!filterValue) return options;
        const lowerFilter = filterValue.toLowerCase();
        return options.filter((option)=>option.value.toLowerCase().includes(lowerFilter) || option.label.toLowerCase().includes(lowerFilter));
    }, [
        filterValue,
        options
    ]);
    // Update aria-activedescendant to announce results/no-results to screen readers.
    // Using direct DOM manipulation (imperative) rather than React props because:
    // 1. Ensures immediate updates that screen readers can detect during keyboard navigation
    // 2. Provides single source of truth (this + ListboxRoot keyboard handler)
    // 3. Matches DialogCombobox pattern for reliable accessibility
    React__default.useEffect(()=>{
        if (!listBoxDivRef?.current) {
            return;
        }
        // Try to find input first (if filter is enabled), fallback to listbox
        const input = listBoxDivRef.current.querySelector('input[type="text"], input[type="search"]');
        const listbox = listBoxDivRef.current.querySelector('[role="listbox"]');
        const activeElement = input || listbox;
        if (!activeElement) {
            return;
        }
        if (filteredOptions.length === 0) {
            // Point to the no results div so screen readers announce it
            activeElement.setAttribute('aria-activedescendant', noResultsId);
        } else if (highlightedValue) {
            // Point to the highlighted option (when navigating)
            activeElement.setAttribute('aria-activedescendant', `${listboxId}-${highlightedValue}`);
        } else {
            // Clear when no highlight and results exist
            activeElement.removeAttribute('aria-activedescendant');
        }
    }, [
        filteredOptions.length,
        highlightedValue,
        listboxId,
        noResultsId,
        listBoxDivRef
    ]);
    return /*#__PURE__*/ jsxs("div", {
        css: /*#__PURE__*/ css({
            display: 'flex',
            flexDirection: 'column',
            gap: '8px'
        }),
        ref: listBoxDivRef,
        children: [
            includeFilterInput && /*#__PURE__*/ jsx(ListboxInput, {
                value: filterValue,
                onChange: setFilterValue,
                placeholder: filterInputPlaceholder,
                "aria-controls": listboxId,
                options: filteredOptions
            }),
            filteredOptions.length > 0 ? /*#__PURE__*/ jsx("div", {
                "aria-live": "polite",
                css: {
                    width: '100%'
                },
                children: /*#__PURE__*/ jsx(ListboxOptions, {
                    options: filteredOptions,
                    onSelect: onSelect,
                    onHighlight: setHighlightedValue,
                    "aria-label": ariaLabel
                })
            }) : /*#__PURE__*/ jsx("div", {
                id: noResultsId,
                css: {
                    color: designSystemTheme.theme.colors.textSecondary,
                    textAlign: 'center',
                    padding: '6px 12px',
                    width: '100%',
                    boxSizing: 'border-box'
                },
                children: filterInputEmptyMessage ?? 'No results found'
            })
        ]
    });
};
const Listbox = ({ options, onSelect, includeFilterInput, filterInputEmptyMessage, initialSelectedValue, filterInputPlaceholder, 'aria-label': ariaLabel, componentId, analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
], valueHasNoPii, className })=>{
    const [filterValue, setFilterValue] = useState('');
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Listbox,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const handleSelect = useCallback((value)=>{
        eventContext.onValueChange(value);
        onSelect?.(value);
    }, [
        eventContext,
        onSelect
    ]);
    const { elementRef: listBoxDivRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: initialSelectedValue
    });
    return /*#__PURE__*/ jsx(ListboxRoot, {
        className: className,
        onSelect: handleSelect,
        initialSelectedValue: initialSelectedValue,
        listBoxDivRef: listBoxDivRef,
        children: /*#__PURE__*/ jsx(ListboxContent, {
            options: options,
            filterValue: filterValue,
            setFilterValue: setFilterValue,
            filterInputPlaceholder: filterInputPlaceholder,
            onSelect: handleSelect,
            ariaLabel: ariaLabel,
            includeFilterInput: includeFilterInput,
            filterInputEmptyMessage: filterInputEmptyMessage,
            listBoxDivRef: listBoxDivRef
        })
    });
};

/**
 * @deprecated Use `DropdownMenu` instead.
 */ const Menu = /* #__PURE__ */ (()=>{
    const Menu = ({ dangerouslySetAntdProps, ...props })=>{
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsx(Menu$1, {
                ...addDebugOutlineIfEnabled(),
                ...props,
                ...dangerouslySetAntdProps
            })
        });
    };
    Menu.Item = Menu$1.Item;
    Menu.ItemGroup = Menu$1.ItemGroup;
    Menu.SubMenu = function SubMenu({ dangerouslySetAntdProps, ...props }) {
        const { theme } = useDesignSystemTheme();
        return /*#__PURE__*/ jsx(ClassNames, {
            children: ({ css })=>{
                return /*#__PURE__*/ jsx(Menu$1.SubMenu, {
                    ...addDebugOutlineIfEnabled(),
                    popupClassName: css({
                        zIndex: theme.options.zIndexBase + 50
                    }),
                    popupOffset: [
                        -6,
                        -10
                    ],
                    ...props,
                    ...dangerouslySetAntdProps
                });
            }
        });
    };
    return Menu;
})();

// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Root$4 = /*#__PURE__*/ React__default.forwardRef((props, forwardedRef)=>{
    return /*#__PURE__*/ jsx(RadixNavigationMenu.Root, {
        ...props,
        ref: forwardedRef
    });
});
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const List = /*#__PURE__*/ React__default.forwardRef((props, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const commonTabsListStyles = getCommonTabsListStyles(theme);
    return /*#__PURE__*/ jsx(RadixNavigationMenu.List, {
        css: {
            ...commonTabsListStyles,
            marginTop: 0,
            padding: 0,
            overflow: 'auto hidden',
            listStyle: 'none'
        },
        ...props,
        ref: forwardedRef
    });
});
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Item$1 = /*#__PURE__*/ React__default.forwardRef(({ children, active, ...props }, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const commonTabsTriggerStyles = getCommonTabsTriggerStyles(theme);
    return /*#__PURE__*/ jsx(RadixNavigationMenu.Item, {
        css: {
            ...commonTabsTriggerStyles,
            height: theme.general.heightSm,
            minWidth: theme.spacing.lg,
            justifyContent: 'center',
            ...active && {
                // Use box-shadow instead of border to prevent it from affecting the size of the element, which results in visual
                // jumping when switching tabs.
                boxShadow: `inset 0 -4px 0 ${theme.colors.actionPrimaryBackgroundDefault}`
            }
        },
        ...props,
        ref: forwardedRef,
        children: /*#__PURE__*/ jsx(RadixNavigationMenu.Link, {
            asChild: true,
            active: active,
            css: {
                padding: `${theme.spacing.xs}px 0 ${theme.spacing.sm}px 0`,
                '&:focus': {
                    outline: `2px auto ${theme.colors.actionDefaultBorderFocus}`,
                    outlineOffset: '-1px'
                },
                '&&': {
                    color: active ? theme.colors.textPrimary : theme.colors.textSecondary,
                    textDecoration: 'none',
                    '&:hover': {
                        color: active ? theme.colors.textPrimary : theme.colors.actionDefaultTextHover,
                        textDecoration: 'none'
                    },
                    '&:focus': {
                        textDecoration: 'none'
                    },
                    '&:active': {
                        color: active ? theme.colors.textPrimary : theme.colors.actionDefaultTextPress
                    }
                }
            },
            children: children
        })
    });
});

var NavigationMenu = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Item: Item$1,
  List: List,
  Root: Root$4
});

const noopSwipeHandler = (event)=>event.preventDefault();
/**
 * Disables "swipe-to-close" gesture if the safex flag is enabled
 */ const preventSwipeToCloseIfEnabled = ()=>{
    return safex('databricks.fe.designsystem.notification.disableSwipeToClose', false) ? {
        onSwipeMove: noopSwipeHandler,
        onSwipeEnd: noopSwipeHandler
    } : {};
};
const hideAnimation = /*#__PURE__*/ keyframes({
    from: {
        opacity: 1
    },
    to: {
        opacity: 0
    }
});
const slideInAnimation = /*#__PURE__*/ keyframes({
    from: {
        transform: 'translateX(calc(100% + 12px))'
    },
    to: {
        transform: 'translateX(0)'
    }
});
const swipeOutAnimation = /*#__PURE__*/ keyframes({
    from: {
        transform: 'translateX(var(--radix-toast-swipe-end-x))'
    },
    to: {
        transform: 'translateX(calc(100% + 12px))'
    }
});
const getToastRootStyle = (theme, classNamePrefix)=>{
    return /*#__PURE__*/ css({
        '&&': {
            position: 'relative',
            display: 'grid',
            background: theme.colors.backgroundPrimary,
            padding: 12,
            columnGap: 4,
            boxShadow: theme.shadows.lg,
            borderRadius: theme.borders.borderRadiusSm,
            lineHeight: '20px',
            borderColor: `1px solid ${theme.colors.border}`,
            gridTemplateRows: '[header] auto [content] auto',
            gridTemplateColumns: '[icon] auto [content] 1fr [close] auto',
            ...getDarkModePortalStyles(theme)
        },
        [`.${classNamePrefix}-notification-severity-icon`]: {
            gridRow: 'header / content',
            gridColumn: 'icon / icon',
            display: 'inline-flex',
            alignItems: 'center'
        },
        [`.${classNamePrefix}-btn`]: {
            display: 'inline-flex',
            alignItems: 'center',
            justifyContent: 'center'
        },
        [`.${classNamePrefix}-notification-info-icon`]: {
            color: theme.colors.textSecondary
        },
        [`.${classNamePrefix}-notification-success-icon`]: {
            color: theme.colors.textValidationSuccess
        },
        [`.${classNamePrefix}-notification-warning-icon`]: {
            color: theme.colors.textValidationWarning
        },
        [`.${classNamePrefix}-notification-error-icon`]: {
            color: theme.colors.textValidationDanger
        },
        '&&[data-state="open"]': {
            animation: `${slideInAnimation} 300ms cubic-bezier(0.16, 1, 0.3, 1)`
        },
        '&[data-state="closed"]': {
            animation: `${hideAnimation} 100ms ease-in`
        },
        '&[data-swipe="move"]': {
            transform: 'translateX(var(--radix-toast-swipe-move-x))'
        },
        '&[data-swipe="cancel"]': {
            transform: 'translateX(0)',
            transition: 'transform 200ms ease-out'
        },
        '&[data-swipe="end"]': {
            animation: `${swipeOutAnimation} 100ms ease-out`
        }
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Root$3 = /*#__PURE__*/ forwardRef(function({ children, severity = 'info', componentId, analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnView
], ...props }, ref) {
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Notification,
        componentId,
        componentSubType: DesignSystemEventProviderComponentSubTypeMap[severity],
        analyticsEvents: memoizedAnalyticsEvents,
        shouldStartInteraction: false
    });
    // A new ref was created rather than creating additional complexity of merging the refs, something to consider for the future to optimize
    const { elementRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    return /*#__PURE__*/ jsxs(Toast.Root, {
        ref: ref,
        css: getToastRootStyle(theme, classNamePrefix),
        ...props,
        ...addDebugOutlineIfEnabled(),
        ...preventSwipeToCloseIfEnabled(),
        children: [
            /*#__PURE__*/ jsx(SeverityIcon, {
                className: `${classNamePrefix}-notification-severity-icon ${classNamePrefix}-notification-${severity}-icon`,
                severity: severity,
                ref: elementRef
            }),
            children
        ]
    });
});
// TODO: Support light and dark mode
const getViewportStyle$1 = (theme)=>{
    return {
        position: 'fixed',
        top: 0,
        right: 0,
        display: 'flex',
        flexDirection: 'column',
        padding: 12,
        gap: 12,
        width: 440,
        listStyle: 'none',
        zIndex: theme.options.zIndexBase + 100,
        outline: 'none',
        maxWidth: `calc(100% - ${theme.spacing.lg}px)`
    };
};
const getTitleStyles = (theme)=>{
    return /*#__PURE__*/ css({
        fontWeight: theme.typography.typographyBoldFontWeight,
        color: theme.colors.textPrimary,
        gridRow: 'header / header',
        gridColumn: 'content / content',
        userSelect: 'text'
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Title = /*#__PURE__*/ forwardRef(function({ children, ...props }, ref) {
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(Toast.Title, {
        ref: ref,
        css: getTitleStyles(theme),
        ...props,
        children: children
    });
});
const getDescriptionStyles = (theme)=>{
    return /*#__PURE__*/ css({
        marginTop: 4,
        color: theme.colors.textPrimary,
        gridRow: 'content / content',
        gridColumn: 'content / content',
        userSelect: 'text',
        wordBreak: 'break-word'
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Description = /*#__PURE__*/ forwardRef(function({ children, ...props }, ref) {
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(Toast.Description, {
        ref: ref,
        css: getDescriptionStyles(theme),
        ...props,
        children: children
    });
});
const getCloseStyles = (theme)=>{
    return /*#__PURE__*/ css({
        color: theme.colors.textSecondary,
        position: 'absolute',
        // Offset close button position to align with the title, title uses 20px line height, button has 32px
        right: 6,
        top: 6
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Close = /*#__PURE__*/ forwardRef(function(props, ref) {
    const { theme } = useDesignSystemTheme();
    const { closeLabel, componentId, analyticsEvents, ...restProps } = props;
    return(// Wrapper to keep close column width for content sizing, close button positioned absolute for alignment without affecting the grid's first row height (title)
    /*#__PURE__*/ jsx("div", {
        style: {
            gridColumn: 'close / close',
            gridRow: 'header / content',
            width: 20
        },
        children: /*#__PURE__*/ jsx(Toast.Close, {
            ref: ref,
            css: getCloseStyles(theme),
            ...restProps,
            asChild: true,
            children: /*#__PURE__*/ jsx(Button, {
                componentId: componentId ? componentId : 'codegen_design-system_src_design-system_notification_notification.tsx_224',
                analyticsEvents: analyticsEvents,
                icon: /*#__PURE__*/ jsx(CloseIcon, {}),
                "aria-label": closeLabel ?? restProps['aria-label'] ?? 'Close notification'
            })
        })
    }));
});
const Provider = ({ children, ...props })=>{
    return /*#__PURE__*/ jsx(Toast.Provider, {
        ...props,
        children: children
    });
};
const Viewport = (props)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(Toast.Viewport, {
        className: DU_BOIS_ENABLE_ANIMATION_CLASSNAME,
        style: getViewportStyle$1(theme),
        ...props
    });
};

var Notification = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Close: Close,
  Description: Description,
  Provider: Provider,
  Root: Root$3,
  Title: Title,
  Viewport: Viewport
});

const oldTagColorsMap = {
    default: 'tagDefault',
    brown: 'tagBrown',
    coral: 'tagCoral',
    charcoal: 'grey600',
    indigo: 'tagIndigo',
    lemon: 'tagLemon',
    lime: 'tagLime',
    pink: 'tagPink',
    purple: 'tagPurple',
    teal: 'tagTeal',
    turquoise: 'tagTurquoise'
};
function getTagEmotionStyles(theme, color = 'default', clickable = false, closable = false) {
    let textColor = theme.colors.tagText;
    let backgroundColor = theme.colors[oldTagColorsMap[color]];
    let iconColor = '';
    let outlineColor = theme.colors.actionDefaultBorderFocus;
    const capitalizedColor = color.charAt(0).toUpperCase() + color.slice(1);
    textColor = theme.DU_BOIS_INTERNAL_ONLY.colors[`tagText${capitalizedColor}`];
    backgroundColor = theme.DU_BOIS_INTERNAL_ONLY.colors[`tagBackground${capitalizedColor}`];
    iconColor = theme.DU_BOIS_INTERNAL_ONLY.colors[`tagIcon${capitalizedColor}`];
    if (color === 'charcoal') {
        outlineColor = theme.colors.white;
    }
    const iconHover = theme.colors.tagIconHover;
    const iconPress = theme.colors.tagIconPress;
    return {
        wrapper: {
            backgroundColor: backgroundColor,
            display: 'inline-flex',
            alignItems: 'center',
            marginRight: theme.spacing.sm,
            borderRadius: theme.borders.borderRadiusSm
        },
        tag: {
            border: 'none',
            color: textColor,
            padding: '',
            backgroundColor: 'transparent',
            borderRadius: theme.borders.borderRadiusSm,
            marginRight: theme.spacing.sm,
            display: 'inline-block',
            cursor: clickable ? 'pointer' : undefined,
            ...closable && {
                borderTopRightRadius: 0,
                borderBottomRightRadius: 0
            },
            ...clickable && {
                '&:hover': {
                    backgroundColor: theme.colors.actionDefaultBackgroundHover
                },
                '&:active': {
                    backgroundColor: theme.colors.actionDefaultBackgroundPress
                }
            }
        },
        content: {
            display: 'flex',
            alignItems: 'center',
            minWidth: 0,
            height: theme.typography.lineHeightBase
        },
        close: {
            height: theme.typography.lineHeightBase,
            width: theme.typography.lineHeightBase,
            lineHeight: `${theme.general.iconFontSize}px`,
            padding: 0,
            color: textColor,
            fontSize: theme.general.iconFontSize,
            borderTopRightRadius: theme.borders.borderRadiusSm,
            borderBottomRightRadius: theme.borders.borderRadiusSm,
            border: 'none',
            background: 'none',
            cursor: 'pointer',
            marginLeft: theme.spacing.xs,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            margin: 0,
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover,
                color: iconHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress,
                color: iconPress
            },
            '&:focus-visible': {
                outlineStyle: 'solid',
                outlineWidth: 1,
                outlineOffset: -2,
                outlineColor
            },
            '.anticon': {
                verticalAlign: 0,
                fontSize: 12
            }
        },
        text: {
            padding: 0,
            fontSize: theme.typography.fontSizeBase,
            fontWeight: theme.typography.typographyRegularFontWeight,
            lineHeight: theme.typography.lineHeightSm,
            '& .anticon': {
                verticalAlign: 'text-top'
            },
            whiteSpace: 'nowrap'
        },
        icon: {
            color: iconColor,
            paddingLeft: theme.spacing.xs,
            height: theme.typography.lineHeightBase,
            display: 'inline-flex',
            alignItems: 'center',
            borderTopLeftRadius: theme.borders.borderRadiusSm,
            borderBottomLeftRadius: theme.borders.borderRadiusSm,
            '& > span': {
                fontSize: 12
            },
            '& + div': {
                borderTopLeftRadius: 0,
                borderBottomLeftRadius: 0,
                ...closable && {
                    borderTopRightRadius: 0,
                    borderBottomRightRadius: 0
                }
            }
        },
        childrenWrapper: {
            paddingLeft: theme.spacing.xs,
            paddingRight: theme.spacing.xs,
            height: theme.typography.lineHeightBase,
            display: 'inline-flex',
            alignItems: 'center',
            borderRadius: theme.borders.borderRadiusSm,
            minWidth: 0
        }
    };
}
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Tag = /*#__PURE__*/ forwardRef((props, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const { color, children, closable, onClose, role = 'status', closeButtonProps, analyticsEvents, componentId, icon, onClick, ...attributes } = props;
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.tag', false);
    const isClickable = Boolean(props.onClick);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Tag,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents
    });
    const { elementRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    const mergedRef = useMergeRefs([
        elementRef,
        forwardedRef
    ]);
    const closeButtonComponentId = componentId ? `${componentId}.close` : undefined;
    const closeButtonEventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.Button,
        componentId: closeButtonComponentId,
        analyticsEvents: [
            DesignSystemEventProviderAnalyticsEventTypes.OnClick
        ]
    });
    const handleClick = useCallback((e)=>{
        if (onClick) {
            eventContext.onClick(e);
            onClick(e);
        }
    }, [
        eventContext,
        onClick
    ]);
    const handleCloseClick = useCallback((e)=>{
        closeButtonEventContext.onClick(e);
        e.stopPropagation();
        if (onClose) {
            onClose();
        }
    }, [
        closeButtonEventContext,
        onClose
    ]);
    const styles = getTagEmotionStyles(theme, color, isClickable, closable);
    return /*#__PURE__*/ jsxs("div", {
        ref: mergedRef,
        role: role,
        onClick: handleClick,
        css: [
            styles.wrapper
        ],
        ...attributes,
        ...addDebugOutlineIfEnabled(),
        ...eventContext.dataComponentProps,
        tabIndex: isClickable ? 0 : -1,
        children: [
            /*#__PURE__*/ jsxs("div", {
                css: [
                    styles.tag,
                    styles.content,
                    styles.text,
                    {
                        marginRight: 0
                    }
                ],
                ...eventContext.dataComponentProps,
                children: [
                    icon && /*#__PURE__*/ jsx("div", {
                        css: [
                            styles.icon
                        ],
                        children: icon
                    }),
                    /*#__PURE__*/ jsx("div", {
                        css: [
                            styles.childrenWrapper
                        ],
                        children: children
                    })
                ]
            }),
            closable && /*#__PURE__*/ jsx("button", {
                css: styles.close,
                tabIndex: 0,
                onClick: handleCloseClick,
                onMouseDown: (e)=>{
                    // Keeps dropdowns of any underlying select from opening.
                    e.stopPropagation();
                },
                ...closeButtonProps,
                ...closeButtonEventContext.dataComponentProps,
                children: /*#__PURE__*/ jsx(CloseIcon, {
                    css: {
                        fontSize: theme.general.iconFontSize - 4
                    }
                })
            })
        ]
    });
});

const Overflow = ({ children, noMargin = false, visibleItemsCount = 1, ...props })=>{
    const { theme } = useDesignSystemTheme();
    const childrenList = children && Children.toArray(children);
    if (!childrenList || childrenList.length === 0) {
        return /*#__PURE__*/ jsx(Fragment, {
            children: children
        });
    }
    const visibleItems = childrenList.slice(0, visibleItemsCount);
    const additionalItems = childrenList.slice(visibleItemsCount);
    const renderOverflowLabel = (label)=>/*#__PURE__*/ jsx(Tag, {
            componentId: "codegen_design-system_src_design-system_overflow_overflow.tsx_28",
            css: getTagStyles(theme),
            children: label
        });
    return additionalItems.length === 0 ? /*#__PURE__*/ jsx(Fragment, {
        children: visibleItems
    }) : /*#__PURE__*/ jsxs("div", {
        ...props,
        css: {
            display: 'inline-flex',
            alignItems: 'center',
            gap: noMargin ? 0 : theme.spacing.sm,
            maxWidth: '100%'
        },
        children: [
            visibleItems,
            additionalItems.length > 0 && /*#__PURE__*/ jsx(OverflowPopover, {
                items: additionalItems,
                renderLabel: renderOverflowLabel,
                ...props
            })
        ]
    });
};
const getTagStyles = (theme)=>{
    const styles = {
        marginRight: 0,
        color: theme.colors.actionTertiaryTextDefault,
        cursor: 'pointer',
        '&:focus': {
            color: theme.colors.actionTertiaryTextDefault
        },
        '&:hover': {
            color: theme.colors.actionTertiaryTextHover
        },
        '&:active': {
            color: theme.colors.actionTertiaryTextPress
        }
    };
    return /*#__PURE__*/ css(styles);
};

const PageWrapper = ({ children, ...props })=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        ...addDebugOutlineIfEnabled(),
        css: /*#__PURE__*/ css({
            paddingLeft: 16,
            paddingRight: 16,
            backgroundColor: theme.isDarkMode ? theme.colors.backgroundPrimary : 'transparent'
        }),
        ...props,
        children: children
    });
};

const RadioGroupContext = /*#__PURE__*/ React__default.createContext('medium');
const Root$2 = /*#__PURE__*/ React__default.forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ size, componentId, analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
], valueHasNoPii, onValueChange, ...props }, forwardedRef)=>{
    const { theme } = useDesignSystemTheme();
    const contextValue = React__default.useMemo(()=>size ?? 'medium', [
        size
    ]);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.PillControl,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const { elementRef: pillControlRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: props.value ?? props.defaultValue
    });
    const onValueChangeWrapper = useCallback((value)=>{
        eventContext.onValueChange?.(value);
        onValueChange?.(value);
    }, [
        eventContext,
        onValueChange
    ]);
    const mergedRef = useMergeRefs([
        forwardedRef,
        pillControlRef
    ]);
    return /*#__PURE__*/ jsx(RadioGroupContext.Provider, {
        value: contextValue,
        children: /*#__PURE__*/ jsx(RadioGroup$1, {
            css: {
                display: 'flex',
                flexWrap: 'wrap',
                gap: theme.spacing.sm
            },
            onValueChange: onValueChangeWrapper,
            ...props,
            ref: mergedRef,
            ...eventContext.dataComponentProps
        })
    });
});
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Item = /*#__PURE__*/ React__default.forwardRef(({ children, icon, ...props }, forwardedRef)=>{
    const size = React__default.useContext(RadioGroupContext);
    const { theme } = useDesignSystemTheme();
    const iconClass = 'pill-control-icon';
    const css = useRadioGroupItemStyles(size, iconClass);
    return /*#__PURE__*/ jsxs(RadioGroupItem, {
        css: css,
        ...props,
        children: [
            icon && /*#__PURE__*/ jsx("span", {
                className: iconClass,
                css: {
                    marginRight: size === 'large' ? theme.spacing.sm : theme.spacing.xs,
                    [`& > .anticon`]: {
                        verticalAlign: `-3px`
                    }
                },
                children: icon
            }),
            children
        ]
    });
});
const useRadioGroupItemStyles = (size, iconClass)=>{
    const { theme } = useDesignSystemTheme();
    return {
        textOverflow: 'ellipsis',
        boxShadow: theme.shadows.xs,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        appearance: 'none',
        textDecoration: 'none',
        background: 'none',
        border: '1px solid',
        cursor: 'pointer',
        backgroundColor: theme.colors.actionDefaultBackgroundDefault,
        borderColor: theme.colors.actionDefaultBorderDefault,
        color: theme.colors.textPrimary,
        lineHeight: theme.typography.lineHeightBase,
        height: 32,
        paddingInline: '12px',
        fontWeight: theme.typography.typographyRegularFontWeight,
        fontSize: theme.typography.fontSizeBase,
        borderRadius: theme.borders.borderRadiusFull,
        transition: 'background-color 0.2s ease-in-out, border-color 0.2s ease-in-out',
        [`& > .${iconClass}`]: {
            color: theme.colors.textSecondary,
            ...size === 'large' ? {
                backgroundColor: theme.colors.tagDefault,
                padding: theme.spacing.sm,
                borderRadius: theme.borders.borderRadiusFull
            } : {}
        },
        '&[data-state="checked"]': {
            backgroundColor: theme.colors.actionDefaultBackgroundPress,
            borderColor: 'transparent',
            color: theme.colors.textPrimary,
            // outline
            outlineStyle: 'solid',
            outlineWidth: '2px',
            outlineOffset: '0px',
            outlineColor: theme.colors.actionDefaultBorderFocus,
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundPress,
                borderColor: theme.colors.actionLinkPress,
                color: 'inherit'
            },
            [`& > .${iconClass}, &:hover > .${iconClass}`]: {
                color: theme.colors.actionDefaultTextPress,
                ...size === 'large' ? {
                    backgroundColor: theme.colors.actionIconBackgroundPress
                } : {}
            }
        },
        '&:focus-visible': {
            outlineStyle: 'solid',
            outlineWidth: '2px',
            outlineOffset: '0px',
            outlineColor: theme.colors.actionDefaultBorderFocus
        },
        '&:hover': {
            backgroundColor: theme.colors.actionDefaultBackgroundHover,
            borderColor: theme.colors.actionLinkHover,
            color: theme.colors.actionDefaultTextHover,
            [`& > .${iconClass}`]: {
                color: 'inherit',
                ...size === 'large' ? {
                    backgroundColor: theme.colors.actionIconBackgroundHover
                } : {}
            }
        },
        '&:active': {
            backgroundColor: theme.colors.actionDefaultBackgroundPress,
            borderColor: theme.colors.actionLinkPress,
            color: theme.colors.actionDefaultTextPress,
            [`& > .${iconClass}`]: {
                color: 'inherit',
                ...size === 'large' ? {
                    backgroundColor: theme.colors.actionIconBackgroundPress
                } : {}
            }
        },
        '&:disabled': {
            backgroundColor: theme.colors.actionDisabledBackground,
            borderColor: theme.colors.actionDisabledBorder,
            color: theme.colors.actionDisabledText,
            cursor: 'not-allowed',
            [`& > .${iconClass}`]: {
                color: 'inherit'
            }
        },
        ...size === 'small' ? {
            height: 24,
            lineHeight: theme.typography.lineHeightSm,
            paddingInline: theme.spacing.sm
        } : {},
        ...size === 'large' ? {
            height: 44,
            lineHeight: theme.typography.lineHeightXl,
            paddingInline: theme.spacing.md,
            paddingInlineStart: '6px',
            borderRadius: theme.borders.borderRadiusFull
        } : {}
    };
};

var PillControl = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Item: Item,
  Root: Root$2
});

const PreviewCard = ({ icon, title, subtitle, titleActions, children, startActions, endActions, image, fullBleedImage = true, onClick, size = 'default', dangerouslyAppendEmotionCSS, componentId, analyticsEvents = [
    DesignSystemEventProviderAnalyticsEventTypes.OnClick
], disabled, selected, href, target, ...props })=>{
    const styles = usePreviewCardStyles({
        onClick,
        size,
        disabled,
        fullBleedImage,
        href
    });
    const tabIndex = onClick && !href ? 0 : undefined;
    const role = onClick && !href ? 'button' : undefined;
    const showFooter = startActions || endActions;
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents, [
        analyticsEvents
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.PreviewCard,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents
    });
    const { elementRef: previewCardRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    const onClickWrapper = useCallback((e)=>{
        if (onClick) {
            eventContext.onClick(e);
            onClick(e);
        }
    }, [
        eventContext,
        onClick
    ]);
    const onHrefClickWrapper = useCallback((e)=>{
        eventContext.onClick(e);
        if (onClick) {
            onClick(e);
        }
    }, [
        eventContext,
        onClick
    ]);
    const content = /*#__PURE__*/ jsxs("div", {
        ...addDebugOutlineIfEnabled(),
        css: [
            styles['container'],
            dangerouslyAppendEmotionCSS
        ],
        tabIndex: disabled ? -1 : tabIndex,
        onClick: onClickWrapper,
        onKeyDown: (e)=>{
            if (!onClick || disabled) {
                return;
            }
            if (e.key === 'Enter' || e.key === ' ') {
                e.preventDefault();
                onClickWrapper(e);
            }
        },
        role: role,
        "aria-disabled": disabled,
        "aria-pressed": selected,
        ...props,
        ref: previewCardRef,
        children: [
            image && /*#__PURE__*/ jsx("div", {
                css: styles['image'],
                children: image
            }),
            /*#__PURE__*/ jsxs("div", {
                css: styles['header'],
                children: [
                    icon && /*#__PURE__*/ jsx("div", {
                        children: icon
                    }),
                    /*#__PURE__*/ jsxs("div", {
                        css: styles['titleWrapper'],
                        children: [
                            title && /*#__PURE__*/ jsx("div", {
                                css: styles['title'],
                                children: title
                            }),
                            subtitle && /*#__PURE__*/ jsx("div", {
                                css: styles['subTitle'],
                                children: subtitle
                            })
                        ]
                    }),
                    titleActions && /*#__PURE__*/ jsx("div", {
                        children: titleActions
                    })
                ]
            }),
            children && /*#__PURE__*/ jsx("div", {
                css: styles['childrenWrapper'],
                children: children
            }),
            showFooter && /*#__PURE__*/ jsxs("div", {
                css: styles['footer'],
                children: [
                    /*#__PURE__*/ jsx("div", {
                        css: styles['action'],
                        children: startActions
                    }),
                    /*#__PURE__*/ jsx("div", {
                        css: styles['action'],
                        children: endActions
                    })
                ]
            })
        ]
    });
    if (href) {
        return /*#__PURE__*/ jsx("a", {
            href: href,
            target: target,
            style: {
                textDecoration: 'none'
            },
            onClick: onHrefClickWrapper,
            children: content
        });
    }
    return content;
};
const usePreviewCardStyles = ({ onClick, size, disabled, fullBleedImage, href })=>{
    const { theme } = useDesignSystemTheme();
    const isInteractive = href !== undefined || onClick !== undefined;
    const paddingSize = size === 'large' ? theme.spacing.lg : theme.spacing.md;
    return {
        container: {
            overflow: 'hidden',
            borderRadius: theme.borders.borderRadiusMd,
            border: `1px solid ${theme.colors.border}`,
            padding: paddingSize,
            color: theme.colors.textSecondary,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
            gap: size === 'large' ? theme.spacing.md : theme.spacing.sm,
            boxShadow: theme.shadows.sm,
            cursor: isInteractive ? 'pointer' : 'default',
            ...isInteractive && {
                transition: 'box-shadow 0.2s, background-color 0.2s, border-color 0.2s, color 0.2s',
                '&[aria-disabled="true"]': {
                    pointerEvents: 'none',
                    backgroundColor: theme.colors.actionDisabledBackground,
                    borderColor: theme.colors.actionDisabledBorder,
                    color: theme.colors.actionDisabledText
                },
                '&:hover, &:focus-within': {
                    boxShadow: theme.shadows.md
                },
                '&:active': {
                    background: theme.colors.actionTertiaryBackgroundPress,
                    borderColor: theme.colors.actionDefaultBorderHover,
                    boxShadow: theme.shadows.md
                },
                '&:focus, &[aria-pressed="true"]': {
                    outlineColor: theme.colors.actionDefaultBorderFocus,
                    outlineWidth: 2,
                    outlineOffset: -2,
                    outlineStyle: 'solid',
                    boxShadow: theme.shadows.md,
                    borderColor: theme.colors.actionDefaultBorderHover
                },
                '&:active:not(:focus):not(:focus-within)': {
                    background: 'transparent',
                    borderColor: theme.colors.border
                }
            }
        },
        image: {
            margin: fullBleedImage ? `-${paddingSize}px -${paddingSize}px 0` : 0,
            '& > *': {
                borderRadius: fullBleedImage ? 0 : theme.borders.borderRadiusSm
            }
        },
        header: {
            display: 'flex',
            alignItems: 'center',
            gap: theme.spacing.sm
        },
        title: {
            fontWeight: theme.typography.typographyBoldFontWeight,
            color: disabled ? theme.colors.actionDisabledText : theme.colors.textPrimary,
            lineHeight: theme.typography.lineHeightSm
        },
        subTitle: {
            lineHeight: theme.typography.lineHeightSm
        },
        titleWrapper: {
            flexGrow: 1,
            overflow: 'hidden'
        },
        childrenWrapper: {
            flexGrow: 1
        },
        footer: {
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
            flexWrap: 'wrap',
            gap: theme.spacing.sm
        },
        action: {
            overflow: 'hidden',
            // to ensure focus ring is rendered
            margin: theme.spacing.md * -1,
            padding: theme.spacing.md
        }
    };
};

const progressContextDefaults = {
    progress: 0
};
const ProgressContext = /*#__PURE__*/ createContext(progressContextDefaults);
const ProgressContextProvider = ({ children, value })=>{
    return /*#__PURE__*/ jsx(ProgressContext.Provider, {
        value: value,
        children: children
    });
};

const getProgressRootStyles = (theme, { minWidth, maxWidth })=>{
    const styles = {
        position: 'relative',
        overflow: 'hidden',
        backgroundColor: theme.colors.progressTrack,
        height: theme.spacing.sm,
        width: '100%',
        borderRadius: theme.borders.borderRadiusFull,
        ...minWidth && {
            minWidth
        },
        ...maxWidth && {
            maxWidth
        },
        /* Fix overflow clipping in Safari */ /* https://gist.github.com/domske/b66047671c780a238b51c51ffde8d3a0 */ transform: 'translateZ(0)'
    };
    return /*#__PURE__*/ css(importantify(styles));
};
const Root$1 = (props)=>{
    const { children, value, minWidth, maxWidth, ...restProps } = props;
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(ProgressContextProvider, {
        value: {
            progress: value
        },
        children: /*#__PURE__*/ jsx(Progress$1.Root, {
            value: value,
            ...restProps,
            css: getProgressRootStyles(theme, {
                minWidth,
                maxWidth
            }),
            children: children
        })
    });
};
const getProgressIndicatorStyles = (theme)=>{
    const styles = {
        backgroundColor: theme.colors.progressFill,
        height: '100%',
        width: '100%',
        transition: 'transform 300ms linear',
        borderRadius: theme.borders.borderRadiusFull
    };
    return /*#__PURE__*/ css(importantify(styles));
};
const Indicator = (props)=>{
    const { progress } = React__default.useContext(ProgressContext);
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(Progress$1.Indicator, {
        css: getProgressIndicatorStyles(theme),
        style: {
            transform: `translateX(-${100 - (progress ?? 100)}%)`
        },
        ...props
    });
};

var Progress = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Indicator: Indicator,
  Root: Root$1
});

const getRadioTileStyles = (theme, classNamePrefix, maxWidth)=>{
    const radioWrapper = `.${classNamePrefix}-radio-wrapper`;
    return /*#__PURE__*/ css({
        '&&': {
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            border: `1px solid ${theme.colors.actionDefaultBorderDefault}`,
            padding: theme.spacing.md,
            gap: theme.spacing.xs,
            borderRadius: theme.borders.borderRadiusSm,
            background: 'transparent',
            cursor: 'pointer',
            ...maxWidth && {
                maxWidth
            },
            // Label, radio and icon container
            '& > div:first-of-type': {
                width: '100%',
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                gap: theme.spacing.sm
            },
            // Description container
            '& > div:nth-of-type(2)': {
                alignSelf: 'flex-start',
                textAlign: 'left',
                color: theme.colors.textSecondary,
                fontSize: theme.typography.fontSizeSm
            },
            '&:hover': {
                backgroundColor: theme.colors.actionDefaultBackgroundHover,
                borderColor: theme.colors.actionDefaultBorderHover
            },
            '&:disabled': {
                borderColor: theme.colors.actionDisabledBorder,
                backgroundColor: 'transparent',
                cursor: 'not-allowed',
                '& > div:nth-of-type(2)': {
                    color: theme.colors.actionDisabledText
                }
            }
        },
        [radioWrapper]: {
            display: 'flex',
            flexDirection: 'row-reverse',
            justifyContent: 'space-between',
            flex: 1,
            margin: 0,
            '& > span': {
                padding: 0
            },
            '::after': {
                display: 'none'
            }
        }
    });
};
const RadioTile = (props)=>{
    const { description, icon, maxWidth, checked, defaultChecked, onChange, ...rest } = props;
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const { value: groupValue, onChange: groupOnChange } = useRadioGroupContext();
    return /*#__PURE__*/ jsxs("button", {
        role: "radio",
        type: "button",
        "aria-checked": groupValue === props.value,
        onClick: ()=>{
            if (props.disabled) {
                return;
            }
            onChange?.(props.value);
            groupOnChange?.({
                target: {
                    value: props.value
                }
            });
        },
        tabIndex: 0,
        className: `${classNamePrefix}-radio-tile`,
        css: getRadioTileStyles(theme, classNamePrefix, maxWidth),
        disabled: props.disabled,
        children: [
            /*#__PURE__*/ jsxs("div", {
                children: [
                    icon ? /*#__PURE__*/ jsx("span", {
                        css: {
                            color: props.disabled ? theme.colors.actionDisabledText : theme.colors.textSecondary
                        },
                        children: icon
                    }) : null,
                    /*#__PURE__*/ jsx(Radio, {
                        __INTERNAL_DISABLE_RADIO_ROLE: true,
                        ...rest,
                        tabIndex: -1
                    })
                ]
            }),
            description ? /*#__PURE__*/ jsx("div", {
                children: description
            }) : null
        ]
    });
};

const STATUS_TO_ICON = {
    online: ({ theme, style, ...props })=>/*#__PURE__*/ jsx(CircleIcon, {
            color: "success",
            css: {
                ...style
            },
            ...props
        }),
    disconnected: ({ theme, style, ...props })=>/*#__PURE__*/ jsx(CircleOutlineIcon, {
            css: {
                color: theme.colors.grey500,
                ...style
            },
            ...props
        }),
    offline: ({ theme, style, ...props })=>/*#__PURE__*/ jsx(CircleOffIcon, {
            css: {
                color: theme.colors.grey500,
                ...style
            },
            ...props
        })
};
const ResourceStatusIndicator = (props)=>{
    const { status, style, ...restProps } = props;
    const { theme } = useDesignSystemTheme();
    const StatusIcon = STATUS_TO_ICON[status];
    return /*#__PURE__*/ jsx(StatusIcon, {
        theme: theme,
        style: style,
        ...restProps
    });
};

// TODO(GP): Add this to common spacing vars; I didn't want to make a decision on the value right now,
// so copied it from `Button`.
const SMALL_BUTTON_HEIGHT$1 = 24;
function getSegmentedControlGroupEmotionStyles(clsPrefix, theme, spaced = false, useSegmentedSliderStyle = false) {
    const classGroup = `.${clsPrefix}-radio-group`;
    const classSmallGroup = `.${clsPrefix}-radio-group-small`;
    const classButtonWrapper = `.${clsPrefix}-radio-button-wrapper`;
    const styles = {
        display: 'flex',
        maxWidth: '100%',
        [`&${classGroup}`]: spaced ? {
            display: 'flex',
            gap: 8,
            flexWrap: 'wrap'
        } : {},
        [`&${classSmallGroup} ${classButtonWrapper}`]: {
            padding: '0 12px'
        }
    };
    const sliderStyles = {
        height: 'min-content',
        width: 'min-content',
        background: theme.colors.backgroundSecondary,
        borderRadius: theme.borders.borderRadiusSm,
        display: 'flex',
        gap: theme.spacing.xs,
        maxWidth: '100%',
        overflow: 'auto'
    };
    const importantStyles = importantify(useSegmentedSliderStyle && !spaced ? sliderStyles : styles);
    return /*#__PURE__*/ css(importantStyles);
}
function getSegmentedControlButtonEmotionStyles(clsPrefix, theme, size, spaced = false, onlyIcon, useSegmentedSliderStyle = false) {
    const classWrapperChecked = `.${clsPrefix}-radio-button-wrapper-checked`;
    const classWrapper = `.${clsPrefix}-radio-button-wrapper`;
    const classWrapperDisabled = `.${clsPrefix}-radio-button-wrapper-disabled`;
    const classButton = `.${clsPrefix}-radio-button`;
    const classIcon = `.${theme.general.iconfontCssPrefix}`;
    // Note: Ant radio button uses a 1px-wide `before` pseudo-element to recreate the left border of the button.
    // This is because the actual left border is disabled to avoid a double-border effect with the adjacent button's
    // right border.
    // We must override the background colour of this pseudo-border to be the same as the real border above.
    const styles = {
        backgroundColor: theme.colors.actionDefaultBackgroundDefault,
        borderColor: theme.colors.actionDefaultBorderDefault,
        color: theme.colors.actionDefaultTextDefault,
        boxShadow: theme.shadows.xs,
        // This handles the left border of the button when they're adjacent
        '::before': {
            display: spaced ? 'none' : 'block',
            backgroundColor: theme.colors.actionDefaultBorderDefault
        },
        '&:hover': {
            backgroundColor: theme.colors.actionDefaultBackgroundHover,
            borderColor: theme.colors.actionDefaultBorderHover,
            color: theme.colors.actionDefaultTextHover,
            '::before': {
                backgroundColor: theme.colors.actionDefaultBorderHover
            },
            // Also target the same pseudo-element on the next sibling, because this is used to create the right border
            [`& + ${classWrapper}::before`]: {
                backgroundColor: theme.colors.actionDefaultBorderPress
            }
        },
        '&:active': {
            backgroundColor: theme.colors.actionTertiaryBackgroundPress,
            borderColor: theme.colors.actionDefaultBorderPress,
            color: theme.colors.actionTertiaryTextPress
        },
        [`&${classWrapperChecked}`]: {
            backgroundColor: theme.colors.actionTertiaryBackgroundPress,
            borderColor: theme.colors.actionDefaultBorderPress,
            color: theme.colors.actionTertiaryTextPress,
            '::before': {
                backgroundColor: theme.colors.actionDefaultBorderPress
            },
            [`& + ${classWrapper}::before`]: {
                backgroundColor: theme.colors.actionDefaultBorderPress
            }
        },
        [`&${classWrapperChecked}:focus-within`]: {
            '::before': {
                width: 0
            }
        },
        [`&${classWrapper}`]: {
            padding: size === 'middle' ? `0 16px` : '0 8px',
            display: 'inline-flex',
            ...onlyIcon ? {
                '& > span': {
                    display: 'inline-flex'
                }
            } : {},
            verticalAlign: 'middle',
            flexShrink: 1,
            textOverflow: 'ellipsis',
            whiteSpace: 'nowrap',
            '&:first-of-type': {
                borderTopLeftRadius: theme.borders.borderRadiusSm,
                borderBottomLeftRadius: theme.borders.borderRadiusSm
            },
            '&:last-of-type': {
                borderTopRightRadius: theme.borders.borderRadiusSm,
                borderBottomRightRadius: theme.borders.borderRadiusSm
            },
            ...spaced ? {
                borderWidth: 1,
                borderRadius: theme.borders.borderRadiusSm
            } : {},
            '&:focus-within': {
                outlineStyle: 'solid',
                outlineWidth: '2px',
                outlineOffset: '-2px',
                outlineColor: theme.colors.actionDefaultBorderFocus
            },
            'span:last-of-type': {
                textOverflow: 'ellipsis',
                overflow: 'hidden',
                whiteSpace: 'nowrap'
            }
        },
        [`&${classWrapper}, ${classButton}`]: {
            height: size === 'middle' ? theme.general.heightSm : SMALL_BUTTON_HEIGHT$1,
            lineHeight: theme.typography.lineHeightBase,
            alignItems: 'center'
        },
        [`&${classWrapperDisabled}, &${classWrapperDisabled} + ${classWrapperDisabled}`]: {
            color: theme.colors.actionDisabledText,
            backgroundColor: 'transparent',
            borderColor: theme.colors.actionDisabledBorder,
            '&:hover': {
                color: theme.colors.actionDisabledText,
                borderColor: theme.colors.actionDisabledBorder,
                backgroundColor: 'transparent'
            },
            '&:active': {
                color: theme.colors.actionDisabledText,
                borderColor: theme.colors.actionDisabledBorder,
                backgroundColor: 'transparent'
            },
            '::before': {
                backgroundColor: theme.colors.actionDisabledBorder
            },
            [`&${classWrapperChecked}`]: {
                borderColor: theme.colors.actionDefaultBorderPress,
                '::before': {
                    backgroundColor: theme.colors.actionDefaultBorderPress
                }
            },
            [`&${classWrapperChecked} + ${classWrapper}`]: {
                '::before': {
                    backgroundColor: theme.colors.actionDefaultBorderPress
                }
            }
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    const sliderStyles = {
        minWidth: 'fit-content',
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap',
        border: 'none',
        display: 'inline-flex',
        alignItems: 'center',
        color: theme.colors.textSecondary,
        borderRadius: theme.borders.borderRadiusSm,
        backgroundColor: 'transparent',
        '::before': {
            display: 'none'
        },
        '&:hover': {
            backgroundColor: theme.colors.tableRowHover
        },
        [`&${classWrapper}`]: {
            padding: size === 'middle' ? onlyIcon ? `0 ${theme.spacing.sm}px` : `0 12px` : `0 ${theme.spacing.sm}px`,
            verticalAlign: 'middle',
            flexShrink: 1,
            textOverflow: 'ellipsis',
            whiteSpace: 'nowrap',
            '& > span:last-of-type': {
                display: 'inline-block',
                textOverflow: 'ellipsis',
                overflow: 'hidden',
                whiteSpace: 'nowrap',
                '& > *': {
                    display: 'flex',
                    alignItems: 'center'
                }
            },
            '& > span': {
                display: 'inline-flex'
            }
        },
        [`&${classWrapper}, ${classButton}`]: {
            height: size === 'middle' ? theme.general.heightSm : SMALL_BUTTON_HEIGHT$1,
            lineHeight: theme.typography.lineHeightBase,
            alignItems: 'center'
        },
        [`&${classWrapperChecked}`]: {
            color: theme.colors.actionDefaultTextDefault,
            [`${classIcon}`]: {
                color: theme.colors.textSecondary
            },
            backgroundColor: theme.colors.backgroundPrimary,
            boxShadow: `inset 0 0 0 1px ${theme.colors.actionDefaultBorderDefault}, ${theme.shadows.sm}`
        },
        [`&${classWrapperDisabled}, &${classWrapperDisabled} + ${classWrapperDisabled}`]: {
            color: theme.colors.actionDisabledText,
            backgroundColor: 'transparent'
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    const importantStyles = importantify(useSegmentedSliderStyle && !spaced ? sliderStyles : styles);
    return /*#__PURE__*/ css(importantStyles);
}
const SegmentedControlGroupContext = /*#__PURE__*/ createContext({
    size: 'middle',
    spaced: false,
    useSegmentedSliderStyle: false
});
const SegmentedControlButton = /*#__PURE__*/ forwardRef(function SegmentedControlButton({ dangerouslySetAntdProps, title, ...props }, ref) {
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const { size, spaced, useSegmentedSliderStyle } = useContext(SegmentedControlGroupContext);
    const buttonRef = useRef(null);
    useImperativeHandle(ref, ()=>buttonRef.current);
    // Check if this is icon-only: either using icon prop without children,
    // or icon prop with children that contain no text (e.g., icon prop with a wrapper component)
    const childrenHasText = React__default.Children.toArray(props.children).some((child)=>typeof child === 'string' || typeof child === 'number');
    const onlyIcon = Boolean(props.icon && !props.children) || Boolean(props.icon && props.children && !childrenHasText);
    const getLabelFromChildren = useCallback(()=>{
        let label = '';
        React__default.Children.map(props.children, (child)=>{
            if (typeof child === 'string') {
                label += child;
            }
        });
        return label;
    }, [
        props.children
    ]);
    useEffect(()=>{
        if (buttonRef.current) {
            // Using `as any` because Antd uses a `Checkbox` type that's not exported
            const labelParent = buttonRef.current.input.closest('label');
            if (labelParent) {
                // Set title: use custom title if provided, otherwise use button text
                const titleText = title !== undefined ? title : getLabelFromChildren();
                if (titleText) {
                    labelParent.setAttribute('title', titleText);
                }
                const labelWidth = labelParent.scrollWidth;
                const threshold = size === 'small' ? 58 : 68;
                if (labelWidth > threshold) {
                    labelParent.style.setProperty('min-width', `${threshold}px`, 'important');
                }
            }
        }
    }, [
        buttonRef,
        getLabelFromChildren,
        size,
        title
    ]);
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Radio$1.Button, {
            css: getSegmentedControlButtonEmotionStyles(classNamePrefix, theme, size, spaced, onlyIcon, useSegmentedSliderStyle),
            ...props,
            ...dangerouslySetAntdProps,
            ref: buttonRef,
            children: props.icon ? /*#__PURE__*/ jsxs("div", {
                css: {
                    display: 'inline-flex',
                    alignItems: 'center',
                    gap: theme.spacing.xs
                },
                children: [
                    props.icon,
                    props.children ? /*#__PURE__*/ jsx("span", {
                        css: {
                            overflow: 'hidden',
                            textOverflow: 'ellipsis',
                            whiteSpace: 'nowrap'
                        },
                        children: props.children
                    }) : props.children
                ]
            }) : props.children
        })
    });
});
const SegmentedControlGroup = /*#__PURE__*/ forwardRef(function SegmentedControlGroup({ dangerouslySetAntdProps, size = 'middle', spaced = false, onChange, componentId, analyticsEvents, valueHasNoPii, newStyleFlagOverride, ...props }, ref) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.segmentedControlGroup', false);
    const { classNamePrefix, theme } = useDesignSystemTheme();
    const useSegmentedSliderStyle = newStyleFlagOverride ?? safex('databricks.fe.designsystem.useNewSegmentedControlStyles', false);
    const uniqueId = useUniqueId();
    const internalRef = useRef();
    useImperativeHandle(ref, ()=>internalRef.current);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.SegmentedControlGroup,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii
    });
    const { elementRef: segmentedControlGroupRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: props.value ?? props.defaultValue
    });
    const mergedRef = useMergeRefs([
        internalRef,
        segmentedControlGroupRef
    ]);
    const onChangeWrapper = useCallback((e)=>{
        eventContext.onValueChange(e.target.value);
        onChange?.(e);
    }, [
        eventContext,
        onChange
    ]);
    const ariaLabelledby = props['aria-labelledby'];
    // A11y helper (FIT-1649): Effect helping to set the aria-labelledby attribute on the radio group if it is not provided
    useEffect(()=>{
        if (ariaLabelledby) {
            return;
        }
        if (props.id) {
            // look for the label of the group pointing to the id
            const label = document.querySelector(`label[for="${props.id}"]`);
            if (label) {
                // If the label already has an id, map the radio group to it
                if (label.hasAttribute('id')) {
                    internalRef.current?.setAttribute('aria-labelledby', label.getAttribute('id') ?? '');
                } else {
                    label.setAttribute('id', `${props.id}${uniqueId}-label`);
                    internalRef.current?.setAttribute('aria-labelledby', `${props.id}${uniqueId}-label`);
                }
            }
        }
    }, [
        ariaLabelledby,
        props.id,
        uniqueId
    ]);
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(SegmentedControlGroupContext.Provider, {
            value: {
                size,
                spaced,
                useSegmentedSliderStyle
            },
            children: /*#__PURE__*/ jsx(Radio$1.Group, {
                ...addDebugOutlineIfEnabled(),
                ...props,
                css: getSegmentedControlGroupEmotionStyles(classNamePrefix, theme, spaced, useSegmentedSliderStyle),
                // @ts-expect-error - role is not a valid prop for RadioGroup in Antd but it applies it correctly to the group
                role: "radiogroup",
                onChange: onChangeWrapper,
                ...dangerouslySetAntdProps,
                ref: mergedRef,
                ...eventContext.dataComponentProps
            })
        })
    });
});

const getRootStyles = ()=>{
    return /*#__PURE__*/ css({
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        '&[data-orientation="vertical"]': {
            flexDirection: 'column',
            width: 20,
            height: 100
        },
        '&[data-orientation="horizontal"]': {
            height: 20,
            width: 200
        }
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Root = /*#__PURE__*/ forwardRef((props, ref)=>{
    return /*#__PURE__*/ jsx(RadixSlider.Root, {
        ...addDebugOutlineIfEnabled(),
        css: getRootStyles(),
        ...props,
        ref: ref
    });
});
const getTrackStyles = (theme)=>{
    return /*#__PURE__*/ css({
        backgroundColor: theme.colors.grey100,
        position: 'relative',
        flexGrow: 1,
        borderRadius: theme.borders.borderRadiusFull,
        '&[data-orientation="vertical"]': {
            width: 3
        },
        '&[data-orientation="horizontal"]': {
            height: 3
        }
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Track = /*#__PURE__*/ forwardRef((props, ref)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(RadixSlider.Track, {
        css: getTrackStyles(theme),
        ...props,
        ref: ref
    });
});
const getRangeStyles = (theme)=>{
    return /*#__PURE__*/ css({
        backgroundColor: theme.colors.primary,
        position: 'absolute',
        borderRadius: theme.borders.borderRadiusFull,
        height: '100%',
        '&[data-disabled]': {
            backgroundColor: theme.colors.grey100
        }
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Range = /*#__PURE__*/ forwardRef((props, ref)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(RadixSlider.Range, {
        css: getRangeStyles(theme),
        ...props,
        ref: ref
    });
});
const getThumbStyles = (theme)=>{
    return /*#__PURE__*/ css({
        display: 'block',
        width: 20,
        height: 20,
        backgroundColor: theme.colors.actionPrimaryBackgroundDefault,
        boxShadow: theme.shadows.xs,
        borderRadius: theme.borders.borderRadiusFull,
        outline: 'none',
        '&:hover': {
            backgroundColor: theme.colors.actionPrimaryBackgroundHover
        },
        '&:focus': {
            backgroundColor: theme.colors.actionPrimaryBackgroundPress
        },
        '&[data-disabled]': {
            backgroundColor: theme.colors.grey200,
            boxShadow: 'none'
        }
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const Thumb = /*#__PURE__*/ forwardRef((props, ref)=>{
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx(RadixSlider.Thumb, {
        css: getThumbStyles(theme),
        "aria-label": "Slider thumb",
        ...props,
        ref: ref
    });
});

var Slider = /*#__PURE__*/Object.freeze({
  __proto__: null,
  Range: Range,
  Root: Root,
  Thumb: Thumb,
  Track: Track
});

/**
 * SnackBarManager is a store for managing SnackBar state.
 * It maintains the list of active snacks and notifies subscribers when state changes.
 */ class SnackBarManager {
    snacks = [];
    listeners = new Set();
    subscribe = (listener)=>{
        this.listeners.add(listener);
        return ()=>{
            this.listeners.delete(listener);
        };
    };
    getSnapshot = ()=>{
        return this.snacks;
    };
    notifyListeners() {
        this.listeners.forEach((listener)=>listener());
    }
    // Adds a snack and returns its ID.
    addSnack(config) {
        const snackId = config.uid ?? crypto.randomUUID();
        if (this.doesSnackExist(snackId)) {
            return snackId;
        }
        const snack = {
            ...config,
            id: snackId,
            open: true,
            vertical: config.vertical || 'top',
            horizontal: config.horizontal || 'center',
            autoDismiss: config.autoDismiss ?? 3000
        };
        this.snacks = [
            ...this.snacks,
            snack
        ];
        this.notifyListeners();
        return snackId;
    }
    // Closes a snack by ID
    closeSnack(id) {
        if (!this.doesSnackExist(id)) {
            return;
        }
        this.snacks = this.snacks.map((snack)=>snack.id === id ? {
                ...snack,
                open: false
            } : snack);
        this.notifyListeners();
    }
    // Deletes a snack by ID (after close animation)
    deleteSnack(id) {
        if (!this.doesSnackExist(id)) {
            return;
        }
        this.snacks = this.snacks.filter((snack)=>snack.id !== id);
        this.notifyListeners();
    }
    // Checks if a snack with the given ID exists
    doesSnackExist(id) {
        return this.snacks.some((snack)=>snack.id === id);
    }
    reset() {
        this.snacks = [];
        this.notifyListeners();
    }
}
const snackBarManager = new SnackBarManager();

const getCloseButtonStyles = (theme)=>{
    return /*#__PURE__*/ css({
        marginLeft: theme.spacing.sm,
        flexShrink: 0,
        // Override design system colors to use dark mode colors in both light and dark modes for proper contrast
        '&&:enabled:not(.du-bois-light-btn-link) > .anticon': {
            color: `${primitiveColors.grey100} !important`
        },
        '&&:enabled:not(.du-bois-light-btn-link):hover > .anticon': {
            color: `${primitiveColors.blue400} !important`
        },
        '&&:enabled:not(.du-bois-light-btn-link):active > .anticon': {
            color: `${primitiveColors.blue300} !important`
        }
    });
};
const fadeInAnimation = /*#__PURE__*/ keyframes({
    from: {
        opacity: 0
    },
    to: {
        opacity: 1
    }
});
const fadeOutAnimation = /*#__PURE__*/ keyframes({
    from: {
        opacity: 1
    },
    to: {
        opacity: 0
    }
});
const getSnackBarRootStyle = (theme, backgroundColor, textColor)=>{
    const defaultBackgroundColor = theme.isDarkMode ? theme.colors.grey600 : theme.colors.grey800;
    const defaultTextColor = theme.colors.white;
    return /*#__PURE__*/ css({
        '&&': {
            position: 'relative',
            display: 'flex',
            alignItems: 'center',
            padding: '12px 16px',
            backgroundColor: backgroundColor || defaultBackgroundColor,
            color: textColor || defaultTextColor,
            boxShadow: theme.shadows.lg,
            borderRadius: theme.borders.borderRadiusSm,
            pointerEvents: 'auto',
            maxWidth: 400
        },
        '&&[data-state="open"]': {
            animation: `${fadeInAnimation} 200ms ease-out`
        },
        '&[data-state="closed"]': {
            animation: `${fadeOutAnimation} 150ms ease-in`
        }
    });
};
const isSnackbarVertical = (value)=>{
    return value === 'top' || value === 'bottom';
};
const isSnackbarHorizontal = (value)=>{
    return value === 'left' || value === 'center' || value === 'right';
};
const getViewportStyle = (theme, vertical, horizontal)=>{
    const base = {
        position: 'fixed',
        display: 'flex',
        flexDirection: vertical === 'bottom' ? 'column-reverse' : 'column',
        padding: 12,
        gap: 12,
        listStyle: 'none',
        zIndex: theme.options.zIndexBase + 100,
        outline: 'none',
        maxWidth: 400,
        pointerEvents: 'none'
    };
    const verticalStyle = vertical === 'top' ? {
        top: 0
    } : {
        bottom: 0
    };
    const horizontalStyle = horizontal === 'center' ? {
        left: '50%',
        transform: 'translateX(-50%)',
        alignItems: 'center'
    } : horizontal === 'right' ? {
        right: 0,
        alignItems: 'flex-end'
    } : {
        left: 0,
        alignItems: 'flex-start'
    };
    return {
        ...base,
        ...verticalStyle,
        ...horizontalStyle
    };
};
const SnackBarRoot = /*#__PURE__*/ forwardRef(function SnackBarRoot({ snack }, ref) {
    const { theme } = useDesignSystemTheme();
    const memoizedAnalyticsEvents = useMemo(()=>[
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ], []);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.SnackBar,
        componentId: snack.componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        shouldStartInteraction: false
    });
    const { elementRef } = useNotifyOnFirstView({
        onView: eventContext.onView
    });
    // Only triggers when snack is auto-dismissed
    const handleOpenChange = useCallback((open)=>{
        if (!open) {
            // Set snack.open to false because Radix Toast auto-dismiss doesn't set it itself. Necessary for animation to work.
            snackBarManager.closeSnack(snack.id);
        }
    }, [
        snack
    ]);
    const handleAnimationEnd = useCallback((event)=>{
        if (event.animationName === fadeOutAnimation.name && !snack.open) {
            snackBarManager.deleteSnack(snack.id);
        }
    }, [
        snack.id,
        snack.open
    ]);
    const handleClick = useCallback(()=>{
        if (snack.closeButton === false) {
            snackBarManager.closeSnack(snack.id);
        }
    }, [
        snack.id,
        snack.closeButton
    ]);
    return /*#__PURE__*/ jsxs(Toast.Root, {
        ref: ref,
        css: getSnackBarRootStyle(theme, snack.backgroundColor, snack.textColor),
        style: snack.closeButton === false ? {
            cursor: 'pointer'
        } : undefined,
        open: snack.open,
        onOpenChange: handleOpenChange,
        duration: snack.autoDismiss,
        onAnimationEndCapture: handleAnimationEnd,
        onClick: snack.closeButton === false ? handleClick : undefined,
        ...addDebugOutlineIfEnabled(),
        children: [
            /*#__PURE__*/ jsx("div", {
                ref: elementRef,
                children: snack.content
            }),
            snack.closeButton !== false && /*#__PURE__*/ jsx(Toast.Close, {
                asChild: true,
                children: /*#__PURE__*/ jsx(Button, {
                    css: getCloseButtonStyles(theme),
                    componentId: "design-system.snackbar.close",
                    icon: /*#__PURE__*/ jsx(CloseIcon, {}),
                    "aria-label": "Close notification"
                })
            })
        ]
    });
});
/**
 * SnackBar component
 * Mount this once near the app root to enable snackbar notifications.
 * Multiple instances can be mounted without errors (though typically one is sufficient).
 */ const SnackBar = ()=>{
    const { theme } = useDesignSystemTheme();
    const snacks = shimExports.useSyncExternalStore(snackBarManager.subscribe, snackBarManager.getSnapshot);
    const snacksByPosition = useMemo(()=>{
        const grouped = {};
        snacks.forEach((snack)=>{
            const key = `${snack.vertical}-${snack.horizontal}`;
            if (!grouped[key]) {
                grouped[key] = [];
            }
            grouped[key].push(snack);
        });
        return grouped;
    }, [
        snacks
    ]);
    return /*#__PURE__*/ jsx(Fragment, {
        children: Object.entries(snacksByPosition).map(([key, snacksAtPosition])=>{
            const [vertical, horizontal] = key.split('-');
            if (!isSnackbarVertical(vertical) || !isSnackbarHorizontal(horizontal)) {
                return null;
            }
            return /*#__PURE__*/ jsx(Toast.Provider, {
                children: /*#__PURE__*/ jsx(Toast.Viewport, {
                    className: DU_BOIS_ENABLE_ANIMATION_CLASSNAME,
                    style: getViewportStyle(theme, vertical, horizontal),
                    "aria-live": "polite",
                    children: snacksAtPosition.map((snack)=>/*#__PURE__*/ jsx(SnackBarRoot, {
                            snack: snack
                        }, snack.id))
                })
            }, key);
        })
    });
};

/**
 * Hook to interact with the SnackBar system.
 * Use this hook to add and close snackbars.
 */ function useSnackBar() {
    const addSnack = useCallback((config)=>{
        return snackBarManager.addSnack(config);
    }, []);
    const closeSnack = useCallback((id)=>{
        snackBarManager.closeSnack(id);
    }, []);
    return {
        addSnack,
        closeSnack
    };
}

const BUTTON_HORIZONTAL_PADDING = 12;
function getSplitButtonEmotionStyles(classNamePrefix, theme, size) {
    const classDefault = `.${classNamePrefix}-btn`;
    const classPrimary = `.${classNamePrefix}-btn-primary`;
    const classDropdownTrigger = `.${classNamePrefix}-dropdown-trigger`;
    const classSmall = `.${classNamePrefix}-btn-group-sm`;
    const styles = {
        [classDefault]: {
            ...getDefaultStyles(theme),
            boxShadow: theme.shadows.xs,
            height: size === 'small' ? theme.general.iconSize : theme.general.heightSm,
            padding: `4px ${BUTTON_HORIZONTAL_PADDING}px`,
            '&:focus-visible': {
                outlineStyle: 'solid',
                outlineWidth: '2px',
                outlineOffset: '-2px',
                outlineColor: theme.colors.actionDefaultBorderFocus
            },
            '.anticon, &:focus-visible .anticon': {
                color: theme.colors.textSecondary
            },
            '&:hover .anticon': {
                color: theme.colors.actionDefaultIconHover
            },
            '&:active .anticon': {
                color: theme.colors.actionDefaultIconPress
            }
        },
        [`${classDefault}:first-of-type`]: {
            borderTopRightRadius: '0px !important',
            borderBottomRightRadius: '0px !important'
        },
        [classPrimary]: {
            ...getPrimaryStyles(theme),
            boxShadow: theme.shadows.xs,
            [`&:first-of-type`]: {
                borderRight: `1px solid ${theme.colors.actionPrimaryTextDefault}`,
                marginRight: 1
            },
            [classDropdownTrigger]: {
                borderLeft: `1px solid ${theme.colors.actionPrimaryTextDefault}`
            },
            '&:focus-visible': {
                outlineStyle: 'solid',
                outlineWidth: '1px',
                outlineOffset: '-3px',
                outlineColor: theme.colors.white
            },
            '.anticon, &:hover .anticon, &:active .anticon, &:focus-visible .anticon': {
                color: theme.colors.actionPrimaryIcon
            }
        },
        [classDropdownTrigger]: {
            // Needs to be 1px less than our standard 8px to allow for the off-by-one border handling in this component.
            padding: 3,
            borderLeftColor: 'transparent',
            width: theme.general.heightSm
        },
        [`&${classSmall}`]: {
            [classDropdownTrigger]: {
                padding: 5
            }
        },
        '&&': {
            [`[disabled], ${classPrimary}[disabled]`]: {
                ...getDisabledSplitButtonStyles(theme),
                boxShadow: 'none',
                [`&:first-of-type`]: {
                    borderRight: `1px solid ${theme.colors.actionPrimaryIcon}`,
                    marginRight: 1
                },
                [classDropdownTrigger]: {
                    borderLeft: `1px solid ${theme.colors.actionPrimaryIcon}`
                },
                '.anticon, &:hover .anticon, &:active .anticon, &:focus-visible .anticon': {
                    color: theme.colors.actionDisabledText
                }
            },
            [`${classPrimary}[disabled]`]: {
                ...getDisabledPrimarySplitButtonStyles(theme),
                '.anticon, &:hover .anticon, &:active .anticon, &:focus-visible .anticon': {
                    color: theme.colors.actionPrimaryTextDefault
                }
            }
        },
        [`${classDefault}:not(:first-of-type)`]: {
            width: theme.general.heightSm,
            padding: '3px !important',
            borderTopLeftRadius: '0px !important',
            borderBottomLeftRadius: '0px !important'
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    const importantStyles = importantify(styles);
    return /*#__PURE__*/ css(importantStyles);
}
const SplitButton = (props)=>{
    const { theme, classNamePrefix } = useDesignSystemTheme();
    const { children, icon, deprecatedMenu, type, loading, loadingButtonStyles, placement, dangerouslySetAntdProps, size, ...dropdownButtonProps } = props;
    // Size of button when loading only icon is shown
    const LOADING_BUTTON_SIZE = theme.general.iconFontSize + 2 * BUTTON_HORIZONTAL_PADDING + 2 * theme.general.borderWidth;
    const [width, setWidth] = useState(LOADING_BUTTON_SIZE);
    // Set the width to the button's width in regular state to later use when in loading state
    // We do this to have just a loading icon in loading state at the normal width to avoid flicker and width changes in page
    const ref = useCallback((node)=>{
        // Skip getBoundingClientRect if the consumer does not intend to use the loading state. Getting the bounding box
        // causes a layout which negatively impacts rendering performance.
        if (loading === undefined) {
            return;
        }
        if (node && !loading) {
            setWidth(node.getBoundingClientRect().width);
        }
    }, [
        loading
    ]);
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx("div", {
            ref: ref,
            css: {
                display: 'inline-flex',
                position: 'relative',
                verticalAlign: 'middle'
            },
            children: loading ? /*#__PURE__*/ jsx(Button, {
                componentId: "codegen_design-system_src_design-system_splitbutton_splitbutton.tsx_163",
                type: type === 'default' ? undefined : type,
                style: {
                    width: width,
                    fontSize: theme.general.iconFontSize,
                    ...loadingButtonStyles
                },
                loading: true,
                htmlType: props.htmlType,
                title: props.title,
                className: props.className,
                size: props.size,
                children: children
            }) : /*#__PURE__*/ jsx(DropdownButton, {
                ...dropdownButtonProps,
                size: props.size,
                overlay: deprecatedMenu,
                trigger: [
                    'click'
                ],
                css: getSplitButtonEmotionStyles(classNamePrefix, theme, size),
                icon: /*#__PURE__*/ jsx(ChevronDownIcon, {
                    css: {
                        fontSize: theme.general.iconFontSize
                    },
                    "aria-hidden": "true"
                }),
                placement: placement || 'bottomRight',
                type: type === 'default' ? undefined : type,
                leftButtonIcon: icon,
                ...dangerouslySetAntdProps,
                children: children
            })
        })
    });
};

/** @deprecated Please use the supported Stepper widget instead. See https://ui-infra.dev.databricks.com/storybook/js/packages/du-bois/index.html?path=/docs/primitives-stepper--docs */ const Steps = /* #__PURE__ */ (()=>{
    function Steps({ dangerouslySetAntdProps, ...props }) {
        return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
            children: /*#__PURE__*/ jsx(Steps$1, {
                ...addDebugOutlineIfEnabled(),
                ...props,
                ...dangerouslySetAntdProps
            })
        });
    }
    Steps.Step = Steps$1.Step;
    return Steps;
})();

const getTableFilterInputStyles = (theme, defaultWidth)=>{
    return /*#__PURE__*/ css({
        [theme.responsive.mediaQueries.sm]: {
            width: 'auto'
        },
        [theme.responsive.mediaQueries.lg]: {
            width: '30%'
        },
        [theme.responsive.mediaQueries.xxl]: {
            width: defaultWidth
        }
    });
};
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const TableFilterInput = /*#__PURE__*/ forwardRef(function SearchInput({ onSubmit, showSearchButton, className, containerProps, searchButtonProps, ignoreFilterMediaSizing = false, componentId, ...inputProps }, ref) {
    const { theme } = useDesignSystemTheme();
    const DEFAULT_WIDTH = 400;
    let component = /*#__PURE__*/ jsx(Input, {
        prefix: /*#__PURE__*/ jsx(SearchIcon, {}),
        allowClear: true,
        componentId: componentId,
        ...inputProps,
        className: className,
        ref: ref
    });
    if (showSearchButton) {
        component = /*#__PURE__*/ jsxs(Input.Group, {
            css: {
                display: 'flex',
                width: '100%'
            },
            className: className,
            children: [
                /*#__PURE__*/ jsx(Input, {
                    allowClear: true,
                    componentId: componentId,
                    ...inputProps,
                    ref: ref,
                    css: {
                        flex: 1
                    }
                }),
                /*#__PURE__*/ jsx(Button, {
                    componentId: componentId ? `${componentId}.search_submit` : 'codegen_design-system_src_design-system_tableui_tablefilterinput.tsx_65',
                    htmlType: "submit",
                    "aria-label": "Search",
                    ...searchButtonProps,
                    children: /*#__PURE__*/ jsx(SearchIcon, {})
                })
            ]
        });
    }
    return /*#__PURE__*/ jsx("div", {
        style: {
            height: theme.general.heightSm
        },
        css: ignoreFilterMediaSizing ? {} : getTableFilterInputStyles(theme, DEFAULT_WIDTH),
        ...containerProps,
        children: onSubmit ? /*#__PURE__*/ jsx("form", {
            onSubmit: (e)=>{
                e.preventDefault();
                onSubmit();
            },
            children: component
        }) : component
    });
});

const TableFilterLayout = /*#__PURE__*/ forwardRef(function TableFilterLayout({ children, style, className, actions, ...rest }, ref) {
    const { theme } = useDesignSystemTheme();
    const tableFilterLayoutStyles = {
        layout: /*#__PURE__*/ css({
            display: 'flex',
            flexDirection: 'row',
            justifyContent: 'space-between',
            marginBottom: 'var(--table-filter-layout-group-margin)',
            columnGap: 'var(--table-filter-layout-group-margin)',
            rowGap: 'var(--table-filter-layout-item-gap)',
            flexWrap: 'wrap'
        }),
        filters: /*#__PURE__*/ css({
            display: 'flex',
            flexWrap: 'wrap',
            flexDirection: 'row',
            alignItems: 'center',
            gap: 'var(--table-filter-layout-item-gap)',
            marginRight: 'var(--table-filter-layout-group-margin)',
            flex: 1
        }),
        filterActions: /*#__PURE__*/ css({
            display: 'flex',
            flexWrap: 'wrap',
            gap: 'var(--table-filter-layout-item-gap)',
            alignSelf: 'flex-start'
        })
    };
    return /*#__PURE__*/ jsxs("div", {
        ...rest,
        ref: ref,
        style: {
            ['--table-filter-layout-item-gap']: `${theme.spacing.sm}px`,
            ['--table-filter-layout-group-margin']: `${theme.spacing.md}px`,
            ...style
        },
        css: tableFilterLayoutStyles.layout,
        className: className,
        children: [
            /*#__PURE__*/ jsx("div", {
                css: tableFilterLayoutStyles.filters,
                children: children
            }),
            actions && /*#__PURE__*/ jsx("div", {
                css: tableFilterLayoutStyles.filterActions,
                children: actions
            })
        ]
    });
});

const TableHeaderResizeHandle = /*#__PURE__*/ forwardRef(function TableHeaderResizeHandle({ style, resizeHandler, increaseWidthHandler, decreaseWidthHandler, children, ...rest }, ref) {
    const { isHeader } = useContext(TableRowContext);
    if (!isHeader) {
        throw new Error('`TableHeaderResizeHandle` must be used within a `TableRow` with `isHeader` set to true.');
    }
    const [isPopoverOpen, setIsPopoverOpen] = useState(false);
    const dragStartPosRef = useRef(null);
    const initialEventRef = useRef(null);
    const initialRenderRef = useRef(true);
    const isDragging = useRef(false);
    const MAX_DRAG_DISTANCE = 2;
    const { theme } = useDesignSystemTheme();
    const handlePointerDown = useCallback((event)=>{
        if (!increaseWidthHandler || !decreaseWidthHandler) {
            resizeHandler?.(event);
            return;
        }
        if (isPopoverOpen && !initialRenderRef.current) return;
        else initialRenderRef.current = false;
        dragStartPosRef.current = {
            x: event.clientX,
            y: event.clientY
        };
        initialEventRef.current = event;
        isDragging.current = false;
        const handlePointerMove = (event)=>{
            if (dragStartPosRef.current) {
                const dx = event.clientX - dragStartPosRef.current.x;
                if (Math.abs(dx) > MAX_DRAG_DISTANCE && initialEventRef.current) {
                    isDragging.current = true;
                    resizeHandler?.(initialEventRef.current);
                    document.removeEventListener('pointermove', handlePointerMove);
                }
            }
        };
        const handlePointerUp = ()=>{
            dragStartPosRef.current = null;
            document.removeEventListener('pointermove', handlePointerMove);
            document.removeEventListener('pointerup', handlePointerUp);
        };
        document.addEventListener('pointermove', handlePointerMove);
        document.addEventListener('pointerup', handlePointerUp);
    }, [
        isPopoverOpen,
        resizeHandler,
        increaseWidthHandler,
        decreaseWidthHandler
    ]);
    const handleClick = useCallback((event)=>{
        if (isDragging.current) {
            event.preventDefault();
            event.stopPropagation();
            isDragging.current = false;
            return;
        }
    }, []);
    const result = /*#__PURE__*/ jsx("div", {
        ...rest,
        ref: ref,
        onPointerDown: handlePointerDown,
        onClick: handleClick,
        css: tableStyles.resizeHandleContainer,
        style: style,
        role: "button",
        "aria-label": "Resize Column",
        children: /*#__PURE__*/ jsx("div", {
            css: tableStyles.resizeHandle
        })
    });
    return increaseWidthHandler && decreaseWidthHandler ? /*#__PURE__*/ jsxs(Root$8, {
        componentId: "codegen_design-system_src_design-system_tableui_tableheader.tsx_114",
        onOpenChange: setIsPopoverOpen,
        children: [
            /*#__PURE__*/ jsx(Trigger$2, {
                asChild: true,
                children: result
            }),
            /*#__PURE__*/ jsxs(Content$2, {
                side: "top",
                align: "center",
                sideOffset: 0,
                minWidth: 135,
                style: {
                    padding: `${theme.spacing.sm} ${theme.spacing.md} ${theme.spacing.md} ${theme.spacing.sm}`
                },
                children: [
                    /*#__PURE__*/ jsxs("div", {
                        style: {
                            display: 'flex',
                            flexDirection: 'column',
                            alignItems: 'center'
                        },
                        children: [
                            /*#__PURE__*/ jsx(Typography.Title, {
                                style: {
                                    marginBottom: 0,
                                    marginTop: 0
                                },
                                children: "Resize Column"
                            }),
                            /*#__PURE__*/ jsxs("div", {
                                style: {
                                    display: 'flex',
                                    flexDirection: 'row',
                                    alignItems: 'center'
                                },
                                children: [
                                    /*#__PURE__*/ jsx(Button, {
                                        onClick: ()=>{
                                            decreaseWidthHandler();
                                        },
                                        size: "small",
                                        componentId: "design_system.adjustable_width_header.decrease_width_button",
                                        icon: /*#__PURE__*/ jsx(MinusSquareIcon, {}),
                                        style: {
                                            backgroundColor: theme.colors.actionTertiaryBackgroundHover
                                        }
                                    }),
                                    /*#__PURE__*/ jsx(Button, {
                                        onClick: ()=>{
                                            increaseWidthHandler();
                                        },
                                        size: "small",
                                        componentId: "design_system.adjustable_width_header.increase_width_button",
                                        icon: /*#__PURE__*/ jsx(PlusSquareIcon, {})
                                    })
                                ]
                            })
                        ]
                    }),
                    /*#__PURE__*/ jsx(Arrow$1, {})
                ]
            })
        ]
    }) : result;
});
const TableHeader = /*#__PURE__*/ forwardRef(function TableHeader({ children, ellipsis = false, multiline = false, sortable, sortDirection, onToggleSort, style, className, isResizing = false, align = 'left', wrapContent = true, column, header, setColumnSizing, componentId, analyticsEvents, 'aria-label': ariaLabel, ...rest }, ref) {
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.tableHeader', false);
    // Pulling props from the column and header props with deprecated fallbacks.
    // Doing this to avoid breaking changes + have a cleaner mechanism for testing removal of deprecated props.
    const resizable = column?.getCanResize() || rest.resizable || false;
    const resizeHandler = header?.getResizeHandler() || rest.resizeHandler;
    const supportsColumnPopover = column && header && setColumnSizing;
    const { size, grid } = useContext(TableContext);
    const { isHeader } = useContext(TableRowContext);
    const [currentSortDirection, setCurrentSortDirection] = useState(sortDirection);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.TableHeader,
        componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: true
    });
    const { elementRef: tableHeaderRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: currentSortDirection
    });
    const mergedRef = useMergeRefs([
        ref,
        tableHeaderRef
    ]);
    if (!isHeader) {
        throw new Error('`TableHeader` a must be used within a `TableRow` with `isHeader` set to true.');
    }
    let sortIcon = /*#__PURE__*/ jsx(Fragment, {});
    // While most libaries use `asc` and `desc` for the sort value, the ARIA spec
    // uses `ascending` and `descending`.
    let ariaSort;
    if (sortable) {
        if (sortDirection === 'asc') {
            sortIcon = /*#__PURE__*/ jsx(SortAscendingIcon, {});
            ariaSort = 'ascending';
        } else if (sortDirection === 'desc') {
            sortIcon = /*#__PURE__*/ jsx(SortDescendingIcon, {});
            ariaSort = 'descending';
        } else if (sortDirection === 'none') {
            sortIcon = /*#__PURE__*/ jsx(SortUnsortedIcon, {});
            ariaSort = 'none';
        }
    }
    useEffect(()=>{
        if (sortDirection !== currentSortDirection) {
            setCurrentSortDirection(sortDirection);
            eventContext.onValueChange(sortDirection);
        }
    }, [
        sortDirection,
        currentSortDirection,
        eventContext
    ]);
    const sortIconOnLeft = align === 'right';
    let typographySize = 'md';
    if (size === 'small') {
        typographySize = 'sm';
    }
    const content = wrapContent ? /*#__PURE__*/ jsx(Typography.Text, {
        className: "table-header-text",
        ellipsis: !multiline,
        size: typographySize,
        title: !multiline && typeof children === 'string' && children || undefined,
        bold: true,
        children: children
    }) : children;
    const getColumnResizeHandler = useCallback((newSize)=>()=>{
            if (column && setColumnSizing) {
                setColumnSizing((old)=>({
                        ...old,
                        [column.id]: newSize
                    }));
            }
        }, [
        column,
        setColumnSizing
    ]);
    const increaseWidthHandler = useCallback(()=>{
        if (column && setColumnSizing) {
            const currentSize = column.getSize();
            getColumnResizeHandler(currentSize + 10)();
        }
    }, [
        column,
        setColumnSizing,
        getColumnResizeHandler
    ]);
    const decreaseWidthHandler = useCallback(()=>{
        if (column && setColumnSizing) {
            const currentSize = column.getSize();
            getColumnResizeHandler(currentSize - 10)();
        }
    }, [
        column,
        setColumnSizing,
        getColumnResizeHandler
    ]);
    const renderResizeHandle = resizable && resizeHandler ? /*#__PURE__*/ jsx(TableHeaderResizeHandle, {
        style: {
            height: size === 'default' ? '20px' : '16px'
        },
        resizeHandler: resizeHandler,
        increaseWidthHandler: supportsColumnPopover ? increaseWidthHandler : undefined,
        decreaseWidthHandler: supportsColumnPopover ? decreaseWidthHandler : undefined
    }) : null;
    const isSortButtonVisible = sortable && !isResizing;
    return /*#__PURE__*/ jsxs("div", {
        ...rest,
        ref: mergedRef,
        // PE-259 Use more performance className for grid but keep css= for compatibility.
        css: !grid ? [
            repeatingElementsStyles.cell,
            repeatingElementsStyles.header
        ] : undefined,
        className: classnames(grid && tableClassNames.cell, grid && tableClassNames.header, {
            'table-header-isGrid': grid
        }, className),
        role: "columnheader",
        "aria-sort": sortable && ariaSort || undefined,
        style: {
            justifyContent: align,
            textAlign: align,
            ...style
        },
        "aria-label": isSortButtonVisible ? undefined : ariaLabel,
        ...eventContext.dataComponentProps,
        children: [
            isSortButtonVisible ? /*#__PURE__*/ jsxs("div", {
                css: [
                    tableStyles.headerButtonTarget
                ],
                role: "button",
                tabIndex: 0,
                onClick: onToggleSort,
                onKeyDown: (event)=>{
                    if (sortable && (event.key === 'Enter' || event.key === ' ')) {
                        event.preventDefault();
                        return onToggleSort?.(event);
                    }
                },
                "aria-label": isSortButtonVisible ? ariaLabel : undefined,
                children: [
                    sortIconOnLeft ? /*#__PURE__*/ jsx("span", {
                        className: "table-header-icon-container",
                        css: [
                            tableStyles.sortHeaderIconOnLeft
                        ],
                        children: sortIcon
                    }) : null,
                    content,
                    !sortIconOnLeft ? /*#__PURE__*/ jsx("span", {
                        className: "table-header-icon-container",
                        css: [
                            tableStyles.sortHeaderIconOnRight
                        ],
                        children: sortIcon
                    }) : null
                ]
            }) : content,
            renderResizeHandle
        ]
    });
});

const TableRowActionHeader = ({ children })=>{
    return /*#__PURE__*/ jsx(TableRowAction, {
        children: /*#__PURE__*/ jsx("span", {
            css: visuallyHidden,
            children: children
        })
    });
};

/**
 * A component that renders a multi-action row in a table. Similar to TableRowAction, but with a gap between the actions.
 * TableRowMultiAction also allows for individual buttons to apply the `skipHideIconButtonClassName` classname and be
 * always-visible when necessary.
 *
 * Child buttons can also apply the `skipHideIconButtonClassName` classname and be always-visible when necessary.
 *
 * @param children - The child nodes for the table row. Should contain one or more small-sized buttons.
 * @param style - The style property.
 * @param className - The class name property.
 * @param css - The CSS property.
 * @param rest - The rest of the props.
 */ const TableRowMultiAction = /*#__PURE__*/ forwardRef(function TableRowMultiAction({ children, style, className, css: emotionCss, ...rest }, ref) {
    const { size } = useContext(TableContext);
    const { isHeader } = useContext(TableRowContext);
    const { theme } = useDesignSystemTheme();
    return /*#__PURE__*/ jsx("div", {
        ...rest,
        ref: ref,
        role: isHeader ? 'columnheader' : 'cell',
        style: {
            paddingTop: size === 'default' ? theme.spacing.xs : 0,
            paddingBottom: size === 'default' ? theme.spacing.xs : 0,
            paddingLeft: size === 'default' ? theme.spacing.xs : 0,
            paddingRight: size === 'default' ? theme.spacing.xs : 0,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'flex-end',
            gap: theme.spacing.xs,
            ...style
        },
        css: emotionCss,
        className: classnames(className, !isHeader && hideIconButtonActionCellClassName),
        children: children
    });
});

const TableRowSelectCell = /*#__PURE__*/ forwardRef(function TableRowSelectCell({ onChange, checked, indeterminate, noCheckbox, children, isDisabled, checkboxLabel, componentId, analyticsEvents, ...rest }, ref) {
    const { theme } = useDesignSystemTheme();
    const { isHeader } = useContext(TableRowContext);
    const { someRowsSelected } = useContext(TableContext);
    if (typeof someRowsSelected === 'undefined') {
        throw new Error('`TableRowSelectCell` cannot be used unless `someRowsSelected` has been provided to the `Table` component, see documentation.');
    }
    if (!isHeader && indeterminate) {
        throw new Error('`TableRowSelectCell` cannot be used with `indeterminate` in a non-header row.');
    }
    return /*#__PURE__*/ jsx("div", {
        ...rest,
        ref: ref,
        css: tableStyles.checkboxCell,
        style: {
            ['--row-checkbox-opacity']: someRowsSelected ? 1 : 0,
            zIndex: theme.options.zIndexBase
        },
        role: isHeader ? 'columnheader' : 'cell',
        // TODO: Ideally we shouldn't need to specify this `className`, but it allows for row-hovering to reveal
        // the checkbox in `TableRow`'s CSS without extra JS pointerin/out events.
        className: "table-row-select-cell",
        children: !noCheckbox && /*#__PURE__*/ jsx(Checkbox, {
            componentId: componentId,
            analyticsEvents: analyticsEvents,
            isChecked: checked || indeterminate && null,
            onChange: (_checked, event)=>onChange?.(event.nativeEvent),
            isDisabled: isDisabled,
            "aria-label": checkboxLabel
        })
    });
});

const SMALL_BUTTON_HEIGHT = 24;
const getStyles = (theme, size, onlyIcon)=>{
    return /*#__PURE__*/ css({
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'center',
        whiteSpace: 'nowrap',
        ...!onlyIcon && {
            boxShadow: theme.shadows.xs
        },
        border: `1px solid ${theme.colors.actionDefaultBorderDefault}`,
        borderRadius: theme.borders.borderRadiusSm,
        backgroundColor: 'transparent',
        color: theme.colors.actionDefaultTextDefault,
        height: theme.general.heightSm,
        padding: '0 12px',
        fontSize: theme.typography.fontSizeBase,
        lineHeight: `${theme.typography.lineHeightBase}px`,
        '&[data-state="off"] .togglebutton-icon-wrapper': {
            color: theme.colors.textSecondary
        },
        '&[data-state="off"]:hover .togglebutton-icon-wrapper': {
            color: theme.colors.actionDefaultTextHover
        },
        '&[data-state="on"]': {
            backgroundColor: theme.colors.actionDefaultBackgroundPress,
            color: theme.colors.actionDefaultTextPress,
            borderColor: theme.colors.actionDefaultBorderPress
        },
        '&:hover': {
            cursor: 'pointer',
            color: theme.colors.actionDefaultTextHover,
            backgroundColor: theme.colors.actionDefaultBackgroundHover,
            borderColor: theme.colors.actionDefaultBorderHover,
            '& > svg': {
                stroke: theme.colors.actionDefaultBorderHover
            }
        },
        '&:disabled': {
            cursor: 'default',
            borderColor: theme.colors.actionDisabledBorder,
            color: theme.colors.actionDisabledText,
            backgroundColor: 'transparent',
            boxShadow: 'none',
            '& > svg': {
                stroke: theme.colors.border
            }
        },
        ...!onlyIcon && {
            '&&': {
                padding: '4px 12px',
                ...size === 'small' && {
                    padding: '0 8px'
                }
            }
        },
        ...onlyIcon && {
            width: theme.general.heightSm,
            border: 'none'
        },
        ...size === 'small' && {
            height: SMALL_BUTTON_HEIGHT,
            lineHeight: theme.typography.lineHeightBase,
            ...onlyIcon && {
                width: SMALL_BUTTON_HEIGHT,
                paddingTop: 0,
                paddingBottom: 0,
                verticalAlign: 'middle'
            }
        }
    });
};
const RectangleSvg = (props)=>/*#__PURE__*/ jsx("svg", {
        width: "16",
        height: "16",
        viewBox: "0 0 16 16",
        fill: "none",
        xmlns: "http://www.w3.org/2000/svg",
        ...props,
        children: /*#__PURE__*/ jsx("rect", {
            x: "0.5",
            y: "0.5",
            width: "15",
            height: "15",
            rx: "3.5"
        })
    });
// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
const RectangleIcon = /*#__PURE__*/ forwardRef((props, forwardedRef)=>{
    return /*#__PURE__*/ jsx(Icon, {
        ref: forwardedRef,
        ...props,
        component: RectangleSvg
    });
});
const ToggleButton = /*#__PURE__*/ forwardRef(// eslint-disable-next-line react-component-name/react-component-name -- TODO(FEINF-4716)
({ children, pressed, defaultPressed, icon, size = 'middle', componentId, analyticsEvents, ...props }, ref)=>{
    const emitOnView = safex('databricks.fe.observability.defaultComponentView.toggleButton', false);
    const { theme } = useDesignSystemTheme();
    const [isPressed, setIsPressed] = React__default.useState(defaultPressed);
    const memoizedAnalyticsEvents = useMemo(()=>analyticsEvents ?? (emitOnView ? [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange,
            DesignSystemEventProviderAnalyticsEventTypes.OnView
        ] : [
            DesignSystemEventProviderAnalyticsEventTypes.OnValueChange
        ]), [
        analyticsEvents,
        emitOnView
    ]);
    const eventContext = useDesignSystemEventComponentCallbacks({
        componentType: DesignSystemEventProviderComponentTypes.ToggleButton,
        componentId: componentId,
        analyticsEvents: memoizedAnalyticsEvents,
        valueHasNoPii: true
    });
    const { elementRef: toggleButtonRef } = useNotifyOnFirstView({
        onView: eventContext.onView,
        value: pressed ?? defaultPressed
    });
    const mergedRef = useMergeRefs([
        ref,
        toggleButtonRef
    ]);
    const handleOnPressedChange = useCallback((pressed)=>{
        eventContext.onValueChange(pressed);
        props.onPressedChange?.(pressed);
        setIsPressed(pressed);
    }, [
        eventContext,
        props
    ]);
    useEffect(()=>{
        setIsPressed(pressed);
    }, [
        pressed
    ]);
    const iconOnly = !children && Boolean(icon);
    const iconStyle = iconOnly ? {} : {
        marginRight: theme.spacing.xs
    };
    const checkboxIcon = isPressed ? /*#__PURE__*/ jsx(CheckIcon, {}) : /*#__PURE__*/ jsx(RectangleIcon, {
        css: {
            stroke: theme.colors.border
        }
    });
    return /*#__PURE__*/ jsxs(Toggle.Root, {
        ...addDebugOutlineIfEnabled(),
        css: getStyles(theme, size, iconOnly),
        ...props,
        pressed: isPressed,
        onPressedChange: handleOnPressedChange,
        ref: mergedRef,
        ...eventContext.dataComponentProps,
        children: [
            /*#__PURE__*/ jsx("span", {
                className: "togglebutton-icon-wrapper",
                style: {
                    display: 'flex',
                    ...iconStyle
                },
                children: icon ? icon : checkboxIcon
            }),
            children
        ]
    });
});

const hideLinesForSizes = [
    'x-small',
    'xx-small'
];
const sizeMap = {
    default: {
        nodeSize: 32,
        indent: 28
    },
    small: {
        nodeSize: 24,
        indent: 24
    },
    'x-small': {
        nodeSize: 24,
        indent: 16
    },
    'xx-small': {
        nodeSize: 24,
        indent: 8
    }
};
/**
 * These styles share some aspects with the styles in the main `Checkbox.tsx` component.
 * However, due to significant differences in the internal implementation and DOM structure of the Tree Checkbox and the
 * main Checkbox, we have forked the styles here.
 * Some notable differences are:
 * 1. Tree checkbox does not have a wrapper div
 * 2. Tree checkbox does not use a hidden input element
 * 3. Tree checkbox does not support the disabled state.
 * 4. Tree checkbox does not support keyboard focus
 */ function getTreeCheckboxEmotionStyles(clsPrefix, theme) {
    const classRoot = `.${clsPrefix}`;
    const classInner = `.${clsPrefix}-inner`;
    const classIndeterminate = `.${clsPrefix}-indeterminate`;
    const classChecked = `.${clsPrefix}-checked`;
    const classDisabled = `.${clsPrefix}-disabled`;
    const styles = {
        [`${classRoot} > ${classInner}`]: {
            backgroundColor: theme.colors.actionDefaultBackgroundDefault,
            borderColor: theme.colors.actionDefaultBorderDefault
        },
        // Hover
        [`${classRoot}:hover > ${classInner}`]: {
            backgroundColor: theme.colors.actionDefaultBackgroundHover,
            borderColor: theme.colors.actionDefaultBorderHover
        },
        // Mouse pressed
        [`${classRoot}:active > ${classInner}`]: {
            backgroundColor: theme.colors.actionDefaultBackgroundPress,
            borderColor: theme.colors.actionDefaultBorderPress
        },
        // Checked state
        [`${classChecked} > ${classInner}`]: {
            backgroundColor: theme.colors.actionPrimaryBackgroundDefault,
            borderColor: 'transparent'
        },
        // Checked hover
        [`${classChecked}:hover > ${classInner}`]: {
            backgroundColor: theme.colors.actionPrimaryBackgroundHover,
            borderColor: 'transparent'
        },
        // Checked and mouse pressed
        [`${classChecked}:active > ${classInner}`]: {
            backgroundColor: theme.colors.actionPrimaryBackgroundPress,
            borderColor: 'transparent'
        },
        // Indeterminate
        [`${classIndeterminate} > ${classInner}`]: {
            backgroundColor: theme.colors.primary,
            borderColor: theme.colors.primary,
            // The after pseudo-element is used for the check image itself
            '&:after': {
                backgroundColor: theme.colors.white,
                height: '3px',
                width: '8px',
                borderRadius: '4px'
            }
        },
        // Indeterminate hover
        [`${classIndeterminate}:hover > ${classInner}`]: {
            backgroundColor: theme.colors.actionPrimaryBackgroundHover,
            borderColor: 'transparent'
        },
        // Indeterminate and mouse pressed
        [`${classIndeterminate}:active > ${classInner}`]: {
            backgroundColor: theme.colors.actionPrimaryBackgroundPress
        },
        // Disabled
        [`${classDisabled} > ${classInner}, ${classDisabled}:hover > ${classInner}, ${classDisabled}:active > ${classInner}`]: {
            backgroundColor: theme.colors.actionDisabledBackground
        },
        ...getAnimationCss(theme.options.enableAnimation)
    };
    return styles;
}
function getTreeEmotionStyles(clsPrefix, theme, size) {
    const classNode = `.${clsPrefix}-tree-treenode`;
    const classNodeSelected = `.${clsPrefix}-tree-treenode-selected`;
    const classNodeActive = `.${clsPrefix}-tree-treenode-active`;
    const classNodeDisabled = `.${clsPrefix}-tree-treenode-disabled`;
    const classContent = `.${clsPrefix}-tree-node-content-wrapper`;
    const classContentTitle = `.${clsPrefix}-tree-title`;
    const classSelected = `.${clsPrefix}-tree-node-selected`;
    const classSwitcher = `.${clsPrefix}-tree-switcher`;
    const classSwitcherNoop = `.${clsPrefix}-tree-switcher-noop`;
    const classFocused = `.${clsPrefix}-tree-focused`;
    const classCheckbox = `.${clsPrefix}-tree-checkbox`;
    const classUnselectable = `.${clsPrefix}-tree-unselectable`;
    const classIndent = `.${clsPrefix}-tree-indent-unit`;
    const classTreeList = `.${clsPrefix}-tree-list`;
    const classScrollbar = `.${clsPrefix}-tree-list-scrollbar`;
    const classScrollbarThumb = `.${clsPrefix}-tree-list-scrollbar-thumb`;
    const classIcon = `.${clsPrefix}-tree-iconEle`;
    const classAntMotion = `.${clsPrefix}-tree-treenode-motion, .ant-motion-collapse-appear, .ant-motion-collapse-appear-active, .ant-motion-collapse`;
    const NODE_SIZE = sizeMap[size].nodeSize;
    const ICON_FONT_SIZE = 16;
    const BORDER_WIDTH = 4;
    const baselineAligned = {
        alignSelf: 'baseline',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    };
    const styles = {
        // Basic node
        [classNode]: {
            minHeight: NODE_SIZE,
            width: '100%',
            padding: 0,
            paddingLeft: BORDER_WIDTH,
            display: 'flex',
            alignItems: 'center',
            // Ant tree renders some hidden tree nodes (presumably for internal purposes). Setting these to width: 100% causes
            // overflow, so we need to reset here.
            '&[aria-hidden=true]': {
                width: 'auto'
            },
            '&:hover': {
                backgroundColor: theme.colors.actionTertiaryBackgroundHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionTertiaryBackgroundPress
            }
        },
        [`&${classUnselectable}`]: {
            // Remove hover and press styles if tree nodes are not selectable
            [classNode]: {
                '&:hover': {
                    backgroundColor: 'transparent'
                },
                '&:active': {
                    backgroundColor: 'transparent'
                }
            },
            [classContent]: {
                cursor: 'default'
            },
            // Unselectable nodes don't have any background, so the switcher looks better with rounded corners.
            [classSwitcher]: {
                borderRadius: theme.legacyBorders.borderRadiusMd
            }
        },
        // The "active" node is the one that is currently focused via keyboard navigation. We give it the same visual
        // treatment as the mouse hover style.
        [classNodeActive]: {
            backgroundColor: theme.colors.actionTertiaryBackgroundHover
        },
        // The "selected" node is one that has either been clicked on, or selected via pressing enter on the keyboard.
        [classNodeSelected]: {
            backgroundColor: theme.colors.actionDefaultBackgroundHover,
            borderLeft: `${BORDER_WIDTH}px solid ${theme.colors.actionPrimaryBackgroundDefault}`,
            paddingLeft: 0,
            // When hovering over a selected node, we still want it to look selected
            '&:hover': {
                backgroundColor: theme.colors.actionTertiaryBackgroundPress
            }
        },
        [classSelected]: {
            background: 'none'
        },
        [classNodeDisabled]: {
            '&:hover': {
                backgroundColor: 'transparent'
            },
            '&:active': {
                backgroundColor: 'transparent'
            }
        },
        [classContent]: {
            lineHeight: `${NODE_SIZE}px`,
            // The content label is the interactive element, so we want it to fill the node to maximise the click area.
            width: '100%',
            display: 'flex',
            alignItems: 'center',
            '&:hover': {
                backgroundColor: 'transparent'
            },
            '&:active': {
                backgroundColor: 'transparent'
            }
        },
        [classContentTitle]: {
            lineHeight: theme.typography.lineHeightBase,
            padding: `${(NODE_SIZE - parseInt(theme.typography.lineHeightBase, 10)) / 2}px 0`,
            // The content inside 'classContent' is wrapped in the title class, which is the actual interactive element.
            width: '100%'
        },
        // TODO(FEINF-1595): Temporary style for now
        [`${classSwitcherNoop} + ${classContent}, ${classSwitcherNoop} + ${classCheckbox}`]: {
            marginLeft: NODE_SIZE + 4
        },
        [classSwitcher]: {
            height: NODE_SIZE,
            width: NODE_SIZE,
            paddingTop: (NODE_SIZE - ICON_FONT_SIZE) / 2,
            marginRight: theme.spacing.xs,
            color: theme.colors.textSecondary,
            backgroundColor: 'transparent',
            // Keyboard navigation only allows moving between entire nodes, not between the switcher and label directly.
            // However, under mouse control, the two can still be clicked separately. We apply hover and press treatment
            // here to indicate to mouse users that the switcher is clickable.
            '&:hover': {
                backgroundColor: theme.colors.actionTertiaryBackgroundHover
            },
            '&:active': {
                backgroundColor: theme.colors.actionTertiaryBackgroundPress
            }
        },
        [classSwitcherNoop]: {
            display: 'none',
            '&:hover': {
                backgroundColor: 'transparent'
            },
            '&:active': {
                backgroundColor: 'transparent'
            }
        },
        [`&${classFocused}`]: {
            backgroundColor: 'transparent',
            outlineWidth: 2,
            outlineOffset: 1,
            outlineColor: theme.colors.actionDefaultBorderFocus,
            outlineStyle: 'solid'
        },
        [classCheckbox]: {
            marginTop: size === 'default' ? theme.spacing.sm : theme.spacing.xs,
            marginBottom: 0,
            marginRight: size === 'default' ? theme.spacing.sm : theme.spacing.xs,
            ...baselineAligned
        },
        [classScrollbarThumb]: {
            background: chroma(theme.isDarkMode ? '#ffffff' : '#000000').alpha(0.5).hex()
        },
        [`${classIcon}:has(*)`]: {
            ...baselineAligned,
            height: NODE_SIZE,
            color: theme.colors.textSecondary,
            marginRight: size === 'default' ? theme.spacing.sm : theme.spacing.xs
        },
        // Needed to avoid flickering when has icon and expanding
        [classAntMotion]: {
            ...getAnimationCss(theme.options.enableAnimation),
            visibility: 'hidden'
        },
        // Vertical line
        [classIndent]: {
            width: sizeMap[size].indent
        },
        [`${classIndent}::before`]: {
            height: '100%',
            borderColor: theme.colors.border
        },
        [classTreeList]: {
            [`&:hover ${classScrollbar}`]: {
                display: 'block !important'
            },
            [`&:active ${classScrollbar}`]: {
                display: 'block !important'
            }
        },
        ...getTreeCheckboxEmotionStyles(`${clsPrefix}-tree-checkbox`, theme),
        ...getAnimationCss(theme.options.enableAnimation)
    };
    const importantStyles = importantify(styles);
    return /*#__PURE__*/ css(importantStyles);
}
const SHOW_LINE_DEFAULT = {
    showLeafIcon: false
};
// @ts-expect-error: Tree doesn't expose a proper type
const Tree = /*#__PURE__*/ forwardRef(function Tree({ treeData, defaultExpandedKeys, defaultSelectedKeys, defaultCheckedKeys, disabled = false, mode = 'default', size = 'default', showLine, dangerouslySetAntdProps, ...props }, ref) {
    const { theme, classNamePrefix } = useDesignSystemTheme();
    let calculatedShowLine = showLine ?? false;
    if (hideLinesForSizes.includes(size)) {
        calculatedShowLine = false;
    } else {
        calculatedShowLine = showLine ?? SHOW_LINE_DEFAULT;
    }
    return /*#__PURE__*/ jsx(DesignSystemAntDConfigProvider, {
        children: /*#__PURE__*/ jsx(Tree$1, {
            ...addDebugOutlineIfEnabled(),
            treeData: treeData,
            defaultExpandedKeys: defaultExpandedKeys,
            defaultSelectedKeys: defaultSelectedKeys,
            defaultCheckedKeys: defaultCheckedKeys,
            disabled: disabled,
            css: getTreeEmotionStyles(classNamePrefix, theme, size),
            switcherIcon: /*#__PURE__*/ jsx(ChevronDownIcon, {
                css: {
                    fontSize: '16px !important'
                }
            }),
            tabIndex: 0,
            selectable: mode === 'selectable' || mode === 'multiselectable',
            checkable: mode === 'checkable',
            multiple: mode === 'multiselectable',
            // With the library flag, defaults to showLine = true. The status quo default is showLine = false.
            showLine: calculatedShowLine,
            ...dangerouslySetAntdProps,
            ...props,
            ref: ref
        })
    });
});

export { AccessibleContainer, Accordion, AccordionPanel, Alert, ApplyDesignSystemContextOverrides, ApplyGlobalStyles, AssistantAvatar, AutoComplete, Avatar, AvatarGroup, Breadcrumb, Button, Card, CheckCircleFillIcon, CheckCircleIcon, CheckIcon, Checkbox, ChevronDownIcon, ChevronLeftIcon, ChevronRightIcon, CircleIcon, CircleOffIcon, CircleOutlineIcon, ClockIcon, CloseIcon, CloseSmallIcon, Col, ComponentFinderContext, ContextMenu$1 as ContextMenu, CursorPagination, DBAssistantAvatar, DU_BOIS_ENABLE_ANIMATION_CLASSNAME, DangerFillIcon, DangerIcon, DatePicker, DesignSystemAntDConfigProvider, DesignSystemEventProvider, DesignSystemEventProviderAnalyticsEventTypes, DesignSystemEventProviderComponentSubTypeMap, DesignSystemEventProviderComponentTypes, DesignSystemEventSuppressInteractionProviderContext, DesignSystemEventSuppressInteractionTrueContextValue, DialogCombobox, DialogComboboxAddButton, DialogComboboxContent, DialogComboboxCountBadge, DialogComboboxCustomButtonTriggerWrapper, EmptyResults as DialogComboboxEmpty, DialogComboboxFooter, DialogComboboxHintRow, DialogComboboxOptionControlledList, DialogComboboxOptionList, DialogComboboxOptionListCheckboxItem, DialogComboboxOptionListSearch, DialogComboboxOptionListSelectItem, DialogComboboxSectionHeader, DialogComboboxSeparator, DialogComboboxTrigger, Drawer, Dropdown, DuboisDatePicker, FormUI, GenericSkeleton, Header, HoverCard, Icon, InfoFillIcon, InfoPopover, InfoSmallIcon, Input, LegacyDatePicker, LegacyInfoTooltip, LegacyPopover, LegacySelect, LegacySkeleton, LegacyTabPane, LegacyTable, LegacyTabs, LegacyTooltip, Listbox, LoadingState, LockIcon, Menu, MinusSquareIcon, Modal, MonthPickerGrid, NavigationMenu, Notification, Overflow, PageWrapper, Pagination, ParagraphSkeleton, PillControl, PlusIcon, PlusSquareIcon, PreviewCard, Progress, RHFControlledComponents, ROW_GUTTER_SIZE, Radio, RadioTile, RangePicker$1 as RangePicker, ResourceStatusIndicator, RestoreAntDDefaultClsPrefix, Row, SMALL_BUTTON_HEIGHT$2 as SMALL_BUTTON_HEIGHT, SearchIcon, SegmentedControlButton, SegmentedControlGroup, Select, SelectContent, SelectContextProvider, SelectOption, SelectOptionGroup, SelectTrigger, SimpleSelect, SimpleSelectOption, SimpleSelectOptionGroup, Slider, SnackBar, SortAscendingIcon, SortDescendingIcon, SortUnsortedIcon, Space, Spacer, SparkleDoubleIcon, Spinner, SplitButton, Steps, Switch, Table, TableCell, TableContext, TableFilterInput, TableFilterLayout, TableHeader, TableRow, TableRowAction, TableRowActionHeader, TableRowContext, TableRowMenuContainer, TableRowMultiAction, TableRowSelectCell, TableSkeleton, TableSkeletonRows, Tag, TitleSkeleton, ToggleButton, Tooltip, Tree, TruncationTooltip, TypeaheadComboboxAddButton, TypeaheadComboboxInput, TypeaheadComboboxMenu, TypeaheadComboboxMultiSelectInput, TypeaheadComboboxRoot, TypeaheadComboboxSectionHeader, TypeaheadComboboxSelectedItem, TypeaheadComboboxSeparator, TypeaheadComboboxToggleButton, Typography, WarningFillIcon, WarningIcon, XCircleFillIcon, YearPickerGrid, __INTERNAL_DO_NOT_USE__HorizontalGroup, __INTERNAL_DO_NOT_USE__VerticalGroup, findClosestOptionSibling, findHighlightedOption, flushFastdomForTesting, getAnimationCss, getComboboxOptionItemWrapperStyles, getContentOptions, getDarkModePortalStyles, getDatePickerQuickActionBasic, getGlobalResetStyles, getGlobalStyles, getInputStyles$2 as getInputStyles, getLegacyTabEmotionStyles, getPaginationEmotionStyles, getRadioStyles, getRangeQuickActionsBasic, getShadowScrollStyles, getValidationStateColor, hideIconButtonRowStyles, highlightFirstNonDisabledOption, highlightOption, importantify, measure, setupDesignSystemEventProviderForTesting, snackBarManager, update, useCallbackOnEnter, useComboboxState, useComponentFinderContext, useDesignSystemEventComponentCallbacks, useDesignSystemFlags, useDesignSystemTheme, useFastDom, useLegacyNotification, useModalContext, useMultipleSelectionState, useNotifyOnFirstView, useRadioGroupContext, useSnackBar, useThemedStyles, useTypeaheadComboboxContext, visuallyHidden, withNotifications };
//# sourceMappingURL=index.js.map
