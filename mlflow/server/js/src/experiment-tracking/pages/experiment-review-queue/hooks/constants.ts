/**
 * Base path for the review-queues REST API. Each hook appends its action
 * (e.g. `/list`, `/items/set-status`) rather than repeating the full prefix.
 */
export const REVIEW_QUEUES_API_BASE = 'ajax-api/3.0/mlflow/review-queues';
