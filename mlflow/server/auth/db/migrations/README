# Database schema migration for MLflow Authentication

## Autogenerate a migration script

```sh
cd mlflow/server/auth/db/migrations
alembic revision --autogenerate -m "<message>"
```

## Run migration

```sh
alembic upgrade head
```
