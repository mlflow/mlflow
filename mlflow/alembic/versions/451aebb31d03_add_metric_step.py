"""add metric step

Revision ID: 451aebb31d03
Revises: 
Create Date: 2019-04-22 15:29:24.921354

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = '451aebb31d03'
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column('metrics', sa.Column('step', sa.BigInteger(), nullable=False, server_default='0'))
    # ### end Alembic commands ###
    # Drop primary key constraint. Note the CASCADE clause - this deletes the foreign key
    # constraint. Use batch mode so that we can run "ALTER TABLE" statements against SQLite
    # databases (see more info at https://alembic.sqlalchemy.org/en/latest/
    # batch.html#running-batch-migrations-for-sqlite-and-other-databases)
    with op.batch_alter_table("metrics") as batch_op:
        batch_op.drop_constraint(constraint_name='metric_pk')
        batch_op.create_primary_key(
            constraint_name='metric_pk',
            columns=['key', 'timestamp', 'step', 'run_uuid', 'value'])
        # Re-create the foreign key constraint
        batch_op.create_foreign_key(
            constraint_name='runs.run_uuid', referent_table='runs', local_cols=['run_uuid'],
            remote_cols=['run_uuid'])


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    pass
    # ### end Alembic commands ###
