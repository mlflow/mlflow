import React from 'react';
import styles from './TabsWrapper.module.css';
export default function TabsWrapper(_a) {
    var children = _a.children;
    return <div className={styles.wrapper}>{children}</div>;
}
